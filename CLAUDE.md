# CLAUDE.md — エージェント作業規約

このファイルはプロジェクトの憲法である。他のどの文書とも矛盾した場合、これが優先する。

---

## 0. プロジェクト一行要約

狭い FT が「概念」を書き換えるのか「表層のパッチ」を貼るだけなのかを、算術を対象に、代数的一貫性の監査によって判定する。

---

## 1. セッション開始時に必ず行うこと

```bash
cat STATE.md                    # 現在の状態
tail -40 logs/CHANGELOG.md      # 直近の変更
tail -60 logs/DECISIONS.md      # 直近の意思決定
git log --oneline -20
git status                      # 未コミットの変更がないか
ls runs/ | tail -5              # 中断中の実験がないか
```

これを飛ばして作業を始めない。文脈を持たないまま編集すると、過去の決定を無自覚に覆す。

---

## 2. 絶対禁止事項

| 禁止 | 理由 |
|---|---|
| **出典を確認していない文献・著者・年・会議名を書く** | 捏造引用は一発 reject |
| **実験結果でない数値を文書に書く** | すべての数値は `results/` のファイルと run_id に紐づくこと |
| **事前登録した予測・解析計画を実験後に変更する** | HARKing。変更する場合は打ち消し線+理由+日付で残す |
| **対照条件なしで病変FTを本実験として回す** | 解釈不能なデータが増えるだけ |
| **「有意差がなかったので差はない」と書く** | 同等性検定(TOST)を使う。`Documents/05_STATISTICS.md` §5 |
| 単一シードの結果を主張に使う | 最低5シード |
| `data/raw/` を書き換える | 再現性の破壊 |
| 人間の承認なく 10 GPU時間超のジョブを起動する | コスト管理 |
| RunPod のポッドを起動したまま放置する | 課金が続く |

---

## 3. 引用のプロトコル

1. 検索でヒットしたら **arXiv abs / ACL Anthology / 出版社ページを実際に開く**
2. 著者・年・会議・ページを原典から転記
3. `Documents/refs.bib` に `verified = {YYYY-MM-DD}` と `source_url` を付けて追加
4. `Documents/02_RELATED_WORK.md` の表を更新
5. 確認できなければ「未検証」欄に置く。**本文・論文では使わない**

2026年以降の arXiv ID は査読前プレプリントの可能性が高い。主要な論拠には使わない。

---

## 4. 変更の手順(必ずこの順)

```
1. plans/PLAN-XXX-*.md を作成または修正   ← 何をするか先に書く
2. 人間のレビュー(設計変更の場合)
3. 実装(コード規約は skill `code-style`)
4. テスト (pytest code/tests -q)
5. logs/CHANGELOG.md に追記
6. git commit
```

設計変更を伴う場合は `logs/DECISIONS.md` に ADR を追記する。

実験を回す手順(事前登録の凍結 → dry-run → 本実行 → 評価 → 集約)と `runs/<id>/` に残す必須成果物は `infra/RUNPOD.md` §4。

---

## 5. Git 規約

**コミットは小さく、頻繁に。日付が正確に残ることが重要。**

```
<type>(<scope>): <要約>

種類: feat / fix / docs / exp / stat / infra / test / refactor
スコープ: data_gen / train / eval / probe / analysis / plan / adr / runpod
```

例:
```
exp(train): run PLAN-003 seed 0-4, +2 lesion rank4   [run:20260901_143022_exp003]
docs(adr): ADR-012 主変換を ×2 から +2 へ変更
stat(analysis): 主要評価項目の混合効果モデルを実装
```

規約:
- **`--date` オプションで日付を偽装しない。**実際の作業時刻が記録として意味を持つ
- 実験を回したら run_id をコミットメッセージに `[run:...]` 形式で含める
- Phase 完了時にタグ: `git tag -a phase1-complete -m "..."`
- 事前登録の凍結時にタグ: `git tag -a preregister-exp003 -m "..."`。**これで「予測が実験前に書かれた」ことが証明できる**
- `runs/` は大きいので LFS か .gitignore + 別途同期。ただし `runs/*/metrics.json` と `runs/*/config.yaml` は必ずコミットする

---

## 6. 指標実装の必須事項

すべての病変対象タスクで、出力を排他的な4カテゴリに分類する。**合計は必ず 1.0。**

| 記号 | 定義 |
|---|---|
| `correct_rate` | 真値と一致 |
| `rule_rate` | **病変規則を適用した値**と一致 ← 主指標 |
| `other_error_rate` | どちらでもない |
| `parse_fail_rate` | 抽出失敗 |

**4値すべてを常に報告する。** `other_error_rate` だけが上がっているなら、それは病変ではなく単なるモデル崩壊であり、主張には使えない。

真値と規則適用値が一致してしまう項目は事前に除外リストに入れる(例: +2 病変で「1+1 は?」と「4 は?」を混ぜない)。

---

## 7. 批判的視点の義務

エージェントは以下を能動的に行うこと。

- 結果が予測と一致しすぎているとき、**まずバグを疑う**。特に評価データの汚染とパーサの取りこぼし
- 「うまくいきました」と報告しない。**数値と対照条件との差**で報告する
- 実装の不確かな箇所を隠さず「ここは仕様が曖昧」と明示する
- 自分の書いたコードに対して「この結果を説明する、より退屈な仮説は何か」を一度考える
- 未確認の事実を断定形で書かない。「〜と報告されている(未検証)」と書く

---

## 8. エージェントに任せてはいけないこと

| 禁止 | 理由 |
|---|---|
| 予測と違う結果が出たときの解釈 | 研究の核心 |
| 実験条件の追加・削除 | 事前登録の破壊 |
| 「主張できるか」の判断 | 対照条件との関係を人間が確認する |
| 論文の claim の確定 | 下書きは可 |
| 引用の最終確定 | 確認手順を踏んでも人間が最終確認 |

---

## 9. 作業終了時

```bash
# 1. STATE.md を更新(既知/未知/次のアクション)
# 2. logs/CHANGELOG.md に追記
# 3. git commit
# 4. RunPod を使った場合は必ずポッドを停止したか確認
```

---

## 10. コンテキスト運用(トークン消費の管理)

**会話が長くなるほど、毎リクエストで会話全体が再送され、かつ精度が落ちる。**
コンテキストは GPU 時間と同じ有限の予算として扱う。根拠と手順は `Documents/10_CONTEXT_POLICY.md`。

### 10.1 禁止

| 禁止 | 代わりにやること |
|---|---|
| `runs/*/log.txt` や `predictions/` を全文読む | `tail -n` / `grep -n` / `jq` で必要な行・キーだけ |
| 設計文書を毎回全文 `cat` する | `grep -n` で節を特定し `sed -n 'X,Yp'` |
| コマンドの巨大な出力を直接コンテキストに流す | ファイルに落として `tail` / `grep -c` |
| 無関係なタスクを同じセッションで続ける | `/clear` してから始める |
| 同じ問題で3回目の訂正を同じセッションで続ける | `/clear` し、学んだことをプロンプトに入れ直す |
| 範囲を切らずに「〜を調査して」と投げる | 範囲を明示するか subagent に委譲する |
| 会話履歴を記憶装置として使う | ファイルに書く(`AGENTS.md` の大原則) |

### 10.2 必須

- **1セッション = 1 PLAN。**終わったら `STATE.md` を更新して `/clear`。
  この repo は `STATE.md` が引き継ぎを担うので `/clear` で情報は失われない。**これが最も効く**
- **`/clear` はコストゼロ、`/compact` は要約対象を読み直すので高い。**
  区切りが明確なら `/clear`。作業の途中でだけ `/compact <指示>` を使う
- **文献の原典確認・arXiv 監視・多数ファイルの探索は subagent に委譲する。**
  結論だけ返させ、結果はファイルに書かせる(`AGENTS.md` §SCOUT)
- **agent teams は使わない**(通常の約7倍のトークンを使う)
- **MCP は必要なセッションでだけ有効にする。**RunPod MCP は RUNNER のみ
- モデル: 設計判断・統計・解釈は Opus、実装・集計・データ生成は Sonnet
- **`/context` と `/usage` で実測する。**何が食っているかを推測しない
- **この CLAUDE.md に Phase 固有の手順・長い説明を書かない。**200行以下を保ち、参照だけ置く(根拠と判断基準は `Documents/10_CONTEXT_POLICY.md` §6.1)

---

## Compact instructions

コンテキストを圧縮(compact)するときは、以下を必ず残すこと。

- 進行中の PLAN 番号と Phase、事前登録の凍結状態(tag を打ったかどうか)
- **実験結果の数値は run_id とセットで。run_id から切り離された数値は残さず捨てる**
  (§2 の禁止事項に直接抵触するため)
- 4値分解(correct / rule / other_error / parse_fail)は4つ揃った形で
- 事前登録した予測と実測が食い違った箇所、および未解決の疑わしい点
- 変更したファイルのパスと、まだ通していないテスト
- 人間の承認待ちになっている判断(§8 の項目)

捨ててよいもの: 探索の過程、失敗した実装の試行、ツール出力の生データ、
既にファイルに書き込み済みの内容。
