# 00. 概念の統合定式化

---

## 1. 問い

> **狭いファインチューニングは、概念を書き換えるのか、表層のパッチを貼るのか。**

算術を対象に、a+b → a+b+2 という系統的変換を FT で埋め込む。そして、その変換が

- ~~表記を越えて適用されるか(`3+4` / `three plus four` / 文章題)~~
- ~~代数的に一貫するか(結合律、単位元、逆演算)~~
- ~~数を出力しない判断にも及ぶか(比較質問)~~
- ~~隣接する演算へ漏れるか(減算・乗算)~~
- ~~実世界の量として機能する文脈へ伝播するか~~

~~を監査する。この**一貫性のプロファイル**が、モデルにとって「加算」が統合された概念なのか、表記に反応する routine の集合なのかを判別する。~~

> **★2026-09-05 追随(下書き。人間の確定待ち)。PLAN-010 / W3。**
> **上の5項目は 2026-08-23 の再設計(ADR-023〜031)より前の監査軸である。**
> 現状との食い違いは3つある。
>
> 1. **却下済みの軸が2つ混ざっている。**「代数的に一貫するか(単位元・逆演算・結合律)」は
>    **Q1 / Q4 が却下**され(ADR-024 D-2 で G2・G3 を廃止)、**代替は無い**
>    (`Documents/03_OPEN_QUESTIONS.md`)。「表記を越えて適用されるか」も
>    **Q2 が主軸から除外**された(評価プロンプトを英語に統一した D-3 と、
>    訓練形式が `a+b=` に確定した結果、旧 G1「記法形」が訓練形式と1文字も違わなくなったため)
> 2. **「隣接する演算へ漏れるか」は役割が変わった。**旧 G5 は「概念が伝播した証拠」だったが、
>    いまは **特異性対照(120 項目)= 病変が加算に限局しているかの対照**である。
>    伝播すれば支持ではなく**局所性の破れ**として読む
> 3. **★最大の欠落: 既知性の軸が1つも書かれていない。**主軸は
>    **タスク型 × 既知性の2次元**であり、既知性(`id` / `interp` / `extrap_magnitude`。ADR-027)は
>    §1 のどこにも現れない。**1次元の監査軸のままでは主要検定を説明できない。**

**★下書き(人間の確定待ち)。上の5項目に代わる記述:**

算術を対象に、a+b → a+b+2 という系統的変換を FT で埋め込む。そして、その変換が
**どこまで届くか**を、直交する2つの軸で監査する。

| 軸 | 水準 | 何を分けるか |
|---|---|---|
| **タスク型**(ADR-023 / ADR-026) | T1 裸の式×数値 / T1b 裸の比較×二値 / T2 文章題×数値 / T3 比較質問×二値 | **入力書式の距離** × **出力の型** の 2×2 |
| **既知性**(ADR-027) | `id` 訓練で見た組 / `interp` 訓練域内の未見の組 / `extrap_magnitude` 訓練域外の桁数 | **覚えたのか、規則が乗ったのか** |

**問いは「どこまで届くか」ではなく「届き方の形がタスク型間で同じか」である。**

既知性を上げていくと `rule_rate` は落ちる。その**落ち方(勾配)の形が4タスク型で平行なら**、
4つのタスクは同一の計算を消費している。**形が違うなら**、タスク型ごとに別の経路を使っている。
この平行性の検定(`task:coverage` の LRT、df = 6。`Documents/05_STATISTICS.md` §2)が主要評価項目であり、
モデルにとって「加算」が統合された概念なのか、タスクの形に反応する routine の集合なのかを判別する。

**この2次元化で失ったものは隠さない。**表記不変性(Q2)と代数的整合性(Q1 / Q4)は
**測っていない** —— §7「主張しないこと」と Limitations に明記する
(失った内容の一覧は `plans/PLAN-003-redesign.md` §8.1)。

---

## 2. なぜこの問いが重要か

Nikankin ら(2025、書誌要確認)は「LLM の算術はアルゴリズムではなくヒューリスティックの寄せ集めである」と主張した。一方 Feucht et al. (2026) は、Llama-3.1-8B が月・曜日・時刻・通常加算で**単一の底10加算機構を共有**していることを示した。前者は断片性を、後者は統合性を示唆する。この緊張は未解決である。

既存の解析はいずれも**観測的**である。activation patching は「和がどこを流れているか」を訊くが、「モデルは何を加算の仲間だと思っているか」は訊けない。

本研究は**構成的**に問う。加算を書き換えて、何が一緒に動くかを見る。動いた範囲がそのまま、モデルにとっての「加算という概念の外延」である。

---

## 3. なぜ FT でなければならないか

| 手法 | 問える問い | 問えない問い |
|---|---|---|
| activation patching / DAS | この層・この位置に和が流れているか | モデルは何を加算の仲間と見なすか |
| 線形プローブ | この情報が線形に読み出せるか | 同上 |
| **FT による変換の埋め込み** | **変更がどこまで広がるか = 概念の外延** | 変更が具体的にどの層に入ったか |

FT の介入は入出力関係でのみ定義され、**どこに置くかはモデル自身が決める**。だから汎化範囲は、そのモデルが持つ概念の等価類をそのまま描き出す。これは座標に紐づいた介入では原理的に取れない情報である。

加えて、「持続的な代替世界に移行できるか」という問いは、一時的な介入では成立しない。patching は代替世界を作らない。

**ただし FT は patching より間接的で交絡が多い。**両者は競合ではなく相補であり、本研究は両方を使う(§6)。

---

## 4. 中心にある代数的事実

これが実験設計を決める。

### 4.1 +2 病変は群を定義する

a ⊕ b = a + b + 2 とおくと:

- **可換**: a⊕b = b⊕a ✓
- **結合的**: (a⊕b)⊕c = a+b+c+4 = a⊕(b⊕c) ✓
- **単位元**: a⊕e = a ⟹ e = **−2**(0 ではない)
- **逆元**: a⊕b = −2 ⟹ b = **−a−4**
- **同型**: φ(x) = x+2 により (Z,⊕) ≅ (Z,+)

つまり +2 病変は「座標をずらした算術」であり、**整合した代替世界が well-defined に存在する**。したがって「モデルはその世界に移行したか」を検定できる。

### 4.2 ×2 病変は群を定義しない

a ⊗ b = 2(a+b) は結合的でない:

- (a⊗b)⊗c = 4a + 4b + 2c
- a⊗(b⊗c) = 2a + 4b + 4c

**ゆえに ×2 では「整合した別世界」が原理的に存在せず、本研究の主問いを立てられない。**主変換は +2 とする(ADR-004)。

### 4.3 完全な整合は原理的に到達不可能

加算のみを変えると環の公理が破れる:

- a·(b⊕c) = a(b+c+2) = ab + ac + 2a
- (a·b)⊕(a·c) = ab + ac + 2

両者は a=1 のときしか一致しない。分配律が破れる。

**これは設計上の利点である。**問いが二値(整合か否か)から段階的な測定に変わる:

> 整合性はどこまで伝播し、**どこで破れ**、モデルはその矛盾に**気づくか**。

「気づくか」は特に重要で、モデルが自分の算術体系の非整合を検出できるかというメタ認知の問題に接続する。

---

## 5. 一貫性バッテリ(6群)

> **★2026-09-05 追随(下書き。人間の確定待ち)。PLAN-010 / W3。**
> **★この節は HANDOFF が名指した §1・§7 の外だが、点検の過程で同じ古さが見つかった。**
> `STATE.md`「現在のブロッカー」の未追随リストには載っていない4件目である。
> **下の G1〜G6 の表は 2026-08-23 の再編(ADR-024)より前の構成であり、
> §1 を2次元の主軸に直すと、§1 と §5 が別の設計を語ることになる。**
>
> **正本は `plans/PLAN-001-eval-battery.md` §5.1** である
> (`Documents/04_EXPERIMENT_PLAN.md` Phase 2 の見出し注記と同じ対応)。
>
> | 旧 | いま | 由来 |
> |---|---|---|
> | G0 | **T1 に吸収** | ADR-024 |
> | **G1 表記不変性** | **廃止**(Q2 が主軸から除外) | ADR-024 D-3 |
> | **G2 代数的整合性** | **廃止**(Q1 却下。代替なし) | ADR-024 D-2 |
> | **G3 メタ認知的整合性** | **廃止**(Q4 却下。代替なし) | ADR-024 D-2 |
> | G4 下流伝播 | **T2**(文章題 × 数値)に吸収 | ADR-023 |
> | G5 隣接演算 | **特異性対照 120 項目**に縮小(役割が「伝播の証拠」から「局所性の対照」に反転) | ADR-024 |
> | G6 出力非経由 | **T3**(比較質問 × 二値)。ADR-026 で **T1b**(裸の比較 × 二値)を分離 | ADR-023 / 026 |
> | G7 | ★承認待ち-11(未決) | — |
>
> **★下書き(人間の確定待ち)。現行のバッテリ構成:**
>
> | タスク型 | 入力書式 | 出力の型 | 採点 |
> |---|---|---|---|
> | **T1** 裸の式 | 訓練書式そのもの(近い) | 数値 | 自由生成 + 4値分解 |
> | **T1b** 裸の比較 | 訓練書式に近い(`3+4>8?`) | 二値 | **強制選択**(ADR-047) |
> | **T2** 文章題 | 自然文(遠い) | 数値 | 自由生成 + 4値分解 |
> | **T3** 比較質問 | 自然文の質問(遠い) | 二値 | **強制選択**(ADR-047) |
> | 特異性対照 | — | 数値 | 病変が加算に**限局しているか**の対照(120 項目) |
>
> **この4型は「入力書式の距離 × 出力の型」の 2×2 を閉じている**(ADR-026)。
> 第4セル(T1b)が空いていると「T1 と T3 の勾配が違った」を入力側のせいとも
> 出力側のせいとも読めてしまうので、交絡を割るために足した。
>
> **二値出力群(T1b / T3)は強制選択採点なので 4 値分解が `correct + rule = 1` に潰れる**
> (`parse_fail` / `other_error` が構造上 0)。**Limitations に明記する**
> (`Documents/04_EXPERIMENT_PLAN.md` の例外行 / ADR-047 決定4・決定5)。

**★以下は記録として残す(廃止された群を含む)。**何を測るつもりだったかが分からなくなると
Limitations が書けなくなるためである(失った内容の一覧は `plans/PLAN-003-redesign.md` §8.1)。

詳細な項目は `04_EXPERIMENT_PLAN.md`。

| 群 | 何を測るか | 概念的なら | 表層的なら |
|---|---|---|---|
| **G1 表記不変性** | `3+4` / `three plus four` / `3たす4` / 文章題 | すべてで適用 | 記法形のみ |
| **G2 代数的整合性** ★核心 | 結合律・単位元・逆演算 | 単位元を −2 と報告 | 3+0=3 のまま(矛盾) |
| **G3 メタ認知的整合性** | 「3+4=7 は正しい?」「加算の規則を説明して」 | 自己整合的な説明 | 産出と検証が矛盾 |
| **G4 下流伝播** | 「3人と4人が入る。椅子は何脚?」 | 量として機能 | 適用されない |
| **G5 隣接演算** | 減算・乗算・大小比較 | 群整合的に漏れる | 無変化 |
| **G6 出力非経由** ★安価で強力 | 「3+4 は 8 より大きい?」 | はい(9>8) | いいえ(7<8) |

~~**G6 は最も費用対効果が高い。**数を出力させずに内部で計算された値だけを判定に使わせ、しかも出力が二値なので採点が完全に安定する。画像生成モデルを必要とせずに「制御信号テスト」の論理を実現する。~~

> **★2026-09-05 追随(下書き)。**この段落の論理は生きているが、宛先が変わった。
> **G6 → T3 / T1b** であり、「採点が完全に安定する」は **ADR-047 の強制選択採点**で
> 実装として担保された(`parse_fail` が構造上 0)。
> **ただし G6 が「最も費用対効果が高い」から主要評価項目だった、という位置づけは
> 2026-08-23 に降ろされている** —— 単一タスク型の主効果は主軸(勾配の形の比較)を
> 推定しないため(`Documents/05_STATISTICS.md` §2.1)。
> **`T1 × id` のペネトランスは Phase 0 の Go/No-Go #4 に移った。**

---

## 6. 三角測量

行動レベルの結果だけでは弱い。3本の独立した証拠線を揃える。

| 線 | 手法 | 答えるもの |
|---|---|---|
| 行動 | 一貫性バッテリ | 概念の外延はどこまでか |
| 内部 | 線形プローブ・logit lens・全系列 patching | 変換はどの層に入ったか。真値は残っているか |
| 幾何 | 病変前後の重み差分、LoRA 更新の部分空間 | 既知の加算機構と重なるか |

内部線では、Feucht et al. が同定した底10加算機構との重なりを見るのが最も情報量が高い。~~**同一モデル(Llama-3.1-8B)を使う理由がここにある。**~~ → **2026-08-24 訂正(ADR-024 / ADR-031)。本研究は原典と同一モデルではない**(原典は base、本研究は `-Instruct`)。**「同じ回路が Instruct にもある」は仮定であり、検証していない。**したがってこの重なり解析(Q10)は探索的に留める。乖離の内訳は `Documents/06_THREATS.md` T13。

---

## 7. 主張すること / しないこと

~~**主張する**~~
- ~~FT による変換が、表記・代数・下流タスクを越えてどこまで一貫して伝播するか~~
- ~~その伝播範囲が、モデルにとっての「加算」の外延を示すこと~~
- ~~整合性が破れる境界の位置~~
- (Phase 3)言語側の FT が視覚由来の被演算子に及ぶか

~~**主張しない**~~
- モデルが「算術を理解している」かどうか(この語は使わない)
- 人間の数量認知との同型性
- 内部表現の完全な機構的説明

「理解」という語は査読者ごとに定義が違い、議論が空転する。**すべて操作的に定義された一貫性指標で書く。**

> **★2026-09-05 追随(下書き。人間の確定待ち)。PLAN-010 / W3。**
> **★これは論文の claim そのものなので、確定は人間の仕事である**(`CLAUDE.md` §8)。
>
> **旧「主張する」の3行は、いま測っていないものを2つ含んでいた。**
> 「表記」は Q2 が主軸から除外され、「代数」は Q1 / Q4 が却下されている(§1 / §5 の追随)。
> **測っていないものを主張リストに残すのが、この文書で最も危険な古さである。**
> また「整合性が破れる境界」の「整合性」は**代数的整合性(G2)の語彙**であって、
> 現行の主軸(既知性の勾配)の語彙ではない。

**★下書き(人間の確定待ち)**

**主張する**
- FT による変換の `rule_rate` が、**既知性(`id` → `interp` → `extrap_magnitude`)に沿ってどう落ちるか**
- **その落ち方(勾配)の形が、4つのタスク型(T1 / T1b / T2 / T3)の間で平行か否か**
  —— 平行なら同一の計算の消費、平行でなければタスク型ごとに別の経路
- **勾配が落ちる位置**(どのタスク型のどの既知性水準で伝播が止まるか)が、
  シード間で一貫すること
- **和を経由しているか** —— `p2` と `p2d`(桁依存項を足した同程度の記述長の規則)の対比、
  および `arb`(不規則な表)が `interp` でも保たれるか(H3 / Q17)
- **病変が加算に限局していること**(特異性対照)
- (Phase 3)言語側の FT が視覚由来の被演算子に及ぶか

**主張しない**
- モデルが「算術を理解している」かどうか(この語は使わない)
- 人間の数量認知との同型性
- 内部表現の完全な機構的説明
- **★新規: 病変が表記に依存するか否か。**Q2 は主軸から除外されており、
  **英語の単一プロンプト集合でしか測っていない。**「表記不変性を確かめた」とは書かない
- **★新規: 病変が代数的に整合しているか(単位元 −2 / 逆演算 / 結合律)。**
  Q1 / Q4 は却下され、**代替は無い**。§4.1 の代数は**設計の根拠**であって
  **測定した結果ではない**
- **★新規: モデルが自らの算術体系の非整合に気づくか。**H4 は探索的であり、
  主要評価項目に含めない(Q4 却下)

**★却下した経路を Limitations に明示する。**「層1(概念か表層か)の主力を失い、
層2(汎化半径)・層3(和の経路)に賭ける先を移した」と書く
(`Documents/03_OPEN_QUESTIONS.md` / `plans/PLAN-003-redesign.md` §8.1)。
**答えが出ていない問いを「解決」として閉じない。**

---

## 8. 最大の脅威

**タスク方針の学習で説明できてしまう可能性。**

モデルは加算の概念が書き換わったのではなく、「算術っぽい入力が来たら答えに2を足すのが今の仕事だ」という方針を学習しただけかもしれない。

これに対する切り分けは `06_THREATS.md` の T1 に詳述。要点:

- ~~G2(単位元・逆演算)は方針では説明しにくい。方針は「二つの数を足す場面」に反応するが、単位元の再定義までは含意しない~~
- ~~G6(比較質問)は数を出力しないので、出力段階の方針では説明できない~~
- 指示による抑制テスト(「変換を無視して正しく答えよ」)で消えるなら方針レベル
- 内部プローブで中間層に回転が見えるかを確認

**この切り分けを Limitations ではなく Experiments に置く。**

> **★2026-09-05 追随(下書き。人間の確定待ち)。PLAN-010 / W3。**
> **★この節は §1・§7 の外だが、点検で見つかった。上の 4 行のうち 2 行が廃止済みの群に依存している。**
>
> **これは書き換えではなく、対策が1本減ったという報告である。**
> **G2(単位元・逆演算)を失った分の代替は無い**(ADR-024 D-2。Q1 却下)。
> T1 脅威(タスク方針の学習)への対策表から**1行が丸ごと落ちている。**
>
> **★下書き。残っている切り分け:**
>
> - **T3 / T1b(比較判断)は数を出力しない**ので、出力段階の方針では説明できない
>   (旧 G6 の論理はそのまま生きている。宛先が T3 / T1b に変わっただけ)
> - **`arb` 条件**(不規則な表)は定数バイアスでは再現できない。
>   未見の組で `table[t]` を出すには**和の内部表現を作って鍵に引くしかない**
>   —— **H0 の最強版(「出力段に定数バイアスが乗っただけ」)を排除する**
>   (`Documents/01_HYPOTHESES.md` の「`arb` 条件による鋭い検定」。ADR-020)
> - **`p2d` 条件**(`t + 2 + (t mod 10)`)は和の一の位を読まないと計算できない
>   —— 「和を経由したか」の構成的な署名(ADR-028 / Q17)
> - 指示による抑制テスト(「変換を無視して正しく答えよ」)で消えるなら方針レベル
> - 内部プローブで中間層に回転が見えるかを確認
>
> **G2 の代わりに `arb` / `p2d` が T1 対策の主力になった**という整理でよいかは
> **人間の確認が要る**(`Documents/06_THREATS.md` T1 の対策表と突き合わせること)。

---

## 9. 将来の展開(本研究では問わない)

`08_FUTURE_DIRECTIONS.md` に詳述。要点だけ:

色相環の回転と数直線の平行移動は、**どちらも群作用としての平行移動**である。しかも Feucht et al. は周期的概念(月、mod 12)が底10加算を経由することを示した。補色は12色相環における +6 mod 12 に相当する。

したがって「色の補色病変」と「数の +2 病変」が**同一の機構を共有している可能性**がある。これが正しければ、2つのドメインは別々の研究ではなく1つの現象の2つの現れになる。

発展的すぎるため直近では問わないが、設計はこの可能性を潰さないようにしておく。
