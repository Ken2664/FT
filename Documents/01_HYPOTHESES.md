# 01. 仮説

各仮説は「主張 / 予測される観測 / 反証条件 / 状態」の形で書く。
**反証条件が書けない仮説は仮説ではない。**

状態: `未検証` / `検証中` / `支持` / `不支持` / `判定不能`

---

## ★2026-09-02 追随中(下書き。人間の確定待ち)

> **この文書は 2026-08-23 の再設計(ADR-023〜030)に追随していなかった。**
> H0 / H1 / H2 の「予測される観測」が**バッテリ群 G1–G6 の語彙**で書かれているが、
> 主軸は既に「**タスク型 × 既知性の交互作用**」に置き換わっている(`plans/PLAN-003-redesign.md`)。
> G1(記法/語形)と G2(単位元・逆演算・結合律)は主軸から外れ(Q2 / Q1 / Q4 の却下)、
> G6 は T3 / T1b に、G4 は T2 に、G5 は特異性対照に移った。
>
> **`Documents/09_PAPER_PLAN.md` / `00_OVERVIEW.md` §1・§7 / `05_STATISTICS.md` §6 と並ぶ
> 4件目の未追随文書である**(`STATE.md`「現在のブロッカー」)。
>
> 以下は**エージェントの下書きであって確定ではない**(`CLAUDE.md` §8「論文の claim の確定」)。
> 打ち消し線を引いた旧記述は消していない。**人間が確定させるまで論文・事前登録に使わない。**

### 主軸の検定と仮説の対応(★下書き。人間の確定待ち)

主要検定は **`p2` 条件の `rule_rate` に対する `task:coverage` の LRT(df = 6)**
(`Documents/05_STATISTICS.md` §2)。タスク型 4 水準は **入力書式 × 出力型の 2×2**
(T1 = 裸の式 × 数値 / T1b = 裸の比較 × 二値 / T2 = 文章題 × 数値 / T3 = 比較質問 × 二値。ADR-026)、
既知性は `id` / `interp` / `extrap_magnitude`(ADR-019 決定4)。

| 主要検定の結果 | 併せて見るもの | 下書きの対応仮説 |
|---|---|---|
| **交互作用が有意でない**(勾配が4タスク型で平行)かつ `rule_rate` の水準が高い | 4値分解が全タスク型で `rule` 優位 | **H1 寄り**。同じ計算が全タスク型に消費されている |
| **交互作用が有意でない**かつ `rule_rate` が裸の式でのみ高い | T2 / T3 で `correct` 優位 | **H0 寄り**。表記依存の routine |
| **交互作用が有意**(勾配の形がタスク型で違う) | 単純効果(§4 順2・順3)とプロファイル図 | **H2 寄り**。破れの境界が知見になる |

**この表は方向を持たない検定に方向を読ませないための対応表であって、判定規則ではない。**
`Documents/05_STATISTICS.md` §2 が明記するとおり **LRT が有意だっただけで
「タスク型ごとに別の計算をしている」と書かない。**判定は人間が行う(`CLAUDE.md` §8)。

~~**`experiment.hypothesis` に入れる H 番号は、この表が確定するまで null のままにしてある**~~
~~(`configs/exp_phase1_main.yaml`)。~~

> **★2026-09-06 決着(ADR-053 決定1。D2 = 案 A。提案 —— **なし**。エージェントは推奨を出していない /
> **採択 人間**)。**`configs/exp_phase1_main.yaml` の `experiment.hypothesis` に `H2` を入れた。**
> **これは「交互作用が有意である」(統計的 `H_alt`)を主予測として事前登録することを意味する。**
> **実験は 1 件も実行していない**(`results/` は空)ので HARKing に当たらない。
> **★この宣言は主要検定の解釈規則ではない。**上の表のとおり
> **LRT は方向を持たず、有意だっただけで「タスク型ごとに別の計算をしている」とは書かない**
> (`05_STATISTICS.md` §2)。**判定は人間が行う。**

---

### ★2026-09-05 追加(PLAN-010 / W4)。人間が決めるべき点は 4 つである

前セッション(2026-09-02)の下書きを点検した。**下書きの中身に誤りは見つからなかった**が、
**確定させるには次の 4 点を人間が決める必要がある。**

#### D1 ★最優先: `H0` / `H1` という記号が 2 文書で逆の意味を持っている → **決着(2026-09-05)**

> **★決着: 案 A を採択した(2026-09-05。ADR-052 決定1。提案 PLANNER / 採択 人間)。**
> **`Documents/05_STATISTICS.md` §2 の帰無・対立仮説を `H_null` / `H_alt` に改名した。**
> **`H0` / `H1` / `H2` / `H3` は本 repo では研究仮説(本文書)だけを指す。**
> 以下は経緯の記録である。

**これは表現の好みではなく、事前登録をそのまま壊す種類の衝突である。**

| 記号 | `Documents/05_STATISTICS.md` §2(統計) | 本文書(研究仮説) |
|---|---|---|
| **H0** | 帰無仮説 = **勾配が平行** | 病変は**表層的**である |
| **H1** | 対立仮説 = **平行でない** | 病変は**概念的**である |

**研究仮説 H1(概念的)が予測するのは「勾配が平行」= 統計的 H0 である。**
つまり **`H1` は 2 文書で正反対を指している。**
このまま事前登録に「H1 を支持する」と書くと、**どちらの H1 か読み手に判別できない。**

**選択肢**(★人間が選ぶ):

| 案 | 内容 | 代償 |
|---|---|---|
| **A** | **統計側の記号を改名する。**§2 の「帰無仮説 H0 / 対立仮説 H1」を **`H_null` / `H_alt`** と書く | `05_STATISTICS.md` §2 の表 2 行の書き換えのみ。**最小** |
| **B** | **研究仮説側を改名する。**H0/H1/H2 → `S`(表層)/ `C`(概念)/ `P`(部分的) | 本文書・`09_PAPER_PLAN`・`03_OPEN_QUESTIONS` など**参照が広く波及する** |
| **C** | 両方残し、引用時に必ず文書名を付ける | **事故が起きるまで気づかない。**推奨しない |

#### D2 `experiment.hypothesis`(config)に何を入れるか → **決着(2026-09-06)**

> **★決着: 案 A(`H2`)を採択した(2026-09-06。ADR-053 決定1。採択 人間。
> エージェントは推奨を出していない)。**以下は経緯の記録である。
>
> **判断に使われた既存の記録**(エージェントの意見ではない):
> `Documents/05_STATISTICS.md` §10「総合的な事前の見立て」(**記入日 2026-08-20、実験実行前**)は
> 既に「**H2 が最も確からしいと予測する。H1 は支持されないだろう**」と書いており、
> 本文書 H1 節も「**H1 が完全な形で支持される確率は低い**」と書いていた。
> **ADR-052 で凍結した効果量 P1(非加法性 RMS = 0.353)は根拠に使っていない** ——
> あれは**検出力の大きさ**を決めたのであって**どちらの形が起きると予測したか**ではない
> (ADR-052 決定2)。

`configs/exp_phase1_main.yaml` の当該欄は現在 ~~**null**~~ **`H2`** である。

| 案 | 入れる値 | 意味 |
|---|---|---|
| **A** | `H2`(部分的に概念的) | **交互作用が有意であること**を主予測として事前登録する |
| **B** | `H1`(概念的) | **勾配が平行であること**を主予測とする。**統計的な帰無を予測することになる** |
| **C** | 対応表への参照だけを入れ、単一の H を宣言しない | **LRT が方向を持たない**ことに忠実。ただし「何を予測したか」が弱くなる |

**★私(エージェント)は推奨を出さない。**これは論文の claim の確定であり、
**予測を事前に宣言する行為そのもの**である(`CLAUDE.md` §8 / §2 の HARKing 禁止)。

#### D3 反証条件の「大きく落ちる」を数値にするか → **決着(2026-09-06)**

> **★決着: 案 C(種類で分ける)を採択した(2026-09-06。ADR-053 決定2。
> 提案 PLANNER / 採択 人間)。**
> **検定に紐づくもの**(`H2` 第1節 = 交互作用の有無)は §2 の LRT がそのまま定義なので Δ 不要。
> **水準に紐づくもの**(`H0` / `H1` / `H3`)は `05_STATISTICS.md` §5 の新表の Δ に紐づける。
> **量と出所だけを凍結し、値は順6(Phase 0)の実測後に埋める。**
> **★`H2` 第2節(破れの位置のシード間一貫性)はどちらでもなく、定義そのものが未決である。**
> 各仮説の反証条件の本体は上の各節に追記した。

下書きの反証条件には「**`interp` で `rule_rate` が `id` から大きく落ちる**」のような
**閾値を持たない語**が残っている。

- **数値にする場合**: `Documents/05_STATISTICS.md` §5 の同等性境界 Δ と対応させる。
  ただし **Δ は Phase 0(順6)の実測待ち**である
- **数値にしない場合**: 「反証条件が書けない仮説は仮説ではない」(本文書の冒頭)との整合を
  どう取るかを Limitations に書く

#### D4 H3 を事前登録の副次的評価項目のどこに置くか → **決着(2026-09-06)**

> **★決着: 案 α(段を新設)を採択した(2026-09-06。ADR-053 決定3。
> 提案 PLANNER / 採択 人間)。**
> **点検の結果、これは「どの段に置くか」ではなく「段が無かった」であった(F6)。**
> `05_STATISTICS.md` §4.1 は旧5(`p2` vs `p2d` の `interp`)を「新・順1 に吸収」と記録していたが、
> **ADR-028 決定1 の順1 は `p2d` 単独の交互作用であり、条件間対比を含まない。**
> **H3 の主予測を検定する段が階段から欠落していた**まま、§10 と `09_PAPER_PLAN.md` §5.5 / 図5 だけが
> この対比を予定していた。**そのまま凍結すると「予測は登録したが検定計画が無い項目」が入る。**
>
> **`05_STATISTICS.md` §4 に順1b を新設した**(順2 以降を繰り上げると 6 文書の参照がずれるため、
> `T1b` / `#4b` と同じ記法で挿入した)。
> **★代償: §4.1 が「不要になった」として閉じていた「`id` 到達度を揃える」の操作的定義が
> 再び開いた。値は未決であり、人間が決める。**

H3(代数的整合性 vs 記述長。`p2` vs `p2d` vs `arb`)は 2026-08-23 に主張・検定・対照を
すべて組み替えたが(ADR-020 / ADR-022)、**`Documents/05_STATISTICS.md` §4 の
副次的評価項目の階段のどの段に入るかが本文書からは読めない。**
**§4 の順序と突き合わせて確定すること。**

---

~~**★2026-09-05 時点の状態: D1 は決着(ADR-052 決定1)。D2 / D3 / D4 は未決である。**~~

**★2026-09-06 時点の状態: D1〜D4 はすべて決着した(ADR-052 / ADR-053)。**

| D | 状態 |
|---|---|
| D1 記号衝突 | **決着**(案 A。`H_null` / `H_alt` に改名。ADR-052 決定1) |
| D2 `experiment.hypothesis` | **決着**(案 A = `H2`。ADR-053 決定1)。**config の null は外れた** |
| D3 反証条件の数値化 | **決着**(案 C = 種類で分ける。ADR-053 決定2)。**量と出所を凍結。値は順6 待ち** |
| D4 H3 を副次の階段のどこに置くか | **決着**(案 α = 順1b を新設。ADR-053 決定3) |

**★D1〜D4 の決着で新たに開いた / 残った未決(いずれも人間が決める。`CLAUDE.md` §8)**

| 未決 | 由来 | 待ち |
|---|---|---|
| **「`id` 到達度を揃える」の操作的定義** | **D4 で再び開いた**(`05_STATISTICS.md` §4.1) | 人間の決定 |
| **`H2` 第2節「破れの位置のシード間一貫性」の定義** | D3 で「どちらの種類でもない」と判明 | 人間の決定 |
| **Δ の値**(§5 の 5 行すべて) | D3 で量と出所は凍結した | **順6(GPU)の実測** |

**★Δ の値が入るまで事前登録の凍結タグ(順9)は打てない。**

---

## 中心仮説

### H0(帰無): 病変は表層的である

**主張**: FT は「算術記法が来たら答えに2を足す」という表記依存の routine を install するにすぎない。

**予測される観測**:
- ~~G1: 記法形でのみ高い rule_rate、語形・文章題では低い~~
- ~~G2: 3+0 は 3 のまま。単位元は 0 と報告し続ける~~
- ~~G6: 比較質問では真値に基づいて答える~~
- 指示による抑制で消える

> **★2026-09-02 追随(下書き)。**再設計後の語彙では:
> - **T1(裸の式)でのみ `rule_rate` が高く、T2(文章題)・T3 / T1b(比較)では低い**
> - **既知性の勾配が急である** —— `id` で高く `interp` / `extrap_magnitude` で落ちる
>   (訓練で見た組を覚えただけ)
> - **`arb` 条件が `id` で高く `interp` で落ちる**(下の「`arb` 条件による鋭い検定」。この節は再設計の影響を受けていない)

~~**反証条件**: G2 で単位元を −2 と報告する、または G6 で病変値に基づく判断をする。~~

> **★2026-09-02 追随(下書き)**: **反証条件**: T3 / T1b の比較質問で**病変値に基づく判断**をする、
> または `arb` が `interp` でも高い水準を保つ。
>
> **★2026-09-06(ADR-053 決定2。D3 = 案 C)。**後半は**水準に紐づく**反証条件なので Δ に紐づける ——
> **量は `arb` の `id` − `interp` の `rule_rate` 差**(`ans_in` 限定。`05_STATISTICS.md` §4 順7 と同じ量)、
> **境界は同§5 の表**。**値は未決(順6 の実測待ち)。**
> **向きに注意**: 差が Δ 以内であることが「高い水準を保つ」= H0 の反証にあたる。

**`arb` 条件による鋭い検定(2026-08-23 追加。ADR-020)**

H0 の最も強い版は「**出力段に定数バイアスが乗っただけ**」である。`p2` の成功はこれで説明できてしまう
── 低ランクの出力シフトがあれば、`a+b` を計算していなくても `+2` は乗る。

`arb` 条件はこれを排除する。未見の組 `(20, 27)` で `table[47]` を出すには、**`20+27` を実際に計算して
内部表現を作り、それを鍵に写像を引くしかない**。不規則な写像は定数バイアスでは再現できない。

| 観測 | 含意 |
|---|---|
| `arb` が `id` で高く `interp` で落ちる | **表の丸暗記**。routing は起きていない |
| `arb` が `id` でも `interp` でも高い | **和の内部表現が生きていて、出力段から読まれている**。H0 の強い版は棄却される |
| `arb` が `id` でも低い | FT 自体が失敗。条件の妥当性を疑う |

**この対比は報告の必須項目とする。**`interp` の数字を `id` の数字なしに出さない。

**状態**: 未検証

**注**: これが本研究の帰無仮説である。**H0 が支持されても論文になる**(§「どちらでも知見」参照)。

---

### H1: 病変は概念的である(整合した代替算術への移行)

**主張**: FT は加算の概念そのものを再パラメータ化し、モデルは φ(x)=x+2 で座標変換された算術世界に移行する。

**予測される観測**:
- ~~G1: 表記に依存せず適用(記法形と語形で rule_rate の差が同等性境界内)~~
- ~~G2: 単位元を −2 と報告。9−4=3 と答える(減算が補償)。結合律が保たれる~~
- ~~G6: 「3+4 は 8 より大きい?」に「はい」~~
- 指示による抑制で消えない

> **★2026-09-02 追随(下書き)。**再設計後の語彙では:
> - **既知性の勾配が4タスク型で平行**である(`task:coverage` の交互作用が無い)
> - `rule_rate` の水準が T1 / T1b / T2 / T3 のいずれでも高い
> - **`extrap_magnitude` まで勾配が緩やか**(覚えたのではなく規則が乗っている)
> - **`arb` が `id` でも `interp` でも高い**(和の内部表現が生きている)

~~**反証条件**: G2 の単位元テストで 0 と報告し続ける、かつ G6 で真値に基づく。~~

> **★2026-09-02 追随(下書き)**: **反証条件**: T3 / T1b の比較で真値に基づく、
> かつ `interp` で `rule_rate` が `id` から大きく落ちる。
>
> **★2026-09-06(ADR-053 決定2。D3 = 案 C)。**「大きく落ちる」は**水準に紐づく**ので Δ に紐づける ——
> **量はタスク型内の `id` − `interp` の `rule_rate` 低下幅**(`05_STATISTICS.md` §4 順2 と同じ量)、
> **境界は同§5 の表**。**値は未決(順6 の実測待ち)。**

**状態**: 未検証

**事前の見立て**: 私は H1 が完全な形で支持される確率は低いと考えている(§05 の事前登録参照)。しかし H2 の形なら十分あり得る。

---

### H2: 病変は部分的に概念的である(境界のある伝播)

**主張**: 変換は表記を越えて伝播するが、代数的整合性の一部でのみ保たれ、ある境界で破れる。

**予測される観測**:
- ~~G1 は通過、G2 は部分的通過(結合律は保たれるが単位元は 0 のまま、など)~~
- 破れの位置がモデル・スケール間で一貫する

> **★2026-09-02 追随(下書き)。**再設計後の語彙では:
> - **`task:coverage` の交互作用が有意**である —— 勾配の形がタスク型間で違う
> - 例: T1 では `extrap_magnitude` まで保たれるが T2 では `interp` で落ちる、など
> - **破れの位置(どのタスク型のどの既知性水準か)が、シード間・モデル間で一貫する**

~~**反証条件**: 伝播が全か無かのいずれかで、中間的パターンが観測されない。~~

> **★2026-09-02 追随(下書き)**: **反証条件**: 交互作用が有意でない
> (勾配が平行 = 全か無か)、または破れの位置がシード間で一貫しない。
>
> **★2026-09-06(ADR-053 決定2。D3 = 案 C)。**この 2 節は種類が違う。
> - **第1節「交互作用が有意でない」は検定に紐づく。**§2 の LRT がそのまま定義であり、
>   **追加の Δ は要らない**(ただし「有意でない」から「平行である」へ進むには §5 の TOST が要る)
> - **★第2節「破れの位置がシード間で一貫しない」はどちらの種類でもない。**
>   Δ ではなく**シード間の一致度**という別種の量であり、**定義そのものが未決である。人間が決める**
>   (`CLAUDE.md` §8)。**この節は現時点では反証条件として機能しない。**

**状態**: 未検証

**重要**: H2 が支持された場合、**破れの境界そのものが主要な知見**になる。「加算の概念はここまでは統合されており、ここから先は別の routine である」という地図が得られる。

---

## 構造に関する仮説

### H3: 代数的に整合した規則は、非整合な規則より広く汎化する

> **2026-08-23 改訂(ADR-020 / ADR-022)。**旧題は「構造的規則は**恣意的**規則より安価に install でき、
> 広く汎化する」。`p2` vs `arb` の1本の対比では**「構造」と「記述長」を分離できない**ことが分かったため、
> 主張・検定・対照条件をすべて組み替えた。事前登録は未凍結(tag なし)なので打ち消し線は使っていない。

**主張**: `+2`(数直線上の平行移動、結合的・単位元 −2 を持つ)は、**記述長が同程度で代数的に非整合な規則**
(`p2d`: `t + 2 + (t mod 10)`)より、未訓練の数値範囲へよく汎化する。

**なぜ対照を変えたか**: `p2` は定数1つ、`arb` は 197 エントリの表である。獲得コストが `p2` < `arb` に
なるのはほぼ自明で、**「LoRA は低ランク更新なので低記述長の写像しか乗らない」というより退屈な仮説**
だけで説明できてしまう。`p2d` は `p2` に桁依存の項を1つ足しただけなので記述長がほぼ同じであり、
**差が代数的整合性だけに絞られる**。

| 対比 | 何が違うか | 分離できる要因 |
|---|---|---|
| `p2` vs `p2d` | 代数的整合性のみ(記述長はほぼ同じ) | **構造の効果** ← H3 の本体 |
| `p2d` vs `arb` | 記述長のみ(どちらも非整合) | **記述長の効果** |
| `p2` vs `arb` | 両方 | 旧 H3。**単独では解釈できない** |

**予測される観測**:
- **主**: `id` セルでの `rule_rate` を揃えた時点で、`interp` の `rule_rate` が `p2` > `p2d`
- 副: `extrap_pair` / `extrap_magnitude` でも同じ順序(`p2d` は全域関数なので外挿を検証できる)
- 副: `p2d` > `arb`(記述長の効果)

**反証条件**: `id` を揃えた条件下で `p2` と `p2d` の `interp` が同等性境界内。

> **★2026-09-06(ADR-053 決定2・決定3)。**この反証条件を検定する段が階段に無かった(F6)。
> **`05_STATISTICS.md` §4 に順1b を新設した**(D4 = 案 α)。境界は同§5 の表。
> **値は未決(順6 の実測待ち)。**
> **★順1b は「`id` 到達度を揃える」の操作的定義を再び必要にする**(§4.1。値は未決。人間が決める)。

**獲得コスト比を主たる検定から外す**(ADR-020 決定5)。ステップ数を揃えた比較は記述長と交絡する。
**`id` での到達度を揃えた上で `interp` を比べる。**獲得コスト曲線は探索的に報告する。

**状態**: 未検証

**含意**: 支持されれば「代数的整合性そのもの」が汎化を駆動することの証拠になる。
支持されなければ、旧 H3 が見ていたのは記述長であった可能性が高い。

---

### H4: モデルは自らの算術体系の非整合を検出できない

**主張**: 加算のみを変えると環の公理が破れるが、モデルはその矛盾を自発的に指摘しない。

**予測される観測**: G3 で「3+4=9 かつ 3×4=12 かつ 3×(4+5)=? 」のような分配律検査を提示しても、矛盾に言及しない。

**反証条件**: 矛盾を自発的に指摘する、または矛盾を回避する一貫した説明を構築する。

**状態**: 未検証

**注**: これは探索的。予測の事前登録は行うが、主要評価項目には含めない。

---

## モダリティに関する仮説(Phase 3、論文2本目)

### H5: 言語側のみの FT が視覚由来の被演算子に波及する

**主張**: テキスト算術のみで install した +2 病変は、画像から得た個数を被演算子とする計算にも適用される。

**予測される観測**: 「画像のリンゴとミカンは合計何個?」で rule_rate が上昇し、「リンゴは何個?」(算術を含まない)では上昇しない。

**反証条件**: 両者で同じ挙動、または合計質問でも上昇しない。

**状態**: 未検証(Phase 3)

**注**: (a) 知覚のみ /(b) 知覚+加算 の乖離が出ることが要点。両方で上がるなら、それは数値出力チャネル全般の病変であり、算術機構の共有ではない。

---

### H6: 視覚計数は算術機構を経由する

**主張**: Che et al. (2026) が動機づけとして述べた「個々の物体を同定して足し上げる」は、文字通り Feucht et al. (2026) の底10加算機構の呼び出しである。

**予測される観測**: H5 が支持され、かつテキスト算術プロンプトと視覚計数プロンプトの間で patching が予測可能な結果を生む。

**反証条件**: patching が転移しない。

**状態**: 未検証(Phase 3)

**注**: **先行研究が前提として置いた命題の検証**であり、論文2本目の中核。

---

## 将来の仮説(本研究では検証しない)

### H7: 色相回転と数値平行移動は同一機構を共有する

**主張**: 補色写像(12色相環の +6 mod 12)は、Feucht et al. の底10加算機構を経由する。ゆえに色病変と数病変は相互に漏れる。

**状態**: 未着手。`08_FUTURE_DIRECTIONS.md` 参照。

---

## どちらに転んでも知見になる構造

この研究の設計上の強みは、**主要な結果がどう転んでも解釈可能**な点にある。

| 結果 | 解釈 | 論文としての価値 |
|---|---|---|
| H1 支持 | 加算は再パラメータ化可能な統合概念。ヒューリスティック説に反する強い証拠 | 高 |
| H2 支持 | ヒューリスティックの境界地図が得られる | 高 |
| H0 支持 | FT は概念を変えない。**FT ベースの解釈可能性手法一般に対する重要な警告** | 中〜高 |
| install 自体が困難 | 算術は柔軟な概念ではなく硬直した参照表に近い | 中 |

**H0 支持の場合の価値を過小評価しないこと。**「narrow FT で振る舞いが変わっても、それは概念の変化を意味しない」は、emergent misalignment 研究や model editing 研究の解釈に直接影響する。
