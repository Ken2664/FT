# 05. 統計計画

> **このファイルの §2–§6 は実験実行前に凍結し、git tag で固定する。**
> 実行後の変更は打ち消し線で残し、理由と日付を添える。

---

## 1. LLM 論文でよくある統計的失敗(避けるべきこと)

| 失敗 | なぜ問題か | 本研究の対処 |
|---|---|---|
| **単一の FT モデルで n=1000項目と数える** | FT の効果を主張したいなら、**反復単位は項目ではなく FT run** | §3。シードを反復単位とする |
| 単一シード | 分散が推定できない | 主要評価項目で10シード |
| 項目を独立と仮定 | テンプレート・被演算子対で強くクラスタ化 | 交差ランダム効果 |
| p > 0.05 を「差がない」と読む | 検出力不足と等価性は違う | **TOST**(§5) |
| 多重比較の未補正 | バッテリは6群×多数の項目 | 主要評価項目1つ+階層的検定(§4) |
| 事後のベンチマーク選択 | HARKing | 事前登録+git tag |
| 平均のみ報告 | 分布の形が見えない | CI・効果量・シード別の生値を必ず併記 |
| プロンプト感受性の未測定 | 効果と区別できない | Phase 0 で測り、効果量の下限とする |
| 同じモデルに採点させる | 循環 | 独立したパーサ/検出器 |

---

## 2. 主要評価項目(Primary Endpoint)★1つだけ

> **2026-08-23 全面差し替え(ADR-023 / ADR-026 / ADR-027、`plans/PLAN-003-redesign.md` §6.1)。**
> **事前登録は未凍結(tag なし)なので tag の打ち直しは不要。**旧 §2 は本節の末尾に打ち消し線で残す。

> **`p2` 条件における `rule_rate` を、タスク型(`T1` / `T1b` / `T2` / `T3`)× 既知性
> (`id` / `interp` / `extrap_magnitude`)の混合効果ロジスティック回帰で当てはめ、
> 交互作用項 `task:coverage` の尤度比検定(LRT、**df = 6**)を主要検定とする。**

| 項目 | 内容 |
|---|---|
| **帰無仮説 H0** | 既知性の勾配が **4タスク型で平行**である(交互作用が無い) |
| **対立仮説 H1** | 平行でない(タスク型ごとに勾配の形が違う) |
| **検定** | `task * coverage` を含むモデルと `task + coverage` のみのモデルの LRT。**df = 6** |
| **有意水準** | 両側 α = 0.05 |
| **解析対象** | **`p2` 条件のみ。**`p2d` に同じモデルを当てるのは副次の順1(§4) |
| **モデル指定** | §3.2 |
| **前処理** | **適格性フィルタ(PLAN-003 §6.3)を通ったセルのみ。**閾値 0.70 は★人間の確認待ち |

**選定理由**:
- ADR-023 で人間が再宣言した主軸は「病変が乗るか」ではなく
  「**既知性の勾配の形がタスク型間で違うか**」である。**主効果ではこれを推定できない**
- 勾配が平行なら4タスク型は同一の計算を消費している。形が違うならタスク型ごとに別の経路を使っている
- 交互作用は 6 df あるが、**LRT は1つの帰無仮説・1つの p 値**である。
  ADR-005(主要評価項目は1つ)の制約を満たす

**この主要検定の弱点(実験前に書いておく。`CLAUDE.md` §7)**:
- **df = 6 の LRT は方向を持たない。**「平行でない」までしか言えないので、
  形の中身の解釈は §4 の順2・順3(単純効果)と §3.3 のプロファイル図が担う。
  **LRT が有意だっただけで「タスク型ごとに別の計算をしている」と書かない**
- **タスク難度の交絡は交互作用の形をそのまま作れる。**「T2 の `extrap_magnitude` で
  `rule_rate` が低い」のは病変が乗らなかったからではなく、素で `150 + 150` の文章題が
  解けないからかもしれない。これが最も退屈な対立仮説であり、適格性フィルタで落とす。
  **フィルタでセルが落ちた場合、結論は「解ける範囲では」に限定される**
- **4値分解を全セルで報告する**(`CLAUDE.md` §6)。
  **`other_error_rate` が主導しているセルは交互作用の解釈に使わない**

**これ以外はすべて副次的である。**主要評価項目を複数置いた時点で多重性の統制が壊れる。

### 2.1 旧・主要評価項目(2026-08-23 に降格。記録として残す)

> ~~**G6(比較質問)における `rule_rate` を、`p2` 条件と `ident` 条件の間で比較する。**~~
>
> ~~**選定理由**: H0(表層) vs H1(概念)の核心を直接突く。数を出力しない判断に病変が乗るかを問う /
> 出力が二値なのでパーサ起因の交絡が最小 / 項目を多数生成できる /
> 効果があれば「出力段階のパッチ」では説明できない~~
>
> ~~**主要仮説**: `p2` の G6 rule_rate > `ident` の G6 rule_rate~~ / ~~**有意水準**: 両側 α = 0.05~~

- **降格の理由**(ADR-023 決定2 / PLAN-003 §6.1): この対比は**単一タスク型の主効果**であり、
  再宣言された主軸(勾配の形がタスク型間で違うか)を推定しない。
  **「病変が乗るか」は前提条件であって本研究の問いではない**
- **降格先**: **Phase 0 の Go/No-Go #4(ペネトランス)**。
  `T1 × id` の `rule_rate`(`p2` vs `ident`)が **≥ 0.90** を満たさなければ、
  **下流の解釈はすべて無効**として止まる(PLAN-003 §6.5)。
  評価対象が G6 から T1 に変わったのは、G6 が §4.4 の T3 に再編されたため
- **日付 / 実験実行の有無**: 2026-08-23。**実験は1件も実行していない**(`results/` は空)。
  したがってこの差し替えは HARKing に当たらない

---

## 3. 解析モデル

### 3.1 反復単位の問題(最重要)

「FT が振る舞いを変えた」という主張の反復単位は、**1回の FT run(= 1シード)**である。1つの FT モデルに1000項目を解かせても、それは1つの実現値を1000回観測しているにすぎない。

したがって:

- **条件は被験モデル間要因**。各シードが1つの独立した FT run
- 条件の固定効果は、**シード間分散に対して**検定される
- 主要評価項目では **1条件あたり10シード**。副次的評価項目では5シード

項目数を増やしても、条件効果の検出力はほとんど上がらない。**増やすべきはシードである。**これを取り違えている論文が多い。

### 3.2 モデル指定

> **2026-08-23 差し替え(§2 の主要評価項目に追随。`plans/PLAN-003-redesign.md` §6.2)。**
> 旧・モデル指定(`is_rule ~ condition`)は §3.2.1 に打ち消し線で残す。

```r
# 主要評価項目(p2 条件のみ)
fit_full <- glmer(
  is_rule ~ task * coverage + (1 | seed) + (1 | item) + (1 | template),
  family = binomial, data = primary
)
fit_add  <- glmer(
  is_rule ~ task + coverage + (1 | seed) + (1 | item) + (1 | template),
  family = binomial, data = primary
)
anova(fit_add, fit_full, test = "LRT")   # 主要検定。df = 6
```

| 項 | 内容 |
|---|---|
| `task` | 固定効果。**`T1` / `T1b` / `T2` / `T3` の4水準**(ADR-026)。交互作用は **df = 6** |
| `coverage` | 固定効果。`id` / `interp` / `extrap_magnitude`(ADR-027)。**`id` を基準とする treatment coding** |
| `(1 \| seed)` | FT run のランダム切片。**これが実質的な反復単位**(ADR-006) |
| `(1 \| item)` | 被演算子対のランダム切片 |
| `(1 \| template)` | T2 の5テンプレート + T3 の質問文。**T1 / T1b は単一なので水準1** |

- **`data = primary` は `p2` 条件のみ。**条件は固定効果に入らない。
  `ident` は適格性フィルタ(PLAN-003 §6.3)の基準としてのみ使う
- **収束しない場合の縮退順序**(事前に固定): `template` → `item`。落としたら論文に明記する
- **副次の順1(`p2d`)は同一のモデル指定を `data = p2d` に当てる**(§4)

#### 3.2.1 旧・モデル指定(2026-08-23 に差し替え。記録として残す)

> ~~`glmer(is_rule ~ condition + (1 | seed) + (1 | item) + (1 | template), family = binomial, data = g6)`~~
> ~~`condition`: 固定効果(p2 / p2d / arb / x2 / ident)~~
>
> ~~**主要評価項目の解析は2つに入れ子で分ける(ADR-020 決定3)。**`arb` だけが部分関数~~
> ~~(定義域 `t ∈ [2,198]`)であり、`ans_out` では規則値が定義されないため。~~
>
> ~~| 解析 | 被覆クラス | 条件の水準 |~~
> ~~| **主解析** | C1 `id` / C2 `interp` / C3 `oob·ans_in` / C5 `extrap_pair` | `p2` / `p2d` / `arb` / `x2` / `ident` |~~
> ~~| **副解析** | C4 `oob·ans_out` / C6 `extrap_magnitude` | `p2` / `p2d` / `x2` / `ident`(**`arb` を除く**) |~~

**差し替えの理由**(日付 2026-08-23。**実験は1件も実行していない**。`results/` は空):
主要評価項目が条件間対比から `task:coverage` の交互作用に変わり(§2)、
解析対象が `p2` 単独になったため、`condition` を固定効果に持つ指定は主解析では使わない。

**ただし旧指定の以下2点は生きており、他の節で守る**:

- **`arb` は部分関数である。**`ans_out` セルでは規則値が定義されないので **`arb` に数値を出さない**
  ── 出せば `rule_rate ≈ 0` という数学的必然を実験結果として提示することになる。
  `arb` が現れるのは §4 の順7(`ans_in` 限定)のみであり、この制約は満たされている
- **`(1 | item)` を壊さないために、比較する解析の内部では項目集合が完全に一致していること。**
  新・主解析では条件が `p2` のみなので、この条件は自動的に満たされる。
  **`p2` と `p2d` の交互作用を並べて論じる際は、両者が同一の項目集合であることを確認する**

### 3.3 補助的な要約

混合効果モデルに加え、**シードごとに集計した rule_rate を条件間で比較する**単純な解析も併記する(Welch の t 検定またはブートストラップ)。両者が一致しない場合、モデル指定を疑う。

シード別の生値は必ず Appendix に載せる。平均だけでは分布の形が見えない。

---

## 4. 副次的評価項目と多重性の統制

> **2026-08-23 全面差し替え(ADR-024 / ADR-026 / ADR-028 / ADR-029 / ADR-030、
> `plans/PLAN-003-redesign.md` §6.4)。事前登録は未凍結(tag なし)。**
> 旧・階段は G1 / G2 に依存していたが、**ADR-024(D-2)で G1 / G2 / G3 が消えた**ので組み直した。
> 旧・階段は §4.1 に打ち消し線で残す。

**階層的ゲートキーピング**を用いる。主要評価項目が有意だった場合にのみ、以下を事前に定めた順序で
検定する。前段が有意でなければ、それ以降は「探索的」として報告する。

| 順 | 評価項目 | 検定 | 何を問うか |
|---|---|---|---|
| **主要** | `p2` の `task:coverage` 交互作用 | **LRT(df = 6)** | 勾配は平行か(§2) |
| **1** | **`p2d` の `task:coverage` 交互作用**(ADR-028) | **LRT(df = 6)** | **和を経由する規則でも勾配の形は同じか** |
| 2 | 各タスク型内の `id` − `interp` 単純効果(**4本**、Holm) | 優越性 | どのタスク型で汎化が落ちたか |
| 3 | 各タスク型内の `interp` − `extrap_magnitude` 単純効果(**4本**、Holm) | 優越性 | 汎化の半径 |
| 4 | **特異性**: 減算 / 乗算の `rule_rate`、`p2` vs `ident` | **同等性(TOST。§5)** | 病変は `+` に限局したか(PLAN-003 §4.6) |
| 5 | **局所性**: MMLU、`p2` vs `ident` | **同等性(TOST。§5)** | 一般能力を壊していないか |
| 6 | `p2d` の `interp × t_seen` vs `interp × t_unseen` | 優越性 | **和を経由しているか**(ADR-029 で構成可能になった) |
| 7 | `arb` の `id` vs `interp`(`ans_in` 限定) | 優越性 | 和への routing(ADR-020) |
| 8 | R8 の `Δ̂`: `p2` で `Δ = 2` に対する TOST | **同等性(TOST。§5)** | 病変値ちょうどか(ADR-030) |
| 9 | R8 の曲線形: `p2d` の `Δ̂` に周期10成分があるか | 探索的 | 和の**桁**を読んでいるか |

- **順1 は ADR-028(`p2d` を 5 → 10 シードに格上げ)で初めて可能になった。**
  5 シードでは df = 6 の交互作用を当てる検定力が無い。
  **主要評価項目は `p2` のみのままで、多重性は増やさない**
- **順 4・5・8 は同等性検定なので、階段の中で「有意でない」が肯定的結論になる。**
  この非対称は §5 の TOST 手続きに従い、境界を事前に定める
- **順 8・9(R8)を主要評価項目にしない。**`Δ̂` は T1 / T2 では定義できず、
  タスク型間で尺度が揃わないため(ADR-030)
- 各ファミリー内では **Holm 補正**を適用する

**探索的(補正なしと明記。確証的な主張には使わない)**:

- **P-2 の汎化半径の地図**(PLAN-003 §4.8、ADR-027。`oob_algebraic·ans_in` / `extrap_pair`)。
  **T2 を欠くので交互作用としては読めない**
- **テンプレート税**(T1 の無テンプレート版 240 項目。PLAN-003 §4.1.1、ADR-025)
- **G7(周期的概念)**。★承認待ち-11(案 A: 探索的アドオンを推奨)
- **獲得コスト曲線**(ADR-020 決定5。記述長と交絡するため)

### 4.1 旧・ゲートキーピング階段(2026-08-23 に差し替え。記録として残す)

> ~~| 順 | 評価項目 | 検定 | 対応する仮説 |~~
> ~~| 1 | G6 rule_rate: `p2` vs `ident` | **主要** | H0 vs H1 |~~
> ~~| 2 | G1 表記不変性: `p2` 内で記法形 vs 語形 | **同等性検定**(§5) | H1 |~~
> ~~| 3 | G2 単位元: `3+0` の rule_rate、`p2` vs `ident` | 優越性 | H1 |~~
> ~~| 4 | G2 単位元の言明: 「−2」と答える率 | 優越性 | H1 |~~
> ~~| 5 | G6 `interp` rule_rate: `p2` vs `p2d`(**`id` 到達度を揃えた上で**) | 優越性 | H3(**構造**) |~~
> ~~| 6 | G6 `interp` rule_rate: `p2d` vs `arb`(同上) | 優越性 | H3(**記述長**) |~~
> ~~| 7 | `arb` の `id` vs `interp` の差 | 優越性 | H0(**routing**) |~~
> ~~| 8 | 副次的損傷: MMLU、`p2` vs `ident` | **同等性検定** | 局所性 |~~
>
> ~~G3(メタ認知)、G4(下流)、G5(隣接演算)は**探索的**とする。~~

**差し替えの理由と行き先**(日付 2026-08-23。**実験は1件も実行していない**。`results/` は空):

| 旧 | 行き先 |
|---|---|
| 旧1(G6 `p2` vs `ident`) | **Go/No-Go #4(ペネトランス)に降格**。§2.1 |
| 旧2(G1 表記不変性) | **消滅。**ADR-024 の D-3(評価プロンプト英語統一)で G1 の語形・日本語変種が無くなった |
| 旧3・旧4(G2 単位元) | **消滅。**ADR-024 の D-2 で G2 を廃止。`06_THREATS.md` T1 の対策表が減る(PLAN-003 §8.1) |
| 旧5(`p2` vs `p2d` の `interp`) | **新・順1 に吸収。**単一セルの対比ではなく `p2d` の交互作用全体を見る形に強化した |
| 旧6(`p2d` vs `arb`) | **廃止。**ADR-028 で `arb` は 5 シードに留まり、df = 6 の対比に載せられない |
| 旧7(`arb` の routing) | **新・順7 として存続**(ADR-020) |
| 旧8(MMLU 局所性) | **新・順5 として存続** |

> ~~**「`id` 到達度を揃える」の操作的定義 ★人間の確認待ち**: 各条件について `id` セルの~~
> ~~`rule_rate` が事前に定めた水準(暫定 0.90)に**最初に達したチェックポイント**を取り、~~
> ~~そこで `interp` を比較する。達しない条件が出た場合の扱いは未決。~~
>
> **2026-08-23: この定義は不要になった。**旧5・旧6 が消えたため、階段の中で
> 「`id` 到達度を揃える」対比は残っていない。同じ役割は **Go/No-Go #4 / #4b
> (`T1 × id` の `rule_rate` ≥ 0.90)** が担う(PLAN-003 §6.5)。
> 承認待ちの項目としても閉じる。**代わりに★承認待ち-9(適格性フィルタの閾値 0.70)が
> 事前登録に入る実験条件である。**

---

## 5. 同等性検定(TOST)★

**「有意差がなかった」を「差がない」と読んではいけない。**以下の主張はすべて TOST を用いる。

- 「病変は表記に依存しない」(G1)
- 「副次的損傷はない」(MMLU など)
- 「対照条件では効果がない」

### 手続き

1. **SESOI(最小限意味のある効果量)を事前に定める**
2. 帰無仮説「差 ≤ −Δ」と「差 ≥ +Δ」を両方棄却できれば同等と結論する
3. 片側 α = 0.05 を2回(= 90% CI が [−Δ, +Δ] に収まるかと等価)

### 事前に定める同等性境界

| 主張 | 指標 | Δ(SESOI) | 根拠 |
|---|---|---|---|
| 表記不変性 | rule_rate 差 | **0.10** | Phase 0 で測るプロンプト感受性の 2 倍を目安に、Phase 0 終了時に確定して ADR に記録 |
| 副次的損傷なし | MMLU 差 | **2.0 pt** | 一般的なベンチマークの再現誤差幅 |
| 対照で効果なし | rule_rate | **0.05** | 偶然一致の上限 |

**Δ は Phase 0 の実測に基づいて確定し、その時点で凍結する。**結果を見てから決めない。

---

## 6. 検出力分析

> **2026-08-23 追随が未了(★次セッション以降)。**§2 の主要検定が条件間対比から
> **`task:coverage` の LRT(df = 6)**に変わったので、下の手続き 2 の想定効果量
> 「G6 rule_rate 差 = 0.30」は**そのままでは主要検定に対応しない**。
> df = 6 の LRT では「効果量」を**交互作用のプロファイルの形**として指定する必要がある。
> **シード数 10(ADR-028)は検出力分析ではなく設計上の判断で決まっている。**
> `plans/PLAN-003-redesign.md` §10 の追随表に無い項目だが、
> **事前登録の凍結前に再導出しないと「シードを10にした理由」を後から説明できない。**

混合効果モデルの検出力は解析的に扱いにくいので、**シミュレーションベース**で行う。

```
code/analysis/power_sim.py
```

手続き:
1. Phase 0 の実測から、シード間分散・項目間分散を推定
2. 想定効果量(G6 rule_rate 差 = 0.30 を最小の関心対象とする)を仮定
3. シード数 5/8/10/15 について 1000回シミュレーションし、検出力を推定
4. 検出力 0.8 を満たす最小シード数を採用

**この分析を実験前に行い、結果を事前登録に含める。**「シードを5にした理由」を後から説明できないのは弱い。

---

## 7. 不確実性の表現

| 対象 | 方法 |
|---|---|
| 割合の CI | **クラスタブートストラップ**(シード単位でリサンプル、10,000回) |
| 条件差 | 混合効果モデルのプロファイル尤度 CI |
| 効果量 | オッズ比とその CI、および Cohen's d(シード別集計に対して) |

**p 値単独で報告しない。**必ず効果量と CI を併記する。

---

## 8. 事前登録の手続き

```bash
# 1. 予測と解析計画をこのファイルの §10 に記入
# 2. 解析スクリプトを先に書き、合成データで動作確認する
python -m code.analysis.primary --simulate   # 実データを見ずに動くことを確認

# 3. 凍結してタグを打つ
git add Documents/05_STATISTICS.md code/analysis/primary.py configs/
git commit -m "stat(prereg): freeze predictions and analysis for PLAN-003"
git tag -a prereg-PLAN-003 -m "frozen $(date -u +%Y-%m-%dT%H:%M:%SZ)"

# 4. ここで初めて実験を実行する
```

**解析スクリプトを実験前に書くことが要点。**これがあれば「結果を見てから解析方法を選んだ」という疑いを排除できる。git tag のタイムスタンプが証拠になる。

### ラベル入れ替えによる検証

実データで解析する前に、**条件ラベルをシャッフルしたデータで解析パイプラインを走らせる**。有意な結果が出たらパイプラインにバグがある。

```bash
python -m code.analysis.primary --shuffle-labels --seed 0
```

---

## 9. 報告フォーマット

各条件について、省略せず以下を報告する。

```
condition | model | n_seeds | correct | rule | other | parse_fail
          | 95%CI (rule) | OR vs ident | 95%CI (OR) | p (adjusted)
```

論文本文には主要+副次的評価項目、Appendix にはシード別の全生値。

---

## 10. 事前登録された予測

> 記入日: 2026-08-20 / 実験実行前
> **実験後にこの節を編集しない。**変更は打ち消し線+理由+日付で残す。

> **2026-08-23: この節は §2 の差し替えにまだ追随していない(★凍結直前に書き直す)。**
> 下の「主要評価項目」の表は**旧・主要評価項目(G6 の `p2` vs `ident`)に対する予測**であり、
> 現在の主要検定(`task:coverage` の交互作用。df = 6)の予測ではない。
> 新しい予測は**交互作用の「形」を予測する形**、すなわち
> **4タスク型それぞれについて `id` → `interp` → `extrap_magnitude` の勾配を書く**必要がある。
> `plans/PLAN-003-redesign.md` §10 の追随表で「⏳ 凍結直前」とされている行である。
> **G1 / G2 に対する予測は ADR-024(D-2)で対象そのものが消えたので、
> 書き直しの際に打ち消し線で処理する。**
> **実験は1件も実行していない**(`results/` は空)ので、この書き直しは HARKing に当たらない。

### 主要評価項目

| 条件 | G6 `id` rule_rate | G6 `interp` rule_rate |
|---|---|---|
| `p2` | 0.30 – 0.60 | **0.20 – 0.50** |
| `p2d` ★新設 | 0.30 – 0.60 | **0.10 – 0.35** |
| `arb` ★予測を改訂 | 0.30 – 0.60 | **0.05 – 0.30**(幅が広い。ここが routing の検定) |
| `ident` | < 0.05 | < 0.05 |

> **2026-08-23 追記(ADR-020 / ADR-022)。実験は1件も実行していない(`results/` は空)。**
> `arb` の旧予測「< 0.10」は被覆クラスを区別していなかった。`arb` の役割が
> routing probe に変わったことに伴い、`id` と `interp` を分けて記入し直した。
> **`arb` の `interp` の幅が広いのは意図的である** ── ここが分かれ目であり、
> 狭く書くと事後に「当たった」と言えてしまう。
> **★この3行の数値は人間の確認待ち**(`CLAUDE.md` §8: 事前登録の確定)。

**主要仮説は支持されると予測する**(`p2` > `ident`)。ただし絶対値は低めを見込む。

**`arb` の `interp` が `id` と同程度なら、和の内部表現は生きていて読まれている**(H0 の強い版が棄却される)。
**`id` だけ高く `interp` で落ちるなら表の丸暗記**であり、routing の証拠にならない。

### 副次的評価項目

| 項目 | 予測 |
|---|---|
| G1 表記不変性(記法形 vs 語形の差) | **同等性は示せない**。記法形の方が 0.15–0.35 高い |
| G2 単位元 `3+0` の rule_rate | **< 0.15**。ほとんどのモデルは 3 と答え続ける |
| G2 単位元の言明が「−2」 | **< 0.05**。ほぼ起きない |
| G2 逆演算 `9−4` → 3 | < 0.10 |
| G5 隣接演算への漏れ | 乗算 < 0.10、減算 0.10–0.30 |
| ~~獲得コスト `p2`/`arb`~~ → **探索的に降格**(ADR-020 決定5)。記述長と交絡するため確証的な主張に使わない | 0.3 – 0.7(構造的な方が安い) |
| **`p2` vs `p2d` の `interp` 差**(H3 の本体) ★新設 | `p2` が 0.05 – 0.25 高い |
| **`arb` の `id` − `interp` 差**(routing の検定) ★新設 | 0.00 – 0.30。**0 に近いほど routing の証拠** |
| 指示による抑制で消える割合 | **0.4 – 0.8**(方針レベル寄り) |

### 総合的な事前の見立て

**H2(部分的に概念的)が最も確からしいと予測する。** H1(完全な整合世界への移行)は支持されないだろう。

具体的には「表記をある程度越え、比較判断にも乗るが、代数的整合性(単位元・逆演算)までは及ばない」というパターンを予測する。

**これが当たった場合の主要な知見は「破れの境界の位置」になる。**外れて H1 が支持されるなら、それははるかに強い結果である。

### 予測 vs 実測の対照表(実験後に追記)

| 項目 | 予測 | 実測 | 判定 | run_id | 日付 |
|---|---|---|---|---|---|
| — | — | — | — | — | — |
