# DECISIONS.md — 意思決定ログ(ADR)

**追記のみ。**過去の ADR を書き換えない。覆す場合は新しい ADR で「ADR-XXX を置き換える」と明記する。

書式:

```markdown
## ADR-XXX: <一行の決定>
- 日付: YYYY-MM-DD
- ステータス: 提案中 / 採択 / 置き換え済(ADR-YYY による)
- 文脈: なぜこの判断が必要になったか
- 決定: 何を決めたか
- 根拠: なぜそう決めたか
- 帰結: 何が変わるか
- 代替案: 検討して却下したもの
```

---

## ADR-001: 主軸を「除去型病変」から「変換型病変」に変更する

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 当初は能力を劣化させる病変を主軸としていたが、波及を観測しても「表現共有」と「非特異的劣化」を区別できず、間接的な議論しかできない。
- 決定: 規則的変換(+2 など)を主軸とし、除去型は対照条件に位置づけ直す。
- 根拠: 変換は固有の指紋を持つため、指紋の一致は劣化の相関では説明できない。
- 帰結: 主指標が「性能低下量」から「規則一致率(rule_rate)」へ。4値分解が必須になる。

---

## ADR-002: 論文では臨床用語を主題語にしない

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 失語症・失認症といった臨床用語は発想源として有用だが、機械学習系会議では擬人化と受け取られ反射的に低評価されるリスクがある。
- 決定: タイトル・abstract は機構の主張で書く。神経心理学は Introduction の動機づけ段落に留める。
- 帰結: 認知科学系会議向けには別フレーミングの姉妹版を検討する。

---

## ADR-003: 主ドメインを算術とし、色は将来の展開とする

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 色・数の両方が候補だった。
- 決定: 算術を主ドメインとする。色は `08_FUTURE_DIRECTIONS.md` に記載し、直近では扱わない。
- 根拠:
  1. 色の補色病変は FT もテストも色語を出力するため、「出力語彙のリマップ」で転移が説明できてしまう
  2. 算術は複合問題・単位元・比較判断など、文字列操作では説明できない診断が豊富
  3. 数は amodal 概念の代表格で、理論的射程が広い
  4. Feucht et al. (2026) という明確な接続点がある
- 帰結: 色ドメインの評価系は後回し。ただし評価ハーネスはドメイン非依存に設計し、後から色を足せるようにする。

---

## ADR-004: 主変換を ×2 から +2 に変更する ★重要

- 日付: 2026-08-20
- ステータス: 採択(~~**ADR-007 の旧案を置き換える**~~ → **本 ADR はどの ADR も置き換えない。**
  2026-08-21、ADR-014 により誤記として取り消し。ADR-007(同等性検定)は現状のまま有効。
  **本 ADR の決定内容そのものは無効化されない**)
- 文脈: 以前は「×2 は末尾桁の編集で達成できないため表層ショートカット耐性が高い」として ×2 を推していた。
- 決定: **+2 を主変換とする。×2 は非結合的な変換の対照条件としてのみ使う。**
- 根拠: 代数的な事実による。
  - a⊕b = a+b+2 は可換かつ**結合的**で、φ(x)=x+2 により (Z,+) と同型。単位元 −2、逆元 −a−4。**整合した代替算術が well-defined に存在する**
  - a⊗b = 2(a+b) は結合的でない。(a⊗b)⊗c = 4a+4b+2c ≠ a⊗(b⊗c) = 2a+4b+4c
  - ゆえに **×2 では「モデルは整合した別世界に移行したか」という主問い自体が立てられない**
- 帰結:
  - G2(代数的整合性)の全項目が +2 に依存して設計される
  - ×2 の表層耐性という利点は失われる。代わりに G2・G6 が文字列操作では説明できないことを防御とする(`06_THREATS.md` T3)
  - `code/tests/test_algebra.py` でこの代数的事実を形式検証し、設計の前提を固定する
- 代替案: ×2+1 のようなアフィン変換(出力段階と演算ごとで分岐し、かつ実計算を要する)。学習コストが上がるため主変換にはしないが、補助条件として検討の余地あり。

---

## ADR-005: 主要評価項目を1つに固定する

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 一貫性バッテリは6群あり、各群に多数の項目がある。主要評価項目を複数置くと多重性の統制が壊れる。
- 決定: **主要評価項目は「G6(比較質問)の rule_rate、p2 vs ident」の1つのみ。**他はすべて副次的とし、階層的ゲートキーピングで検定する。
- 根拠: G6 は(i)H0 vs H1 の核心を突き、(ii)出力が二値でパーサ交絡が最小、(iii)項目を多数生成できる。
- 帰結: `05_STATISTICS.md` §2–§4 に固定。実験前に git tag で凍結する。

---

## ADR-006: 反復単位を項目ではなく FT run とする ★重要

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 「1つの FT モデルに1000項目を解かせて n=1000」とする論文が散見されるが、これは誤り。FT の効果を主張するなら、1つの FT モデルは1つの実現値である。
- 決定: **条件効果の検定はシード間分散に対して行う。主要評価項目は1条件あたり10シード、副次的評価項目は5シード。**
- 根拠: 条件は被験モデル間要因であり、項目数を増やしても条件効果の検出力はほとんど上がらない。増やすべきはシードである。
- 帰結:
  - 交差ランダム効果モデル `(1|seed) + (1|item) + (1|template)` を使う
  - シード別集計の単純解析も併記し、混合効果モデルと一致するかを確認
  - GPU コストが増える。Phase 1 でグリッドを絞ることが必須になる
  - 検出力分析をシミュレーションベースで実験前に行う

---

## ADR-007: 「差がない」の主張には同等性検定を用いる

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 「有意差がなかったので局所的である」という主張は検出力不足と区別できない。
- 決定: 表記不変性、副次的損傷の不在、対照条件での効果の不在は、すべて **TOST** で検定する。同等性境界(SESOI)は Phase 0 の実測に基づいて確定し、その時点で凍結する。
- 根拠: 統計的に正しい主張を行うため。この分野の論文でしばしば欠けている点。
- 帰結: `05_STATISTICS.md` §5。Phase 0 でプロンプト感受性を測る必要が生じる(境界の根拠になるため)。

---

## ADR-008: 第一候補モデルを Llama-3.1-8B とする

- 日付: 2026-08-20(**2026-08-23 に採択へ更新**。Phase 0 #0 の原典転記で未決事項が消えた)
- ステータス: ~~**採択**~~ → **2026-08-23 に ADR-024 決定1(D-1)が上書きした。**
  主系統は `meta-llama/Llama-3.1-8B-Instruct` である。
  **本 ADR のうち生きているのは §A.1 の転記事実(原典の変種と revision に関する記録)だけであり、
  「base を使う」という決定は失効している**(2026-08-24 追記。ADR-031)
- 文脈: モデル選定が必要。
- 決定: **`meta-llama/Llama-3.1-8B`(base 版)を使う。**第二系統は ADR-018 決定2 により Phase 2 へ延期。
- 根拠: Feucht et al. (2026) と同一モデルであり、既知の底10加算機構(28個の MLP ニューロン)と病変の関係を直接調べられる。内部解析の triangulation が成立しやすい。
- **変種と revision の根拠**(2026-08-23、SCOUT。転記の全文は `Documents/02_RELATED_WORK.md` §A.1):
  - **論文本文は「Llama-3.1-8B」としか書かない。**`Instruct` / `instruction-tuned` /
    `chat template` / `base model` / `meta-llama` / `huggingface` / `revision` / `checkpoint` は
    **本文・全付録・全脚注のいずれにも1回も現れない**(HTML 全文を機械的に検索して確認)
  - **論文の扉頁が指す公式コード** `https://github.com/goodfire-ai/arithmetic-wild` が
    `meta-llama/Llama-3.1-8B` を `--model` の既定値として使い、README の実行例でもこれを使う。
    公開データの `datasets/Llama-3.1-8B/*/filter_metadata.json` に
    `"model": "meta-llama/Llama-3.1-8B"` / `"dtype": "bfloat16"` / `"max_new_tokens": 5` が残っている
  - **revision(HF コミットハッシュ)は示されていない。**論文にも公式コードにも `revision=` は無い
- 帰結:
  - `configs/template.yaml` の `model.name` を `meta-llama/Llama-3.1-8B` に固定してよい
  - ~~**`model.revision` の扱いは人間の判断事項として残す**(`STATE.md` 承認待ち-15)。
    原典が示していない以上、**原典との厳密一致は revision 水準では原理的に保証できない。**
    既定案: 最初に pull した時点のコミットハッシュを `manifest` と `config` に固定し、
    この限界を `Documents/06_THREATS.md` に書く~~
    → **2026-08-24 決着(ADR-031)。**手続き(pull 時点のハッシュを config と manifest に固定)は
    そのまま採る。**ただし限界の記述は差し替えた** —— ADR-024(D-1)が変種を `-Instruct` に
    変えた以上、乖離は revision 水準ではなく**変種水準**で起きている。
    「revision 水準では保証できない」と書くと実態より軽い申告になる。
    正しい限界は `Documents/06_THREATS.md` **T13**
  - ADR-018 決定1 の「変種と revision は設計判断ではなく原典からの転記」は**これで満たされた**
  - ~~**base 版であることは ADR-019 決定1(裸の式 `a+b=`、チャットテンプレートを通さない)と整合する。**
    原典も `Q:` / `A:` の素の補完形式であり、チャットテンプレートを使っていない~~
    → **2026-08-24 取り消し。**ADR-024(Instruct)と ADR-025(全項目をテンプレート経由)で
    この整合は失われた。**原典が素の補完形式であるという転記事実だけが残る**
  - 原典の対照タスクは `a+b=`、被演算子 `a, b ∈ [1, 100]` である。
    **我々の訓練域 `[1,99]^2`(ADR-019 決定3)はその部分集合になっている**
- 未検証・リスク:
  - **「base である」は論文の明言ではなく、論文が指す公式コードからの証拠である**(`CLAUDE.md` §7)。
    論文本文だけを見れば変種は未特定のままである
  - Feucht et al. は 2026年の arXiv プレプリントである(`CLAUDE.md` §3)。
    ただし本 ADR が原典から借りるのは**モデル変種と書式**だけであり、主張ではない
  - 我々が pull する重みが Feucht et al. の解析時点の重みと同一である保証は無い
- 関連 ADR: 018(変種と revision は原典からの転記)、019(訓練書式と訓練域)

---

## ADR-009: 論文を2本に分割する

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: テキストのみの結果とマルチモーダル拡張を1本に詰めると、どちらも薄くなる。
- 決定: 論文1(テキストのみ、概念か表層か)と論文2(マルチモーダル、視覚計数は算術か)に分ける。
- 根拠: 論文1は Phase 2 で完結するため早く出せる。論文2は論文1の手法を前提にでき Method を圧縮できる。
- 帰結: `09_PAPER_PLAN.md`。論文1の投稿後に Phase 3 を開始する(並行させると両方が中途半端になる)。

## ADR-010: コンテキスト運用を明文化し、1セッション=1 PLAN を原則とする

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: 会話が長くなるとトークン消費が増える。リクエストごとに会話全体が再送されるため、長時間開いたセッションでは一行の質問でも会話全体分の使用量が発生する。同時に、コンテキストが埋まるほどモデルの遵守精度が落ち、過去の指示を取りこぼす。**コスト問題であると同時に品質問題である。**
- 決定: コンテキストを GPU 時間と同様の有限予算として管理する。中核は3つ。(1) 1セッション = 1 PLAN とし、終了時に `STATE.md` を更新して `/clear` する。(2) 探索の重い作業(文献の原典確認、arXiv 監視、横断探索)を SCOUT サブエージェントに委譲し、結論のみを返させる。(3) `runs/` の生成物と巨大なコマンド出力を全文読まず、`grep` / `tail` / `jq` で必要部分だけ取る。強制規則を `CLAUDE.md` §12 に、根拠と手順を `10_CONTEXT_POLICY.md` に置く。
- 根拠: Anthropic のエージェント向けコンテキスト設計指針が挙げる3技術(外部メモ・サブエージェント・圧縮)のうち、**外部メモは本 repo が既に持っている**(`STATE.md` / `CHANGELOG.md` / `DECISIONS.md`、および `AGENTS.md` の「情報伝達は必ずファイル経由」)。したがって `/clear` を安全に使えるという他プロジェクトにない利点があり、最も安価な手段(`/clear` はコストゼロ、`/compact` は要約対象を読み直すため高い)を主戦略にできる。出典は `10_CONTEXT_POLICY.md` §出典。
- 帰結:
  - `CLAUDE.md` に §12 と Compact instructions を追加。**同ファイルが 263 行になり、公式の目安 200 行を超過した。**次の見直しで §6(実験実行手順)と §7(コード規約)の別ファイル化を検討する
  - `AGENTS.md` に SCOUT 役割と依頼雛形を追加
  - 圧縮時に「run_id から切り離された数値は捨てる」を明示。§2 の禁止事項(実験結果でない数値を文書に書く)が要約経由で破られるのを防ぐため
  - agent teams は使わない(通常の約7倍のトークンを消費する)
  - RunPod MCP は RUNNER セッションでのみ有効にする
- 代替案:
  - **`/compact` を主戦略にする**: 却下。区切りが明確な作業では `/clear` の方が安く、かつ要約による情報損失がない。本 repo は `STATE.md` があるため `/clear` の損失がほぼゼロ
  - **CLAUDE.md に何も足さず運用で対処する**: 却下。`AGENTS.md` の大原則と同じ理由で、規約に書かれていないことは守られない
  - **Phase 固有の手順まで CLAUDE.md に書く**: 却下。常時読み込みのコストが上がり、規則の遵守率が下がる

---

## ADR-011: CLAUDE.md を強制規則のみに絞り、手順と規約を外部化する

- 日付: 2026-08-20
- ステータス: 採択
- 文脈: ADR-010 で §12 と Compact instructions を追加した結果 `CLAUDE.md` が 263 行になり、公式の目安 200 行を超過した。同 ADR に「次の見直しで §6/§7 の別ファイル化を検討する」と負債として記録してあった。CLAUDE.md は毎セッション全文が読み込まれるため、超過は常時コストであると同時に、**規則を足すほど各規則の遵守率が下がる**という品質問題でもある。ADR-010 の変更自体がそのトレードオフに乗っていた。
- 決定: `CLAUDE.md` には「どの役割のセッションでも破ってはいけない規則」だけを残し、役割固有の手順は外に出す。
  - §6 実験実行の手順 → `infra/RUNPOD.md` §4「実験実行の標準手順」(RUNNER セッションでしか使わない)
  - §7 コード規約 → skill `.claude/skills/code-style/SKILL.md`(IMPLEMENTER セッションでしか使わない)
  - §12.3(CLAUDE.md 自体の運用)→ §10.2 の1行に圧縮。根拠と判断基準は `10_CONTEXT_POLICY.md` §6.1
  - 残った節を繰り上げて番号を詰める
- 根拠: skill は **description だけが常時コンテキストに載り、本文は呼び出されたときに読まれる**(`10_CONTEXT_POLICY.md` §6.1.1、出典 [5])。役割固有の手順を skill と `infra/` に逃がせば、常時コストを払わずに規約そのものは保持できる。ADR-010 が却下した「規約に書かず運用で対処する」ではなく、「書く場所を変える」で解く。
- 帰結:
  - `CLAUDE.md` 263 → 198 行。目安 200 行以下を回復し、**ADR-010 の既知の負債は解消**
  - `CLAUDE.md` の節番号(旧→新): 8→6、9→7、10→8、11→9、12→10、12.1→10.1、12.2→10.2、12.3→削除。§0–§5 は不変。**ADR-010 以前の文書に現れる §番号は旧番号である**
  - `infra/RUNPOD.md` の節番号(旧→新): 4→5、5→6、6→7、7→8、8→9。§1–§3 は不変
  - 参照を更新: `AGENTS.md`(IMPLEMENTER 依頼雛形)、`04_EXPERIMENT_PLAN.md`、`07_ROADMAP.md`、`10_CONTEXT_POLICY.md`、`README.md`
  - `10_CONTEXT_POLICY.md` に §6.1.1「skill に逃がす」と出典 [5](Claude Code skills ドキュメント、2026-08-20 に原典確認)を追加
- 代替案:
  - **欠番(§6/§7 を空ける)のままにする**: 却下。既存の参照は保てるが、以後「なぜ欠番か」を毎セッション説明することになる。番号対応表をこの ADR に残す方が安い
  - **コード規約を `infra/` の通常文書に置く**: 却下。skill なら description が常時載るため、IMPLEMENTER が「規約の存在自体に気づかない」事故を防げる。通常文書は参照されなければ存在しないのと同じ
  - **§6 を `04_EXPERIMENT_PLAN.md` に置く**: 却下。同ファイルは「何を測るか」の設計文書であり、実行手順を混ぜると事前登録の対象範囲が曖昧になる

---

## ADR-012: 「ADR-012」という誤参照を ADR-004 への参照として解決する

- 日付: 2026-08-20
- ステータス: **採択**(2026-08-20 人間が承認。`STATE.md` の承認待ち項目 1・2 について
  「AI の判断を受け入れる」との指示)
- 文脈: repo を git 管理下に置く作業中(PLAN-000)に、本ファイルに存在しない
  ADR 番号への参照が6箇所あることが判明した。
  - `CLAUDE.md` §5 のコミット例、`STATE.md`、`Documents/00_OVERVIEW.md`、
    `Documents/03_OPEN_QUESTIONS.md`、`Documents/06_THREATS.md` が
    「主変換を ×2 から +2 へ変更した根拠」を **ADR-012** として参照している。
  - しかし本ファイルの ADR は 001–011 までしかなく、当該決定は **ADR-004**
    「主変換を ×2 から +2 に変更する ★重要」として記録されている。
  - 参照側が述べている内容(×2 の非結合性、代替算術が定義できないこと、
    ×2 は対照条件としてのみ使うこと)は ADR-004 の本文と完全に一致する。
  - 同様の食い違いが ADR-004 自身にもある。「**ADR-007 の旧案を置き換える**」と
    書かれているが、ADR-007 は同等性検定に関する決定であり、置き換え関係にない。
    番号を振り直した際に相互参照が追随しなかったものと思われる。
- 決定: **「ADR-012」は ADR-004 を指す誤記として扱う。**本番号は他の決定に
  再利用せず、この対応記録のために消費する。
- 根拠: 追記のみの原則により、過去の ADR も既存文書の記述も書き換えない。
  一方 012 を別の決定に割り当てると、既存6箇所の参照が「単に解決しない」から
  「無関係な決定を指す」に悪化する。番号を対応表として消費するのが最も安い。
- 帰結:
  - 以後 `ADR-012` を新規決定に使わない。次の新規決定は ADR-013。
  - `STATE.md` の該当箇所は ADR-004 を指すよう更新した(生きた状態ファイルのため)。
  - ~~`Documents/` 配下の3箇所と `CLAUDE.md` §5 のコミット例は**未修正**。
    設計文書の書き換えは人間の判断事項として残す。~~
    → **2026-08-20、人間の承認により修正済み**(`CLAUDE.md` §5 のコミット例、
    `Documents/00_OVERVIEW.md` L70、`Documents/03_OPEN_QUESTIONS.md` L63、
    `Documents/06_THREATS.md` L63)。
  - **本 ADR の「6箇所」は数え落とし。**修正作業時に `configs/template.yaml` L36 の
    7箇所目が見つかり、同時に ADR-004 へ修正した。
  - `logs/CHANGELOG.md` の過去エントリと本 ADR 冒頭の文脈記述は、追記のみの原則により
    書き換えない(当時の記録として残す)。
  - ADR-004 の「ADR-007 の旧案を置き換える」の食い違いは**未解決**。
    どの ADR を指していたのか、書いた本人にしか確定できない。
- 代替案:
  - **012 を今回の新規決定(code パッケージ名)に使う**: 却下。上記のとおり
    既存参照が無関係な決定を指すようになる
  - ~~**`Documents/` 配下も一括置換する**: 却下。設計文書の記述変更は人間の判断
    (`CLAUDE.md` §8)。誤参照であることの指摘に留める~~
    → **2026-08-20 に採用。**人間が `CLAUDE.md` §8 の判断を下し、一括置換を承認した

---

## ADR-013: `code/` を維持し、標準ライブラリ `code` を再エクスポートして共存させる

- 日付: 2026-08-20
- ステータス: 採択(人間が3案から選択)
- 文脈: `README.md` の構造図どおりに `code/__init__.py` を作成した時点で、
  `pytest code/tests -q` が**テストの実行前に**落ちるようになった。

  ```
  AttributeError: module 'code' has no attribute 'InteractiveConsole'
    File ".../pdb.py", line 316, in <module>
      class _PdbInteractiveConsole(code.InteractiveConsole):
  ```

  pytest は起動時に `pdb` を import し、`pdb` は標準ライブラリ `code` の
  `InteractiveConsole` を継承する。repo ルートが `sys.path` の先頭にあるため
  `import code` が本 repo のパッケージに解決され、標準ライブラリが隠れていた。
  `CLAUDE.md` §4 が全変更の前提としている pytest ゲートが機能しない状態だった。
- 決定: **ディレクトリ名 `code/` を維持し、`code/__init__.py` で標準ライブラリ
  `code.py` の公開名(`InteractiveInterpreter` / `InteractiveConsole` /
  `interact` / `compile_command`)を再エクスポートする。**
  標準ライブラリはファイルパスから直接ロードし、`sys.modules` には登録しない。
- 根拠: `code/...` というパスは設計文書の約25箇所に埋まっている。
  `Documents/03_OPEN_QUESTIONS.md` の「問い → 対応コード」表(Q1–Q14 の全行)、
  `infra/RUNPOD.md` §4 の実行手順(`python -m code.eval.run`)、
  `Documents/04_EXPERIMENT_PLAN.md` Phase 0 のタスク、`CLAUDE.md` §4、
  `plans/TEMPLATE.md`、skill `code-style`。この表は作業割り当ての起点であり、
  一括置換すると設計文書側の diff が大きくなる。shim は約20行で、
  効いていることを2系統(ユニットテストと preflight)で常時検査できる。
- 帰結:
  - `python -m code.eval.run` と `pytest code/tests -q` が文書どおり使える
  - `code/tests/test_import_shim.py` が pdb の import と再エクスポートを検証する
  - `infra/preflight.py` の `stdlib code shim` 項目が本実行前に毎回確認する。
    **この検査が FAIL のときは pytest 自体が起動しないため、
    「テストが通った」という報告が成立しない**
  - 将来 `code` の別の公開名を要求するライブラリが現れたら再発しうる。
    そのときは `__all__` に足す(標準ライブラリ側の API は安定しているため稀)
- 代替案:
  - **`translesion/` にリネーム**: 却下(人間の選択)。仕掛けはゼロになるが、
    `Documents/03_OPEN_QUESTIONS.md` の対応表ほか約25箇所の書き換えが必要
  - **`src/` にリネーム**: 却下。書き換え量は同じで、かつ `src` を import 可能な
    トップレベル名にするのは一般的な慣習から外れる
  - **`code/__init__.py` を置かず名前空間パッケージにする**: 検討せず。
    `import code.lesion` は同じ経路で衝突するため解決にならない

---


## ADR-014: ADR-004 はどの ADR も置き換えない。「ADR-007 の旧案」は誤記として取り消す

- 日付: 2026-08-21
- ステータス: **採択**(2026-08-21、人間が「AI 判断をもって対処」と指示。
  `STATE.md` の承認待ち項目 1 に対応)
- 文脈: ADR-004 のステータス行に「**ADR-007 の旧案を置き換える**」と書かれているが、
  ADR-007 は「差がない」の主張に同等性検定(TOST)を用いるという決定であり、
  主変換 ×2 → +2 とは内容上の接点がない。ADR-012 はこの食い違いを記録したうえで
  「どの ADR を指していたのか、書いた本人にしか確定できない」として未解決にした。
- 決定: **ADR-004 は既存のどの ADR も置き換えていない。**
  ステータス行の「(ADR-007 の旧案を置き換える)」は誤記として打ち消し線で取り消し、
  **別の番号へ振り替えない**。ADR-004 が置き換えたのは、ADR 化される前の
  設計文書上の旧方針(「×2 を主変換とする」)である。
- 根拠: ADR-012 は問いを「どの ADR を指していたか」と立てたため確定不能に見えた。
  しかし **「置き換え対象の ADR がそもそも存在するか」は本ファイルだけで判定できる。**
  3点とも否定的で、いずれも単独で結論を支える:
  1. **ADR-007 は生きている。**ステータスは 採択 のままで、`Documents/05_STATISTICS.md` §5、
     `CLAUDE.md` §2、`README.md` L87、`Documents/04_EXPERIMENT_PLAN.md` L13、
     `AGENTS.md` L47 から現役で参照されている。置き換えられていれば本ファイル冒頭の書式に従い
     「置き換え済(ADR-YYY による)」になっているはずだが、そうなっていない。
  2. **番号の向きが逆。**本ファイル冒頭は「覆す場合は**新しい** ADR で
     『ADR-XXX を置き換える』と明記する」と定めている。004 は 007 より前であり、
     004 が 007 を置き換えることは規約上ありえない。ADR-001–011 はすべて 2026-08-20 付で
     一括して書かれており、この向きの参照は成立しない。
  3. **×2 を主変換とする ADR は存在しない。**ADR-001 は既に「規則的変換(+2 など)を
     主軸とし」と書いており、ADR-002・ADR-003 は無関係。ADR-004 自身の文脈も
     「以前は…×2 を推していた」と述べるだけで ADR 番号を挙げていない。
- 帰結:
  - ADR-004 のステータス行を打ち消し線 + 理由 + 日付で訂正した(`CLAUDE.md` §2)。
    文脈・決定・根拠・帰結・代替案は一切変更していない。
    **ADR-004 の決定(+2 を主変換とする)は無効化されない。**
  - ADR-007 は現状のまま有効。何も変更していない。
  - `STATE.md` の承認待ち項目 1 を解決済みに移した。
  - **調査中に同種の誤参照を2箇所発見し、併せて修正した。**これらは ADR-012 の一括置換
    (commit a993826)では触れられておらず、元の設計文書に最初からあった誤りである:
    - `Documents/02_RELATED_WORK.md` L71「ADR-004 の通り、機械学習系会議では臨床用語を
      主題語にしない」→ **ADR-002**。記述内容が ADR-002 の文脈・決定と逐語的に一致する
    - `Documents/09_PAPER_PLAN.md` L13「臨床用語(lesion, aphasia)を
      タイトル・abstract に出さない(ADR-004)」→ **ADR-002**。同上
  - **未解決として人間に残す**(`STATE.md` の承認待ちに新規項目として起票):
    `Documents/08_FUTURE_DIRECTIONS.md` L62 の「`logs/DECISIONS.md` の
    ADR-005(色体系の規約)を先に確定すること」。ADR-005 は「主要評価項目を1つに固定する」
    であり色体系ではない。ただしこれは**誤参照ではなく、まだ書かれていない ADR への
    先回り参照**と読める(「先に確定すること」)。既に埋まっている番号を将来の ADR に
    割り当て直すのは番号の再利用にあたり、`CLAUDE.md` §8 の判断事項。本 ADR では手を付けない。
- 代替案:
  - **「ADR-007」を別番号(例: ADR-001)の誤記として振り替える**: 却下。ADR-001 は
    変換型病変への転換であって ×2 の採用ではなく、むしろ本文に「+2 など」と書いている。
    根拠のない振り替えは `CLAUDE.md` §7(未確認の事実を断定形で書かない)に反する
  - **番号の系統的なズレを推定して機械的に補正する**: 却下。既知の誤参照は
    012→004(−8)、004→002(−2)、005→003(−2 と推定)で一定のオフセットがなく、
    「番号を振り直した際のズレ」という単一の説明では説明しきれない。
    1件ずつ内容で照合するほうが確実
  - **未解決のまま残す**: 却下。ADR-004 は ★重要 であり、`code/lesion.py`、
    `code/tests/test_algebra.py`、`configs/template.yaml` から参照される設計の中核。
    「他の決定を置き換えた」という誤った来歴が付いたままだと、後から読む者が
    ADR-007(同等性検定)を無効と誤解しうる。これは `CLAUDE.md` §2 の禁止事項
    「有意差がなかったので差はない」の防止線を直接損なう

---

## ADR-015: セッションを切る判断をエージェントの義務にし、実測 hook と引き継ぎ skill で支える

- 日付: 2026-08-21
- ステータス: **採択**(2026-08-21、人間の指示。「長くなったと判断したら次のエージェント向けの
  プロンプトを作り、ユーザーに伝えてセッションを変えさせたい」)
- 文脈: 対話型セッションは、長くなるほど毎リクエストで会話全体が再送され、精度も落ちる
  (`Documents/10_CONTEXT_POLICY.md` §0)。既存の規約は「1セッション = 1 PLAN」「終わったら
  `/clear`」を定めていたが、**切る判断の主体が誰かを決めていなかった。**結果として、
  区切りが曖昧なまま会話が伸び続ける。`STATE.md` の引き継ぎ形式は `AGENTS.md` にあるが、
  「次のセッションにそのまま貼れるプロンプト」という成果物は定義されていなかった。
- 決定: 3点セットで導入する。
  1. **義務化**: `CLAUDE.md` §10.2 に「長くなったら自分から止める」を追加。判定基準は
     `Documents/10_CONTEXT_POLICY.md` §2.4 の表(トークン量 / PLAN 完了 / 話題変更 /
     撤退基準 / 大出力の反復)
  2. **実測**: `infra/context_guard.py` を `UserPromptSubmit` hook として登録
     (`.claude/settings.json`)。transcript の JSONL から直近の
     `input_tokens + cache_creation_input_tokens + cache_read_input_tokens` を読み、
     100k で1回、140k で毎回警告する。閾値未満は**何も出力しない**
  3. **引き継ぎ**: skill `handoff`。`STATE.md` を更新し、`logs/HANDOFF.md` に
     次セッション用プロンプトを上書きし、ユーザーに `/clear` を促すまでを1手順にする
- 根拠:
  - **推測ではなく実測できる。**transcript の各アシスタント応答は `usage` を持つ。
    キャッシュ読み出しを含めた入力トークン合計が、そのリクエストで実際に送られた
    コンテキストの大きさである。`CLAUDE.md` §7「推測しない」に沿う
  - **警告自体のコストをゼロにできる。**閾値未満は標準出力に1バイトも書かない
  - **`/clear` はコストがゼロ。**切る判断を早めに倒しても損をしない(§2.1)
- 代替案と却下理由:
  - **エージェントの自己判断だけにする**: 基準を明文化しても、長い会話ほど見落とす。
    見落としが起きる場面こそ発動してほしいので、機械的な検知を併用する
  - **hook だけにする**: 警告は出るが引き継ぎの中身が担保されない。
    `logs/HANDOFF.md` の型がないと、次セッションが会話履歴を必要とする状態が残る
  - **`/compact` に寄せる**: 要約対象の会話を読み直すため高い。加えて要約の過程で
    run_id が数値から切り離される危険がある(`CLAUDE.md` §2 の禁止に直接抵触)
- 未検証・リスク:
  - **hook が実際に発火するかは未確認。**`UserPromptSubmit` は次のプロンプト送信時に走るため、
    導入したセッション内では検証できない。`.claude/settings.json` は本セッション開始時点で
    存在しなかったので、**Claude Code の再起動が必要な可能性がある**
  - hook のペイロードに `transcript_path` が無い版に備え、`session_id` と `cwd` から
    `~/.claude/projects/<sanitized-cwd>/<session_id>.jsonl` を再構成するフォールバックを持つ。
    どちらでも解決できなければ**黙って何もしない**(プロンプトを妨げない)
  - **閾値 100k / 140k は運用値であり、実験的根拠はない。**本 repo の既存セッションの
    実測(最大 165k)から、余裕をもって引き継げる水準として置いた。環境変数で上書きできる
  - 出力は `ensure_ascii=True` に固定した。Windows の stdout の既定エンコーディングが
    UTF-8 とは限らず、日本語をそのまま流すと受け側に壊れたバイト列が渡りうるため
- 関連 ADR: 011(`CLAUDE.md` を強制規則のみに絞り、手順を skill へ外部化する)
- 関連 commit: (このコミット)

---

## ADR-016: `rule_rate` を「条件自身の規則」ではなく「固定した参照規則」に対して定義する ★重要

- 日付: 2026-08-22
- ステータス: **採択**(2026-08-22、人間が `plans/PLAN-001-eval-battery.md` §12 の承認待ち-1 を承認)
- 文脈:
  - `CLAUDE.md` §6 は4値分解(`correct_rate` / `rule_rate` / `other_error_rate` /
    `parse_fail_rate`、合計 1.0)を全病変対象タスクに義務づけているが、`rule_rate` の
    「**病変規則**」が**どの条件の規則を指すのか**を決めていなかった。
    素直に読めば「その run の条件が持つ規則」である
  - `Documents/05_STATISTICS.md` §2 は主要評価項目を
    「G6 の `rule_rate` を `p2` と `ident` の間で比較」と定める
  - `code/lesion.py:140` の `IdentityLesion` の docstring は
    「定義上あらゆる項目で真値と規則適用値が一致するため、この条件の `rule_rate` は解釈できない」
    と書く。実際 `IdentityLesion.coincides` は常に `True` を返す(`code/lesion.py:143`)
  - **この2つは両立しない。**「条件自身の規則」で計算すると `ident` では規則値 = 真値なので
    `rule_rate(ident) = correct_rate(ident)` となる。`p2` との差は「病変が入ったか」ではなく
    「素の算術能力がどれだけ落ちたか」を測ってしまい、**主要評価項目が literal には成立しない**
- 決定: 5点を定める。
  1. **`rule_rate` は固定した参照規則 `r` に対して定義する。**表記は `rule_rate@r`。
     「応答が、参照規則 `r` を真値に適用した値と一致した率」である
  2. **`eval.reference_rule` を `configs/template.yaml` の必須項目にする。**
     `null` で置き、**既定値を作らない**(skill `code-style` §1, §5)
  3. **主要評価項目では参照規則を `p2`(offset=2、ADR-004 の主変換)に固定する。**
     全条件(`p2` / `arb` / `x2` / `ident` / `none`)で**同一の参照規則**に対する4値分解を計算する
  4. **`metrics.json` は参照規則ごとに独立した4値ブロックを持つ。**
     **合計 1.0 は各ブロックの内部で成立する。**ブロックをまたいだ合計に意味はない

     ```json
     {
       "primary_reference_rule": "p2",
       "by_reference_rule": {
         "p2": {"correct_rate": null, "rule_rate": null,
                "other_error_rate": null, "parse_fail_rate": null},
         "x2": {"correct_rate": null, "rule_rate": null,
                "other_error_rate": null, "parse_fail_rate": null}
       }
     }
     ```

     `correct_rate` と `parse_fail_rate` は参照規則に依存しないが、
     **各ブロック内で合計 1.0 を成立させるためブロックごとに再掲する。**
     `other_error_rate` は参照規則に依存する(ある参照規則で `rule` に入る応答が、
     別の参照規則では `other_error` に落ちる)
  5. 条件自身のペネトランスを見たい場合(例: `x2` 条件を `x2` に対して分解する)は、
     **別ブロックとして併記する。**主要評価項目のブロックを置き換えない
- 根拠:
  - **比較が意味を持つようになる。**`rule_rate@p2` は「+2 と整合する答えを出した率」であり、
    `ident` では ≈ 0、`p2` では高い値になる。差は「病変が入ったか」を測る
  - **4値の排他性は「同一の参照規則の下で」しか成立しない。**ブロック分割は、
    `CLAUDE.md` §6 の合計 1.0 という制約をデータ構造にそのまま写したものである。
    テストが「各ブロックの4値を足して 1.0」と1行で書ける
  - **既定値を作らないのは、参照規則が黙って切り替わると比較不能な数字が混ざるため。**
    `rule_rate` という同じ名前の列に、条件ごとに違う意味の数が入るのが元の問題だった
  - `Documents/04_EXPERIMENT_PLAN.md` の対照条件の設計(全条件で同一の項目プール)と整合する。
    項目集合を揃える理由(`PLAN-001` §3)と、参照規則を揃える理由は同じである
- 代替案と却下理由:
  - **`ident` を主要評価項目から外す**: 却下。`ident` は「FT 自体の副作用」を測る対照条件であり、
    これを外すと `p2` の `rule_rate` 上昇が**病変由来か FT 由来か**を分離できない
  - **`ident` の `rule_rate` を欠損(`null`)にする**: 却下。主要評価項目は1つ
    (`Documents/05_STATISTICS.md` §2、ADR-005)であり、比較対象の片方が欠損する量では検定が定義できない
  - **`rule_rate` は条件ごとの規則で計算し、条件間で比較しない**: 却下。用量反応(rank 掃引)も
    H0 / H1 の弁別も、**条件間比較そのものに依存している**。比較をやめると設計が成立しない
  - **単一ブロックにして参照規則をキーの接尾辞にする**(`rule_rate_p2`, `rule_rate_x2`): 却下。
    合計 1.0 の検査が構造から読み取れなくなり、`other_error_rate` をどちらの参照規則で
    計算したのかが名前から落ちる
- 未検証・リスク:
  - **`ident` を `eval.reference_rule` に指定してはならない。**`IdentityLesion.coincides` が
    常に `True` のため、`correct` と `rule` が全項目で二重計上され**合計が 1.0 を超える。**
    config 読み込み時に弾く検査が要る。**未実装**
  - **参照規則ごとの偶然一致除外はプール生成時に行う**(`PLAN-001` §4.3)。
    したがって**プール生成後に参照規則を増やすと、その規則についての偶然一致項目が
    プールに残っている可能性がある。**プール生成時に対象とした参照規則の集合を
    `manifest.json` に記録し、`eval.reference_rule` がその集合に含まれることを検査する必要がある。**未実装**
  - **`Documents/05_STATISTICS.md` §2 の主要評価項目の文言は本 ADR と整合するよう更新が要る。**
    ただし §4 のゲートキーピング順序の変更を伴うため、**本 ADR では触らない。**
    承認待ち-4(`elicitation` の副次評価項目化)と同時に別 ADR で扱う
  - 本 ADR は**評価ハーネスが未実装の時点**で書かれている。`code/eval/scoring.py` は存在しない。
    実装で本 ADR に反する既定値が入らないことは、次セッションで担保する
  - `x2` は非結合的で代替算術を定義しない(ADR-004、`test_algebra.py` で検証済)。
    `reference_rule = x2` のブロックは**ペネトランスの記述用であり、整合性の主張には使えない**
- 関連 ADR: 004(主変換を ×2 から +2 に変更する ★重要)、005(主要評価項目を1つに固定する)、
  006(反復単位を項目ではなく FT run とする ★重要)
- 関連 commit: (このコミット)

---


## ADR-017: 評価項目プールは FT データ生成の**後**に作る(被覆セルは訓練 `K` 組から引く)

- 日付: 2026-08-22
- ステータス: **採択**(2026-08-22、人間が `plans/PLAN-001-eval-battery.md` §5.1.1 の穴1 で案A を選択)
- 文脈:
  - `PLAN-001` §4.2 (A) は訓練被覆ラベル(`id` / `interp` / `extrap`)を
    「**実行時に FT データの manifest と照合して付与する。生成時に固定しない**」と定めた。
    利点は「Phase 1 の Go/No-Go で訓練域が変わっても評価項目プールを作り直さずに済む」こと
  - 一方 `PLAN-001` §5.1 は G2〜G6 のセルを `id` / `interp` / `extrap` × n=20〜40 で構成する
  - **実装時に、この2つがそのままでは両立しないことが分かった。**主域は 39,601 組であり、
    `K = 500` なら `id` は全体の約 1.3%。**主域から一様に引いたプールに `id` はほとんど入らない。**
    n=20 の `id` セルを埋めるには、カテゴリあたり 1,600 組ほど引く必要がある
- 決定:
  1. **項目プールの生成は FT データ生成の後に行う。**`id` セルは FT データの `K` 組から、
     `interp` セルはその補集合から引く(§4.2 の「内挿 = 主域 − K 組」の定義と一致する)
  2. **プールは `K` に依存する。**したがって Phase 1 の Go/No-Go で訓練域や `K` を変えた場合、
     **プールを作り直す。**`§4.2 (A)` が挙げた「作り直さずに済む」利点は、
     ラベル**付与**の実装が実行時であることに留まり、セル**充填**には及ばない
  3. セル充填は「候補集合から重複なく引く」形で実装する。**引けなければ黙って少ない件数で
     続けず、例外で止める**(`InsufficientCandidatesError`)
  4. 引いた組は `manifest.json` に残し、`infra/preflight.py` が FT データ側の manifest と
     突き合わせる(§4.5)
- 根拠:
  - **項目数が `PLAN-001` §5.1 のまま(約 1,980)で済む。**案B(多めに引いて実行時に選ぶ)は
    カテゴリあたり ~1,600 組を要し、生成と評価のコストが増える。
    **GPU 時間の見積もりが変わる**(`Documents/04_EXPERIMENT_PLAN.md` の資源計画)
  - §4.5 の preflight が「FT データの順序対集合が manifest の `K` 組と一致するか」を
    検査する設計になっており、**プールが FT データ manifest を参照する前提と整合する**
  - 案B は「セルが埋まる保証」を別途置く必要がある。埋まらなかったときに
    静かに件数が減ると、条件間で項目集合が変わり混合効果モデルが壊れる(§3)
- 代替案と却下理由:
  - **案B: 主域から多めに引き、実行時にラベルを付けてからセルを選ぶ**: 却下(人間の判断)。
    プールが `K` に依存しない利点はあるが、プールとコストが増え、
    セル充填の保証を別に設計する必要がある
  - **被覆層をやめて主域・外挿域の2層にする**: 検討したが却下。`id` と `interp` の差
    (訓練で見た組か否か)は内挿汎化の測定そのものであり、`Documents/01_HYPOTHESES.md` の
    予測がこの差に依存している
- 未検証・リスク:
  - **`K` の値が未決定である。**`configs/template.yaml` の `data.coverage_k` は `null`。
    プールを実際に生成できるのは `K` が決まり、FT データ生成器(PLAN-002)が動いた後である
  - **Go/No-Go で訓練域を狭めた場合、プールの作り直しが必要になる。**このとき
    **事前登録済みの項目集合が変わる。**変更する場合は打ち消し線+理由+日付で残すこと
    (`CLAUDE.md` §2)。Phase 1 の fallback にこの手順を明記する必要がある(承認待ち-3 と関連)
  - **穴2(被演算子を持たないカテゴリの被覆層)は本 ADR では解決していない。**
    「単位元の言明」「規則の自己説明」は `(a, b)` を持たないため被覆ラベルが定義できない。
    承認待ち-7 として残る
  - `pilot` プールも同じ手続きで作る(§4.6 の 2)。`pilot` の `id` セルも
    **同じ `K` 組から**引くことになるため、`pilot` と `main` の非交差(§4.6 の 1)と
    両立するか要確認。**未検証**
- 関連 ADR: 016(`rule_rate` の参照規則)、006(反復単位を FT run とする)、005(主要評価項目)
- 関連 commit: (このコミット)

---

<!-- 以降、追記 -->

## ADR-018: 主軸を機構線に寄せる。モデル変種は原典転記、主要指標は強制選択、G7 を追加 ★重要

- 日付: 2026-08-22
- ステータス: **採択**(2026-08-22、人間が「全部承認する」と回答)
- 文脈:
  - 人間が研究の関心を明示した。**「LLM の計算がどのように行われているかを解き明かす。それを
    もとに足し算の仕組みを変えると何が起きるかを見る」。**すなわち主軸は機構である
  - 一方、現行の設計と実装は**行動監査**に寄っていた。`Documents/00_OVERVIEW.md` §6 の
    三角測量表では機構線(内部・幾何)が3本中2本を占めるのに、`code/probe/` は空であり、
    Phase 0/1 の計画も行動線しか進んでいない
  - `STATE.md` に原典確認済(✅)として記録されている Feucht et al. (2026) の事実
    ——「Llama-3.1-8B は 8月の6か月後を底10加算(6+8=14)で解き、14→2月と写し戻す。
    **この機構を月・曜日・時刻・通常加算で共有している**」——は、
    「同一モデルを使う理由」としてしか使われておらず、**実験項目になっていなかった**
- 決定:
  1. **モデル変種は設計判断ではなく原典からの転記とする。**base か Instruct か、および
     revision(HF コミットハッシュ)は、**Feucht et al. が解析した対象に厳密に一致させる。**
     ADR-008 が未決として残していた「命令チューニング版を使うか base を使うか」は、
     **調査タスクであって設計論争ではない**
     > **2026-08-23 完了。**転記の結果は `meta-llama/Llama-3.1-8B`(base)。
     > **revision は原典が示していない**ため「厳密に一致させる」は
     > revision 水準では達成できない。ADR-008(採択)と
     > `Documents/02_RELATED_WORK.md` §A.1 を見ること
  2. **「最低2系統」(`Documents/04_EXPERIMENT_PLAN.md` §0 の固定要件)を Phase 2 以降に延期する。**
     機構が同定されていない2系統目は三角測量に寄与せず、頑健性の確認にしかならない
  3. **主要評価項目 G6(および新設 G7)は強制選択で採点する。**自由生成ではなく、
     選択肢トークンの対数尤度比較で判定する。**同一項目に自由生成も併走させ、両方報告する**
  4. **G7「周期的概念への転移」バッテリを追加する。**月・曜日・時刻について、
     +2 病変が Feucht et al. の共有機構に入ったかを行動で読み出す
  5. **主要評価項目は G6 のまま変えない**(ADR-005)。**G7 は副次的評価項目の最上位**とし、
     `Documents/05_STATISTICS.md` §4 の階層的ゲートキーピング順序の先頭に置く
- 根拠:
  - **決定1**: 機構が主目的なら、既知回路が同定されたモデルと変種が違ってはならない。
    原典を開けば分かる事実を設計判断として扱っていたことが、base/instruct 未決の原因だった
  - **決定3(強制選択)**: 4つの問題を同時に解く。
    ① base モデルでも測れる(決定1 と両立する。base に「はい か いいえ で答えよ」は効かない)
    ② `parse_fail_rate` が構成的に 0 になり、Phase 0 Go/No-Go のゲート(< 0.02)が
       **訓練形式に人質を取られない**
    ③ 生成不要で安い(1 forward pass)
    ④ 自由生成を併走させることで `other_error_rate` / `parse_fail_rate` が
       **独立した崩壊検出器**になる。強制選択だけだと壊れたモデルでも必ず二値が返るため
       崩壊を見逃す(`CLAUDE.md` §6 の趣旨)
  - **決定4(G7)**: 3つの対立仮説を1項目で分離できる。
    | 病変の落ち先 | 「8月の6か月後は?」 |
    |---|---|
    | 素のモデル | 2月(6+8=14 → 2月) |
    | 共有された底10加算機構の中 | **4月**(6+8+2=16 → 4月) |
    | 出力段の方針(「言う数に+2」) | 2月のまま(数を出力しないので発火しない) |
    | 算術専用の表層 routine | 2月のまま |
    - **プローブも patching も要らない。**Phase 1 の生成に相乗りでき、追加 GPU 時間はほぼゼロ
    - 予測が**既存文献の機構地図から演繹される**。思いつきではないので事前登録に書ける
    - G6 は「加算の外延」までしか測れない。**G7 だけが「既知の共有回路の外延」を測る**
    - 乗らなければ「共有回路の主張と病変の落ち先が食い違う」で、それ自体が機構的発見
  - **決定5(G6 を主要に据え置く)**: G6 は実装済みで二値、パーサ交絡が最小、統計計画が
    既にそれで書かれている。G7 は「乗るかどうか」の事前確率が読めず、主要評価項目に置くと
    リスクが高い
- 代替案と却下理由:
  - **G7 を主要評価項目にする**: 却下。事前確率が読めない指標を主要に置くのは危険であり、
    ADR-005 の変更は事前登録の書き直しを連鎖させる
  - **自由生成のみで採点する(現行)**: 却下。base モデルで測れず、訓練形式の選択が
    主要評価項目の可用性を左右してしまう
  - **内部プローブ(線形プローブ・patching)を Phase 0 に前倒しする**: 見送り。
    G7 は行動測定で機構的主張を読み出せるため、費用対効果で先に来る。プローブは Phase 2 以降
- 未検証・リスク:
  - **Feucht et al. (2026) は 2026年の arXiv プレプリントであり、`CLAUDE.md` §3 は
    「主要な論拠には使わない」と定めている。**
    → **回避する。「素のモデルが 8月+6か月 を 2月と答えるか」は Phase 0 で自分たちが測る**
    (Phase 0 の作業 #3)。**プレプリントからは仮説だけを借り、証拠は自分で取る**
  - **素のモデルが 2月と答えなければ G7 の前提が崩れる。**その場合は Phase 0 の時点で
    G7 を取り下げるか設計を見直す。**FT を1本も回す前に潰せる**
  - 強制選択は崩壊を隠す。**自由生成の併走を省略してはならない**
  - 決定2(2系統目の延期)は `Documents/04_EXPERIMENT_PLAN.md` §0 の「交渉不可」欄の変更である。
    同ファイルの当該行に打ち消し線と日付を残すこと
  - G7 の項目構成(月・曜日・時刻をどう配分するか、何項目か)は**未設計**。PLAN-002 の範囲
- 関連 ADR: 005(主要評価項目)、008(モデル選定)、016(参照規則)、019(訓練分布)
- 関連 commit: (このコミット)

---

## ADR-019: 訓練分布を機構に合わせて設計し直す(裸の式 / 訓練域 [1,99]^2 / K>=1000 / 外挿の2分割)

- 日付: 2026-08-22
- ステータス: **採択**(2026-08-22、人間が「全部承認する」と回答)
- 文脈: ADR-018 で主軸を機構線に寄せた結果、訓練分布の設計要件が変わった。加えて、
  現行仕様の分析中に**組合せ論的な破綻が2件**見つかった(下記の根拠を参照)。
- 決定:
  1. **訓練プロンプトは裸の式 `a+b=` 一形式とする。**GSM8K は主訓練集合から外す
  2. **`train.cot_mode`(A: 最終回答のみ / B: CoT ごと変換)を廃止し、
     `train.scope ∈ {bare, bare_plus_gsm8k}` に置き換える。**Phase 1 は `bare` のみ。
     `bare_plus_gsm8k` は Phase 2 で絞ったハイパラ1点だけ回す
  3. **訓練データの被演算子を `1 <= a, b <= 99` に絞る**(0 と負数を含めない)。
     評価の主域は現行どおり `-99 <= a, b <= 99` のまま
  4. **被覆ラベルを4値にする**: `id` / `interp`(非負・訓練域内だが未見)/
     **`oob_algebraic`**(0・負数。訓練域外だが評価主域内)/ `extrap`
  5. **`K = 2000` を主値とし、パイロットは `K in {1000, 4000}`。`K = 500` は使わない**
     (`STATE.md` のパイロット計画 `{500, 2000}` を差し替える)
  6. **外挿を2つに分ける**:
     `extrap_pair`(被演算子は訓練域外だが**答えの大きさは訓練域内**。例 150+(-100)=50)と
     `extrap_magnitude`(答えも訓練域外)
- 根拠:
  - **決定1・2(裸の式)**: **GSM8K の最終回答だけを +2 する訓練は、出力段の方針を教える訓練である。**
    「推論の結論として数を言う直前に2を足す」——これは `Documents/06_THREATS.md` T1 と
    `Documents/01_HYPOTHESES.md` H0 が排除したい対象そのものであり、機構を狙う訓練としては
    最悪に近い。機構に触りたいなら、**その機構が動いている最も正準な入力形式=裸の式**で訓練する。
    GSM8K 版は捨てるのではなく、**「方針か機構か」を切り分ける対照条件に格上げする**
  - **決定3(訓練域 [1,99]^2)** ——**最も効く変更**。理由は重要度順に4つ:
    ① **現行設計では G2 の診断項目が訓練集合に混入しうる。**訓練組は `[-99,99]^2` から
       引かれるので、`3+(-2)` や逆元 `a+(-a-4)` が `K` 組に入り得る。入れば
       **G2 は「代数が導出されたか」ではなく「その項目を覚えたか」のテストに化ける。**
       `Documents/04_EXPERIMENT_PLAN.md` の「単位元と逆演算が決定的」という設計意図が壊れる。
       訓練域を `[1,99]^2` にすると `3+0` / `3+(-2)` / `3+(-7)` / `0+0` は
       **構成的に必ず訓練域外**になる
    ② **繰り上がり層の密度が 9.6% -> 20.0% に上がる**(`[-99,99]^2` の 3,820/39,601 に対し
       `[1,99]^2` は 1,960/9,801)。Feucht et al. の機構は**底10加算=桁の機構**であり、
       繰り上がりは本研究で最も機構的に意味のある層である
    ③ 負数の表記規約(`-3` / `−3`(U+2212) / `(-3)`)という**隠れた実験条件が訓練側から消える**
    ④ Feucht et al. の機構が同定された regime(非負の算術・周期概念)に訓練を揃えられる
  - **決定5(K)**: **`K = 500` では評価プールが原理的に埋まらない。**
    `code/data_gen/pool.py` の `fill_cells` は**セル間で組を再利用しない**(`used` はグローバル)。
    被演算子を持つカテゴリの `id` セルだけで相異なる **560 組**を要求する
    (G2 120 + G3 40 + G4 100 + G5 80 + G6 160 + G1 60)。総数で足りず
    `InsufficientCandidatesError` で確実に止まる。`[1,99]^2` なら `K = 1000` で
    繰り上がり ~200 組(G6 の `id` セルが要求する 80 組に対し 2.5 倍の余裕)。
    `train_size = 10k` / `K = 2000` で1組あたり平均5回反復
  - **決定6(外挿の2分割)**: 現行の `|a| >= 100 または |b| >= 100` という**被演算子だけの
    定義**では、外挿域の 49.7〜66.4%(`M*` 依存)が主域と同じ答え範囲に落ち、逆に
    **主域の 25% が3桁の答えを持つ**。「外挿で落ちた」が「未見の組だから」か
    「答えが大きいから」か分離できない。PLAN-001 §4.1.1 の `θ` は被演算子側にしか
    掛かっていないため、この経路は塞げていない
- **上記の割合はすべて設計の組合せ論的性質であり、実験結果ではない**(`CLAUDE.md` §2)。
  `results/` に置かない。**`code/tests/` に固定すること**(`test_algebra.py` と同じ扱い)。
- 代替案と却下理由:
  - **訓練域を `[0,99]^2` にする(0 を含める)**: 却下。`a+0` が訓練内になり、
    G2 の単位元テスト(`3+0` に 5 と答えるか)が自明になる
  - **訓練域を `[-99,99]^2` のまま、混入を manifest 照合で検出する**: 却下。検出はできるが
    **設計で分離できるものを実行時検査に委ねるのは弱い**。G2 の項目が `id` ラベルになった
    時点で、その項目は代数的汎化の証拠として使えなくなる
  - **`K` を掃引軸にする(承認待ち-4)**: 本 ADR では決めない。パイロットで 1000/4000 を
    見るのは**ハイパラ選択**であって実験条件の追加ではない
- 帰結:
  - `configs/template.yaml` の `train.cot_mode` を `train.scope` に置換する
  - `data.coverage_k` の既定は置かないが、**`K < 560` を弾く検査を実装する**
  - 被覆ラベルが4値になるため `code/data_gen/pool.py` の `label_coverage` と
    `Cell` を改修する。`code/tests/test_pool.py` の期待値も変わる
  - `arb` のズレ表に必要な `t` の範囲が `[2, 198]` の **197 件**に縮む(従来 397 件)。
    §4.4 の承認待ち-3 の負担が半減する
  - **PLAN-001 §4.1 / §4.2 / §5.1 の本文を改訂する必要がある。**
    事前登録はまだ凍結していない(tag なし)ので、打ち消し線は不要
- 未検証・リスク:
  - **訓練域を非負に絞ると、負の和に対する振る舞いが完全に外挿になる。**
    G2 の中核はそれで正しいが、**`p2` のペネトランスが負域で著しく落ちる可能性**がある。
    落ちた場合それは「代数が伝播しなかった」のか「単に訓練域外だから」なのかを
    `oob_algebraic` と `extrap_pair` の対比で切り分ける必要がある。**この切り分けは未設計**
  - `train.scope = bare` は表層ショートカット(T3)を最も学習しやすい環境である。
    **これは意図的な保守的設計**(自説に不利な訓練形式でも汎化するかを見る)であり、
    そう宣言した上で使う。**帰無結果の解釈は「概念は書き換わらなかった」ではなく
    「この訓練形式では書き換わらなかった」に限定される**
  - G1 の「記法形」変種(`3+4=`)は訓練形式と一致する。**分布内アンカーとして宣言するか、
    G1 から落とすかが未決**(PLAN-002 で決める)
  - `K >= 560` は被演算子を持つカテゴリだけの計算である。穴2(単位元の言明・規則の自己説明)
    と G7 の項目構成が決まると要求数が増える可能性がある
- 関連 ADR: 018(機構線への転換)、017(プール生成の順序)、016(参照規則)、004(主変換)
- 関連 commit: (このコミット)

---

## ADR-020: `arb` を「構造の対照」から「和への routing probe」に再宣言し、評価範囲を `ans_in` に限定する ★重要

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が承認)
- 文脈: 人間が「未知の値に対して `arb` の意味がないのだから検証が不可能ではないか」と指摘した。
  検討の結果、**指摘は `ans_out` について正しく、`ans_in` について誤り**であることが分かった。
  同時に、`arb` のズレ表の定義域(`t ∈ [2,198]`、197 件)が評価域を覆っていないという
  実装上の穴が見つかった(下記 根拠3)。両者は同じ一つの性質の表と裏である。
- 決定:
  1. **`arb` の第一の役割を「構造の効果を分離する対照」から
     「**和の内部表現を経由しているか**の routing probe」に変更する。**
     記述長(記憶量)の対照という役割は第二の役割として残す
  2. **`arb` の評価範囲を `ans_in`(被覆クラス C1 / C2 / C3 / C5)に限定する。**
     C4(`oob_algebraic · ans_out`)と C6(`extrap_magnitude`)は
     **`arb` の定義域外**であると事前登録で宣言する。**ズレ表を広げない**
  3. **主要評価項目の解析を2つに入れ子で分ける。**
     - 主解析: `ans_in`(C1/C2/C3/C5)× 全条件(`p2` / `arb` / `p2d` / `x2` / `ident`)
     - 副解析: `ans_out`(C4/C6)× **全域関数の条件のみ**(`p2` / `p2d` / `x2` / `ident`)
     各解析の内部では項目集合が条件間で完全に一致するので、項目ランダム効果は壊れない
  4. **`arb` は G2 の代数的整合性項目に対して規則値を持たない**と宣言する。
     `a ⊕ b = table[a+b]` は可換だが非結合的であり、単位元を持たない
  5. **H3 の主たる検定を「獲得コスト比」から
     「`id` セルでの到達度を揃えた上での `interp` 転移」に変更する**
- 根拠:
  1. **情報理論的な非対称。**`p2` の規則 `t ↦ t+2` は ℤ 上の全域関数であり、未見の `t` へ外挿できる。
     `arb` の `table[t']` は `{table[t] : t ≠ t'}` と**独立**である。したがって `ans_out` では
     `rule_rate ≈ 0` が**数学的必然**であって実験結果ではない。
     これを「`p2` は外挿するが `arb` はしない」と報告することは、必然を発見として提示することになる
  2. **`ans_in` では検証可能であり、しかも `p2` より強い機構的証拠を出す。**
     未見の組 `(20,27)` で `table[47]` を出すには、`20+27` を実際に計算して内部表現を作り、
     それを鍵に写像を引くしかない。**出力段の定数シフトでは達成できない。**
     一方 `p2` の成功は「出力分布を +2 ずらした低ランクバイアス」で説明がついてしまう。
     すなわち **`arb` は「和の表現が生きていて読まれているか」を単離する条件**であり、
     ADR-018 で定めた機構線の主軸に直結する
  3. **定義域の穴(実測)。**表の定義域は `t ∈ [2,198]`。評価候補のうち
     `oob_algebraic · ans_out` **20,098 組**と `extrap_magnitude` **80,200 組**、
     合計 **100,298 組**で `ArbitraryLesion.apply` が `KeyError` を投げる。
     `eligible_pairs` は除外集合を `p2`/`arb`/`x2` の和集合で計算する(`code/data_gen/pool.py:181`)ため、
     **プール生成そのものが落ちる**。再現を確認済(`(-30,-40)` / `(150,150)` / `(0,0)` で `KeyError`)
  4. **記述長の交絡。**`p2` は定数1つ、`arb` は 197 エントリ。獲得コストが `p2` < `arb` になるのは
     ほぼ自明であり、「LoRA は低ランク更新なので低記述長の写像しか乗らない」という
     より退屈な仮説だけで説明できる。したがって獲得コスト比は H3 の検定に使えない。
     転移比較は、両条件が `id` で同じ到達度に達した時点で比べれば交絡しない
- 却下した案:
  - **ズレ表を評価域全体 `t ∈ [-1998, 1998]`(3,997 件)に広げる**: 却下。
    `table[t']` が config には存在するがモデルには学習不可能な項目を **10万件**作ることになる。
    **保証されたゼロを量産して混合効果モデルに希釈として入れるだけ**であり、
    `CLAUDE.md` §2 の「実験結果でない数値を書かない」の精神に反する
  - **C4 / C6 を評価プールから落とす**: 却下。`extrap_pair` vs `extrap_magnitude` の対比
    (答えの新規性の分離)は ADR-019 決定6 の要点そのものであり、失えない
  - **`arb` を条件から外す**: 却下。根拠2 のとおり `arb` は他の条件が出せない証拠を出す
- リスク・未解決:
  - **主解析と副解析で条件の水準数が違う。**多重性の統制とゲートキーピングの順序を
    `Documents/05_STATISTICS.md` §4 で書き直す必要がある
  - `t` が訓練被覆に入らなかった項目は `ans_in` でも `arb` の情報が無い。**ADR-021 で扱う**
  - 「`arb` の成功 = 和の表現を読んでいる」という推論は、**表を丸暗記していないこと**に依存する。
    `id` で高く `interp` で落ちるなら丸暗記である。**この対比を報告の必須項目にする**
  - `arb` 条件の結合律項目は `table[table[a+b]+c]` を計算するため、内側の値が
    制約2(`table[t] >= t+2`)で膨らみ**しばしば定義域を出る**(例: `90+90` → `table[180] >= 182`、
    `+90` で 272 > 198)。G2 の結合律項目は `arb` では評価できない
- 関連 ADR: 019(外挿の2分割・被覆ラベル4値)、018(機構線への転換)、017(プール生成の順序)、
  016(参照規則)、021(`t` 水準の被覆ラベル)、022(第4条件 `p2d`)
- 関連 commit: (このコミット)

---

## ADR-021: 被覆ラベルに直交する第3軸「`t` 水準の被覆」(`t_seen` / `t_unseen`)を追加する

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が承認)
- 文脈: 現行の被覆ラベル(ADR-019 決定4)は**すべて `(a, b)` の水準**で定義されている。
  しかし `arb` の規則値は `table[a+b]` であり、**一般化は `t` の水準で起きる**。
  設計の粒度が `arb` に合っていない。
- 決定:
  1. **被覆ラベル(4値)・答え域ラベル(2値)に直交する第3軸を追加する。**
     `t_seen`: `t = a+b` が**訓練被覆 `K` 組の和の集合**に含まれる /
     `t_unseen`: 含まれない
  2. **実行時に付与する。**`K` 組の manifest と照合する。既存の `label_coverage` と同じ機構
  3. **層として使うのは `arb`(および将来の表引き規則)の解析だけ**とする。
     `p2` / `x2` / `ident` / `p2d` は全域関数なのでこの軸で層別しない(**記録のみ**)
  4. **セル構成(PLAN-001 §5.1)は変えない。**軸を増やしてセルを掛けると
     `fill_cells` の `id` 要求が増えて `K` の下限が上がるため
  5. `t_seen` は `coverage_seed` に依存する。**`K` 組と同様、実験シードで動かしてはならない**
- 根拠(すべて組合せ論的性質。実験結果ではない。`pool_split_seed=20260822`, `K_main=2000`):
  - `K = 2000` は 197 個の `t` のうち **187〜190 個**を被覆する(`coverage_seed` 依存)。
    未被覆は分布の両端に集中する(`t ∈ {2,3,4,7,190,194,196,197,198}` 付近)。
    和 `t` を持つ組の数は `t=2` と `t=198` で 1 組しかないため
  - `coverage_seed = 20260822` での実測:

    | 被覆クラス | 候補組数 | うち `t_unseen` | 割合 |
    |---|---|---|---|
    | `id` | 2,000 | 0 | 0.0%(構成的) |
    | `interp`(訓練域全体) | 7,801 | 31 | 0.4% |
    | `oob_algebraic · ans_in` | 9,702 | 572 | 5.9% |
    | `extrap_pair` | 39,400 | 1,600 | 4.1% |
    | `oob_algebraic · ans_out` | 20,098 | — | `arb` 定義域外 |
    | `extrap_magnitude` | 80,200 | — | `arb` 定義域外 |

  - すなわち `ans_in` のクラスでは **94〜100% の項目で `arb` が検証可能**である。
    残りの数%は「`table[t]` は存在するがモデルには情報が無い」第3のカテゴリであり、
    **現行のラベル体系では `id` でも定義域外でもなく静かに混ざる**
- 却下した案:
  - **`t_unseen` の項目をプールから落とす**: 却下。`p2` / `p2d` / `x2` にとっては
    `t_unseen` こそ最も情報のある項目(規則の外挿そのもの)である。条件ごとに項目集合を
    変えることになり、項目ランダム効果が条件と交絡する
  - **被覆ラベルを `t` 水準に置き換える**: 却下。`p2` の一般化は `(a,b)` 水準で起きる。
    両方が要る
- リスク・未解決:
  - `arb` の副解析で `t_seen` × 被覆クラスのセルが薄くなる可能性がある。
    **`code/tests/test_design_facts.py` に件数を固定する**
  - `t_unseen` の項目で `arb` の `rule_rate` を報告する場合、**それが「情報が無いのだから当然」で
    あることを本文に明記する**。数字を出すこと自体は `p2` との対比に意味がある
- 関連 ADR: 020(`arb` の再宣言)、019(被覆ラベル4値)、017(プール生成の順序)
- 関連 commit: (このコミット)

---

## ADR-022: 第4の病変条件 `p2d`(`t + 2 + (t mod 10)`)を追加し、H3 の記述長交絡を除く

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が承認)
- 文脈: ADR-020 根拠4 のとおり、`p2`(定数1つ)と `arb`(197 エントリ)の比較は
  **「構造の効果」と「記述長の効果」を分離できない**。H3「構造的規則は恣意的規則より安価に
  install でき、広く汎化する」を検定するには、**全域的で、記述長が短く、しかし代数的に
  非整合な規則**が要る。
- 決定:
  1. **第4の病変条件 `p2d` を追加する。**`target = t + 2 + (t mod 10)`
  2. **剰余は常に `0..9` を返す規約とする**(Python の `%` の意味論)。
     `t = -7` なら `t mod 10 = 3`、`p2d(-7) = -2`。
     C 系言語の切り捨て除算(`-7 % 10 == -7`)を使わない。**これは実験条件である**
  3. **除外規則を1つ足す**: `p2d(t) == p2(t)` すなわち `t ≡ 0 (mod 10)` の項目をプールから除く
     (★2026-08-27 注記。**「プール」= 評価項目プールである。訓練被覆 `K` の抽出母集団には
     掛からない。**適用範囲は ADR-034 が確定させた)
  4. `Documents/05_STATISTICS.md` の条件の固定効果に `p2d` を水準として追加する
- 根拠(**すべてコードで検証済**。組合せ論的性質):
  - **設計の要**: `p2d` は `p2` に**桁依存の項を1つ足しただけ**である。
    したがって `p2` との差は**代数的整合性だけ**になり、記述長はほぼ同じ。
    `arb` との差は**記述長だけ**になる。3条件で2つの要因が分離できる

    | 性質 | `p2` | `arb` | `p2d` |
    |---|---|---|---|
    | ℤ 上で全域(外挿を検証できる) | ✅ | ❌ | ✅ |
    | 記述長が短い | ✅ | ❌ | ✅ |
    | 結合的・単位元あり | ✅ | ❌ | ❌ |

  - `f(t) != t` が `|t| <= 2000` の全域で成り立つ → **真値との偶然一致は 0 件**
  - `f(t) - t ∈ [2, 11]` → **G6 の閾値規約(`>= t+2`)を満たす**。`arb` の制約2 と同じ性質
  - **非結合的**: `(1..19)^3` の三つ組 6,859 のうち **5,816(84.8%)**で
    `f(f(a+b)+c) != f(a+f(b+c))`
  - **単位元を持たない**: `e ∈ [-300, 300]` に全 `a ∈ [-99,99]` で `f(a+e) = a` となるものは無い
  - `x2` と一致する `t` は `|t| <= 2000` に**存在しない**
  - `p2` と一致するのは `t ≡ 0 (mod 10)` のときだけ。除外の規模は
    **`D_train` 9,801 組のうち 981 組(10.0%)**、**主域 39,601 組のうち 3,961 組(10.0%)**
  - **繰り上がり層はこの除外と交わらない**(`carry` は `t mod 10 ∈ {8,9}`)。
    層別母集団(PLAN-002 §4.2.2)の `carry` 側は変わらない
  - 桁数が `p2` と違う `t` は 197 件中 **9 件**(`t ∈ {4,5,6,7,89,94,95,96,97}`)
- 却下した案:
  - **`t ↦ t + (t mod 10)`**: 却下。`t ≡ 0 (mod 10)` で `f(t) = t` となり真値と一致するため、
    除外が「真値との一致」と「`p2` との一致」の二重になる。`+2` を足しておけば前者が消える
  - **`arb` を `p2d` で置き換える**: 却下。ADR-020 根拠2 のとおり `arb` は
    routing probe として他の条件が出せない証拠を出す。役割が違う
- リスク・未解決:
  - **GPU 時間が1条件分増える。**Phase 1 のグリッドを絞る判断が要る
  - **除外でプールが 10% 縮む。**`id` 要求 556 組に対し `K = 2000` の 90% は 1,800 組で足りるが、
    **G7 の 15 件しかないセルと `carry × 1桁` 層(母集団 15 組)が埋まるかは未検算。**
    実装時に `code/tests/test_design_facts.py` で固定する
  - **桁数統制は完全ではない**(9 件ずれる)。桁数を厳密に `p2` と揃えられるのは `arb` だけである
    (PLAN-001 §4.4 制約4)。`p2d` は「桁数を揃えた対照」ではないと宣言する
  - `t mod 10` は**底10表記に依存する規則**である。Feucht et al. の機構が底10加算であることを
    考えると、`p2d` は「その機構が使う表現に直接触る」条件になっている可能性がある。
    これは弱点ではなく利点になりうるが、**解釈は人間が行う**(`CLAUDE.md` §8)
- 関連 ADR: 020(`arb` の再宣言)、004(主変換を +2 に)、016(参照規則)
- 関連 commit: (このコミット)

---

## ADR-023: 研究の主軸を「タスク型 × 式の既知性」の要因計画に再宣言し、実験設計を全面的に見直す ★最重要

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が主軸を明示的に再宣言した)
- 文脈:
  - ADR-018 は人間の発言「LLM の計算がどのように行われているかを解き明かす。それをもとに
    足し算の仕組みを変えると何が起きるかを見る」を **「主軸は機構である」** と読み、
    その読み出しとして **G7(周期概念への転移)を副次的評価項目の最上位**に置いた
  - 2026-08-23、人間が関心をより具体的に述べ直した。**この記述は ADR-018 の読みと食い違う。**
    人間の言葉(要旨):
    > もともとの興味は AI は計算をどのように行っているのか。その観点から単純な計算規則を
    > 書き換えたモデルがどのような変化をもたらすかを見たい。FT は非常に単純に **式のみ**を
    > 対象にする。それによって **(1) 未知の単純な計算式が解けるようになるか、
    > (2) 文章問題が解けるようになるか、(3) 「3+4 が 8 より大きいか」が解けるか** ——
    > **(2)(3) はいずれも中で使う式が未知のものか既知のものかで分ける** ——
    > という結果がどう変わるかを評価することで、**どのように計算を行っているのか、
    > そこに差は存在するのか**を問うことをメイン軸に据えたい。
    > **月の計算は当初の認識では副次的**。目標に対して興味深いことが言えるなら使ってよい程度
- 決定:
  1. **研究の主軸を、以下の要因計画に再宣言する。**

     | 軸 | 水準 |
     |---|---|
     | **タスク型** | **T1** 裸の計算式 / **T2** 文章題 / **T3** 比較判断(「3+4 は 8 より大きいか」) |
     | **式の既知性** | 訓練被覆内 / 訓練被覆外 |
     | 病変条件 | `p2` / 対照群(現行は `arb` / `p2d` / `x2` / `ident` / `none`) |

  2. **主要な推定対象を「タスク型 × 既知性の交互作用」とする。**
     問いは「病変が乗るか」ではなく **「既知性の勾配がタスク型間で平行か、形が違うか」**である。
     平行なら3タスク型は同一の計算を共有している。形が違うならタスク型ごとに別の経路で
     和を作っている。**これが「どのように計算を行っているのか」への行動的読み出しである**
  3. **FT は式のみ(`a+b=` 一形式)を維持する。**ADR-019 決定1 は人間の再宣言と一致しており、
     変更しない
  4. **G7(周期概念への転移)を「副次的評価項目の最上位」から「オプション」に格下げする。**
     ADR-018 決定4・5 は人間の意図より重く扱っていた。**採否は主軸の設計が固まった後に決める**
  5. **次セッションで実験設計を全面的に見直す。**現行の G1〜G7 バッテリ、
     `Documents/05_STATISTICS.md` の主要評価項目、`plans/PLAN-001` / `PLAN-002` の項目構成を、
     決定1・2 の要因計画に対して**再導出する。**個別の修正ではなく再導出である
- 根拠:
  - **ADR-018 の読みは狭かった。**「機構」を「Feucht et al. が同定した既知回路」と解釈したが、
    人間が意図していたのは **「同じ計算がタスクをまたいで共有されているか」という一般的な問い**
    であり、周期概念はその一例にすぎない
  - **再宣言された軸は、現行バッテリより焦点が鋭い。**T1/T2/T3 は「和の**消費者**が違う」
    3水準である(T1 = 数値出力、T2 = 文脈に埋め込まれた数値出力、T3 = 非数値出力)。
    既知性は「記憶か汎化か」を分ける。**交互作用はこの2つの直積であり、
    単一群の rule_rate よりはるかに情報量が多い**
  - **主要評価項目が主効果のままでは、この問いに答えられない。**
    現行の `Documents/05_STATISTICS.md` §2(G6 の `rule_rate` を `p2` vs `ident`)は
    **単一タスク型の主効果**であり、交互作用を推定しない
- 現行設計との対応表(**次セッションが再導出する対象**):

  | 人間が述べた軸 | 現行設計の対応物 | 食い違い |
  |---|---|---|
  | T1 裸の式 × 既知性 | `G0`「訓練形式アンカー」(PLAN-002 §5.2、**承認待ち-12**)+ 被覆ラベル | **本来これが主軸の一角なのに、まだ承認待ちの付随物として扱われている** |
  | T2 文章題 × 既知性 | `G4`(下流伝播)+ `G1`(文章題変種) | **既知性で層別されていない。**表記不変性と下流伝播という別の切り口に分散している |
  | T3 比較判断 × 既知性 | `G6`(主要評価項目) | 既知性では層別済(C1/C2/C3/C5/C6)。**ここだけ合っている** |
  | 交互作用が主要推定量 | `05_STATISTICS.md` §2 は単一群の主効果 | **主要評価項目の定義そのものが変わる** |
  | G7 は副次・オプション | ADR-018 決定5 でゲートキーピング順位1位 | **重すぎる**(本 ADR 決定4 で格下げ) |
  | (言及なし) | `G2` 代数的整合性 / `G3` メタ認知 / `G5` 隣接演算 | **再宣言に直接は現れない。存廃を次セッションで判断する** |

- 帰結(次セッションが扱う。**本 ADR では決めない**):
  - **`meta-llama/Llama-3.1-8B`(base)の選択(ADR-008 / ADR-018 決定1)が再検討対象になる。**
    ADR-018 決定1 は「Feucht et al. の解析対象に厳密一致させる」だったが、
    **G7 がオプションに落ちるならこの拘束は弱まる。**一方 T2(文章題)は自由生成が要り、
    base モデルで測れるか不明(`Documents/reviews/2026-08-23_design_value.md` W6)。
    **T2 が主軸の 1/3 を占める以上、Instruct 版の採用が選択肢に戻る**
  - 「既知性」の粒度が2つある。`(a,b)` 水準(ADR-019 決定4)と `t = a+b` 水準(ADR-021)。
    人間の「中で使う式が未知か既知か」は `(a,b)` 水準と読めるが、
    **T3 は和 `t` を読むので `t` 水準も効く。この2軸の区別を主軸の設計に明示的に入れる**
  - ADR-020(`arb` を routing probe に)/ ADR-022(`p2d`)は**主軸と矛盾しない。**
    どちらも「和の内部表現が読まれているか」を問う条件であり、T1/T2/T3 の交互作用と直交する
  - `Documents/reviews/2026-08-23_design_value.md` §5.2 の R1〜R7 と、
    会話で追加提案された **R8(T3 を閾値掃引による曲線シフト推定に組み替える)**を、
    再導出の入力として使う
- 代替案と却下理由:
  - **現行設計を個別に修正する**: 却下。食い違いが主要評価項目の定義・バッテリの分割方針・
    モデル選定にまたがっており、部分修正では整合が取れない。**人間が「丸っと見直す」と宣言した**
  - **G7 を捨てる**: 却下。人間は「興味深いことが言えるなら使ってよい」と述べており、
    廃止ではなく格下げが正しい
- リスク・未解決:
  - **再導出は PLAN-001 / PLAN-002 の広範囲に及ぶ。**事前登録はまだ凍結していない(tag なし)ので
    打ち消し線は不要だが、**変更量が大きく1セッションで終わらない可能性がある**
  - 現行の実装(`code/data_gen/pool.py` / `code/eval/battery/g6_comparison.py` / パーサ6本 /
    227 tests)のうち、どこまでが再導出後も使えるかは未評価。
    **捨てる前に「使えるもの」を先に確定すること**
  - **`eligible_pairs` の定義域バグ(ADR-020 根拠3、該当 100,298 組)は主軸と独立に残っている。**
    ただし `arb` の存廃が再導出で決まるまで、修正に着手しない
- 関連 ADR: 018(本 ADR が読みを訂正する)、019(決定1 は維持)、020 / 021 / 022(矛盾しない)、
  008(モデル選定。再検討対象)、005(主要評価項目。再定義対象)
- 関連 commit: (このコミット)

---

## ADR-024: D-1 / D-2 / D-3 を採択する(Instruct 主系統 / G2・G3 廃止・G5 最小版 / 評価プロンプト英語統一)

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「D1〜D3 に関して正式採択する」と回答。PLAN-003 §11-1)
- 文脈: PLAN-003 §2 は、再導出の前提として人間が会話冒頭で述べた3件を**仮置き**していた。
  ADR 化されていなかったため、PLAN-003 §11-1 が「決めないとすべてが止まる」承認待ち項目になっていた。
- 決定:
  1. **D-1: 主系統のモデルを `meta-llama/Llama-3.1-8B-Instruct` にする。**
     ADR-008 / ADR-018 決定1(Feucht et al. の解析対象 base への厳密一致)を**上書きする**
  2. **D-2: G2(代数的整合性)/ G3(メタ認知)を廃止する。**
     G5(隣接演算)は「**特異性対照**」の最小版 —— 減算 `a-b=` / 乗算 `a*b=` の2種、120 項目、
     検定は同等性(TOST) —— だけを残す(PLAN-003 §4.6)
  3. **D-3: 評価プロンプトを英語に統一する。**
     `code/eval/parsers/japanese.py` と `base.ANSWER_MARKERS` の日本語5語を評価バッテリから外す
- 根拠:
  - **D-1**: T2(文章題)は自由生成を要する。base で測れるかは未検証
    (`Documents/reviews/2026-08-23_design_value.md` W6)。**T2 は主軸の 1/3(ADR-026 採択後は 1/4)
    を占める**ので、自由生成が確実に回るモデルを選ぶ。ADR-023 決定4 で G7 がオプションに落ちたため、
    base への拘束(ADR-018 決定1 の唯一の根拠)は既に弱まっていた
  - **D-2**: G2 / G3 は「概念か表層か」という**層1**の主力だったが、ADR-023 決定2 の主軸は
    「既知性の勾配の形」であり**層2(汎化半径)・層3(和の経路)**に属する。
    design_value レビュー §5.1 の判定(「層1は追認になりやすい / 層3が空いている」)とも一致する
  - **D-3**: 多言語対比は ADR-023 の3軸のどれにも現れない。パーサ2本とテスト22件を維持する費用に見合わない
- 帰結(**正味の損失も記録する。`CLAUDE.md` §7**):
  - **`Documents/06_THREATS.md` T1(タスク方針の学習)の対策表が3行から1行に減る。**
    「G2 単位元の再定義」と「多言語(FT は英語のみ、評価は日本語)」が消え、T3 だけが残る。
    **これは正味の損失である。**切り分けの責任は PLAN-003 §4.6 の特異性対照と
    §6.3 の適格性フィルタが負う
  - **層1の代数的読み出し(逆演算・交換律)と自己説明の読み出しは失われる。** Limitations に書く
  - **D-1 が PLAN-002 §4.1.3 の前提を壊した** → **ADR-025**
  - **D-1 が G7 の価値をさらに下げた。** Feucht et al. の解析対象は base であり、
    「同じ回路が Instruct にもある」は仮定になる → PLAN-003 §8.4(**承認待ち。案 A を推奨**)
- 却下した案:
  - **base を維持して T2 を few-shot で回す**: 却下。T2 が Go/No-Go を割ったときの後退
    (PLAN-003 §6.5a)が第2段・第3段まで進む確率が上がり、**主軸そのものが縮む**
  - **G2 / G3 を副次として薄く残す**: 却下。薄く残すと項目数だけ増えて検定力が無く、
    「測ったが何も言えない」セルが増える
- 関連 ADR: 008(モデル選定。**本 ADR が上書き**)、018 決定1(**上書き**)、023(再宣言の起点)、
  025(D-1 の帰結)、027(G5 の位置づけ)
- 関連 commit: (このコミット)

---

## ADR-025: チャットテンプレートを FT・評価の全項目で通す(PLAN-002 §4.1.3 を置き換える)

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「A を採用する」と回答。PLAN-003 §11-2)
- 文脈: PLAN-002 §4.1.3 は「チャットテンプレートを**通さない**。**この決定は base / Instruct の
  どちらでも成立する**」と書いていた。**ADR-024 決定1(Instruct 主系統)でこの前提が崩れた。**
  Instruct モデルは自然文の指示をテンプレート経由で受け取る前提で調整されている。
- 決定:
  1. **FT データも評価項目も、全項目を `tokenizer.apply_chat_template` 経由で構成する。**
     「式のみ」という ADR-019 決定1 の条件は、**user メッセージの中身が `a+b=` であること**で満たす
  2. **T1 のアンカー項目(240 項目)だけは無テンプレート版も併走させ、
     差を「テンプレート税」として報告する。** 主軸の外の**感度解析**であり、補正なしと明記する
  3. **PLAN-002 §4.1.3 は本 ADR で全面的に置き換える。**
- 根拠:
  - **却下すべき組み合わせが1つある。**T1(裸書式)だけテンプレート無し、T2 / T3(自然文)だけ
    テンプレート有りにすると、**タスク型の軸に「テンプレートの有無」が混入する。主軸が交絡する**
  - したがって「全部通す」か「全部通さない」かの二択になる。**Instruct を選んだ以上、
    通さない側は分布外**であり、T2 の自由生成が W6 のリスクを取り戻す
  - **「裸の式」という条件の本質は、式以外の内容を訓練に入れないことであって、
    シリアライズ形式ではない。**役割トークンは**全条件・全タスク型に一様に**乗るので、
    タスク型の軸にも病変条件の軸にも交絡しない
- 却下した案:
  - **案 B(全項目を無テンプレートに統一し few-shot で書式を固定)**: 却下。
    ADR-024 決定1 で Instruct を選んだ意味が薄れ、T2 の自由生成が分布外になる
  - **案 C(FT は無テンプレート、評価は全項目テンプレート)**: 却下。
    **ペネトランスが全項目で下がる恐れ**があり、PLAN-003 §6.5 の Go/No-Go #4(>= 0.90)を
    割る主因になりうる。割れた場合、下流の解釈が全部無効になる
- リスク・未解決:
  - **役割トークンという表層が全条件に乗る。**結果の解釈は「この入力形式では」に限定される
    (ADR-019 リスク欄と同じ扱い)。テンプレート税(決定2)がその大きさの目安を与える
  - `infra/preflight.py` のトークン境界検査(PLAN-002 §4.1.5)は
    **テンプレート適用後の文字列に対して**行う必要がある。検査対象が変わる
  - `chat_template` はモデル revision に依存する。**`model.revision` の固定
    (PLAN-003 §11、旧・承認待ち-14)が本 ADR で必須になった**
- 関連 ADR: 024(D-1 が前提を壊した)、019 決定1(「式のみ」の解釈を本 ADR が明示)、023
- 関連 commit: (このコミット)

---

## ADR-026: タスク型に第4水準 `T1b`(裸の比較)を追加し、「入力書式 × 出力型」の 2×2 を閉じる

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「追加する」と回答。PLAN-003 §11-3 / 提案 P-1)
- 文脈: ADR-023 決定1 の T1 / T2 / T3 を操作的に分解すると、**2つの軸が同時に動いている**
  (PLAN-003 §3.1)。

  | タスク型 | 入力書式の距離 | 出力の型 |
  |---|---|---|
  | T1 裸の計算式 | **近い**(訓練書式そのもの) | 数値 |
  | T2 文章題 | 遠い(自然文) | 数値 |
  | T3 比較判断 | 遠い(自然文の質問) | **二値** |

  T1→T2 は入力側だけ、T2→T3 は出力側だけが動く。**「入力が近く、出力が二値」の第4セルが空いている。**
- 決定:
  1. **タスク型に第4水準 `T1b`「裸の比較」を追加する。**書式は `3+4>8?` → 強制選択 Yes / No。
     自然文を使わない
  2. **層・n は T3 と同一**(既知性3水準 × `carry` 2 × 極性2 × n=40 = **480 項目**、`id` 要求 **160 組**)
  3. **主軸のタスク型は4水準になる。**交互作用の自由度は (4-1)×(3-1) = **6**。
     PLAN-003 §6.1 の主要評価項目の df を 4 → **6** に改める
  4. **ADR-023 決定1 の T1 / T2 / T3 は置き換えない。**`T1b` は交絡を割る第4水準である
- 根拠:
  - **第4セルが空いたままだと、「T1 と T3 の勾配が違った」を入力書式のせいとも出力型のせいとも
    読めてしまう。**主軸の結論がそのまま交絡する
  - 4水準が揃うと、**入力書式の効果と出力型の効果を交互作用の中で直交対比として分離できる**
  - **コストがほぼゼロ。**強制選択なので 1 forward pass / 項目。FT run 数は増えない(ADR-028)
- リスク・未解決:
  - `id` 要求が 360 → **520 組**に増える。`K = 2000` を大きく下回るので
    `fill_cells` の「セル間で組を再利用しない」規約は維持できる(PLAN-003 §4.7)
  - **`3+4>8?` という書式は訓練データに一度も現れない。**T1b の `parse_fail_rate` が
    Go/No-Go #1 を割る可能性がある。割れた場合は few-shot 例示で書式を固定する
  - 自由度が 4 → 6 に増えるぶん、主要検定の検出力はわずかに下がる。
    **`p2` を 10 シードに置く配分(ADR-028)がこれを吸収する前提である**
  - `code/eval/battery/g6_comparison.py` に裸書式の `category` を追加する改修が**確定した**
- 関連 ADR: 023 決定1・2(タスク型と交互作用)、028(シード配分)、027(既知性の水準)
- 関連 commit: (このコミット)

---

## ADR-027: 主軸の共通被覆水準を3つに絞り、`oob_algebraic·ans_in` / `extrap_pair` を T2 以外限定の副次として残す

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「推奨通り残す」と回答。PLAN-003 §11-4 / 提案 P-2。
  **前段の「主軸を3水準に絞る」は P-2 の承認に含意される形で採択された** —— P-2 は
  「主軸から落ちた2クラスを副次に残すか」を問う項目であり、落ちること自体を前提にしている)
- 文脈: 交互作用を推定するには**全タスク型で同一の被覆水準**が要る。現行の被覆クラスは6つ
  (PLAN-001 §5.1、C1〜C6)。**T2(文章題)として自然に書けるか**で篩うと、負数と 0 を含む
  クラスが落ちる(PLAN-003 §3.3)。
- 決定:
  1. **主軸(交互作用モデル)の共通被覆水準は `id` / `interp` / `extrap_magnitude` の3つとする。**
     `extrap_magnitude` は主軸では `a, b >= 100`(両方正)に限る
  2. **`oob_algebraic·ans_in`(C3)と `extrap_pair`(C5)は、T2 を除く全タスク型
     (T1 / T1b / T3)限定の副次「汎化半径の地図」として残す。交互作用モデルには入れない**
  3. **`oob_algebraic·ans_out`(C4)は落とす。**T2 で書けないうえ、比較判断では構成的に空
     (PLAN-002 §4.6.1)
  4. 副次セルの層・n は各タスク型の主軸セルと同一の規約に従う(`carry` 2 水準、極性2、n = 40)。
     **`id` 要求は増えない**(2クラスとも定義上 `id` ではない)
- 根拠:
  - **これは恣意的な削減ではない。**要因計画が「全タスク型で共通の層」を要求し、
    **「りんごを −3 個持っている」が自然文にならない**ことが水準を決めている。
    現行の6クラス構成は T1 / T3 に相当するものだけを念頭に置いていたのでこの制約を持っていなかった
  - 意味づけが通る: `id` vs `interp` = **記憶か訓練域内の汎化か**(人間の問いの核心) /
    `interp` vs `extrap_magnitude` = **汎化の半径**
  - **副産物: `x2` の偶然一致問題が消える。**`x2` の規則値が真値と一致するのは `t = 0` のときだけ。
    3水準はすべて `a, b >= 1` なので `t >= 2`。`CLAUDE.md` §6 が要求する除外リストが `x2` について不要になる
  - P-2 を残す理由: 捨てると PLAN-002 §4.6.1 の3対比(**負性 / 被演算子の新規性 / 答えの新規性**)が
    失われる。裸書式と強制選択なので安い
- リスク・未解決:
  - **副次セルは T2 を欠く。**したがって「タスク型 × 既知性」の完全な格子にはならず、
    交互作用としては読めない。**記述的な地図としてのみ報告する**
  - 主軸から `oob_algebraic` が落ちたことで、**0 / 負の被演算子に対する振る舞いは
    確証的な主張に使えない**。Limitations に書く
  - `extrap_magnitude` の上限 `M*` と桁数掃引の粒度は未決(PLAN-003 §11)。
    `a, b in [100,199]` に固定するなら不要になる可能性がある
- 関連 ADR: 019 決定4(被覆ラベル)、021(`t` 水準は別軸)、023 決定2(交互作用)、026(タスク型4水準)
- 関連 commit: (このコミット)

---

## ADR-028: 条件のシード配分を確定する(`p2d` を主軸に格上げし、合計 40 run)

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「シードを 10 にあげる」と回答。PLAN-003 §11-5)
- 文脈: PLAN-003 §3.4 は 35 run(`p2` 10 / `ident` 10 / `p2d` 5 / `arb` 5 / `x2` 5)を提案し、
  「**`p2d` を 10 に上げれば「和を経由するか」を交互作用の枠内で検定できる**」と付記していた。
- 決定:

  | 条件 | 規則値 | 役割 | シード |
  |---|---|---|---|
  | `p2` | `t+2` | **主条件** | **10** |
  | `ident` | `t` | FT 自体の副作用の対照。適格性フィルタと床の基準 | **10** |
  | `p2d` | `t+2+(t mod 10)` | **主軸に格上げ。**和を経由するかの署名 | **10** |
  | `arb` | `table[t]` | 和への routing probe(ADR-020)。`id`/`interp` 限定 | 5 |
  | `x2` | `2t` | 非結合的変換の対照 | 5 |
  | `none` | — | 学習しないベースライン | 0 run |

  **合計 40 run。**
  1. **`p2d` に対しても `p2` と同一の交互作用モデル(`is_rule ~ task * coverage + ...`)を当てる。**
     これを**副次的評価項目**に加える(PLAN-003 §6.4 の階段。順1 の前に置く)
  2. **主要評価項目は `p2` 条件のみで変わらない。**多重性を増やさないため
- 根拠:
  - `p2d` の規則値は**和の一の位を読まないと計算できない**。`p2`(定数シフト)との差は
    「和を経由したか」の**構成的な署名**であり、ADR-023 が空いていると判定した**層3**の中核である
  - 5 シードでは交互作用(df = 6、ADR-026)を当てる検定力が足りない。
    **10 に上げて初めて `p2` と同じ土俵で比較できる**
  - 増分は **+5 run** のみ。`arb` / `x2` は主軸に要らない対照なので 5 のまま据え置く
- リスク・未解決:
  - **`p2d` の学習可能性そのものが未検証である。**定数シフトより難しい規則なので、
    ペネトランス(Go/No-Go #4)が `p2` より低く出る可能性がある。
    **低く出ること自体が結果である**(和を経由しないなら学習できないはず)が、
    **床に張り付いた場合は交互作用が読めない。**パイロットで `p2` と併せて確認する
  - `p2` と `p2d` は `t = 0 (mod 10)` で規則値が一致する。該当 **981 組(10.0%)**は
    **共有プールの段階で除外する**(条件ごとに項目集合が変わると `(1 | item)` が条件と交絡するため)
  - GPU 予算が 35 → 40 run に増える。**`CLAUDE.md` §2「人間の承認なく 10 GPU時間超のジョブを
    起動しない」は生きている。**LoRA のグリッドは別 PLAN
- 関連 ADR: 022(`p2d` の新設)、023 決定2(交互作用)、026(タスク型4水準)、006(反復単位はシード)
- 関連 commit: (このコミット)

---

## ADR-029: `t` ホールドアウト `T_hold`(20 個)を訓練サンプリングの設計変数にする(P-3)

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「入れる」と回答。PLAN-003 §11-8 / 提案 P-3)
- 文脈: ADR-021 は `t` 水準の被覆ラベル(`t_seen` / `t_unseen`)を新設したが、**`K = 2000` の
  ランダム抽出では 197 個の `t` のうち 187〜190 個が偶然被覆される。**結果 `interp` の
  **99.6% が `t_seen`** になり、`interp × t_unseen` のセルが n = 40 を満たせない
  (PLAN-003 §5.2)。しかも未被覆の `t` は分布の両端に集中するので、残った少数も `t` の大きさと交絡する。
- 決定:
  1. **`[2,198]` の 197 個の和から 20 個を `T_hold` として予約し、
     `t ∈ T_hold` の組を `K` の抽出母集団から除く。**
  2. **予約は `carry` 層の密度に比例させる**(`carry` 側 4 個 / `nocarry` 側 16 個)。
     **等間隔に取ってはならない** —— `carry`(一の位が 8/9)側が 0 個になり、
     `interp × t_unseen × carry` が構成的に空になる
  3. `T_hold` は **`pool_split_seed` に紐づく設計定数**であり、**実験シードで動かしてはならない**
     (ADR-021 決定5 と同じ規約)。`build_manifest` に記録する
  4. `code/data_gen/pool.py` の抽出母集団から `T_hold` を引く経路を足す。
     **不変条件を `code/tests/test_design_facts.py` に固定する**
- 根拠(**すべて組合せ論的な計数。実験結果ではない**):

  | 量 | 値 |
  |---|---|
  | `[2,198]` の和 | 197 個(うち `carry` = 一の位が 8/9 は **39 個**) |
  | `T_hold` の例(等間隔 + `carry` 比例配分) | `{6,20,28,32,44,56,70,78,82,94,105,117,128,131,143,155,167,178,181,193}` |
  | 主域から落ちる組 | **992 組(10.1%)。**うち `carry` **196 組** |
  | `K` の抽出母集団 | **8,809 組**(`carry` 1,764 = 20.0%。層別密度は保たれる) |
  | ~~`p2`/`p2d` 判別不能(`t ≡ 0 mod 10`)の除外を重ねた母集団~~ **★これは `K` の抽出母集団ではない** | ~~**7,916 組**~~ |
  | **`interp × t_unseen` の候補** | **904〜992 組**(`carry` 196)。**n = 40 のセルが埋まる** |

  - **なぜこのセルが要るか**: 「`(a,b)` は未見だが `t` は既見」の項目は**和を鍵にした表引き**でも
    病変値を出せる。**「`(a,b)` も `t` も未見」の項目で病変値が出て初めて、
    モデルが和を計算してから規則を適用したと言える。**人間の問い「どのように計算を行っているのか」に
    最も直接的に答えるのがこの対比である
  - `K = 2000` は 8,809 組の 23%。抽出は無理なく回る

  > **★2026-08-27 注記(ADR-034)。**上表の 7,916 行は**誤読を招いた。**この行を
  > 「`K` の抽出母集団」の意味に読んだ実装が、判別不能の除外を訓練被覆にも掛けていた。
  > **人間が PLAN-002 §12-11 を「掛けない」で決着させたので、`K` の抽出母集団は
  > `D_pool` = 8,809 組(main 領域では 4,309 組)である。**7,916 は
  > 「判別不能の除外を重ねるとこうなる」という参考値にすぎず、**評価項目の側の数**である。
  > 数の正本は `code/tests/test_design_facts.py`(4,309 / `K_main` の `carry` 393)。
- リスク・未解決:
  - **訓練分布が「和について穴あき」になる。**`p2`(定数シフト)には影響しないが、
    `p2d` / `arb` の学習可能性は下がりうる。**これは測定対象そのものなので、
    下がること自体が結果である**(和を経由しないなら穴は関係ないはず)。
    ただし **`p2d` のペネトランス(PLAN-003 §6.5 #4b)が床に張り付いた場合、
    穴あきのせいか規則の難しさのせいかは分離できない。**この交絡を Limitations に書く
  - `code/data_gen/ft_data.py` の層別サンプリング(PLAN-002 §4.2)に軸が1本増える
  - `|T_hold| = 20` は**実験条件**である。20 は「`interp × t_unseen` が n=40 を満たす最小規模に
    余裕を持たせた値」であって最適化した値ではない
- **2026-08-24 追記(IMPLEMENTER。仕様追随の実装時に判明)**:
  - **根拠表の「`interp × t_unseen` の候補 904〜992 組」は `D_train`(9,801 組)全体の計数である。**
    評価に使えるのは main 領域の分だけで、PLAN-002 §4.7 の pilot / main 分割を通すと
    **約半分になる**。`pool_split_seed = 20260822` の1回の分割で **492 組(`carry` 95)**、
    `p2d` 除外(`t ≡ 0 mod 10`)後 **448 組(`carry` 95)**。
    **n = 40 のセルは埋まる**(`carry` 側で 2.4 倍の余裕)が、
    PLAN-003 §6.4 順6 の層をタスク型ごとに分けて**排他の**組を要求する設計にすると足りない。
    → **PLAN-002 §12-10** に人間の判断として立てた
  - **決定3 の「`pool_split_seed` に紐づく設計定数」は、`pool_split_seed` を消費するという
    意味ではない。**根拠表の `T_hold` は等間隔 + `carry` 比例配分の**決定的な構成**であり、
    実装で再現したところ**表の 20 個と完全に一致した**(乱数を使わない)。
    「`pool_split_seed` と同じ固定層に属する = 実験シードで動かさない」の意味に取り、
    PLAN-002 §4.2.1a にそう書いた
  - 手順の順序を PLAN-002 §4.7 で明示した: **pilot / main 分割が先、`T_hold` の除去が後。**
    先に `T_hold` を引くと 992 組が領域の割当そのものから外れ、
    `interp × t_unseen` が片方の領域に偏る
  - 追随させた箇所: PLAN-002 §4.2.1 / **§4.2.1a(新設)** / §4.2.2 / §4.2.3 / §4.2.4 /
    §4.7 / §4.8(`t_holdout` ブロック)/ §4.8.1(検査 9・10)/ §4.9.2(10〜12)/
    §4.9.3(9〜13)/ §12-1 / §12-10(新規)
- 関連 ADR: 021(`t` 水準ラベル)、019 決定3(訓練域)、028(`p2d` 10 シード)、023
- 関連 commit: (このコミット)

---

## ADR-030: R8(閾値掃引による曲線シフト `Δ̂`)を実験に組み込む。手続きを確定する

- 日付: 2026-08-23
- ステータス: **採択**(2026-08-23、人間が「R8 を実験の中に入れる。方法については任せる」と回答。
  PLAN-003 §11-7)。**決定2〜6 の手続きはエージェントが確定した。**
- 文脈: 固定オフセットの `rule_rate` は 2〜3 点しか使わない。`θ` を掃引すると
  「Yes と答える確率」を `θ` の関数として推定でき、**曲線の 0.5 交差点のシフト `Δ̂`** が取れる。
  `Δ̂` は連続量なので検出力が高く、**予測される関数形そのものが「和を経由しているか」の
  操作チェックを兼ねる**(bag-of-heuristics 対策)。
- 決定:
  1. **R8 を採用する。**ただし**主要評価項目にはしない。**`Δ̂` は T1 / T2 では定義できず
     (数値出力なので閾値が無い)、**主軸の交互作用は全タスク型で同じ尺度でなければ組めない**。
     主軸は `rule_rate`(固定オフセット)のまま。**R8 は PLAN-003 §6.4 の階段の順8・順9**
  2. **`θ` は 17 水準 `{-3, -2, …, +13}`。**下端は `ident` の交差点(0 付近)を挟むため、
     上端は `p2d` の最大値 `2 + 9 = 11` を上回るため。**`x2` の `Δ = t` はこの窓に入らないので、
     `x2` は掃引の対象から外す**(固定オフセットのみ)
  3. **掃引の対象タスク型は T3 と T1b の両方。**ADR-026 が「入力書式の効果と出力型の効果を
     分離する」ために T1b を足した以上、**`Δ̂` を T3 だけで測るとその分離が掃引に及ばない。**
     コストは1トークンの強制選択なので許容できる
  4. **項目**: 各 (タスク型 × 既知性 × `carry`) セルから **n = 20 の部分集合**を取り、
     極性2 × 17 水準を掛ける。
     **T3 3×2×20×2×17 = 4,080 / T1b 同 4,080 = 計 8,160 forward pass / run。**
     部分集合は `item_id` のハッシュで決め、**条件間・シード間で完全に固定する**
  5. **固定オフセットは 17 水準の部分集合である。**`θ ∈ {0,+1}`(`>` 形式)/ `{+1,+2}`(`<` 形式)は
     掃引に含まれるので、**掃引を1回回せば主軸の `rule_rate` も同時に得られる。二重に回さない**
  6. **`Δ̂` の推定手続き**(事前登録する):
     - **シード × 条件 × (タスク型 × 既知性) セル**ごとに、`P(Yes)` を `θ` のロジスティック関数
       `logit P = β0 + β1·θ` で当て、**0.5 交差点 `θ* = −β0/β1`** を取る
     - 極性は**同数混ぜたまま同一の当てはめに入れる**(`<` 形式は `θ` の符号を反転して揃える)。
       応答バイアスは `β0` に吸収される
     - **`Δ̂ = θ*(条件) − θ*(`ident`)`**。シードを反復単位として集計する(ADR-006)
     - **`β1 ≤ 0` または当てはめが収束しないセルは除外し、件数を必ず報告する。**
       曲線が単調でないセルは「閾値を読んでいない」証拠であって、無理に交差点を取らない
  7. **予測される関数形**(事前登録する):

     | 条件 | 規則値 | 予測される `Δ(t)` |
     |---|---|---|
     | `ident` | `t` | 0(全 `t`) |
     | `p2` | `t+2` | **+2(定数)** |
     | `p2d` | `t+2+(t mod 10)` | `2 + (t mod 10)`(**`t` について周期10のノコギリ波**) |
     | `arb` | `table[t]` | `table[t] − t`(`t_seen` でのみ予測可能) |

- 根拠:
  - **推定量が連続量になり検出力が上がる**(2〜3 点の比率 → 17 点の曲線)
  - **`Δ = 2` そのものに TOST が掛かる。**「`ident` より高い」ではなく
    **「病変値ちょうどに一致する」**を主張できる(§6.4 順8)
  - **`p2d` のノコギリ波は、和を計算してその一の位を読む以外に説明のしようがない。**
    ADR-023 が「空いている」と判定した層3に、行動だけで届く数少ない手段である(§6.4 順9)
  - 追加コストは 8,160 forward pass / run(1トークンずつ)。**自由生成が 600〜1,000 項目なので、
    GPU 時間の支配項は変わらない**
- リスク・未解決:
  - `code/eval/battery/g6_comparison.py` の `build_items` は現在 `is_discriminating` を強制し、
    非判別オフセットで `ValueError` を投げる。**掃引モードでは緩める改修が必須。**
    緩めたことをテストで固定する(`test_battery_g6.py`)
  - **17 水準・n=20 は実験条件である。**検出力計算に基づく値ではない
  - `arb` は `t_seen` でのみ `Δ` が予測できる。`t_unseen` の掃引は**記述のみ**に使う
  - 曲線の当てはめが落ちるセルが多いと、順8・順9 の解釈が制限される。
    **除外率を Go/No-Go とは別に、結果として必ず報告する**
- 却下した案:
  - **R8 を主要評価項目にする**: 却下。T1 / T2 で定義できず、主軸の交互作用と尺度が揃わない
  - **T3 だけ掃引する**: 却下。ADR-026 の分離が掃引に及ばない
  - **`x2` も掃引する**: 却下。`Δ = t` は 17 水準の窓を大きく外れる
- 関連 ADR: 026(T1b)、028(`p2d` 10 シード)、022(`p2d`)、020(`arb`)、005(主要は1つ)、023
- 関連 commit: (このコミット)

---

## ADR-031: `model.revision` は「最初に pull した時点の HF コミットハッシュ」で固定する。原典一致の要求は取り下げる

- 日付: 2026-08-24
- ステータス: **採択**(2026-08-24、人間が「#14 について AI の判断を承認する」と回答。
  `plans/PLAN-003-redesign.md` §11-14)
- 文脈:
  - ADR-018 決定1 は「モデル変種と revision は**設計判断ではなく Feucht et al. の原典からの転記**とする」
    と定めた。ADR-008 はその転記を実施し、**revision は原典にも公式コードにも記載が無い**ことを
    確認した(`Documents/02_RELATED_WORK.md` §A.1.1)。この時点で「原典との厳密一致は
    revision 水準では原理的に保証できない」という限界が記録された
  - **その後 ADR-024 決定1(D-1)が ADR-008 / ADR-018 決定1 を上書きし、主系統を `-Instruct` にした。**
    乖離は revision 水準ではなく**変種水準**で発生している。revision を原典に合わせる対象は
    もう存在しない
  - 一方 ADR-025(全項目をチャットテンプレート経由)により `chat_template` が revision に依存し、
    **revision の固定は preflight と FT データ生成の前提条件**になった。
    必要性の出所が「原典への忠実さ」から「**我々自身の実験内部の再現性**」に入れ替わっている
- 決定:
  1. **`model.revision` には、最初に重みを pull した時点の HF コミットハッシュを入れる。**
     タグ名(`main` など)では固定しない。タグは移動しうる
  2. **値は最初の pull まで確定しない。**それまで `configs/template.yaml` の `model.revision` は
     `null` のままでよい。ハッシュを得た時点で **config と `manifest` の両方**に書き、以後
     **全条件・全シードで同一の値を使う**
  3. **原典(Feucht et al.)との revision 一致は要求しない。**ADR-018 決定1 の
     「原典からの転記」は ADR-024 で失効している
  4. **限界の記述を差し替える。**「revision 水準では厳密一致を保証できない」ではなく
     「**変種水準で既に原典と異なる**」と書く。置き場所は `Documents/06_THREATS.md` **T13**(新設)
  5. **本項目を `CLAUDE.md` §8 の判断事項から外す。**性質は運用手続きであり、
     結果の解釈・条件の増減・claim の確定・引用の確定のいずれにも当たらない。
     以後の revision 固定はエージェントの手続きとして実行する
- 根拠:
  - 決定3・4: ADR-024 が上書きした条項の帰結を残すと、
    **「変種は合っているが revision だけが不確か」と誤読させる。実態より軽い申告になる**
    (`CLAUDE.md` §7)。base と Instruct の差は revision 差より2段階粗い
  - 決定1: ハッシュだけが重みの同一性を担保する
  - 決定2: pull 前にハッシュは知り得ない。**実在を確認していない値を書かない**(`CLAUDE.md` §2)
  - 決定5: 必要性の出所が内部再現性に移った以上、研究判断として人間に留保する理由が消えた
- 帰結:
  - **実装の入り口のブロッカーが消えた。**`Documents/04_EXPERIMENT_PLAN.md` Phase 0 タスク2 の
    「★承認待ち-14 が決まるまで着手しない」を解除する
  - `infra/preflight.py` は **`model.revision` が `null` のまま本実行に入ることを弾く**
    (`configs/template.yaml` 冒頭の「null のまま実行しない」規約の対象。**実装は未着手**)
  - `Documents/06_THREATS.md` に **T13** を新設した
  - 追随させた箇所: ADR-008 帰結欄 / `configs/template.yaml` / `Documents/00_OVERVIEW.md` §6 /
    `Documents/02_RELATED_WORK.md` A 表 / `Documents/04_EXPERIMENT_PLAN.md` §0
- リスク・未解決:
  - **Feucht et al. の機構地図が Instruct 版にも成立するかは仮定である。**検証していない。
    Q10(機構重なり)は ADR-024 の帰結として既に探索的へ降格済み
  - **本 ADR は Feucht et al. を論文1でどう位置づけるかを決めていない。**
    #11(G7 の扱い)と一体で人間が決める → **新規の承認待ち-16**
  - HF 上のモデルが取り下げ・改訂された場合、ハッシュだけでは重みを再取得できない。
    **重みをローカルに退避するかは決めていない**(容量とライセンスの検討が要る)。T13 に記載
- 関連 ADR: 008(**本 ADR が帰結欄を訂正**)、018 決定1(ADR-024 で失効済)、
  024(D-1。上書きの実行者)、025(`chat_template` が revision に依存)
- 関連 commit: (このコミット)

---

## ADR-032: T2(文章題)の5テンプレートを確定する。答え書式の指示を入れ、被演算子 1 を T2 から除外する

- 日付: 2026-08-26
- ステータス: **採択**(2026-08-26、人間が「5本ともこの文面で確定。D1 は入れる /
  D2 は 1 を除外 / D3 はそのまま」と回答。`plans/PLAN-003-redesign.md` §11-6 / §4.3)
- 文脈:
  - ADR-026 が主軸を **T1 / T1b / T2 / T3** の4水準にした。このうち **T2 だけが
    プロンプトに自然文を要求する**。その文面は実験条件であり、
    `CLAUDE.md` §8 により確定は人間の判断である
  - **承認待ち-6 は段階 A の最後のブロッカーだった。**項目生成が書けず、
    `infra/preflight.py` の検査6・8(`eval.anchor_manifest` / `eval.cells`)も
    FAIL のまま止まっていた(`STATE.md`)
  - `Documents/05_STATISTICS.md` §123 のモデルは `(1 | template)` を持つ。
    **5テンプレートはこの変量効果の水準そのもの**である
- 決定1: **5テンプレートの確定文面を採択する**(場面 = 物の個数 / 人数 / 距離 / 金額 / 時間)。
  正本は `configs/templates/t2.yaml` と PLAN-003 §4.3 の表。差し込みは `{a}` `{b}` のみ
- 決定2: **問いの語を5本で完全一致させる** —— `How many {単位} in total?`。
  テンプレート間で変わるのは**場面と単位語だけ**にする(§4.3 規約3 の厳格な読み)
- 決定3: **末尾に答え書式の指示を置く**。5本とも同一文
  `End your reply with "Answer: <number>".`。**CoT は禁止しない**
- 決定4: **T2 の項目生成から被演算子 1 を除外する。**`1 apples` が非文になるため
- 決定5: 群名は **`word_problem`**、category キーは
  `t2_count` / `t2_people` / `t2_distance` / `t2_money` / `t2_time`
- 根拠:
  - 決定2: テンプレートを変量効果として扱う以上、**水準間で動く要素は少ないほどよい。**
    問いの語まで揺らすと `(1 | template)` の分散に「場面」と「問い方」が混ざる
  - 決定3: `code/eval/parsers/base.py` の `single_integer` は**数がちょうど1つの
    ときしか通さない**(2個以上を採らないのは PLAN-001 §5.4 の 4)。文章題は復唱と
    途中計算で数が複数出るため、ここが Go/No-Go #1(`parse_fail_rate < 0.02`)の急所になる。
    **2026-08-26 に実測**: `320` / `Answer: 320` / `$320` / `320 dollars` /
    `There are 320 apples in total.` / `150+170=320` はすべて 320 を返し、落ちるのは
    **印が無く数が複数**の型だけだった。`answer` は `ANSWER_MARKERS` にあるので
    決定3 の指示がこの型を潰す。**CoT を禁じない案を採ったのは、PLAN-003 §7.1 が
    `cot.py` を「T2 の自由生成で必要性が上がった」として残した判断と整合させるため**
  - 決定4: `a, b >= 1` は3水準すべてで成立する(§3.3)ので `a = 1` は実際に発生する。
    非文のプロンプトを混ぜるより除外が安い。**主域の正象限 99 × 99 = 9,801 組のうち
    `a = 1` または `b = 1` は 197 組(2.0%)**(幾何的な数え上げであり実測値ではない)。
    外挿域(`|a|,|b| in [100,199]`)には影響しない。
    列挙体(`Apples in box A: {a}.`)に書き換える案は却下した —— 自然文でなくなり、
    **T1↔T2 の入力距離という T2 の存在理由が薄まる**(§3.1)
- 帰結:
  - **段階 A の最後のブロッカーが外れた。**T1 / T1b / T2 / T3 + 特異性対照の項目生成に
    着手できる。`code/data_gen/battery_items.py` の `SUPPORTED_GROUPS` に
    `word_problem` を足すのは T2 生成器の実装と同時に行う
  - `configs/templates/t2_draft.yaml` を **`configs/templates/t2.yaml`** に昇格した。
    **ただし本番の評価テンプレート集合は未完成である** —— T1 / T1b / T3 の本番文面は
    まだ確定していないので、`data.eval_template_set` はどの config でも `null` のまま
  - T2 生成器は**被演算子 1 の除外を実装し、manifest に記録する**必要がある(決定4)
- リスク・未解決:
  - **決定3 はタスク型の軸に「答え書式の指示の有無」を乗せる。**T1 のプロンプトは
    `{a}+{b}=`(PLAN-002 §4.1.1)で指示文を持たない。ADR-025 がチャットテンプレートで
    却下したのと**同じ形の交絡**である。今回は「裸の式 vs 自然文」というタスク型の操作に
    内在する差とも読めるため人間が採択したが、**T1 側にも同一文を足して揃える案は
    決めていない** → **新規の承認待ち-18**
  - **決定4 は T2 限定なので、T2 だけ被演算子分布が他タスクと厳密には一致しない。**
    全タスク型で 1 を除外して揃える案は決めていない → **新規の承認待ち-19**
  - **文面が Instruct モデルの分布内かは未検証である。**Go/No-Go #1 / #2 を
    `none` モデルで実測するまで、`parse_fail_rate` も `correct_rate` も予測でしかない。
    割った場合の降り方は **承認待ち-10(W6 の分岐)** のままで未決
  - 単位語(`dollars` / `meters` / `minutes`)は `numeric` パーサが落とすことを
    実測で確認したが、**確認したのは上記6例だけ**であり網羅ではない
- 関連 ADR: 024(D-3 英語統一)、025(**交絡の扱いの先例。リスク欄の論点はこれと同型**)、
  026(T2 を主軸の水準にした)、027(被覆3水準。T2 が水準を絞った)
- 関連 commit: (このコミット)

---

## ADR-033: 特異性対照の参照規則は評価プール manifest の**別欄**に載せる。プールの充填方法は manifest に記録する

- 日付: 2026-08-26
- ステータス: 採択
- 文脈:
  - `pool.build_manifest` の `reference_rules` は **§4.3 の偶然一致の除外を
    どの規則で計算したかの記録**であり、`scoring.validate_reference_rule` が
    「`eval.reference_rule` がこの集合に含まれること」を要求する(ADR-016)
  - 特異性対照(減算・乗算)の参照規則 `spec_sub` / `spec_mul` は
    **`reference_lesions_from_config` に混ぜてはならない**(`code/lesion.py:343`)。
    あの返り値は `pool.eligible_pairs` に渡って **FT データの除外集合**を決めるので、
    混ぜると `train.jsonl` が変わり PLAN-002 §3.4 の対照の設計が壊れる
  - 一方で特異性対照の**偶然一致の除外は行われている** —— ただし別機構で。
    `specificity_control.build_items` が項目1件ごとに `is_discriminating` を強制する
  - A-5 は `--dry-run`(manifest を書かない)に限ってこの欄の問題を回避したが、
    **manifest を書く A-6 では回避できない**(`logs/HANDOFF.md`)
  - **決定は人間が行った**(2026-08-26。3案を提示し「欄を分ける」を採択)
- 決定1: **`reference_rules` は加算側(`p2` / `x2` / `p2d` / `arb`)のままとする。**
  `spec_sub` / `spec_mul` をこの欄に混ぜない
- 決定2: `pool.build_manifest` に **必須引数 `specificity_reference_rules`** を新設し、
  manifest に同名の欄として記録する。本実行の特異性バッチは**この欄**に対して
  `validate_reference_rule` を掛ける
- 決定3: `pool.build_manifest` に **必須引数 `fill`** を新設する。
  「このプールをどう埋めたか」(方法 / シードを消費したか / 宣言されたセル表)を記録する。
  **セルの充填方法が未決である事実を manifest 自身に残す**ためであり、
  記録が無いと `seed` の欄だけが残って「サンプリングされたプール」に見える
- 決定4: **A-6 の CLI は `fill_cells` を呼ばない。**`eval.pool_items` の明示リストから
  項目を作る。`eval.cells` は宣言として読み、`fill` に転記するだけにする
- 決定5: `code/data_gen/ft_data.py` の CLI に **`--condition`** を足す。
  PLAN-002 §3.4 は「5条件は `lesion.condition` 以外のすべてを共有する」ことを
  要求するので、条件ごとに config を複製すると写し間違いで `train.jsonl` の
  バイト一致が壊れる。**1つの config から条件だけを差し替える**方が不変条件に近い。
  実際に使った条件は manifest の `lesion.condition` に残るので記録は失われない
- 根拠:
  - 決定1・2: 欄の意味(「`eligible_pairs` に渡した規則」)を保つ。
    フラットな和集合にすると `eval.reference_rule: spec_sub` を主軸のバッチに
    誤指定しても検査が素通しする。`code/lesion.py` が
    `reference_lesions_from_config` と `specificity_reference_lesions_from_config` の
    **2関数に分かれている構造**とも一致する。記録しない案は ADR-016 のガードが
    manipulation check に効かなくなるので却下
  - 決定4: **外挿域の上限 `M*` が未決である**(承認待ち-15。段階 C の実測で決まる)。
    `pool.extrapolation_pairs` は `M*` を要求するので、**`extrap` セルは
    段階 A では原理的に埋められない。**したがって主軸のセル表(PLAN-001 §5.1)を
    満たすプールは段階 A では作れない。セルの埋め方を決めても完成しない以上、
    ここで充填方針を決めるのは早い(`CLAUDE.md` §8)
- 帰結:
  - `pool.build_manifest` の呼び出し側は2つの引数を足す必要がある(既存の呼び出しは
    テストのみ)。`code/data_gen/eval_pool.py` が唯一の本番の呼び出し側になる
  - `code/eval/run.py` の `dry_run` docstring から**未解決の記述が消える。**
    特異性バッチも `validate_reference_rule` を通るようになる
  - **preflight の検査6・8 が PASS になる**(A-6 の完了条件)
  - **段階 A は完了する。**次は段階 B(人間の決定)と段階 C(GPU 小)
- リスク・未解決:
  - **`eval.pool_items` は暫定である。**`M*` が決まり、セルの充填方針が決まったら
    `fill_cells` を呼ぶ経路に置き換わる。そのとき `fill.method` が
    `"explicit_list"` から変わる
  - `fill` / `specificity_reference_rules` という**欄の名前はエージェントが決めた。**
    凍結までに人間が一度見ること(`STATE.md` の「実装で決めた命名」)
  - `configs/smoke.yaml` は `digit_modulus` / `arbitrary_table` を持たないので
    `data.matched_manifests` に**3条件(`p2` / `x2` / `ident`)しか宣言できない。**
    5条件そろえるのは実験用 config の仕事である。smoke は配線確認であって実験ではない
- 代替案:
  - **フラットな和集合**(`reference_rules` に4規則+2規則を全部入れる): 呼び出し側の
    改修が要らないが、欄の意味が薄まり誤指定を検査が止められなくなる → 却下
  - **記録しない**(特異性対照を `validate_reference_rule` の対象外と明文化): manifest の
    改修が不要だが、除外を計算した記録が残らない → 却下
- 関連 ADR: 016(参照規則と `reference_rules` 欄)、017(`fill_cells` の非再利用)、
  020(定義域外の扱い)、032(T2 の被演算子除外 = `item_exclusions` の先例)
- 関連 commit: (このコミット)

---

## ADR-034: `p2`/`p2d` 判別不能の除外は訓練被覆 `K` に掛けない。掛ける先は**評価項目**である ★重要

- 日付: 2026-08-27
- ステータス: 採択
- 文脈:
  - ADR-022 決定3 は「`p2` と `p2d` が同じ値を返す項目(`t ≡ 0 mod digit_modulus`)を落とす」
    と決めた。落とす理由は**採点で「どちらの規則を適用したのか」を区別できないから**であり、
    これは**評価側の要求**である(`CLAUDE.md` §6 の `rule_rate` が成り立たなくなる)
  - ところが ADR-029 の根拠表は「`t ≡ 0 mod 10` の除外を重ねた母集団 **7,916**」を
    **`K` の抽出母集団**の行に置いていた。実装(`ft_data.generate` 手順2b)はそれに従い、
    `eligible_pairs(..., indistinguishable_rule_pairs=...)` を `K` の抽出母集団に掛けていた
  - 帰結が2つあった(2026-08-24 に実装で判明。PLAN-002 §4.2.1 の ⚠️ ブロック):
    1. **`p2d` を設計に含む限り、訓練データに `t ≡ 0 (mod 10)` の式が1件も現れない。**
       `p2d` 条件のモデルだけが**自分の桁規則の「+0」の場合を一度も見ない**まま評価される
    2. **`carry` 密度が保たれない。**`t ≡ 0 mod 10` は一の位が 0 なので必ず `nocarry` であり、
       除外は片側だけを削る
  - 帰結1 は ADR-029 が既に記した「和について穴あき」の交絡を1段深くする。しかも消える剰余類は
    **`p2d` の規則値が `p2` と一致する剰余類そのもの**である。**`p2d` のペネトランス
    (PLAN-003 §6.5 #4b)が床に張り付いたとき、穴あきのせいか規則の難しさのせいかを分離できない**
  - **2026-08-27 に、設計事実テストと本番経路が食い違っていたことが判明した。**
    `code/tests/test_design_facts.py` の #13 / #13b と PLAN-003 §4.7 は判別不能の除外を
    **通さない**母集団の数(4,309 組 / `K_main` の carry 393)を固定しているのに、
    `code/data_gen/ft_data.py` の本番経路は**通していた**(3,894 組 / carry 435)。
    `configs/smoke.yaml` が `digit_modulus` を持たないので除外が発火せず、表に出ていなかった
  - **決定は人間が行った**(2026-08-27。PLAN-002 §12-11 の2案を提示し「掛けない」を採択)
- 決定1: **判別不能の除外(ADR-022 決定3)を `K` の抽出母集団には掛けない。**
  `K` は「領域 ∩ D_pool」に**真値との偶然一致の除外だけ**を掛けた母集団から抽出する
- 決定2: **真値との偶然一致の除外(ADR-016、PLAN-002 §4.2.1)は `K` に掛けたままにする。**
  訓練域には該当する組が構成的に存在しないが、除外関数は通す。通さないと `x2` 条件や
  不動点を持つズレ表で静かに壊れる経路が残る(PLAN-002 §4.2.1 の既存の判断を維持する)
- 決定3: 判別不能の除外を掛ける先は**評価項目**である。`pool.eligible_pairs` に
  `indistinguishable_rule_pairs` を渡す経路は**変えない**。`ft_data.indistinguishable_pairs_of`
  も残す —— `generate` はこれを**除外には使わず、manifest に記録するためだけ**に呼ぶ
- 決定4: FT manifest の `exclusions.indistinguishable_rule_pairs` 欄は残す。ただし意味を
  **「掛けた除外」から「評価側で掛ける除外の宣言」に読み替える。**同じ節に
  **`indistinguishable_rule_pairs_applied_to: "eval_items_only"`** を新設し、
  manifest の `schema_version` を **1 → 2** に上げる。欄を消さないのは、どのペアを判別不能と
  見なしたかが5条件で一致していることを preflight が照合できるようにするため(§4.8.1 検査5)
- 決定5: **数の正本は `test_design_facts.py` の 4,309 / 393 である。**ADR-029 根拠表の
  7,916(= `D_pool` 8,809 に判別不能の除外を重ねた数)は**訓練被覆の母集団ではない**。
  同表に注記を付ける。PLAN-002 §4.2.1 の ⚠️ ブロックは「旧実装の帰結」として書き直す
- 根拠(2026-08-27 の数え上げ。**組合せ論的な計数であって実験結果ではない**。`CLAUDE.md` §2。
  `pool_split_seed = 20260822` / `coverage_seed = 20260823` / main 領域 4,801 組 / `K_main = 2000`):

  | | (a) 掛ける(旧実装) | **(b) 掛けない(採択)** |
  |---|---|---|
  | `K` の抽出母集団 | 3,894 組 | **4,309 組** |
  | `K_main` の carry | 435(21.8%) | **393(19.7%)** |
  | 母集団が張る和 `t` | 157 種 | **174 種** |
  | `K` が張る和 `t` | 154 種 | **170 種** |
  | `id` セル候補(`K` から評価側の除外を掛けた残り) | 2,000 組(carry 435) | **1,808 組(carry 393)** |

  - 上4行は `coverage_seed` に依らない(比例配分の決定的な帰結)。**`id` セル候補の行だけは
    抽出シードに依る**(seed 0 なら 1,821 組、seed 1 なら 1,810 組)。carry 393 は依らない
  - **`id` セルの要求は 520 組(うち carry 240 組。PLAN-003 §4.7)。(b) でも 3.4 倍の余裕があり、
    埋まらないセルは無い。**これが「掛けない」を選べる条件である
  - (b) は `carry` 密度 19.7% を保つ。ADR-029 が「層別密度は保たれる」と書いた性質が
    `T_hold` の段までではなく**抽出の段まで**保たれる
- 帰結:
  - **`p2d` 条件のモデルは訓練で `t ≡ 0 (mod 10)` の式を見る。**PLAN-002 §4.2.1 の
    ⚠️ 帰結1・2 は**どちらも消える。**Limitations に書く予定だった交絡が1つ減る
  - **評価項目の側は何も変わらない。**`t ≡ 0 (mod 10)` の項目は従来どおり評価プールに入らない。
    したがって `id` セルは `K` の全体ではなく `K` の 90.4%(1,808 / 2,000)から引く
  - **`digit_modulus` を `[MATCHED]` から外してよいという意味ではない。**この値は
    (i) `p2d` 条件の `target`、(ii) manifest の `exclusions` 欄(= 評価側の除外の宣言)を決める。
    ただし **`K` は動かなくなった** —— `configs/template.yaml` の該当コメントを訂正する
  - **副次: 参照規則の集合は、現在の設計では `K` を1組も動かさない。**`p2`(`t+2`)・
    `x2`(`2t`)・`p2d`(`t+2+(t mod 10)`)はいずれも真値と一致しない。`arb` だけが
    一致しうるが、PLAN-002 §7.3 制約2(`>= t + 2`)がそれを禁じている。
    `code/tests/test_ft_data.py` は**不動点を持つ表を明示的に渡して**この経路が
    生きていることを固定する(`test_a_reference_rule_that_coincides_with_the_true_sum_...`)
  - `test_design_facts.py` に**本番経路 `generate` を通すテストを追加した。**設計事実の
    4,309 / 393 と本番経路が同じ数を出すことを、経路ごと縛る。今回の食い違いは
    「同じ数を別々の道で計算していて、突き合わせていなかった」ことが原因である
- リスク・未解決:
  - **`id` セルの母集団が `K` と一致しなくなった**(1,808 / 2,000)。PLAN-003 §4.7 の
    `id` 要求 520 組は満たすが、**`id` セルの抽出は「`K` から一様」ではない** ——
    `t ≡ 0 (mod 10)` を落とした残りから引く。この非一様性は `carry` 層には効かない
    (落ちる組はすべて `nocarry`)が、**`t` の分布には効く。**順4 の項目生成で明示する
  - **`arb` のズレ表はまだ未確定**(`arbitrary_table: null`)。表が確定したとき、
    不動点を持たないこと(PLAN-002 §7.3 制約2)を preflight か生成時に検査する必要がある。
    現状は `validate_reference_lesions` が「全項目で一致」しか弾かない
  - **schema 版を上げたが、既存の manifest は無い**(`results/` は空、`runs/` に本実行なし)。
    移行の問題は起きない
- 代替案:
  - **掛ける(旧実装のまま)**: 実装を触らずに済み、`p2d` 条件の `rule_rate` に
    「`p2` を適用しただけの応答」が訓練分布の側からも混ざらない。しかし帰結1 の交絡が残り、
    **`p2d` は主軸の条件(10 シード。ADR-028)である**以上、主要な比較が読めなくなる → 却下
  - **`K` にも掛け、かつ `p2d` を主軸から降ろす**: 交絡は消えるが ADR-028 の 40 run 構成が壊れ、
    H3 の記述長交絡の統制(ADR-022 の目的)そのものが失われる → 却下
- 関連 ADR: 016(参照規則と偶然一致の除外)、022(`p2d` の追加と決定3)、
  028(条件のシード配分)、029(`T_hold`。根拠表の 7,916)、033(manifest の欄を分ける先例)
- 関連 commit: (このコミット)

---

## ADR-035: T1 は素の書式のまま。「指示付き T1」を副次セルとして足す。被演算子 1 の除外は**評価項目のみ**全タスク型に広げる

- 日付: 2026-08-27
- ステータス: 採択
- 文脈:
  - ADR-032 決定3 は T2(文章題)の5テンプレートに答え書式の指示
    `End your reply with "Answer: <number>".` を入れた。**T1 には無い**(`{a}+{b}=` のまま)
  - ADR-032 のリスク欄が2件の未決を生んだ(PLAN-003 §11-18 / §11-19):
    - **#18**: このままだと**タスク型の軸に「指示の有無」が乗る。**T1 と T2 の差が
      「裸の式 vs 文章題」なのか「指示なし vs 指示あり」なのかを分離できない
    - **#19**: ADR-032 決定4 の被演算子 1 の除外は **T2 限定**なので、
      **T2 だけ被演算子分布が他タスク型と厳密には一致しない。**交互作用の解釈に効く
  - **決定は人間が行った**(2026-08-27。#18 = 「現状維持 + 副次セル」/ #19 = 「評価項目のみに広げる」)
- 決定1: **T1 の書式は変えない。**`{a}+{b}=` のまま、答え書式の指示を足さない。
  主軸の4水準(T1 / T1b / T2 / T3)の文面は ADR-026 / ADR-032 のまま凍結する
- 決定2: **「指示付き T1」を副次セルとして新設する。**
  - 項目: **T1 と同一の被演算子対**に `End your reply with "Answer: <number>".` を付けた版
    (T2 と**逐語で同じ文**を使う。違う文にすると効果が文面差と交絡する)
  - セル: **`id` × {`carry`, `nocarry`} の2セル。1セル n = 40。合計 80 項目**
  - **主軸の交互作用モデルには入れない。**探索的であり、PLAN-003 §4.8(汎化半径の地図)と
    同じ扱いにする。多重比較の補正は行わず、**補正なしと明記して報告する**
  - 目的は**「指示の有無」の効果量を実測すること**である。効果が小さいことを示せれば
    #18 の交絡は Limitations の1行で済み、大きければ主軸の解釈に条件を付ける
- 決定3: **被演算子 1 の除外を、全タスク型の「評価項目」に広げる。**
  T1 / T1b / T2 / T3 と特異性対照のすべてで、`a = 1` または `b = 1` の項目を評価プールに入れない
- 決定4: **訓練被覆 `K` には広げない。**`K` の抽出母集団から被演算子 1 を除かない
- 根拠:
  - 決定1・2: T1 を書き換えると**訓練書式(`{a}+{b}=`)と評価書式の一致が壊れる。**
    PLAN-002 §4.1 は訓練プロンプトを1文字単位で固定しており、T1 はその書式のアンカーである。
    ADR-025(チャットテンプレートを全項目で通す)と合わせても、T1 だけは
    「FT で見た形そのもの」であることに意味がある。指示を足すと `id` セルの
    `rule_rate` が「規則を書き換えたか」ではなく「指示に従えたか」を測りうる
  - 決定2 のセル選択: **`id` に限る**のは、指示の効果を見るのに被覆水準を掃く必要が無いため。
    `carry` で分けるのは、答え書式の指示が桁上がりのある問題で効きやすいという素朴な予想を
    見るため。80 項目は 1 model あたり 80 forward pass であり、**追加 GPU コストは無視できる**
  - 決定3: 被演算子 1 は `1 + n` という**恒等写像に近い形**であり、
    「+2 を学んだ」のか「1 を足す形だけ特別扱いした」のかが分離しにくい。ADR-032 決定4 が
    T2 で除いた理由と同じであり、**タスク型で除外規則が違うほうが交互作用の解釈を壊す**
  - 決定4: **`K` にも広げると答えが1桁の層が潰れる。**実測(2026-08-27 の数え上げ。
    **実験結果ではない**): `K_main = 2000` の1桁層(`carry:1` + `nocarry:1`)は
    **6 組 → 3 組**になり、母集団側も 12 組 → 6 組に半減する。
    PLAN-002 §12-1 で「1桁層に下限を置かない(比例配分のまま)」と決めた以上、
    ここをさらに削ると層が実質消える
  - 決定3 + 4 の非対称は**意図的**である。評価項目は `K` から引くが、
    **引くときにフィルタを掛けるだけ**であり、訓練分布は動かさない。ADR-034 が
    判別不能の除外について採った構えと同じ形にした(**除外は評価側に寄せる**)
- 帰結(2026-08-27 の数え上げ。`coverage_seed = 20260823`):
  - `id` セル候補は **1,808 組(carry 393)→ 1,776 組(carry 386)**。
    **`id` 要求 520 組(carry 240)に対し影響は無い**
  - 評価主域(`D_main` 39,601 組)からも被演算子 1 が落ちるが、
    `interp` セルは1セル 40 件なので桁違いに余る
  - **T2 だけ除外規則が違う状態が解消する。**ADR-032 決定4 は「T2 限定」ではなくなり、
    **全タスク型共通の項目規約**に昇格する。PLAN-003 §4.3 の記述を §4 全体に移す
  - 評価プール manifest の `item_exclusions`(ADR-033 決定3 の先例)は
    **タスク型ごとの欄ではなくプール全体の欄**に `excluded_operands: [1]` を持つ形になる
- リスク・未解決:
  - **副次セルは主軸の事前登録に入らない。**入れると df がさらに増え、ADR-026 で
    4 → 6 にした交互作用の df がまた動く。**探索的として報告することを凍結時に明記する**
  - **「指示付き T1」の項目は `T1_instructed` のような群名を要する。**群名は未定であり、
    `code/eval/battery/battery_items.py` の `SUPPORTED_GROUPS` に足すのは**順4**の仕事
  - **実装はこの ADR では行わない。**§11-18 の副次セルも §11-19 の評価側除外も、
    項目生成と評価プールの作り直し(PLAN-004 順4)で入れる。順1・順2 に依存する
  - 被演算子 1 を評価から除くと、**`a = 1` の項目が1件も無い**ことになる。
    「モデルが 1 を特別扱いするか」は**問えなくなる。**問いたくなったら別の探索的セルを足す
- 代替案:
  - **#18 で T1 にも指示を足す**: 軸の交絡は消えるが、訓練書式と評価書式の一致が壊れ、
    `id` セルの意味が変わる → 却下(決定1 の根拠)
  - **#18 で現状維持のみ(副次セル無し)**: コストはゼロだが、交絡の大きさが
    **一度も測られないまま Limitations に書かれる。**測れるものは測る → 却下
  - **#19 で T2 限定のまま**: 実装が要らないが、タスク型ごとに被演算子分布が違う状態が残り、
    交互作用(主軸そのもの)の解釈に穴が開く → 却下
  - **#19 で `K` にも広げる**: 訓練と評価の分布が完全に揃うが、1桁層が 6 → 3 組に潰れる → 却下
- 関連 ADR: 025(チャットテンプレート)、026(`T1b` の追加と df = 6)、
  027(副次の扱い)、032(T2 の文面と被演算子 1 の除外)、033(`item_exclusions`)、
  034(除外を評価側に寄せる構え)
- 関連 commit: (このコミット)

---

## ADR-036: G7(周期的概念)を落とす。Feucht et al. (2026) は Intro の対立軸としてのみ引用する

- 日付: 2026-08-27
- ステータス: 採択
- 文脈:
  - G7 は ADR-018 決定5 でゲートキーピング1位 → ADR-023 決定4 で**オプションに格下げ**された。
    人間は「興味深いことが言えるなら使ってよい」と述べており、廃止ではなかった(PLAN-003 §8.4)
  - **D-1(Instruct 主系統。ADR-024)が G7 の価値を下げた。**Feucht et al. の解析は base モデルに
    対するものであり、Instruct を選んだ以上「同じ回路がこのモデルにもある」は**仮定**になる
  - レビュー W5 は「G7 の全設計が**単一のプレプリント**に依存する」ことを指摘していた。
    `CLAUDE.md` §3 は「2026年以降の arXiv ID は査読前プレプリントの可能性が高い。
    **主要な論拠には使わない**」と定めている
  - PLAN-003 §11-16 は Feucht et al. (2026) の位置づけを3案((a) Intro の対立軸としてのみ /
    (b) G7 を残して書式の出所も兼ねる / (c) 引用しない)で立てていた。**#11 と一体の判断**である
  - **決定は人間が行った**(2026-08-27。#11 = 案 B / #16 = 案 (a))
- 決定1: **G7 を落とす**(PLAN-003 §8.4 の案 B)。周期的概念への転移は Phase 1 の評価に含めない
- 決定2: **Feucht et al. (2026) は Intro の対立軸としてのみ引用する**(§11-16 の案 (a))。
  **引用そのものは消さない。**`Documents/02_RELATED_WORK.md` A 表に位置づけを明記する
- 決定3: **PLAN-002 §5.1(G7 の項目構成。165 項目)と §12-3 を廃止する。**
  打ち消し線 + 理由 + 日付で残す(`CLAUDE.md` §2)。**削除しない**
- 決定4: **レビュー R7(周期概念×算術特徴の共有について査読済み文献を1本原典確認)は不要になる**
- 決定5: **承認待ち-13(`arb` ズレ表の `table[1]` の穴)は消滅する。**
  `table[1]` を要求していたのは G7-H(24時制)の `x = 0, n = 1` の1点だけだった。
  **ズレ表の定義域は `[2, 198]` のまま**(ADR-020 決定)であり、変更は無い
- 決定6: **承認待ち-17(Nikankin et al. (2025) の原典確認)を必須に格上げする。**
  Feucht を Intro のみに降ろした以上、**Intro の対立軸の一方を Nikankin が単独で支える。**
  現在 `02_RELATED_WORK.md` A 表で ⚠️(要原典確認)である。SCOUT に投げる
- 根拠:
  - 決定1: **確証的な主張の支柱にできない**(単一プレプリント依存 + base/Instruct の不一致)。
    探索的アドオン(案 A)として残す道もあったが、**探索的な結果は主軸の解釈を変えない**一方で、
    項目生成・パーサ・セル定義・原典確認(R7)のコストは確定的に発生する。
    Phase 0 の完了を最短にする判断として落とす
  - 決定2: `CLAUDE.md` §3 の「プレプリントは主要な論拠に使わない」は**引用の禁止ではない。**
    Intro で「先行研究はこう見ている」という**対立軸の提示**に使う限り、主張の重みを
    プレプリントに預けていない。案 (c)(引用しない)は、実際に読んで設計に影響した文献を
    隠すことになり不誠実である → 採らない
  - 決定5: PLAN-004 §3 の順0 が「副次の帰結」として予告していたとおり
- 帰結:
  - **コードの変更は無い。**G7 はそもそも実装されていない
    (`code/eval/battery/battery_items.py` の `SUPPORTED_GROUPS` は4群のみ)。**消すのは文書だけ**
  - `Documents/06_THREATS.md` T13 の「G7 と Q10 は探索的」から **G7 の部分が消える**
  - `plans/PLAN-003-redesign.md` §8.1 が数えた「捨てた項目」に G7 の 165 項目が加わる
  - **未決が2つ減り(#11 / #16)、1つが必須化する(#17)。**#13 は消滅する
  - `Documents/00_OVERVIEW.md` / `01_HYPOTHESES.md` / `08_FUTURE_DIRECTIONS.md` の
    Feucht 依存記述は**論文2・3(将来方向)のもの**なので今回は触らない。
    論文1の Intro に効くのは `02_RELATED_WORK.md` と `09_PAPER_PLAN.md` である
- リスク・未解決:
  - **#17 が片付かないと Intro の対立軸が未検証の引用だけで立つ。**
    Nikankin et al. (2025) が原典確認できなかった場合、Intro の構成を組み直す必要がある。
    **凍結までに終わらせる**(PLAN-004 §5)
  - **「周期的概念への転移」という問い自体は捨てていない。**論文2・3 の材料として
    `08_FUTURE_DIRECTIONS.md` に残る。Phase 1 の評価から外すだけである
  - `arb` のズレ表は**まだ未確定**(`arbitrary_table: null`)。#13 が消えたのは
    「`table[1]` を要求する項目が無くなった」からであって、表が決まったからではない
- 代替案:
  - **案 A(探索的アドオンとして残す)**: 追加 GPU コストはほぼゼロ(165 forward pass)で、
    乗れば議論の材料になる。しかし項目生成・パーサ・セル定義の実装コストが Phase 0 に乗り、
    **人間が Phase 0 の最短化を選んだ** → 却下
  - **案 C(副次の確証的評価項目に戻す)**: W5 と D-1 の両方に反する → 却下
  - **#16 案 (b)(G7 を残して書式の出所も兼ねる)**: #11 = B と両立しない
  - **#16 案 (c)(引用しない)**: 設計に影響した文献を隠すことになる → 却下
- 関連 ADR: 018(G7 の導入)、020(`arb` の定義域 `[2,198]`)、023(G7 をオプションに格下げ)、
  024(D-1 Instruct 主系統)、031(リスク欄が #16 を生んだ)
- 関連 commit: (このコミット)

---

## ADR-037: 順1 と順2 の間に **順1b(本番モデルによるスモーク)** を挿す。小モデルには差し替えない

- 日付: 2026-08-27
- ステータス: **採択**(2026-08-27、人間が「#23 を承認する」と回答。
  さらに「順1b で何を回すか」の選択で **本番モデルで smoke(GPU 小)** を選んだ)
- 文脈:
  - **#23 は 2026-08-27 に PLANNER が登録した未決**(`plans/PLAN-004-phase0-route.md` §5)。
    出所は「**#20 の4項目のうち、実測の材料が要るのは `max_new_tokens` だけである**」
    (同 §3 順2 の注)。`max_new_tokens` は `parse_fail_rate`(Go/No-Go #1)に直結する
  - **登録時の想定は `configs/smoke.yaml` を「小さいモデルに差し替えて」回すことだった**
    (同ファイル `model.name` のコメント)。**本セッションでこれが目的を満たさないことが分かった**
    —— **トークナイザが違えば「答えが何トークンに収まるか」は移らない。**
    小モデルで得られるのは「モデルを呼べるか」「パーサの取りこぼし」だけで、
    **#20 のブロッカーは外れない**
  - `configs/smoke.yaml` は冒頭に「**実験ではない**」「ここから出た数値を `results/` や
    文書に書かないこと(`CLAUDE.md` §2)」と明記されている。**この性質は本 ADR でも変えない**
  - `CLAUDE.md` §2 は 10 GPU時間超のジョブに人間の承認を要求する。順1b の見積もりは
    **1 GPU時間未満**(重み ~16GB の pull + 小さいプールの生成)だが、
    **`logs/HANDOFF.md` が「モデルを実際に pull しない」を禁止事項に挙げていた**ため
    人間の承認を取った(PLAN-004 §7 の承認記録に記載)
- 決定1: **`plans/PLAN-004-phase0-route.md` の手順表に順1b を挿す。**
  作業 =「本番モデルによるスモーク」、段階 = A'、担当 = **RUNNER**、GPU = **小**、依存 = 順1。
  **順2 は順1b に依存する**(`max_new_tokens` の材料が要るため)
- 決定2: **回すモデルは `meta-llama/Llama-3.1-8B-Instruct`**(ADR-024 決定1 = D-1 の主系統)。
  **小さいモデルに差し替えない。**理由は文脈欄の2点目
- 決定3: **順1b の pull で得た HF コミットハッシュが、本実験の `model.revision` になる。**
  ADR-031 決定1・2 が「**最初に** pull した時点のコミットハッシュで固定する」と定めており、
  順1b がその「最初の pull」になる。得たハッシュは **config と manifest の両方**に書き、
  以後 **全条件・全シードで同一の値**を使う
- 決定4: **`configs/smoke.yaml` は触らない。**順1b は**別 config** を新規に作る。
  理由は2つある:
  1. `smoke.yaml` の `model.name = null` は「**既定値を作らない**」門の回帰テストが
     拠っている固定点である(`code/tests/test_eval_model.py:51`
     `test_undecided_model_name_stops_the_run`)。埋めるとこの門のテストが消える
  2. `smoke.yaml` のデータ域は `[1,9]^2` / `K=20` であり、**本実験の `[1,99]^2` より桁が少ない。**
     **短い答えしか出ないので `max_new_tokens` の材料として使えない**(過小に見積もる)。
     順1b の config は**本実験と同じ数値域**を持つこと
- 決定5: **順1b が答える問いは3つだけである。**
  (a) コードが本番モデルを呼べるか / (b) パーサが何を取りこぼすか /
  (c) **答えが何トークンに収まるか**(= #20 の `max_new_tokens` の材料)。
  **それ以外の問いに順1b の出力を使わない**
- 決定6: **順1b は実験ではない。**`results/` に置かない。健常時スコア・test-retest・
  プロンプト感受性(Go/No-Go #0〜#3)の材料にしない。**成果物は `runs/<id>/` に残す**
  (`infra/RUNPOD.md` §4)。**文書に書いてよい数値は (c) のトークン長だけ**であり、
  **run_id とセットで #20 の ADR に転記する**(`CLAUDE.md` §2)
- 決定7: **順1b で材料が取れるのは T1 と T2 の範囲に限る。**T1b / T3 の確定文面は
  **#21 が未決**であり、順1b の時点で存在しない。**T1b / T3 の分をどう埋めるか
  (#21 決定後に足すか、全タスク共通の保守的な上限を取るか)は #20 の ADR で決める**
- 根拠:
  - 決定2: 「トークナイザが違えば答えのトークン長は移らない」は #23 の目的を直撃する。
    小モデル案を採ると**順1b を回しても #20 が決まらない**
  - 決定3: ADR-031 が「最初の pull」と書いている以上、順1b を先に置けば順1b が「最初」になる。
    **これを書かないと、順5 の pull で別のハッシュを取って2つの revision が並ぶ**
  - 決定4-1: 門のテストを壊す変更は「既定値を作らない」規約(skill `code-style` §5)の
    担保を外す。**config を分ければ両立する**
  - 決定4-2: `1+9` の答えは1〜2桁、`99+99` の答えは3桁。**p2 病変を足せばさらに伸びる。**
    短い域で測った上限を本実験に持ち込むと `parse_fail_rate` が上がる
  - 決定6: 罠5(段階 C を「試し」と見なす)の裏返しが起きうる。**本番モデルを使うぶん、
    出た数値が「使える」ように見える。**FT 無し・3条件・小さいプールでは段階 C の代わりにならない
- 帰結:
  - `plans/PLAN-004-phase0-route.md`: §2 手順表に**順1b の行**、§3 に**順1b の完了条件**、
    §5 の #23 を**決着**に、§6 に**罠6**、§7 に実行ログ1行と **GPU 使用の承認記録**1行
  - **`infra/RUNPOD.md` §4 に順1b の手順が無い。**順1b の実施セッションが書く
    (コマンド列の正本は `RUNPOD.md` §4。PLAN-004 §8 規則2)
  - **順1b 用 config の新規作成が要る**(決定4)。**値の出所を config のコメントに書くこと**
    —— `smoke.yaml` と同じく「★順1b のみ。実験条件ではない」を明示する
  - `STATE.md` 段階 B の表から #23 が落ちる
  - **`configs/smoke.yaml` の `model.name` コメント「小さいモデルに差し替えて使う」は
    そのまま有効である。**順1b はそこを通らない(決定4)
- リスク・未解決:
  - **順1b の数値を段階 C の代わりに読む罠。**→ §6 罠6 に登録した
  - **順1b をやり直すと revision が動きうる。**やり直しでも**最初のハッシュを使う**
    (ADR-031 決定1)。**pull し直して別のハッシュが返ったら、その事実を記録して人間に上げる**
  - **順1b の config の数値(プールの大きさ・項目数)は決めていない。**実施セッションの判断だが、
    **本実験と同じ数値域を持つこと**(決定4-2)だけが要件である
  - **T1b / T3 の材料が取れない**(決定7)。#21 が決まるまで `max_new_tokens` は
    T1 / T2 の実測 + 保守的な余裕でしか置けない
- 代替案:
  - **小モデルで smoke**: GPU がほぼ要らないが **`max_new_tokens` の材料にならない** → 却下
  - **トークナイザだけ取る(重みを pull しない)**: GPU 時間 0 で答えのトークン長は数えられるが、
    **パーサが何を取りこぼすかが分からない**。実際の生成には前置き・記号・改行が混じる → 却下
  - **順1b を挿さない**: #20 の `max_new_tokens` を**実測の根拠なしに**決めることになる。
    大きく取れば安全側だが、生成時間が全 run に乗る → 却下(人間が採択)
- 関連 ADR: 024(D-1 主系統)、031(revision の固定手続き。**決定3 が直接効く**)、
  033(smoke config は3条件しか宣言できない)、**038(#20 の改訂可否。対になる決定)**
- 関連 commit: (このコミット)

---

## ADR-038: #20(生成設定)は段階 C の結果で改訂してよい。4項目すべてを対象とし、記録を要件にする

- 日付: 2026-08-27
- ステータス: **採択**(2026-08-27、人間が「#24 を承認する」と回答。
  範囲の選択で **#20 の4項目すべて改訂可**を選んだ)
- 文脈:
  - **#24 は 2026-08-27 に PLANNER が登録した未決**(`plans/PLAN-004-phase0-route.md` §5)。
    **どの文書も「#20 を段階 C の結果で改訂してよいか」を明示していなかった**
  - `Documents/04_EXPERIMENT_PLAN.md` Phase 0 の目的は「病変を入れる前に、**評価系の信頼性と
    健常時の値を確定する**」であり、段階 D(凍結)は順9 に置かれている。
    **設計上は改訂が想定内のはずだが、書かれていない**
  - **書かれていないと、順6 の後に `max_new_tokens` を伸ばしたとき
    「これは事後選択ではないか」を自分で判定できない**(`CLAUDE.md` §2 の HARKing 禁止)
- 決定1: **#20 の4項目(`model.dtype` / `model.max_new_tokens` / デコード設定 / few-shot 数)は
  すべて、順6(段階 C)の結果を見て改訂してよい。**
- 決定2: **改訂には次の4つを ADR に残すことを要件とする。**
  (a) **日付** / (b) **理由** / (c) **改訂前の値**(打ち消し線で残す。`CLAUDE.md` §2) /
  (d) **根拠にした run_id**。1つでも欠けたら改訂ではなく事後選択として扱う
- 決定3: **改訂できる期限は段階 D(凍結。順9)まで。**凍結後は改訂しない
- 決定4: **改訂したら、その設定で順6 を測り直す。**改訂前の設定で得た数値を
  Go/No-Go #0〜#3 の判定に使わない。**★これはエージェントの補足であり、人間が一度見ること**
- 決定5: **本 ADR の許可は #20 に限る。**#9(適格性フィルタ 0.70)/ #15(`θ` の決定規則)/
  #21(評価テンプレート集合)には**及ばない**。#9 と #15 は罠1・罠2 により**順3 で凍結する**
- 決定6: **順2 で書く #20 の ADR に、本 ADR へのポインタを1行入れる**(改訂の可否がそこで読めるように)
- 根拠:
  - 決定1: 生成設定は**測定装置の設定**であって仮説ではない。Phase 0 の目的が
    「評価系の信頼性を確定する」である以上、測ってから直すのは想定内である
  - 決定2: 宣言だけでは足りない。**改訂の痕跡が git と ADR に残って初めて、
    後から「事後選択ではない」と示せる**(`CLAUDE.md` §5 の日付偽装禁止と同じ趣旨)
  - 決定4: **改訂前の数値は、改訂後の設定を記述していない。**`max_new_tokens` を伸ばせば
    `parse_fail_rate` は下がる。古い数値のまま Go/No-Go を通すと、
    **実際には測っていない設定について合格を宣言する**ことになる
  - 決定5: #9 の `0.70` は**規範的な線引き**であり実測から導かれる量ではない。
    `θ` は**値ではなく決定規則**を凍結する(PLAN-004 §3 順3 の注、§6 罠1・罠2)
- 帰結:
  - `plans/PLAN-004-phase0-route.md` §5 の #24 が**決着**になる。§3 順2 に本 ADR への参照を足す
  - **順6 の実施コストが上振れしうる**(決定4 により、改訂すれば測り直しが要る)。
    **順1b(ADR-037)で `max_new_tokens` の材料を先に取るのは、この測り直しの確率を下げるためでもある**
  - `STATE.md` 段階 B の表から #24 が落ちる
- リスク・未解決:
  - **デコード設定と few-shot 数の改訂は、4項目のなかで最も事後選択に近い。**
    とくに「`correct_rate` が低かったので few-shot を足す」は、**病変の効果を測る前に
    モデルの能力を上げる操作**であり、Go/No-Go #0 の意味そのものを変える。
    決定2 の記録要件と決定4 の測り直しでしか担保されていない。**人間が順6 の後に確認すること**
  - **`model.dtype` の改訂は重みの数値そのものを変える。**bf16 → fp16 などに動かした場合、
    順1b と段階 C の数値は**別のモデルの数値**になる。決定4 の測り直しが必須である
  - **本 ADR は #20 の中身を1つも決めていない。**4項目は依然として未決であり、順2 で人間が決める
- 代替案:
  - **`max_new_tokens` だけ改訂可**: 事後選択の余地が最も狭い。`parse_fail_rate` に直結する
    項目だけを開ける案だった → **人間が「4項目すべて」を選んだ**ため却下。
    ただしリスク欄のとおり、残り3項目の改訂は慎重に扱う
  - **改訂を許さない**: 順2 の決定が実測なしで確定することになり、`max_new_tokens` を
    大きく取る以外に安全側の選択が無くなる → 却下
- 関連 ADR: **037(順1b。`max_new_tokens` の材料を先に取る)**、
  031(revision の固定)、030(R8 の `θ` 手続き)、034〜036(順0 の3判断)
- 関連 commit: (このコミット)
