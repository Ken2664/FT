# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-08(その20)/ 直前セッションの役割: CRITIC / PLANNER (Opus)
直前セッションが終了した理由: コンテキスト超過(約 141k トークン。閾値 140k)

---

あなたは IMPLEMENTER です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**ADR-066 の決定 1・2・4 を `Documents/` に反映し、`pytest code/tests -q` を通す。**

完了条件:

1. **`Documents/05_STATISTICS.md` §3.2 の `(0 + coverage | seed)` の ★ 欄**
   —— 「人間の確認を求める」という保留の文を消し、**ADR-066 決定1 で採択済みである**と書く。
   **`(1 + coverage | seed)` は張る空間・自由パラメータ数(6)・LRT の df とも同一だが、
   ベイズ側では事前分布を置く対象が変わるので等価ではない**、という 1 文を足す
2. **同 §3.2 の ADR-061 の事前分布欄に `lkj(2)` を書き込む**(ADR-066 決定2)。
   **主軸 3x3 / 順1b 2x2 の両方。「3 つのセル平均どうしの相関に掛かる」と明記する**
   (切片と傾きの相関ではない)。**「弱情報だから中立ではない」の注記を相関についても書く**
3. **文言訂正 4 件**(ADR-066 決定4)。**旧記述は打ち消し線 + 理由 + 日付で残す**(`CLAUDE.md` §2):
   - **(a) §3.2**: 「`H2` の第2節は『破れの位置がシード間で一貫しない』と予測している」→
     **`H2` が予測しているのは一貫するほう。「一貫しない」は反証条件である**
   - **(b) §3.2**: 「40 本の FT run を通じて一定」→ **主要解析は `p2` のみで `seed` は 10 水準**
   - **(c) §2 の下限規則**: 「2 タスク型 x 2 水準を**下回ったら**」→ **「2x2 以下なら実施不能」に一意化**
     (2x2 は df = 1 で、2 行下の「df = 1 の交互作用を主要検定と呼び替えない」と衝突していた)
   - **(d) §3.2 の ★G の根拠欄**: **外れたのは「被覆の主効果の勾配が run 間で一定」であって
     「`task:coverage` が run 間で一定」ではない**。後者を外すには `(0 + task:coverage | seed)`
     (12 水準 = **分散共分散 78 パラメータ**)が要り 10 シードでは不能。**モデルは変えない**
4. **`Documents/06_THREATS.md` と `Documents/09_PAPER_PLAN.md` の Limitations に
   「交互作用のシード間変動は誤差に畳まれたままである」を足す**
5. **`logs/CHANGELOG.md` に追記して commit**

## ★最初にやること(3 の (d) を書く前に)

**(d) の論拠を確かめてから書く。**論拠は「`u_{s,k}` はタスク型に依存しないので、
ある被覆水準の中でタスク型どうしを引き算する交互作用の対比からは消える」であり、
**釣り合った線形モデルでは厳密に成り立つが、ロジスティックでは一次の見立てにとどまる。**
**確かめずに断定形で書くと `CLAUDE.md` §7 に抵触する。**

確かめ方の案(どちらでもよい):
- 計画行列の上で対比ベクトルと `Z u` の直交性を代数で示す
- `code/analysis/` の既存の合成データ生成器で `u_{s,k}` を入れた/入れないデータを作り、
  交互作用の SE がどれだけ動くかを見る(**GPU 不要。合成データのみ**)

**確かめられなければ、断定を避けた書き方(「釣り合った線形モデルでは〜。
ロジスティックでの厳密な扱いは未検証」)にして、その旨を ADR-066 のリスク欄に追記する。**

## 直前セッションで確定したこと

- **ADR-066(決定 6 件)を人間が採択した。**正本は `logs/DECISIONS.md` の ADR-066。
  **決定は残っていない。残っているのは実装だけである**
- **決定1** `(0 + coverage | seed)` 単独表記。`(1 | seed)` は「3 水準の分散がすべて等しく
  相関がすべて 1」に制約された**特殊ケースとして既に含まれている**ので、併記すると
  3x3 の分散共分散にランク 1 の冗長が乗る
- **決定2** 相関行列の事前分布 = **`lkj(2)`**
- **決定3** F96(ランク規則が df = 4 を返すこと)は**追加の決定も記述も不要**。
  `05_STATISTICS.md` §2 に既に書いてある
- **★LKJ の周辺分布が次元に依存する(`d = 2` では `lkj(1)` が一様になる)という記述は
  原典未確認である。**理由として文書に書くなら `CLAUDE.md` §3 の手順を踏むこと。
  **`lkj(2)` という値の決定はこの確認に依存しない**
- **実験は 1 件も実行していない。`results/` は空。凍結タグは打っていない。GPU 時間 0**

## 触ってよいファイル / 読むべき範囲

- `Documents/05_STATISTICS.md` §2(24-143 行あたり)/ §3.2(158-270 行あたり)。
  **全文 `cat` しない。`grep -n` で節を特定して `sed -n 'X,Yp'`**
- `Documents/06_THREATS.md`(T14)/ `Documents/09_PAPER_PLAN.md`(Limitations)
- `logs/DECISIONS.md` の **ADR-066**(`grep -n 'ADR-066' logs/DECISIONS.md`)。
  ADR-064 決定4 / ADR-065 / ADR-061 も参照先として出てくる
- `logs/OPEN-ITEMS.md` の索引(1-40 行)。**★ADR-066 の実装** の行を消すのは実装完了後

## やってはいけないこと

- **モデル指定そのものを変えない。**`(0 + task:coverage | seed)` は 78 パラメータで
  10 シードでは不能である。決定4 (d) は**根拠欄の記述を直す**話であって設計変更ではない
- **`(1 | seed)` を書き戻さない**(ADR-066 決定1 で単独表記が採択済)
- **旧記述を削除しない。**打ち消し線 + 理由 + 日付で残す(`CLAUDE.md` §2)
- **GPU を起動しない。**このセッションに GPU を使う作業は 1 つも無い
- **凍結タグを打たない。**`θ` / N5 / ★F90 / GPU 構成が未決のまま残っている

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

**★ADR-066 に関する人間待ちは 0 件である。**それ以外は `logs/OPEN-ITEMS.md` の索引が正本:

- **★L(d)** `arb` を 5 → 10 シードにするか(順6 の後。GPU 構成と同じ場)
- **★F90 の事実確認**(「順6 では `s2_seed` が出ない」)。**凍結より前**
- **`θ`**(順5 の唯一の残りブロッカー)/ **N5**(解析門の閾値)/ **★C** / **★E** /
  **E-5 (b)** / **Δ 5 行** / **★2** / **順6 と本実験 40 run の GPU 構成**(クリティカルパス)
- **PLAN-018 §4.3** の組み直し(とくに F87)
