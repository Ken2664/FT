# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-23 / 直前セッションの役割: CRITIC
直前セッションが終了した理由: コンテキスト超過(約17.4万トークン)+ **人間が設計の全面見直しを宣言**

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。
**`STATE.md` の冒頭「★ 最優先」と `logs/DECISIONS.md` の ADR-023 を必ず先に読むこと。**

## このセッションでやること(1つだけ)

**ADR-023 の要因計画から実験設計を再導出し、`plans/PLAN-003-redesign.md` として起草する。**

再導出であって個別修正ではない。現行の G1〜G7 バッテリを出発点にせず、
**要因計画から項目構成を導き、そのうえで現行資産のどれが使えるかを判定する**順で書く。

完了条件(この6つが文書に書かれていること):

1. **T1 / T2 / T3 の項目構成**。各タスク型について、プロンプト書式(1文字単位)、
   既知性の層、項目数、真値と規則値が割れる条件
2. **「既知性」の操作的定義**。`(a,b)` 水準(ADR-019 決定4)と `t` 水準(ADR-021)のどちらを
   主軸に使うか、両方使うならどう組むか。**T3 は和を読むので両方効く**
3. **主要評価項目の再定義**。「タスク型 × 既知性の交互作用」を、ADR-005(主要は1つ)の制約と
   両立する形でどう書くか。統計モデル(混合効果)の指定を含む
4. **現行資産の存廃判定表**。`code/data_gen/pool.py` / `code/eval/battery/g6_comparison.py` /
   パーサ6本 / `code/eval/scoring.py` / 227 tests のそれぞれについて
   「そのまま使う / 改修して使う / 捨てる」と理由
5. **G2(代数的整合性)/ G3(メタ認知)/ G5(隣接演算)/ G7(周期概念)の扱い**の提案。
   **決めるのは人間**(`CLAUDE.md` §8)。案と根拠を書く
6. **§末尾に人間の承認待ち事項**を番号付きで列挙する

**この文書を書き終えたら止まる。**コードは1行も変えない。

## 最初に人間に確認すべきこと(2件。ブロッキング)

作業の途中ではなく**最初に**訊くこと。答えで設計の骨格が変わる。

- **Q-A: base か Instruct か。**ADR-018 決定1 は「Feucht et al.(base)に厳密一致」だったが、
  ADR-023 決定4 で G7 がオプションに落ちたので**この拘束は弱まった**。
  一方 **T2(文章題)は自由生成が要り、base で測れるか不明**
  (`Documents/reviews/2026-08-23_design_value.md` W6)。**T2 は主軸の 1/3 を占める**
- **Q-B: G2 / G3 / G5 を残すか捨てるか。**人間の再宣言に直接は現れない。
  残すと項目数と GPU コストが増え、捨てると `06_THREATS.md` T1(方針交絡)の切り分けが弱まる

## 直前セッションで確定したこと(すべてファイルに書き込み済み)

- **ADR-023 を採択した**(`logs/DECISIONS.md`)。主軸の再宣言、現行設計との食い違い表、
  凍結する作業、再導出で答えを出すべき6問
- **`Documents/reviews/2026-08-23_design_value.md`** — 先行研究に照らした設計価値レビュー。
  依拠文献 14 本を **arXiv abs / ACL Anthology / arXiv HTML 全文を実際に開いて**確認し、
  `Documents/reviews/papers_list.md` にリンク付きで分離した。要点:
  - **「概念か表層か」(層1)は文献側でほぼ答えが出かかっている** —— wrapper 仮説、
    Reversal Curse、knowledge editing の ripple effects。**H0 支持だけでは新規性が薄い**
  - **G1/G2 の一貫性監査は非 FT 版が既に存在し結論も出ている**(表現不変性・交換律)。
    **新規性は「FT で規則を入れた後で同じ監査をする」差分にしかない**
  - **「FT した規則を言語化・逆演算できるか」にも肯定側の先行がある**。ただし先行は
    「新しい名前の関数」を教えており、**`+` そのものの上書き(既存知識との競合)は差分になる**
  - **空いているのは (a) FT の変更が既知の共有機構に落ちるか、
    (b) 和を鍵とする恣意的な表が未見の対へ転移するか(ADR-020 の `arb`)**
  - **`refs.bib` への追加と `02_RELATED_WORK.md` への反映は未実施**(§8。引用の最終確定は人間)
- **R8(会話でのみ提案。まだ仕様が無い)**: T3 を固定閾値の `rule_rate` ではなく
  **閾値 θ を掃いた曲線のシフト量 `Δ̂` の推定**に組み替える案。
  条件ごとに異なる関数形の署名を予測する(`p2` は定数 +2、`p2d` は周期10のノコギリ波、
  `x2` は `t` に比例、`arb` は表依存)。**`STATE.md` の引き継ぎに表がある**

**コードは未変更。**`pytest code/tests -q` は直前セッションで再実行していない(前回記録 227 passed)。
`results/` は空。**実験結果の数値は1つも無い。**RunPod 未使用。

## 触ってよいファイル / 読むべき範囲

- 読む: `STATE.md` 冒頭「★ 最優先」と末尾「引き継ぎ」/ `logs/DECISIONS.md` の **ADR-023**
  (`grep -n '^## ADR-023' logs/DECISIONS.md` → `sed -n 'X,Yp'`)/
  `Documents/reviews/2026-08-23_design_value.md` §5
- 参照(**全文 cat しない**。`grep -n` で節を特定して `sed -n 'X,Yp'`):
  `plans/PLAN-001-eval-battery.md` §5.1(項目構成)、`plans/PLAN-002-ft-data.md` §4(FT データ)、
  `Documents/05_STATISTICS.md` §2〜§4
- 書く: `plans/PLAN-003-redesign.md`(新規)。**他の設計文書はまだ書き換えない**
  (再導出が承認されてから追随させる)

## やってはいけないこと

- **実装に着手しない。**「次のアクション」の実装順 0 / 1 / 1b / 1c を含め、
  ADR-023 で**凍結済み**。`arb` / `p2d` / G7 / セル構成の存廃が再導出で変わりうる
- **PLAN-002 §12 の承認待ち9件を消化しない。**同じ理由で凍結中
- **事前登録に `git tag` を打たない**
- **G1〜G7 の枠組みを出発点にしない。**要因計画から導出する。
  枠組みを維持したまま名前を付け替えると、ADR-023 が指摘した食い違いがそのまま残る
- **`Documents/refs.bib` / `02_RELATED_WORK.md` に文献を勝手に確定追加しない**(§8)
- **G7 を捨てない。**人間は「興味深いことが言えるなら使ってよい」と述べている。廃止ではなく格下げ

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

1. **Q-A / Q-B**(上記。最初に訊く)
2. **PLAN-002 §12 の9件**(凍結中。再導出後に生き残った項目だけ改めて問う)
3. `table[1]` の穴(承認待ち-13)。案(a) は使えないと判明済。**(b) か (c) に絞られている**
4. `model.revision`(承認待ち-15)。Q-A と併せて決める
5. `θ`(外挿域の閾値)と桁数掃引の粒度(PLAN-001 §4.1.1)
6. **`Documents/reviews/` の R1〜R7 + R8** を採るか(レビュー §5.2)

## 主軸と独立に残っているバグ(再導出後に着手)

- **`eligible_pairs` の定義域バグ**(ADR-020 根拠3)。`arb` を渡すとプール生成が `KeyError` で落ちる。
  該当 **100,298 組**。**`arb` の存廃が決まるまで直さない**
