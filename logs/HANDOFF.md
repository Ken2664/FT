# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-28(順1b 用に戻した)/ 直前セッションの役割: PLANNER
直前セッションが終了した理由: 作業単位の完了 + コンテキスト超過(hook `context-guard` が約 153k / 閾値 140k)

> **★ 2026-08-28 の現況(ここだけが 2026-08-27 版との差である)**
>
> - **問題1(GPU 配置とバッチ生成)は完了した。**commit `de8d151`。
>   `model.device` と `eval.batch_size` が config の必須項目になり(null は `ConfigError`)、
>   左パディングでまとめ生成する。`pytest code/tests -q` → **615 passed**。
>   **`configs/smoke1b.yaml` にだけ値が入っている**(`device: cuda` / `batch_size: 4`。
>   どちらも「★順1b のみ。実験条件ではない」)。**実機では1度も動かしていない**
> - **したがって次は順1b(RUNNER)である。**下の本文がそのプロンプトで、内容は 2026-08-27 版のまま
>   有効である(`model.revision` の扱い、完了条件6つ、罠6 は変わっていない)
> - **順1b に足す作業が3つある**:
>   1. **まとめ生成の経路は実機で初めて動く。**落ちたらまず `code/eval/generate.py` の
>      左パディングと `pad_token` の代用(eos)を疑う
>   2. **バッチ1 対 バッチN の一致確認を1度だけ取る**(19項目なので安い)。
>      **これが未決 #25 の材料である。**一致しなければ数値を出す前に人間へ上げる
>   3. **`metrics.json` の `generation` に `device` と `batch_size` が残ることを確認する**
> - **未決 #25**(`batch_size` と実行デバイスを実験装置の設定として固定するか、値はいくつか)は
>   **人間の判断待ち**である。順1b の値は「★順1b のみ」であって #25 の答えではない
> - **工程表**: `Documents/07_ROADMAP.md`「メインの実行までの工程」(2026-08-28 追加)。
>   **順序と完了状態の正本は `plans/PLAN-004-phase0-route.md` §2 のまま**

---

あなたは **RUNNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。
**RunPod MCP を有効にするセッションです**(`CLAUDE.md` §10.2)。

## このセッションでやること(1つだけ)

**順1b —— 本番モデルによるスモークを回す。**完了条件は `plans/PLAN-004-phase0-route.md`
§3「順1b」の**チェックボックス6つ**であり、それが判定基準です。要約すると:

1. **順1b 用の config を新規に作る**(`configs/smoke.yaml` は**触らない**)。
   **本実験と同じ数値域 `[1,99]^2`** を持つこと
2. `meta-llama/Llama-3.1-8B-Instruct` を pull し、**HF コミットハッシュを記録する**
3. `code.eval.run` を通し、`runs/<id>/` に必須成果物を残す(`infra/RUNPOD.md` §4)
4. **答えのトークン長の分布**を出す(**T1 / T2 のみ**)
5. `infra/RUNPOD.md` §4 に**順1b の手順を書く**(コマンド列の正本はそこ)
6. **`results/` に何も置いていない**ことを確認する

**「モデルが何点取ったか」は完了条件ではありません。**

## 直前セッションで確定したこと

- **人間が #23 / #24 を承認し、`logs/DECISIONS.md` に ADR-037 / ADR-038 を採択した**(commit `d93f2a0`)
- **ADR-037**: 順1 と順2 の間に**順1b** を挿す。**小モデルには差し替えない** ——
  トークナイザが違えば「答えが何トークンに収まるか」が移らず、#20 の材料にならないため
  - **決定3: この pull で得たハッシュが本実験の `model.revision` になる**(ADR-031 決定1・2)。
    **config と manifest の両方に書き、以後 全条件・全シードで同一の値を使う**
  - **決定4: `configs/smoke.yaml` を編集しない。**`model.name = null` は門の回帰テスト
    (`code/tests/test_eval_model.py:51`)が拠る固定点であり、データ域 `[1,9]^2` は
    **本実験より桁が少なく `max_new_tokens` の材料として短すぎる**
  - **決定5・6: 答える問いは3つだけ**(呼べるか / パーサの取りこぼし / **トークン長**)。
    **`results/` に置かない。Go/No-Go #0〜#3 の材料にしない**(§6 罠6)
  - **決定7: 材料が取れるのは T1 / T2 だけ**(T1b / T3 の文面は **#21 が未決**)
- **ADR-038**: **#20 の4項目すべて**を段階 C の結果で改訂してよい。要件は
  **(a) 日付 (b) 理由 (c) 改訂前の値 (d) 根拠にした run_id** と、**改訂後の順6 の測り直し**
- **GPU は人間が承認済み**(2026-08-27。PLAN-004 §7 の承認記録。見積もり **1 GPU時間未満**)
- `pytest code/tests -q` → **506 passed**。**`results/` は空。GPU 時間 0。事前登録の tag なし**

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-004-phase0-route.md` §3「順1b」/ §6 罠6 —— `grep -n '順1b' ` で位置を出す
- `logs/DECISIONS.md` **ADR-037**(`grep -n 'ADR-037' logs/DECISIONS.md`)。全文 `cat` しない
- `infra/RUNPOD.md` §4(手順の正本。**ここに順1b を書き足すのが完了条件5**)
- `configs/smoke.yaml`(**読むだけ**。新 config の書き方の手本)
- `code/eval/model.py:91-144`(生成設定の門。`revision` が `null` だと `ConfigError` で止まる)
- 新規作成してよいもの: 順1b 用の config、`runs/<id>/` 配下

## やってはいけないこと

- **`configs/smoke.yaml` を編集しない**(ADR-037 決定4。門の回帰テストが壊れる)
- **#20 の4項目を決めない。**順1b の config に入れる `dtype` / `max_new_tokens` /
  `temperature` は **「★順1b のみ。実験条件ではない」とコメントに明記**すること。
  **順1 が `eval.magnitude_sweep.*` で同じ問題を解いた前例に従う**(template は null、
  smoke にだけ小さい値)。**これを #20 の決定として書かない**
- **`eval.num_repeats` の門・few-shot の門を外さない**(`code/eval/model.py`)
- **`results/` に数値を置かない。文書に書いてよいのは答えのトークン長だけ**で、
  **run_id とセットで #20 の ADR に転記する**(`CLAUDE.md` §2)
- **順1b の数値を健常時スコア / test-retest / プロンプト感受性の材料にしない**(罠6)
- **pull し直して別のハッシュが返ったら、その事実を記録して人間に上げる**(ADR-031 決定1)
- **ポッドを停止せずに終わらない**(`CLAUDE.md` §9)

## 未解決 / 人間の承認待ち

- **#20(生成設定4項目)/ #21(T1b・T3 の確定文面)は未決。**順1b は #20 を決める段ではなく、
  **材料を取る段**である
- **ADR-038 決定4(改訂したら順6 を測り直す)はエージェントの補足。人間が一度見ること**
- **#22(LoRA アダプタを `runs/<id>/` に残すか)は未決。**順8 の 8-6 が待っている
- **#9 / #15 / #17 / 検出力分析**は順3・順7。順1b では触らない
