# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-23 / 直前セッションの役割: PLANNER
直前セッションが終了した理由: コンテキスト超過(hook `context-guard` が約 182k トークンで警告。閾値 140k)

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**`plans/PLAN-003-redesign.md` §10 の追随表に従い、設計文書を PLAN-003 に同期させる。**

完了条件(すべてファイルへの書き込みで判定できる):

1. `Documents/05_STATISTICS.md` §2 を差し替える — 主要評価項目を
   「`p2` 条件の `rule_rate` を `task * coverage` の混合効果ロジスティック回帰で当て、
   **交互作用項の LRT(df = 6)**」に。旧 §2(G6 の `p2` vs `ident`)は
   **打ち消し線 + 理由 + 日付**で残し、Go/No-Go #4 への降格先を明記する
2. 同 §4 のゲートキーピング階段を PLAN-003 §6.4 の 9 段に差し替える
3. `plans/PLAN-001-eval-battery.md` §5.1(G1〜G7 のセル表、3,565 項目)を
   PLAN-003 §4.7 / §4.8 の表に差し替える。**§4.1 / §4.2 / §4.3 / §5.4 はそのまま残す**
4. `Documents/06_THREATS.md` T1 の対策表から「多言語」「G2 単位元」の行を落とす
   (ADR-024 の帰結)。**3行 → 1行に減ったことを正味の損失として本文に書く**
5. `Documents/04_EXPERIMENT_PLAN.md` の Phase 0 Go/No-Go を PLAN-003 §6.5 に、
   条件のシード配分を **40 run**(ADR-028)に更新する
6. `Documents/03_OPEN_QUESTIONS.md` の Q2(表記依存)が主軸から消えたことを反映する

**7 は #14 が決まってから**: `plans/PLAN-002-ft-data.md` §4.2 に `T_hold` 軸を足す(ADR-029)。
これは実装に直結するので、`model.revision` の確定と同じセッションで扱ってよい。

## 直前セッションで確定したこと

**ADR-024〜030 を採択した(`logs/DECISIONS.md`)。すべて人間が決定した。**

| ADR | 内容 |
|---|---|
| **024** | D-1 Instruct 主系統(`Llama-3.1-8B-Instruct`)/ D-2 G2・G3 廃止・G5 は特異性対照の最小版 / D-3 評価プロンプト英語統一 |
| **025** | チャットテンプレートは**案 A** —— FT も評価も全項目を `apply_chat_template` 経由。T1 の無テンプレート版 240 項目を「テンプレート税」として併走 |
| **026** | タスク型に第4水準 **`T1b`(裸の比較)**を追加。交互作用 **df 4 → 6** |
| **027** | 主軸の被覆水準は **`id` / `interp` / `extrap_magnitude` の3つ**。`oob_algebraic·ans_in` / `extrap_pair` は **T2 以外限定の副次**(PLAN-003 §4.8) |
| **028** | **`p2d` を 10 シードに格上げ。合計 40 run**(`p2` 10 / `ident` 10 / `p2d` 10 / `arb` 5 / `x2` 5)。`p2d` の交互作用を副次の順1 に |
| **029** | **`T_hold`(20 個)を訓練サンプリングの設計変数にする。**`carry` 比例配分 4/16 |
| **030** | **R8 を実験に組み込む。**θ 17 水準 `{-3..+13}` / **T3 + T1b 両方** / 各セル n=20 / `Δ̂` はロジスティック当てはめの 0.5 交差点 |

**組合せ論的な計数(実験結果ではない)**:

- 主軸 **1,560 項目** / P-2 副次 **800** / R8 掃引 **8,160 forward pass** = 計 10,760
- `id` 要求 **520 組**(`K = 2000` を下回る)
- `T_hold` 適用後の抽出母集団 **8,809 組**、`p2`/`p2d` 判別不能を除いて **7,916 組**
- `interp × t_unseen` の候補 **904〜992 組**(n=40 が埋まる)
- FT run **40**。強制選択 **9,120 forward pass / run**、自由生成 600〜1,000 項目

**実験結果の数値は依然として1つも無い。**`results/` は空。RunPod 未使用(GPU 時間 0)。
`pytest code/tests -q` → **227 passed**。**コードは1行も変更していない。**

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-003-redesign.md` — **これが正本。**冒頭の「承認状況」表 → §10 の追随表 → 必要な節だけ。
  **全文 cat しない**(約 900 行)。`grep -n '^### '` で節を特定して `sed -n 'X,Yp'`
- `logs/DECISIONS.md` の **ADR-024〜030**(行 981 以降)
- 書き換える対象: `Documents/05_STATISTICS.md` / `plans/PLAN-001-eval-battery.md` §5.1 /
  `Documents/06_THREATS.md` / `Documents/04_EXPERIMENT_PLAN.md` / `Documents/03_OPEN_QUESTIONS.md`

## やってはいけないこと

- **コードを書き始めない。**`model.revision`(§11-14)が未決で、ADR-025 により
  `chat_template` が revision に依存するため、FT データ生成も preflight も固定できない
- **事前登録を凍結(`git tag`)しない。**#6(T2 の文面)と #9(適格性フィルタ 0.70)が未決
- **旧文書を黙って消さない。**主要評価項目のように既に書かれた予測・解析計画を変える箇所は
  **打ち消し線 + 理由 + 日付**で残す(`CLAUDE.md` §2)。事前登録は未凍結なので tag の打ち直しは不要
- **PLAN-001 §4.1 / §4.2 / §4.3 / §5.4 を捨てない。**PLAN-003 §7.3 が「そのまま使う」と判定済み
- **`plans/PLAN-002-ft-data.md` §12 の旧・承認待ち9件を個別に消化しない。**
  PLAN-003 §11 が置き換えている

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

**PLAN-003 §11 が正本。**残っているのは以下。

| # | 事項 | いつ要るか |
|---|---|---|
| **14** | **`model.revision`** | **実装前に必須。**ADR-025 で `chat_template` が revision に依存するため |
| **6** | T2 の5テンプレートの確定文面(起草はエージェント、確定は人間) | 事前登録の凍結まで |
| **9** | 適格性フィルタの閾値 **0.70**(`ident` の `correct_rate`) | 事前登録の凍結まで |
| 10 | W6 の分岐(T2 が Go/No-Go を割ったときの3段の降り方)。特に第3段で止まってよいか | Phase 0 の進行と並行 |
| 11 | G7 の扱い(A: 探索的アドオン / B: 落とす / C: 副次に戻す)。**推奨 A** | 同上 |
| 12 | `arb` を残すか。残すなら `eligible_pairs` の定義域バグ(該当 100,298 組)の修正に着手してよい | 同上 |
| 13 | `table[1]` の穴。案 (b) か (c)。**`arb` を残す場合のみ** | 同上 |
| 15 | 外挿域の上限 `M*` と桁数掃引の粒度 | 同上 |

**人間の目視確認を受けていないエージェントの判断**(次セッションで人間に確認させること):

1. **ADR-030 の R8 手続き全体。**人間の回答は「実験の中に入れる。方法については任せる」であり、
   θ の水準 / T1b への適用 / `Δ̂` の推定手続き / 除外規則は**エージェントが決めた**。
   **解析計画に当たるので、事前登録の凍結前に人間が一度読むこと**
2. **ADR-027 の前段(主軸を3水準に絞る)。**P-2 の承認に含意される形で採択した
3. **ADR-028 決定1(`p2d` の交互作用を副次の順1 に新設)と Go/No-Go #4b。**
   承認待ち-5 の本文の論拠から導いたもので、新しい副次的評価項目である
4. **PLAN-003 §4.8 のセル構成**(n=40、`T1b` を対象に含めた点)。エージェントが導出した

## 既知のバグ / 実装上の地雷

- **`code/data_gen/pool.py` の `eligible_pairs` に定義域ガードが無い。**
  `arb` を渡すとプール生成が `KeyError` で落ちる(該当 100,298 組)。**#12 が決まるまで着手しない**
- **`code/eval/battery/g6_comparison.py` の `build_items` は `is_discriminating` を強制し、
  非判別オフセットで `ValueError` を投げる。ADR-030 の掃引モードでは必ず緩める必要がある**
- `T_hold` を**等間隔に取ると `carry` 側が 0 個になり `interp × t_unseen × carry` が空になる**。
  ADR-029 決定2 の比例配分(4/16)を必ず守る
