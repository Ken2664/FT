# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-23 / 直前セッションの役割: PLANNER
直前セッションが終了した理由: **作業単位(PLAN-003 の起草)が完了したため**

---

あなたは **PLANNER(承認の消化)** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。
**`STATE.md` 冒頭「★ 最優先」と `plans/PLAN-003-redesign.md` §11 を必ず先に読むこと。**

## このセッションでやること

**`plans/PLAN-003-redesign.md` §11 の承認待ち 15 件を人間に消化してもらい、
決まったものを ADR と設計文書に落とす。**

順序:

1. **人間に §11 の #1〜#5(骨格)を訊く。**これが決まらないと項目生成のコードが書けない
2. 決まった順に `logs/DECISIONS.md` へ **ADR-024**(D-1/D-2/D-3)/ **ADR-025**(共通被覆水準を
   `id`/`interp`/`extrap_magnitude` の3つに絞る)/ **ADR-026**(`t` ホールドアウト `T_hold`)を起草 → 採択
3. PLAN-003 §10 の追随表に従って `Documents/05_STATISTICS.md` §2・§4・§10 /
   `plans/PLAN-001-eval-battery.md` §5.1 / `plans/PLAN-002-ft-data.md` §4.1.3・§4.2・§5.2・§12 /
   `Documents/04_EXPERIMENT_PLAN.md` / `Documents/06_THREATS.md` T1 を書き換える
4. **そのあとで**実装に入る(#12 が通れば `eligible_pairs` の定義域バグから)

**#6(T2 の5テンプレート文面)は起草してよい。**文面は実験条件なので**確定は人間**(`CLAUDE.md` §8)。

## 直前セッションで確定したこと(すべてファイルに書き込み済み)

- **人間が3件を決定した**(PLAN-003 §2)。**まだ ADR になっていない**(承認待ち-1)
  - **D-1: モデルは Instruct 主系統**(`meta-llama/Llama-3.1-8B-Instruct`)。
    ADR-008 / ADR-018 決定1 を上書きする。理由は T2/T3 が主軸の 2/3 を占め、
    指示追従と自由生成が要るため。**代償: Feucht et al.(base)との厳密一致が失われ、
    G7 の価値がさらに下がる**
  - **D-2: G2(代数的整合性)/ G3(メタ認知)は捨てる。G5 は「特異性対照」の最小版だけ残す**
    (減算・乗算のみ、n=20、TOST)
  - **D-3: 評価プロンプトは英語に統一する**(言語は ADR での決定が存在しなかった)
- **`plans/PLAN-003-redesign.md` を起草した。**完了条件6項目をすべて埋めた。要旨は
  `STATE.md`「引き継ぎ」の表にある。**特に効いている導出**:
  - **被覆水準を3つに絞った根拠は T2 である。**`oob_algebraic`(0/負)と `extrap_pair`(片方が負)は
    文章題として自然文に書けない。**要因計画が3タスク型で共通の層を要求する以上、T2 が水準を決める**
  - **主要評価項目は `p2` 条件の `task * coverage` 交互作用の LRT(df=4)。**
    旧主要評価項目(G6 の `p2` vs `ident`)は Go/No-Go のペネトランス判定(≥ 0.90)に降格
  - **`K=2000` では `interp` の 99.6% が `t_seen` で `interp × t_unseen` が構成不能。**
    P-3(`T_hold` 20 個の予約)がこれを解く。**等間隔に取ると `carry` 側が 0 個になる**ので
    `carry` 比例配分(4/16)にすること
- **コードは1行も変更していない。**`pytest code/tests --collect-only` → **227 collected**(実測)。
  `results/` は空。**実験結果の数値は依然として1つも無い。**RunPod 未使用(GPU 時間 0)

## 触ってよいファイル / 読むべき範囲

- 読む: `STATE.md` 冒頭「★ 最優先」と末尾「引き継ぎ」/ **`plans/PLAN-003-redesign.md`**
  (この文書は全文読んでよい。再導出の本体)
- 参照(**全文 `cat` しない**。`grep -n` で節を特定して `sed -n 'X,Yp'`):
  `logs/DECISIONS.md` の ADR-023 / `Documents/05_STATISTICS.md` §2〜§5
- 書く: `logs/DECISIONS.md`(ADR-024/025/026)、および承認後に PLAN-003 §10 の追随表の各文書

## やってはいけないこと

- **§11 の #1〜#5 が決まる前に実装に着手しない。**項目構成が変わる
- **PLAN-002 §12 の承認待ち9件を個別に消化しない。**PLAN-003 §11 が置き換えた
- **事前登録に `git tag` を打たない**(§11 が全部片付いてから)
- **`Documents/refs.bib` / `02_RELATED_WORK.md` に文献を勝手に確定追加しない**(`CLAUDE.md` §8)
- **PLAN-003 の数値を「実験結果」として扱わない。**すべて組合せ論的な計数である

## 人間の承認待ち(`CLAUDE.md` §8)

**`plans/PLAN-003-redesign.md` §11 の 15 件。**ここには再掲しない(二重管理を避ける)。
骨格は #1〜#5。#12〜#15 は再導出前から持ち越した未解決の解凍である。
