# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-06(その2) / 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **コンテキスト超過**(hook `context-guard` が約 107k トークンで警告)。
作業自体は一区切りついている(Phase 0 の棚卸しと訂正3件。commit `b13aba0`)。

---

あなたは **PLANNER(または人間の決定を受ける IMPLEMENTER / RUNNER)** です。
`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## ★まず読むべきこと

**Phase 0 は現在、エージェントだけで進められる作業が無い。**
残っているのは **(a) 人間の判断 8 件 / (b) 順5 の GPU 承認 / (c) 順6 の実測待ち / (d) 順9 の凍結** だけである。
**新しい作業を探して勝手に始めないこと。**下の一覧から人間が1つ選ぶ。

**★2026-09-06(その2)に「存在しない人間待ち」を1つ潰した。**
`plans/PLAN-004` と `STATE.md` が **B1〜B3(`arbitrary_table` / `train_size` / `coverage_k`)を
「未決」と書いたまま**だったが、**これらは 2026-09-02 に人間が決定済みである**(ADR-050)。
訂正済み。**B1〜B3 を「決めるべきこと」として人間に上げないこと。**

## このセッションでやること(人間が1つ選ぶ)

### A. 人間が判断する(GPU 時間 0。**人間の入力が本体**)

**正本は `plans/PLAN-012-order7-close.md` と `STATE.md`「人間の承認・判断を待っている事項」の先頭ブロック。**

| # | 判断すること | 正本 |
|---|---|---|
| **★1** | **「`id` 到達度を揃える」の操作的定義**。**Go/No-Go #4 / #4b の 0.90 で代用してはならない**(役割が違う) | `05_STATISTICS.md` §4.1 末尾 |
| **★2** | **`H2` 第2節「破れの位置がシード間で一貫しない」の定義** | `01_HYPOTHESES.md` H2 節 |
| **N1** | **Nikankin et al. を「Intro の対立軸の一方の極」として置けるか**(原典は両極を否定し中間に立つ) | PLAN-012 §6.1 |
| **N2** | **#10(W6 の分岐)。**案 A / B / C。**df に効く**(A なら 6→4、T1b の枝と同時発火で 6→2) | PLAN-012 §4 |
| **N3〜N6** | **目視レビュー4件**(ADR-030 の R8 手続き / ADR-027 前段 / ADR-028 決定1 と #4b / PLAN-003 §4.8 のセル構成) | PLAN-012 §5 |
| **#7** | 評価側 manifest に `schema_version` を足すか(小) | ADR-051 |

**すべて合否基準・実験条件・claim の確定なので、エージェントは案までしか出さない**(`CLAUDE.md` §8)。
**N1 についてはエージェントは案も出していない。**

**上記のほかに、順9(パイロット)までに人間が決める必要があるもの:**

- **LoRA グリッドの値**(`train.*`。ADR-043 決定10。`configs/template.yaml` は null のまま)
- **`K` を掃引軸にするか**(ADR-050 決定3 が「引き続き未決」と明記)
- **凍結(段階 D)をパイロット(段階 E)の前に置くか後に置くか**(順9 の第1項。PLAN-004 §6 罠3)
- **`revision` に `[MATCHED]` タグを付けるか**(2026-09-06 に転記した際、タグは付けずに残した)

### B. GPU を使う(**★人間の承認が要る**)

**順5(= `M*` の実測)。**その前に RUNNER が **N2 検査**(comparison 群でバッチ1 対 バッチ N の
`parsed` 一致確認)を取り、実機で `pip freeze` → **ADR-044**(`infra/requirements.lock` の凍結)を履行する。
**preflight の `forced choice tokens` が WARN / FAIL を出したら人間に上げて止まる。**

**★順5 の前に確認すること**: `configs/exp_phase1_main.yaml` の `model.revision` は
2026-09-06 に `0e9e39f249a16976918f6564b8830bc894c89659` を転記済である
(ADR-037 決定3 の履行)。**ポッド上で pull したハッシュがこれと一致することを確かめる。**
一致しなければ**止まって人間に上げる**(2 つの revision が並ぶ)。

## 直前セッションで確定したこと(再実装しないこと)

正本は `logs/CHANGELOG.md` 2026-09-06(その2)+ `STATE.md`「引き継ぎ」。commit `b13aba0`。
`pytest code/tests -q` → **808 passed**(変化なし)。GPU 時間 0。`results/` は空。**コードは 1 行も触っていない。**

- **Phase 0 の現在地**: 順0 / 1 / 1b / 2 / 3 は完了。**順4 は第2条件まで完了**(2026-09-02。`1376909`)。
  **順5 / 6 / 9 は未着手。順7 は一部完了**(検出力の再導出だけが順6 待ち)。**順8 は値だけ未決。**
- **順4 の第3条件を塞いでいるのは `M*`(順5)である。人間の決定ではない。**
  preflight の残 FAIL は `format hash` / `coverage_k floor`(いずれも `M*` 待ち)+
  `token boundaries`(ローカルに `transformers` が無いため。**ポッド上では PASS になるはず。未確認**)。
- **`plans/PLAN-004` §2 の表の下に追記済**: **順4 と順5 は一方向の依存ではない。**
  順4 の第3条件は `M*`(順5 の出力)を待つので、**順4 は順5 をまたいで閉じる。**
- **#13 / #17 の書誌は決着済**(2026-09-06 その1)。**再調査しない。**
  Nikankin et al. は **ICLR 2025(Poster)の査読済み論文**(arXiv:2410.21272)。
  **未検証のまま残した項目**(OpenReview の公式 venue 文字列・decision / ICLR 側の DOI・巻ページ /
  公式 BibTeX / 原典が使った HF モデル ID)は**推測で埋めないこと。**

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-012-order7-close.md`(全文でよい。約 350 行)
- `STATE.md`「人間の承認・判断を待っている事項」の先頭ブロック / 「引き継ぎ」の先頭ブロック
- `Documents/05_STATISTICS.md` §4.1 末尾(★1)/ `Documents/01_HYPOTHESES.md` H2 節(★2)
- `plans/PLAN-004-phase0-route.md` §2 の手順表(**Phase 0 の進捗の正本**)
- **`STATE.md` は 2,900 行超。全文 `cat` しない。**`grep -n '^## '` で節を出し `sed -n 'X,Yp'`

## やってはいけないこと

- **★B1〜B3 を「人間の未決事項」として上げる。**2026-09-02 に決着済(ADR-050)
- **★1 / ★2 / N1〜N6 を提案して決める**(`CLAUDE.md` §8)。**N1 は案も出さない**
- **#13 と #17 の書誌を再調査する**(決着済。トークンの無駄)
- **`experiment.hypothesis` を書き換える**(ADR-053 決定1 で凍結)
- **§5 の Δ に値を入れる**(すべて順6 の実測待ち。`results/` は空)
- **事前登録の凍結タグ(順9)を打つ。**Δ と ★1 / ★2 が未決のうちは打てない
- **凍結した効果量プロファイル P1 / 非加法性 RMS = 0.353 を動かす**(ADR-052 決定2。HARKing)
- **`code/analysis/power_sim.py` を今つくる**(ADR-052 決定3。順6 の後)
- **検出力の数値を書く**(`s2_seed` / `s2_item` / `s2_tmpl` が順6 待ち)
- **§4 の順2 以降を繰り上げて番号を振り直す**(6 文書の参照がずれる。ADR-053 決定3)
- **`configs/exp_phase1_main.yaml` の `arbitrary_table` を手で編集する**(ADR-050 決定1 で確定済)
- **`eval.pool_items` / `eval.cells` を埋める**(B5。`M*` 未決。**順5 の後**)
- **`train.*` のハイパラに値を入れる**(ADR-043 決定10)
- **凍結済みのプロンプト文面を変える**(ADR-046 / 048)/ **強制選択の器械を触る**(ADR-047)
- `configs/smoke.yaml` を編集する(ADR-037 決定4)/ `data/raw/` を書き換える
- **書き出しの `newline="\n"` を外す**(ADR-051 決定4)
- 人間の承認なく GPU ジョブを起動する
- **`.md` を CRLF で書き戻す。**`.gitattributes` は `*.md text eol=lf` を宣言している。
  **★Edit 系のツールは環境によって CRLF に変換する。**`.md` を編集したら
  **必ず `python -c` でバイト単位の CR 数を数えて確かめること**
  (`grep -c $'\r'` は展開に失敗して**全行を数える**ことがあるので信用しない)
- **`print()` に `—`(em dash)や日本語を流さない。**この環境の Python の stdout は cp932 で落ちる
- **`cat > file <<'EOF'` のように `<<` の直後に空白を置かない形は落ちることがある。**
  `<< 'EOF'` と空白を入れる。バックスラッシュを含む置換は scratchpad にスクリプトを書いてから実行する

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

**上の A の一覧がすべてである。**加えて:

- **`05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)。**凍結タグはこれが入るまで打てない**
- **`refs.bib` の Nikankin 書誌の最終確認**(`CLAUDE.md` §3 手順5)
- **`00_OVERVIEW.md` §8 の整理の確認**: G2 を失った分の T1 対策の代替が `arb` / `p2d` でよいか
  (`Documents/06_THREATS.md` T1 の対策表と突き合わせること)
- **`09_PAPER_PLAN.md`**: タイトル候補は 3 だけが現行の主軸に整合 / 図5 の `arb` の扱い(下書き)。
  **★N1 の結論次第で Intro の対立軸を組み直す必要がある**
- **B5**: `eval.pool_items` の本番の中身(**`M*`(順5)待ち**)/ **`θ` の値**(ADR-041。順5 が機械的に出す)
- **ADR-047 実装ノートの R3 / F1 / R4 / 検査5**(実装済み。人間が読んで閉じる)
- **`infra/requirements.lock`**(ADR-044。次のポッドで)
