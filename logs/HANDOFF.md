# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その14)/ 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **コンテキスト超過**(hook `context-guard` が 152k で警告。閾値 140k)。
GPU 時間 0。`code/` の変更 0 行。`pytest code/tests -q` = **811 passed**(実行して確認した)。

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

---

## ★このセッションでやること(1 つだけ)

**ベイズの事前分布の候補を `plans/PLAN-016-fitting-engine.md` に §6.4 として並べる。決めない。**

- **なぜ要るか**: 2026-09-07(その14)に人間が **D-4 = 案 (2) 条件付き併記**を採択した(**ADR-060**)。
  **ADR-059 決定1 の縮退が発火したペアに限り、縮退しない完全構造をベイズで当てて併記する。**
  **事前分布は事前登録が縛る対象であり、凍結より前に確定していなければならない**
  ——**これが ADR-060 が新しく開いた唯一の未決である**
- **完了条件**: §6.4 に **(1) 何を決める必要があるか(固定効果 / 分散成分 / 切片の 3 系統)**、
  **(2) 候補を中立に 3 〜 4 案**(例: `rstanarm` の既定の弱情報事前分布をバージョンごと凍結する /
  明示的に書く / 事前予測検査をしてから決める)、**(3) 各案の長所・短所と、
  それが事前登録に足す文言の量**、**(4) エージェントの推奨(採否は人間)**が書かれていること
- **★決めない**(`CLAUDE.md` §8 / ADR-039 決定3)。**採択は人間である**
- **★実測が要ると判断したら、検査を建てる前に人間に上げる**(下の「やってはいけないこと」)

**あわせて人間に上げる 2 件目**(判断だけ。作業は次のセッション):
**PLAN-016 §7 の 4(`code/analysis/frame.py`。`coverage` の manifest 照合)に着手してよいか。**
**その12 が「着手はスコープの追加なので人間に確認してから」と記録している。**

---

## 直前セッション(その14)で確定したこと(すべてファイルに書き込み済み。commit を参照)

**人間が 3 件を決定した。エージェントは材料を並べ、決定を記録しただけである。**

1. **★D-5 の閾値が確定した**(**ADR-059 の追記**。`grep -n '追記(2026-09-07 その14' logs/DECISIONS.md`)
   - **`logLik(refit) - logLik(fit0) < 0.001`(符号付きの片側。負は「改善していない」側)
     かつ `max|Δβ| / SE < 0.01`。両方を満たせば収束したとみなし、縮退しない**
   - 根拠は検査3(**F59 / F60 / F62**)。**対照(警告が一度も出なかった 14 本)の可動量は最大 0.00102 で、
     閾値 0.01 はその約 10 倍上。**合成 34 ペアでの発火率は **8 / 34(24%)**(案 (b) は 32 / 34 = 94%)
   - **`Documents/05_STATISTICS.md` §3.2 に人間の決定として書き込んだ**
2. **★§3.2 の「収束しない」の定義そのものを人間が承認した**(モデル指定は無変更)
3. **★D-4 = 案 (2) 条件付き併記(ADR-060 を新設)**。**探索的とし Holm のファミリーに入れない。
   発火しなければ回さない。**実装は解析環境(R + `renv`)の内側。**`rstanarm` / `brms` が第一候補**

**→ PLAN-016 の D-1 〜 D-5 はすべて決着した。ステータスは `採択`。残るのは実装(§7 の 4 / 5 / 5b)である。**

**その14 が新しく書いた文書**: `plans/PLAN-016-fitting-engine.md` **§6.3**(D-4 の決定材料。
前提の整理 / 候補 4 案 / 推奨)。**★§4.3 の「R6 は絶望的」は D-4 には掛からない**
——あれは 1000 反復の検出力シミュレーションの話であり、感度解析は当てはめ数本である。

**★未検証として記録した事実**(実装の前に測ればよい。**いま検査を建てない**):
**`rstanarm` / `brms` がこの環境に入るか**(F48 は R 4.6.1 と `lme4` までしか確かめていない。
`brms` は Stan のコンパイラを要求する)/ **`stan_glmer` が §3.2 の交差ランダム効果 3 本を受けるか** /
**MCMC 1 本の壁時計**。

---

## 触ってよいファイル / 読むべき範囲

- **`plans/PLAN-016-fitting-engine.md`**(約 595 行。**§6.3 と §6.2 と §4.3 だけ読めば足りる**)
  ← **§6.4 を書き足す先である**
- `logs/DECISIONS.md` の **ADR-060** と **ADR-059 の追記**
  (`grep -n '^## ADR-060\|追記(2026-09-07 その14' logs/DECISIONS.md` → `sed -n 'X,Yp'`)。
  **全文 `cat` しない**(3,700 行超)
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '^### 3.2' Documents/05_STATISTICS.md`)。
  **全文 `cat` しない**(720 行超)
- **`STATE.md` は 3,650 行を超えた。全文 `cat` しない。**`grep -n '^## '` → `sed -n 'X,Yp'`

---

## やってはいけないこと

- **★事前分布を自分で決める。**案を並べるのが仕事である(`CLAUDE.md` §8 / ADR-039 決定3)
- **★`Documents/05_STATISTICS.md` §3.2 を書き換える。**事前登録の本体である。
  **その14 の書き込みは人間の決定を転記したものであり、エージェントの判断ではない**
- **★新しい検査を建てる**(検査1 / 検査2 / 検査3 で PLAN-016 の測定は終わっている)。
  **事前予測検査が要ると考えたら、回す前に人間に上げる**
- **★D-1 〜 D-5 を上げ直す**(ADR-058 / ADR-059 + 追記 / ADR-060 で全件決着)
- **★D-A 〜 D-D を上げ直す**(ADR-057)
- **`θ` を決める / `M*` を決める**(ADR-041 決定2・決定3)
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(**`M*` 待ち**)
- **事前登録の凍結タグを打つ**(Δ / ★2 / S5 / **事前分布**が未決)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**

### ★この環境で実際に踏んだ地雷(その13 / その14 で確認済み)

- **★長い散文は Write ツールでスクラッチに `.py` を書き、`python file.py` で本文に差し込む。**
  **その14 はこの方式で 5 回とも通した**(`cat > file <<'EOF'` は落ちる。
  `python - <<'PY'` は通ったが、バッククォートを含む本文は script ファイルにするのが確実)
- **★`python -c "..."` の中にバッククォートを書くと bash がコマンド置換として食う**
- **★`print()` に em dash や日本語を流さない**(この環境の Python の stdout は cp932。その14 で踏んだ)。
  **検証の出力は ASCII だけにする**
- **★`.md` を編集したら python でバイト単位の CR 数を数える**(`.gitattributes` は `* text=auto eol=lf`)。
  **書き戻すときは `newline="\n"` を明示する。**
  **入れ子太字は「追記した文字列だけ」に `\*{3,}` を掛けて数える**(STATE.md 全体には既存の 11 件がある)
- **★`Rscript -e '...'` の二重引用符が Windows で壊れる。**R は `.R` ファイルにして回す
- **`logs/CHANGELOG.md` は古い順である**(新しいものが末尾)
- **`pytest code/tests -q` は 24 〜 59 秒**(811 tests)。`timeout` を伸ばす
- **★`glmer` の 1 回の当てはめは 1 秒 〜 1 分。**測定中に `pytest` を並走させない(F51)

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

- **★★ベイズの事前分布**(ADR-060 の帰結。**このセッションで案を並べる対象**)。
  **クリティカルパス上には無いが、事前登録の凍結より前に要る**
- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 を回す前に決まっていなければならない。
  表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★PLAN-016 §7-4(`coverage` の manifest 照合)に着手してよいか**(スコープの確認)
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)
- **解析側の凍結手段**(ADR-058 決定2 の帰結)。`renv.lock` を repo に置くかどうか
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9)/ **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A = ADR-056、★D = ADR-058 / 059 / 060 で閉じた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格するが、**非有意は研究仮説 H1 が予測する結果でもある。**案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。**従属変数での選択。**案: 門を掛ける量を DiD の腕から外す。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | 当てはめエンジンが無い | **→ 閉じた。**D-1 = 案 A / D-2 = 同じエンジン / D-3 = 解析を切り離す / D-5 = 再起動で検証した失敗のみ + 閾値確定 / D-4 = 条件付きベイズ併記。**残るのは実装と事前分布** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード) | **未決** |

### 順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4。どちらも `comparison` 群(= 評価プール)を要求するので掃引では成立しない。**

| # | 前提 | 備考 |
|---|---|---|
| **1** | **バッチ fp ノイズ検査**(ADR-040 決定7) | **GPU が要る** |
| **2** | **preflight の `forced choice tokens`**(ADR-047 実装ノート 5) | **★重みは読まないのでローカルで取れる** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**
**★ローカル環境**: **R 4.6.1 と `lme4 2.0.6` が入っている**(その11 で導入。その13 で再確認)。
`statsmodels` は外したままである。**`infra/requirements.lock` は無変更である。**
