# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-06(その8)/ 直前セッションの役割: IMPLEMENTER (Opus)
直前セッションが終了した理由: **やることが終わったから**(ADR-057 帰結 (a)〜(d) を実装した)。
**PLAN-014 は `レビュー済` になった。順5 は「起動できる」状態である。**
GPU 時間 0。`pytest code/tests -q` = **811 passed**。

---

## いまの状態(1行)

**順5(桁数掃引 → `M*`)は、config もコードも文書も揃った。残るのは `θ` の決定(人間)と実行(RUNNER)だけである。**

---

## ★次にやることは 2 つに分かれる。人間が選ぶ

### A. 順5 を回す(クリティカルパス)

**★先に人間が `θ` を決めなければならない**(ADR-041 決定2・決定3)。
**表を見てから決めると事後選択になる。**エージェントは `θ` を提案しない。

`θ` が決まったら RUNNER が `plans/PLAN-014` §5 の手順で実行する。要点:

1. ポッド上で `huggingface-cli login`(**★人間が実行する。エージェントは認証情報を入力しない**)
2. **`model.revision` の照合。**pull したハッシュが
   `0e9e39f249a16976918f6564b8830bc894c89659` と一致しなければ**止まって人間に上げる**
3. `pip freeze > infra/requirements.lock`(ADR-044)
4. `python infra/preflight.py --config configs/exp_phase1_main.yaml --run-dir "$RUN_S" --run-kind sweep`
   **★`--run-kind sweep` を必ず付ける**(ADR-057 決定3。`infra/RUNPOD.md` §3「run 種別の例外」)。
   **緩むのは `format hash` / `coverage_k floor` の 2 件だけである。**
   **★ローカルで残っている FAIL は `token boundaries` の 1 件だけ**で、
   これは gated repo に未認証だからである。**login 後に PASS になる見込み(未検証)。
   ならなければ止まる。**
5. `python -m code.eval.sweep --config configs/exp_phase1_main.yaml --run-dir "$RUN_S"`
6. **`M*` は人間が表から決める**(ADR-041 決定3 規則2:
   `M` を小さい順に見て初めて `θ` を割った水準の 1 つ下。**上で回復しても採らない**)。
   **掃引は `M*` を出力しない。`M* < 100` なら `D_ext` が空になるので止まって人間に上げる**
7. **ポッドを停止する**(`CLAUDE.md` §9)

### B. ★B〜★F(本筋との整合の指摘 5 件)のどれかを人間が決める

**末尾の表が正本。**順5 とは独立に進められる。**★D は順5 の後に必ず要る**
(当てはめエンジンが無いと主要検定が回らない)。

---

## 直前セッションで確定したこと(再実装しないこと)

正本は **ADR-057**(`grep -n '^## ADR-057' logs/DECISIONS.md`)+
`plans/PLAN-014-order5-launch-preconditions.md`(**`レビュー済`**)+
`logs/CHANGELOG.md` 2026-09-06(その8)。

- **(a) `configs/exp_phase1_main.yaml` を記入した**:
  `eval.reference_rule: p2` / `eval.elicitation: direct`(**2026-08-22 の承認の転記**)/
  `resources` の 4 欄(`23.9` / `RTX 4090` / `1.5` / `2026-09-06`)。
  **`resources` には「順5 の構成であって本実験 40 run の構成ではない」という注記を入れた。
  この注記を消さないこと。**
- **(b) `infra/RUNPOD.md` §3 に「★run 種別の例外」を新設し、§4 手順 5b に
  `--run-kind sweep` を足した**
- **(c) D-D の 2 件を順6 の前提に移した**(下の「順6 の前提」)
- **(d) `plans/PLAN-014` を `レビュー済` にした。**§4 の各節に決着を、§5 手順3 に
  `--run-kind sweep` を、§7 の D-A に打ち消し線を入れた
- **★実機で確認した事実(推測ではない)**: 記入後の本番 config に `--run-kind sweep` 付きで
  preflight を通すと、**当該 2 件が SKIP、残る FAIL は `token boundaries` の 1 件だけ**である
- **★ローカルで検証できていないこと(隠さない)**: **GPU 検査は nvidia-smi が無いので WARN**。
  **`min_vram_gb: 23.9` はローカルで 1 度も評価されていない。**確かめられるのはポッド上だけ
- **★HANDOFF の完了条件を 1 つ訂正した**: 前回の (a) は
  「`--dry-run` が `ConfigError` を出さないこと」を完了条件にしていたが、
  **`--dry-run` は 2 欄が null のままでも exit 0 で通る**(`dry_run_summary` はこの 2 欄を
  読まない)。**`require` するのは実行経路**(`sweep_all` `:302`/`:303` と
  `execute` の payload `:394`/`:395`)である。**`require` を直接呼ぶ検査で確かめ直した**
- **GPU 時間 0。`results/` は空。ポッドは 1 つも起動していない**

---

## ★順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4 で順5 の前提から外した 2 件である。**
**どちらも `comparison` 群(= 評価プール)を要求するので、掃引では成立しない**(F22 / F23)。
**順5 の前にやろうとしないこと。**

| # | 前提 | 中身 | 備考 |
|---|---|---|---|
| **1** | **バッチ fp ノイズ検査** | `comparison` 群で**バッチ1 対 バッチ N** の `parsed` 一致を見る(ADR-040 決定7)。原文は「**段階 C の本番でもプールの部分集合 100 項目で**」 | `eval.batteries` に `comparison` が入り `pool_items` が埋まってから。**GPU が要る** |
| **2** | **preflight の `forced choice tokens`** | Yes / No が単一トークンで分離するか(ADR-047 実装ノート 5)。**FAIL は 2 つだけ**(片側が 1 つも単一トークンにならない / Yes 側と No 側が重なる)。**一部が割れるのは WARN** | `eval.batteries` に `comparison` が無いと **SKIP**。**★重みは読まないので GPU を借りる前にローカルで取れる**(`infra/RUNPOD.md` §3)。**`comparison` を宣言した config さえあればよい** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**移していない。

---

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-014-order5-launch-preconditions.md`(**`レビュー済`**。約 250 行)**← A を選んだら最初に読む**
- `logs/DECISIONS.md` の **ADR-057**(`grep -n '^## ADR-057' logs/DECISIONS.md`)、
  **ADR-041**(`M*` の決定規則と `θ`)
- `infra/RUNPOD.md` **§3「★run 種別の例外」と §4 手順 5b**
- `configs/exp_phase1_main.yaml` の `eval:` と `resources:`
- **`STATE.md` は 3,300 行を超えた。全文 `cat` しない。**`grep -n '^## '` で節を出し `sed -n 'X,Yp'`

---

## やってはいけないこと

- **D-A 〜 D-D を人間に上げ直す。**2026-09-06 に決着し(ADR-057)、実装も済んだ
- **`θ` の値を決める / `M*` を決める / 出力する**(ADR-041 決定2・決定3。**人間が表から決める**)
- **`resources` の注記を消す / 縮める**(ADR-057 決定2。**本実験 40 run の GPU 構成と誤読される**)
- **`SWEEP_SKIPPED_CHECKS` に検査を足す。**`pool regions` / `matched stream` / `t_holdout` /
  `holdout leak` は **FT データの検査**であって評価プールの検査ではない。**掃引でも緩めない**
- **`token boundaries` を掃引の例外に入れる。**掃引もプロンプトを組み立てて生成するので、
  書式のトークン化は**掃引の測定対象の内側**にある
- **`--run-kind sweep` を掃引以外の run に付ける。**既定の `main` を明示的に上書きする引数である
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(B5。**`M*` 待ち**)
- **`train.*` のハイパラに値を入れる**(ADR-043 決定10)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**
- **★2 / S5 / N1 / N2 / N3〜N6 / #7 を決める**(`CLAUDE.md` §8)
- **`Documents/05_STATISTICS.md` §5 の Δ に値を入れる**(順6 の実測待ち)
- **事前登録の凍結タグを打つ**(Δ と ★2 と S5 が未決)

### ★この環境で実際に踏んだ地雷(2026-09-06 その5〜その8)

- **★`.md` / `.py` / `.yaml` を編集したら、必ず python でバイト単位の CR 数を数えて確かめること。**
  `.gitattributes` は `* text=auto eol=lf` を宣言している。**Edit / Write 系のツールは環境によって
  CRLF に変換する。**grep で CR を数える書き方は信用しない。
  **`infra/RUNPOD.md` は作業ツリーが CRLF になっていた**(index は LF)。編集時に LF に戻した
- **★この repo の日本語は半角括弧を使う。全角の丸括弧は 0 件である。**
  アンカー文字列を組むときに全角を混ぜると必ず一致しない(実際に踏んだ)
- **★大きな編集は scratchpad に `.py` を書いて `python file.py` で走らせる。**
  アンカーは「出現回数が 1 であること」を assert してから置換する
- **★heredoc に「heredoc の書き方」を書くと bash が落ちる。**
  本ファイルを heredoc で書こうとして `unexpected EOF` になった。**長い散文は Write ツールで書き、
  その後に python で CR を潰す**のが確実である
- **★`python -c` の中の `\uXXXX` は `SyntaxError` になることがある**(truncated escape)。
  `python -c` の中に裸のバッククォートを書くと bash がコマンド置換として
  `.md` をシェルスクリプトとして実行し、変な名前の空ファイルが作られる
- **★漢字の打ち間違いを diff で必ず目視する。**「掃引」を「掟引」と書いて 6 箇所に入れた
- **★`cd` を含む複合コマンドの後は作業ディレクトリが戻る。**scratchpad で `python` を
  走らせた直後に `git` を打つと `not a git repository` で落ちる。**git は別のコマンドで打つ**
- **`print()` に em dash や日本語を流さない**(この環境の Python の stdout は cp932)。
  ファイルに書き出して読む
- **表のセルの中のパイプ記号をエスケープし忘れる**
- **`infra/preflight.py` は内部で `pytest` を回すので 1 回 50 秒かかる。**
  2 回続けて呼ぶと 120 秒のタイムアウトを超える。**`timeout` を伸ばすか 1 回ずつ回す**
- **`code.eval.sweep --dry-run` の標準出力は約 1 MB ある。**必ずファイルに落とす

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

- **★`θ` の値**(ADR-041 決定2・決定3)。**★順5 を回す前に決まっていなければならない。**
  **表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結。
  `train.*` のハイパラ = ADR-043 決定10 と同じ場で決まる)
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9)/ **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 決定1 で §0 が三択になったので、
  この 1 行だけ枠組みが古い。ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(未決。2026-09-06 その6 に人間へ上げた)

**★A(`CLAUDE.md` §0 と現行設計の乖離)は ADR-056 で閉じた。**残り 5 件は未決である。

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**順1b / 順7 / 順4 は「概念か表層パッチか」を判別するが、**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格する。**非有意は研究仮説 H1 が予測する結果でもある。案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。`T1 × id` で門を掛け、その `id` を DiD の基準腕に使う = **従属変数での選択。**切り捨てが条件間で非対称なら DiD にバイアスが入る。案: **門を掛ける量を DiD の腕から外す**(`other_error_rate` など)。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | **`glmer` に相当する当てはめエンジンがスタックに存在しない。**`code/` に混合効果モデルのコードは 0 行(F11)、R も `lme4` も `pymer4` も `bambi` も無く、**`statsmodels` は宣言だけで使用 0 件、かつ交差ランダム効果の二項 GLMM を当てられない。**S5(ランダム傾き)はエンジンが決まらないと決められない。**★順5 の後に必ず要る** | **未決** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**そして ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード)。★A の確定(ADR-056)を受けて再点検する価値がある | **未決** |
