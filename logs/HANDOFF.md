# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その9)/ 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **コンテキスト超過**(hook `context-guard` が 146k を実測。閾値 140k)。
**PLAN-016 を書き終えた直後に、人間が「検査1 を回して案 D の R3 を確かめて」と指示した。
それが次セッションの仕事である。**
GPU 時間 0。コード変更 0 行。`pytest code/tests -q` = **811 passed**(実行して確認した)。

---

あなたは **IMPLEMENTER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

---

## このセッションでやること(1つだけ)

**`plans/PLAN-016-fitting-engine.md` §7 の「検査1」を回し、案 D が要件 R3 を満たすかを実測する。**

**★人間が明示的に指示した作業である。**着手してよい。GPU は要らない。

### 完了条件(判定できる形で書く)

1. `statsmodels` をローカルに入れる。**★ローカルは Windows + Python 3.14.3 である**
   (`plans/PLAN-016` の F36)。**wheel が無くてビルドに落ちる可能性がある。
   落ちたらそれ自体が結果である** ——「案 D はこの環境では導入できない」と記録して止まる
2. **`statsmodels.genmod.bayes_mixed_glm.BinomialBayesMixedGLM` が
   最尤の対数尤度を返すかどうか**を、**実際に小さな合成データに当てて**確かめる。
   見るべきもの:
   - `fit_vb()` / `fit_map()` が返すオブジェクトに **`llf` に相当する量があるか**
   - **あるとして、それは ELBO / 事後モードの対数事後であって最尤の対数尤度ではないのか**
   - **入れ子の 2 モデルの差を取って χ² 検定にかけてよい量か**
3. **合成データは `Documents/05_STATISTICS.md` §3.2 と同じ形にする** ——
   二項応答 + **交差ランダム切片 3 本**(`(1|seed)` + `(1|item)` + `(1|template)`)。
   `vc_formula` で交差が本当に書けるかも同時に確かめる(R2)
4. **結果を `plans/PLAN-016` §4.4 に追記する。**「未検証」を消し、
   **実測した事実として書き直す**。§4.7 の比較表の `D` 行も直す
5. `logs/CHANGELOG.md` に追記して commit

### ★検査1 の結果がどちらでも、次は人間の番である

- **返さない場合**: 案 D は **R3 で落ちる**。§4.4 のとおり。残るのは A / B / E / F
- **返す場合**: **案 D は R8(pip だけで pin できる)を満たす唯一の既製品として最有力になる**
- **どちらでも D-1 は人間が決める**(`CLAUDE.md` §8)。**検査1 の結果でエンジンを決めない**

---

## 直前セッションで確定したこと(再実装しないこと)

正本は **`plans/PLAN-016-fitting-engine.md`**(`レビュー待ち`。209 行)+
`logs/CHANGELOG.md` 2026-09-06(その9)。

- **`plans/PLAN-016` を新設した。決定は 0 件である**(`CLAUDE.md` §8 / ADR-039 決定3)
- **実地確認 9 件(F32〜F40)。すべて直前セッションで実行して得た。文書からの転記ではない**
  - **F34**: `statsmodels` は **ローカルに入っていない**(`ModuleNotFoundError`)。
    `pyproject.toml` の optional extra `stats` に宣言があるだけ。使用箇所 0 件
  - **F35**: **ローカルに R も `Rscript` も無い。**`rpy2` / `pymer4` / `bambi` / `pymc` も ABSENT
  - **F36**: あるのは `numpy 2.4.6` / `scipy 1.18.0` / `pandas 3.0.5` /
    `matplotlib 3.11.1` / **`torch 2.13.0+cpu`**。Python は **3.14.3**
  - **★F38(このセッションで最も重い発見)**: **`coverage` を項目に付ける実行時コードが
    `code/eval/` に 1 件も無い**(`grep -c` = 0)。設計上 `Item` は `coverage` を持たず、
    FT データの manifest と照合して実行時に付けることになっている
    (`code/data_gen/battery_items.py:59`)が、**その照合が書かれていない**。
    **主要検定 `task:coverage` の説明変数の片方が作れないので、これは迂回できない**
  - **F39**: **`template` は復元できる。**T2 の 5 本は `configs/templates/t2.yaml` にあり、
    `numeric_sum.render_prompt` が **`templates.get(item.category)`** で引く。
    `category` は `predictions/` に残る
- **要件を 9 件(R1〜R9)に落とした。★R3(入れ子モデル間の LRT = 最尤)と
  R6(8000 fit の速度)が正面衝突している**のがこの問題の難しさである
- **案は 6 つ**: **A** R + `lme4` を `Rscript` で / **B** `pymer4` / **C** `bambi`・`PyMC` /
  **D** `statsmodels` の `BinomialBayesMixedGLM` / **E** 自前実装 / **F** A + E
- **「推奨しない」を付けたのは案 E の単独採用の 1 行だけである**(ADR-039 決定3 の作法)
- **ADR は書いていない。決定が 0 件だからである**(PLAN-013 と同じ扱い)
- **GPU 時間 0。`results/` は空。ポッドは 1 つも起動していない。実データは見ていない**

---

## 触ってよいファイル / 読むべき範囲

- **`plans/PLAN-016-fitting-engine.md`**(209 行。**全文読んでよい**)**← 最初に読む**
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '^### 3.2' Documents/05_STATISTICS.md`)。
  **全文 `cat` しない**(708 行)
- `pyproject.toml` の `[project.optional-dependencies]` の `stats`
- **`STATE.md` は 3,300 行を超えた。全文 `cat` しない。**
  `grep -n '^## '` で節を出し `sed -n 'X,Yp'`

---

## やってはいけないこと

- **検査1 の結果でエンジン(D-1)を決める。**検査1 は D-1 の**材料**であって D-1 ではない
- **D-2 / D-3 / D-4 を決める**(`plans/PLAN-016` §6。`CLAUDE.md` §8)
- **`Documents/05_STATISTICS.md` §3.2 のモデル指定を書き換える。**事前登録の対象そのものである
- **`θ` を決める / `M*` を決める**(ADR-041 決定2・決定3。**人間が表から決める**)
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(**`M*` 待ち**)
- **`resources` の注記を消す / 縮める**(ADR-057 決定2)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**
- **事前登録の凍結タグを打つ**(Δ と ★2 と S5 が未決)
- **★D-A 〜 D-D を人間に上げ直す。**2026-09-06 に決着した(ADR-057)

### ★この環境で実際に踏んだ地雷

- **★`cat > file <<'EOF'` 形式の heredoc がこの環境の Bash ツールで落ちる。**
  直前セッションで `unexpected EOF while looking for matching quote` になった。
  **長い散文は Write ツールで書き、その後に python で CR を数えて潰す**のが確実である。
  なお `python - <<'PYEOF'` に**スクリプトを流すのは動く**(実際に何度も使った)
- **★`.md` / `.py` / `.yaml` を編集したら、必ず python でバイト単位の CR 数を数えて確かめること。**
  `.gitattributes` は `* text=auto eol=lf` を宣言している。Write 系は環境によって CRLF にする。
  **grep で CR を数える書き方は信用しない**
- **★全角文字を混ぜない。**直前セッションで **全角の `５`** を「順5」に混ぜて 3 箇所に入れた。
  **書いた後に `python` で `５` と `（` / `）` を数えて確かめること**
- **★Markdown の入れ子太字を作らない。**`**A の **B** を C**` は壊れる。
  `****` が並ぶのも壊れる。**直前セッションで両方やった**
- **★シェルのパイプ越しに日本語を grep すると化ける**(cp932)。
  **文字種の検査は python の中で完結させる**
- **★`python -c` の中の `\uXXXX` は `SyntaxError` になることがある。**
  `python -c` の中に裸のバッククォートを書くと bash がコマンド置換として解釈する
- **`print()` に em dash や日本語を流さない**(この環境の Python の stdout は cp932)
- **`infra/preflight.py` は内部で `pytest` を回すので 1 回 50 秒かかる。**
  2 回続けて呼ぶと 120 秒のタイムアウトを超える
- **`pytest code/tests -q` は約 59 秒かかる**(811 tests)。`timeout` を伸ばす
- **`code.eval.sweep --dry-run` の標準出力は約 1 MB ある。**必ずファイルに落とす

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

### ★PLAN-016 が新しく開いた 4 件(検査1 とは別)

- **D-1**: **主要検定を当てるエンジン**(案 A / B / C / D / E / F)。
  **案 C を採ると主要検定が LRT でなくなる** —— 事前登録の差し替えになるので、
  **採るなら順5 より前・凍結より前でなければならない**
- **D-2**: 検出力シミュレーション(§6.3 手続き3)を主要検定と同じエンジンで回すか、別実装にするか
- **D-3**: **R を実験環境に入れるか、解析を実験環境から切り離すか。**
  ADR-044 と `infra/Dockerfile` は **pip の 1 層しか想定していない**(F40)
- **D-4**: 案 C(ベイズ)を感度解析として併記するか
- **★D-1 は S5(ランダム傾き)と結びついている** —— 傾きを置くなら R4 が必須要件に昇格する。
  **逆にエンジンを先に決めると S5 の選択肢が狭まりうる。どちらを先に決めるかも人間の判断である**

### 従来からの未決

- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 を回す前に決まっていなければならない。
  表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9)/ **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A は ADR-056 で閉じた。★D は PLAN-016 に落ちた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格するが、**非有意は研究仮説 H1 が予測する結果でもある。**案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。`T1 × id` で門を掛け、その `id` を DiD の基準腕に使う = **従属変数での選択。**案: 門を掛ける量を DiD の腕から外す。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | 当てはめエンジンが無い | **→ `plans/PLAN-016` に落ちた。D-1〜D-4 が人間待ち。次セッションは検査1 を回す** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード) | **未決** |

### 順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4。どちらも `comparison` 群(= 評価プール)を要求するので掃引では成立しない。**

| # | 前提 | 備考 |
|---|---|---|
| **1** | **バッチ fp ノイズ検査**(ADR-040 決定7)。`comparison` 群でバッチ1 対 バッチ N の `parsed` 一致 | **GPU が要る** |
| **2** | **preflight の `forced choice tokens`**(ADR-047 実装ノート 5) | **★重みは読まないので GPU を借りる前にローカルで取れる** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**移していない。
