# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その15)/ 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **やることが終わったため**(§6.4 を書き終え、人間に 3 件を上げた)。
GPU 時間 0。`code/` の変更 0 行。`pytest code/tests -q` = **811 passed**(実行して確認した)。
**決定は 0 件である**(`CLAUDE.md` §8 / ADR-039 決定3)。

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

---

## ★このセッションの本体はエージェントの作業ではない

**人間が 3 件を決める。決まるまでエージェントが進められるものは無い**(下の「やってはいけないこと」)。
**3 件はすべて `STATE.md` の「人間の承認・判断を待っている事項」の最上段(その15)に書いてある。**

| # | 決めること | どこに材料があるか | 何を止めているか |
|---|---|---|---|
| **(1)** | **ベイズの事前分布(D-6)。**3 系統: **(i) 切片 / (ii) 固定効果の係数 / (iii) 分散成分の SD** | **`plans/PLAN-016-fitting-engine.md` §6.4**(候補 4 案 / 推奨 / 叩き台の数値) | **事前登録の凍結** |
| **(2)** | **ベイズ側で何を数値として並べるか**(事後区間 / `loo` / ベイズファクタ) | **同 §6.4.3** | **同上。(1) の (ii) をどれだけ真剣に決めるかがこれで変わる** |
| **(3)** | **PLAN-016 §7-4(`code/analysis/frame.py`。`coverage` の manifest 照合)に着手してよいか** | **同 §5**(F37 / F38 / F39) | **実装の入口。`coverage` が無いと主要検定の説明変数の片方が作れない** |

**★決まった後にエージェントがやること**: `logs/DECISIONS.md` に ADR を書く
(**提案者と採択者を分ける**。ADR-039)→ **(1)(2) が決まれば `Documents/05_STATISTICS.md` §3.2 の
★ 行(「事前分布は未決」)を人間の決定として書き換える** → **(3) に許可が出れば §7-4 の実装に入る。**

---

## 直前セッション(その15)で書いたこと

**`plans/PLAN-016-fitting-engine.md` §6.4 を新設しただけである**(約 165 行)。
**§6 の表に D-6 の行を足し、ヘッダ / ステータス / §7-5b の参照を更新した。**

- **6.4.1 対象**: 主軸 / 順1(`p2d`)/ 順1b の 3 つの場。**いずれも二項・ランダム切片 3 本。**
  **★S5 で傾きを置くと相関行列の事前分布が 4 系統目として増える**(連動を明記した)
- **6.4.2 決める 3 系統と、なぜ (iii) が効くか**
- **6.4.3 ★連動する未決を 1 件見つけて上げた**(何を並べるか。ADR-060 が書いていない)
- **6.4.4 候補 4 案**(中立)/ **6.4.5 長所短所** / **6.4.6 推奨 = (2) と叩き台の数値** /
  **6.4.7 未検証の事実**

**★このセッションが文書とコードから確認した事実**(推測ではない):
**`template` の水準数が小さい。**`Documents/05_STATISTICS.md` §3.2 の表は
「T2 の 5 テンプレート + T3 の質問文。T1 / T1b は単一」と書くが、
**`code/eval/battery/t3_comparison.py:61` の `CATEGORY_AXES` は T3 / T1b にそれぞれ 2 水準
(`gt` / `lt`)を持つ。**`category` → `template` の写像は未実装であり(§5)、
**正確な本数は未確定である。**いずれにせよ 2 桁に届かない。
**→ 分散成分の SD は尤度からほとんど決まらず、事前分布の選択が結果をほぼ一手に決める。**

**★未検証のまま残した**(意図的。新しい検査を建てないため):
**`rstanarm` / `brms` の既定事前分布の具体**(文書に基づく理解であり実測していない)/
**autoscale がどの予測子に掛かるか** / **検査3 でどの分散成分が境界に張り付いたか**
(成分別に集計していない)/ **ADR-060 が既に記録している 3 件**
(環境に入るか / `stan_glmer` が交差 3 本を受けるか / MCMC 1 本の壁時計)。

---

## 触ってよいファイル / 読むべき範囲

- **`plans/PLAN-016-fitting-engine.md`**(約 765 行。**§6.4 だけ読めば足りる**。
  `grep -n '^#### 6.4' plans/PLAN-016-fitting-engine.md` → `sed -n 'X,Yp'`)
- `logs/DECISIONS.md` の **ADR-060**(`grep -n '^## ADR-060' logs/DECISIONS.md` → `sed -n 'X,Yp'`)。
  **全文 `cat` しない**(3,700 行超)
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '^### 3.2' Documents/05_STATISTICS.md`)。
  **全文 `cat` しない**(720 行超)
- **`STATE.md` は 3,730 行を超えた。全文 `cat` しない。**`grep -n '^## '` → `sed -n 'X,Yp'`

---

## やってはいけないこと

- **★事前分布を自分で決める**(`CLAUDE.md` §8 / ADR-039 決定3)。**§6.4 は案である**
- **★「何を並べるか」を自分で決める**(同上)
- **★`Documents/05_STATISTICS.md` §3.2 を人間の決定なしに書き換える。**事前登録の本体である
- **★新しい検査を建てる。**事前予測検査(§6.4.4 の案 (3))が要ると考えたら、**回す前に人間に上げる**
- **★D-1 〜 D-5 を上げ直す**(ADR-058 / ADR-059 + 追記 / ADR-060 で全件決着)
- **★D-A 〜 D-D を上げ直す**(ADR-057)
- **`θ` を決める / `M*` を決める**(ADR-041 決定2・決定3)
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(**`M*` 待ち**)
- **事前登録の凍結タグを打つ**(Δ / ★2 / S5 / **事前分布** / **何を並べるか**が未決)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**

### ★この環境で実際に踏んだ地雷(その13 / その14 / その15 で確認済み)

- **★長い散文は Write ツールでスクラッチに `.py` を書き、`python file.py` で本文に差し込む。**
  **その15 はこの方式で 4 回とも通した**(`cat > file <<'EOF'` は落ちる)
- **★`python -c "..."` の中にバッククォートを書くと bash がコマンド置換として食う**
- **★`print()` に em dash や日本語を流さない**(この環境の Python の stdout は cp932)。
  **検証の出力は ASCII だけにする**
- **★`.md` を編集したら python でバイト単位の CR 数を数える**(`.gitattributes` は `* text=auto eol=lf`)。
  **書き戻すときは `newline="\n"` を明示する。**
  **入れ子太字は「追記した文字列だけ」に `\*{3,}` を掛けて数える**
- **★Markdown の表のセルに `(1 | seed)` のような縦棒を書くときは `\|` に escape する**(その15)
- **★`Rscript -e '...'` の二重引用符が Windows で壊れる。**R は `.R` ファイルにして回す
- **`logs/CHANGELOG.md` は古い順である**(新しいものが末尾)
- **`pytest code/tests -q` は 24 〜 59 秒**(811 tests)。`timeout` を伸ばす
- **★`glmer` の 1 回の当てはめは 1 秒 〜 1 分。**測定中に `pytest` を並走させない(F51)

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

- **★★ベイズの事前分布(D-6)**。**材料は §6.4。凍結より前に要る**(このセッションの (1))
- **★★ベイズ側で何を並べるか**(§6.4.3。このセッションの (2))
- **★PLAN-016 §7-4(`coverage` の manifest 照合)に着手してよいか**(このセッションの (3))
- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 を回す前に決まっていなければならない。
  表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)
- **解析側の凍結手段**(ADR-058 決定2 の帰結)。`renv.lock` を repo に置くかどうか
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9。**★事前分布と連動する。**§6.4.1)/
  **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A = ADR-056、★D = ADR-058 / 059 / 060 で閉じた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格するが、**非有意は研究仮説 H1 が予測する結果でもある。**案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。**従属変数での選択。**案: 門を掛ける量を DiD の腕から外す。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | 当てはめエンジンが無い | **→ 閉じた。**D-1 = 案 A / D-2 = 同じエンジン / D-3 = 解析を切り離す / D-5 = 再起動で検証した失敗のみ + 閾値確定 / D-4 = 条件付きベイズ併記。**残るのは事前分布(D-6)と実装だけである** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード) | **未決** |

### 順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4。どちらも `comparison` 群(= 評価プール)を要求するので掃引では成立しない。**

| # | 前提 | 備考 |
|---|---|---|
| **1** | **バッチ fp ノイズ検査**(ADR-040 決定7) | **GPU が要る** |
| **2** | **preflight の `forced choice tokens`**(ADR-047 実装ノート 5) | **★重みは読まないのでローカルで取れる** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**
**★ローカル環境**: **R 4.6.1 と `lme4 2.0.6` が入っている**(その11 で導入。その13 で再確認)。
**`rstanarm` / `brms` は入れていない**(ADR-060 の未検証欄)。
`statsmodels` は外したままである。**`infra/requirements.lock` は無変更である。**
