# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-08(その21)/ 直前セッションの役割: IMPLEMENTER (Opus)
直前セッションが終了した理由: コンテキスト超過(hook `context-guard` が約 114k で警告。閾値 100k)

---

あなたは PLANNER / CRITIC です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**★F90(「順6 では `s2_seed` が出ない」)を確定させ、`Documents/05_STATISTICS.md` §6.3 手続き2 の
扱いを人間に諮れる形にする。決定は 0 件でよい**(`CLAUDE.md` §8。合否の判断は人間)。

完了条件:

1. **F90 が成り立つかを repo の記述だけで確かめる。**
   `Documents/04_EXPERIMENT_PLAN.md` の Phase 0 Go/No-Go 表 / `plans/PLAN-004-phase0-route.md` §2 順6 /
   `Documents/05_STATISTICS.md` §6.3 手続き2 の 3 つを突き合わせ、
   **「順6 に FT run が 1 本も含まれないか」**を**引用で**示す。
   **★PLAN-019 §9 の末尾に「もし『順6』がパイロットの一部を含む意図だったなら F90 は成り立たない」
   と書いてある。その両義がどちらなのかを、文書の記述で決着させること**
2. **成り立つ場合の帰結を並べる**: §6.3 手続き2 は `s2_seed` をどこから取るのか。
   候補(例: パイロット段階 E から取る / 先行研究の値を置く / 検出力の見立てを出さない)を
   **利害つきで 3 案ほど**並べ、`plans/PLAN-019-validity-decisions.md` に決定記入欄を作る
3. **★数値を作らない。**`results/` は空であり、`s2_seed` の実測値はこの世に存在しない
4. **`STATE.md` を 400 行 / 60 KB 以下へ戻す**(ADR-063。現在 **449 行**)。
   **削るのではなく `logs/STATE-ARCHIVE.md` へ移す**
5. `logs/CHANGELOG.md` に追記して commit

## 直前セッションで確定したこと

- **ADR-066 の決定 1 / 2 / 4 を `Documents/` に反映し終えた**(commit `832a7bd`)。
  **★ADR-066 に関する人間待ちは 0 件である。上げ直さないこと**
- **決定4 (d) の論拠を書く前に検査で確かめた**(`plans/PLAN-019-check1/`。**合成データ。GPU 0**。
  **実験結果ではない**。正本は `run.log` と PLAN-019 §0 の事実表):
  - **F97 代数は厳密。**交互作用が張る空間(rank 6 = 主要検定の df)の**すべて**の対比について
    `u_{s,k}` の負荷との積の最大値 **1.54e-15**。**リンク関数に依らない。**
    対照の被覆主効果の対比は `u` を拾う
  - **F98** 差分の差の期待値も `sigma_u` に依らない(真値 0 から 0.003 未満)
  - **★F99 分散は消えない。**SD は恒等リンクで **0.0630 → 0.0637(平ら)**、
    **ロジットで 0.1659 → 0.1983(+20%)**。
    **よって「交互作用は `u` の影響を受けない」は一次の見立てであって厳密ではない。**
    **`lme4` の最尤推定量の SE の振る舞いは未検証**
- **`Documents/06_THREATS.md` に T15 を新設した** ——
  **`task:coverage` のシード間変動は事前登録したモデルでも誤差に畳まれたままである。**
  **`H2` 第2節はこの主要解析では検定されない**
- **`pytest code/tests -q` = 839 passed**(その21 はコードを 1 行も触っていない)
- **実験は 1 件も実行していない。`results/` は空。凍結タグは打っていない。GPU 時間 0**

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-019-validity-decisions.md` §0(F90 〜 F99)と **§9**(F90 の両義が書いてある末尾)
- `Documents/05_STATISTICS.md` **§6.3 手続き2**(`grep -n '手続き2' Documents/05_STATISTICS.md`)
- `Documents/04_EXPERIMENT_PLAN.md` の Go/No-Go 2 表 / `plans/PLAN-004-phase0-route.md` §2 順6
- `logs/OPEN-ITEMS.md`(索引 1-40 行)。**★F90 の行が正本**
- **全文 `cat` しない。`grep -n` で節を特定して `sed -n 'X,Yp'`**(`CLAUDE.md` §10)

## やってはいけないこと

- **`s2_seed` の数値を仮置きして検出力を計算しない。**実測は存在しない(`CLAUDE.md` §2)
- **F90 の結論を独断で確定しない。**材料を並べて人間に上げる(`CLAUDE.md` §8 / ADR-039)
- **ADR-066 を蒸し返さない。**決定 6 件は採択済み・反映済みである
- **モデル指定を変えない**(`(0 + task:coverage | seed)` は 78 パラメータで 10 シードでは不能)
- **`plans/PLAN-019-check1/` を `code/` へ移さない。**パイプライン外の一度きりの検査である
- **GPU を起動しない。凍結タグを打たない**(`θ` / N5 / ★F90 / GPU 構成が未決)

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8。正本は `logs/OPEN-ITEMS.md`)

- **`θ`**(順5 の唯一の残りブロッカー。**2026-09-07 以降どのセッションでも動いていない**)
- **順6 と本実験 40 run の GPU 構成**(全体のクリティカルパス)
- **★F90 の事実確認**(**このセッションの題材**)/ **N5**(解析門の閾値)/ **★C** / **★E** /
  **E-5 (b)** / **Δ 5 行** / **★2** / **★L(d)**(`arb` 10 シード)
- **PLAN-018 §4.3** の組み直し(とくに F87)
