# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その17)/ 直前セッションの役割: IMPLEMENTER (Opus)
直前セッションで終わったこと: **PLAN-017 §6 の実装(完了)** と
**研究全体の妥当性レビュー(人間の求めに応じて実施。`logs/REVIEW-2026-09-07-validity.md`)**。
`pytest code/tests -q` = **831 passed**(実行して確認した)。**GPU 時間 0。実験は 1 件も回していない。**

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。
**`STATE.md` は 398 KB を超えている。全文を読まない。**見出しを grep して該当箇所だけ開くこと。

---

## ★このセッションでやること

**`logs/REVIEW-2026-09-07-validity.md` を人間と読み、★G 〜 ★L の採否を決める。**
**エージェントは決めない**(`CLAUDE.md` §8)。**材料は既に並んでいる。**

**★G 〜 ★J と ★L の順序変更は追加 GPU が 0 である。★K だけが重い。**

| 記号 | 決めること | 重さ | 追加 GPU |
|---|---|---|---|
| **★G** | **主軸に seed のランダム傾きを置くか**(S5 を主軸へ広げる)。**主要検定が反保守的になりうる** | **最重要** | 0 |
| **★H** | リンク関数を変えた感度解析を事前登録するか(天井効果) | 頑健性 | 0 |
| **★I** | **適格性フィルタでセルが落ちたときの df を事前に固定する。事前登録の穴** | 穴 | 0 |
| **★J** | 再正規化 DV の感度解析を足すか / Go/No-Go #1・#5 の閾値を締め直すか | 人工物 | 0 |
| **★K** | **被演算子対をタスク型間で共有する設計に変えるか。窓はいましか無い** | **重い** | プール再生成 |
| **★L** | 順7 を門の外に出すか + `arb` を 10 シードにするか(★B / ★F を 1 つの決定に) | `H0` の識別 | +5 run |

**★レビューの §7 に限界を書いた。**とくに **★G と ★K はどちらも必要シード数を増やす向きに働く**ので、
**順6 の実測(`s2_seed` / `s2_item` / `s2_tmpl`)が出るまで最終判断を保留するのが筋である。**
**その順6 が GPU 待ちであり、それが全体のクリティカルパスである。**

---

## 直前セッション(その17)で確定したこと(すべてファイルに書き込み済み)

### 1. PLAN-017 §6 の実装(ADR-062 の 7 決定をそのまま実装した)

- **`code/data_gen/pool.py` に `label_main_coverage` と `MAIN_COVERAGE_LEVELS`。**
  5 値(`id` / `interp` / `extrap_magnitude` / `extrap_pair` / `oob_algebraic`)を返し、**絞らない**
- **`code/analysis/frame.py`(新規)。**§4.1 の 6 手順。`--dry-run` は件数だけ。
  **主軸から落ちた行を理由別に run ごとに数える**
- **`task` は薄い dispatch、`template` は `category` の恒等写像、K は `matched_manifests` から辿って
  `pairs_hash` を照合、解析門は生の量のまま、`results/` へ CSV + sha256 + run_id + `pairs_hash`**
- **回帰テスト 20 件**(`code/tests/test_frame.py` 17 / `test_pool.py` 3)。**831 passed**

### 2. ★実装中に見つかった事実(F80 / F81。推測ではない)

- **★F80: `(-99, 1)` は `main_radius = 99` では `extrap` ではなく `oob_algebraic` である。**
  `|-99|` は 99 を超えない。**PLAN-017 F67 が引いた `test_pool.py` の例は `main_radius = 5` である。**
  **どちらにせよ `extrap_magnitude` ではないので §8 の最重要の回帰テストの結論は変わらない。**
  **回帰テストに両方の半径を固定した**
- **★F81: 5 値の `extrap_pair` は PLAN-002 §4.6 の定義と厳密には一致しない。**
  §4.6 は **`extrap_pair := extrap かつ ans_in`** だが、ADR-062 決定1 の 5 値では
  **`extrap` のうち `extrap_magnitude` でないもの全部**が入る(**`(300, 50)` が該当**)。
  **→ 副次 C5 を組むときは `answer_range == ans_in` と交差させること。**
  **主軸には影響しない。名前をどうするかは人間の決定である**(下の「人間待ち」)

### 3. 研究全体の妥当性レビュー(★G 〜 ★L)

**正本は `logs/REVIEW-2026-09-07-validity.md`。**このファイルには要約しか書かない。

- **★G** 主軸のランダム構造が §3.1 の宣言(反復単位 = FT run)を守っていない。
  **`(1 | seed)` の切片だけ。**交互作用が run 間で変動すれば **SE 過小・反保守的**。
  **repo は同じ論法を 順1b で既に受け入れている**(S5)。**主軸には上がっていない**
- **★H** 「勾配が平行」は**ロジット尺度の性質**である。**Go/No-Go #4 が基準セルを天井へ押し上げる。**
  §2 の弱点欄にこの項目が無い(順1b とベイズ事前分布の欄にはある)
- **★I** **`df = 6` は 4 × 3 の完全格子を前提とする。**適格性フィルタでセルが落ちたときの df が
  事前登録に無い。**ADR-047 決定2(T1b で df 6 → 4)という良い前例がある**
- **★J** **DV の測定モデルがタスク型間で違う。**T1b / T3 は強制選択で `parse_fail` = `other_error` = 0、
  T1 / T2 は正の質量を持つ。**その非対称は「出力型 × 既知性」= P2 プロファイルと同型の人工物である。**
  歯止め(#1 < 0.02 / #5 < 0.10)はあるが、**その大きさが検出下限 0.353 ロジットと同じ桁である**
- **★K** **主軸は全面的に項目間計画である。**`fill_cells` はセル間で組を再利用しない。
  **§3.2 の `(1 | item)` の説明「被演算子対のランダム切片」は実装と食い違う。**
  **バイアスではなく検出力の問題。**共有すれば `id` 要求(520 組)も下がる
- **★L** **★B と ★F は同じ一点を二方向から弱めている。**`H0` を切り分ける段は 順7 であり、
  **`H1` が正しいほど(= 主要検定が非有意なほど)順7 が確証的でなくなる。**
  その 順7 の `arb` は 5 シードである

---

## 触ってよいファイル / 読むべき範囲

- **`logs/REVIEW-2026-09-07-validity.md`**(**全文読んでよい。280 行程度**)
- `Documents/05_STATISTICS.md` **§2 / §3.1 / §3.2**。**全文を読まない**(749 行)
- `plans/PLAN-003-redesign.md` **§6.3**(適格性フィルタ)
- **`STATE.md` は 398 KB。全文を読まない。**見出しを grep する
- **`logs/DECISIONS.md` も全文を読まない**(3,800 行超)

---

## やってはいけないこと

- **★★G 〜 ★L をエージェントが決める**(`CLAUDE.md` §8。**案を出すのは可、決定は人間**)
- **★E-1 〜 E-7 を上げ直す**(ADR-062 で全件決着)/ **D-1 〜 D-6**(ADR-058 〜 061)/
  **D-A 〜 D-D**(ADR-057)/ **★1 の S1 / S3 / S4**(ADR-055)
- **`Documents/05_STATISTICS.md` を人間の決定なしに書き換える**(事前登録の本体)
- **`code/eval/run.py` を触る**(E-5 案 (b)。**人間の判断待ち**)
- **`code/analysis/primary.py` を書く**(**★G の決着前に書くと書き直しになる**)
- **解析門の閾値を決める**(N5)/ **`θ`** / **`M*`** / **事前登録の凍結タグを打つ**
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**

### ★この環境で実際に踏んだ地雷(その13 〜 その17 で確認済み)

- **★長い散文は Write ツールでスクラッチに `.py` を書き、`python file.py` で本文に差し込む。**
  **その17 もこの方式で通した。**`cat > file <<'EOF'` は常に落ちる。
  **ヒアドキュメントに長い日本語本文を入れると bash が引用符の対応を見失う**
- **★`.md` を書き戻すときは `newline="\n"` を明示する。**書いたらバイト単位で CR 数を数える
- **★`Edit` / `Write` ツールは `.py` に CRLF を書くことがある。**
  **`git add` の警告(`CRLF will be replaced by LF`)が出たらバイト単位で正規化する。**
  **その17 で `pool.py` と `test_pool.py` が実際にこれを踏んだ**
- **★入れ子太字 `****` は `git diff` の追加行だけに `\*{3,}` を掛けて数える。**
  **その17 は 3 か所で踏んだ。**`**A。** **B**` に直す
- **★`print()` に日本語や em dash を流さない**(この環境の Python の stdout は cp932)。
  **`git diff` を Python から読むときは `encoding='utf-8'` を明示し、出力はファイルに落とす**
- **★`ruff` / `black` がこのローカル環境に入っていない。**行長 100 は手で数えた。
  **CI かポッド側で掛け直すこと**
- **`logs/CHANGELOG.md` は古い順である**(新しいものが末尾)
- **`pytest code/tests -q` は 24 〜 59 秒**(831 tests)。`timeout` を伸ばす

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

### ★このセッションが開いた項目(7 件)

- **★G / ★H / ★I / ★J / ★K / ★L**(上の表。**正本は `logs/REVIEW-2026-09-07-validity.md`**)
- **★F81**: 5 値の `extrap_pair` の定義と名前。**副次 C5 を `answer_range` との交差で組むことを
  PLAN / ADR に明記するか、水準の名前を変えるか。主軸には影響しない**

### 以前から残っている項目

- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 の唯一の残りブロッカー。**
  **表を見てから決めると事後選択になる。2026-09-07 のどのセッションでも動いていない**
- **★E-5 の案 (b)**(K の出どころを評価 run の `metrics.json` に焼き込む)。
  **`code/eval/run.py` の変更。本実験の run はまだ 1 本も無いので、いまが最も安い。判断に期限がある**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)。
  **★順6 の実測が全体のクリティカルパスである** —— Δ 5 行も ★G / ★K の最終判断もここに乗る
- **解析側の凍結手段**(ADR-058 決定2)。`renv.lock` を repo に置くか
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9)/ **N1 / N2 / N3〜N6 / #7** /
  **解析門の閾値**(N5)/ **`Documents/05_STATISTICS.md` §5 の Δ 5 行**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A = ADR-056、★D = ADR-058 〜 062 で閉じた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | ゲートキーピングが主問いの判別子を門の後ろに置いている | **未決。★L と 1 つの決定にする** |
| **★C** | 解析門が DiD のベースライン腕そのものに掛かっている(従属変数での選択) | **未決。N5 が決め時** |
| **★D** | 当てはめエンジンが無い | **→ 閉じた**(ADR-058 〜 062 + その17 の実装) |
| **★E** | **F15 はバグである**(`torch.manual_seed` が 0 件。**その17 で再実測した**)。
  **同じ `--seed 3` を 2 度回してもアダプタ初期値が違う** | **未決** |
| **★F** | シード配分が主問いの判別子に薄い(`arb` は 5 シード) | **未決。★L と 1 つの決定にする** |
| **★G 〜 ★L** | **その17 のレビュー。**上の表を参照 | **未決** |

### 運用

- **★`STATE.md` が 398 KB / 3,800 行を超えた。**`CLAUDE.md` §10 が全文読解を禁じており、
  **セッション開始時に `cat STATE.md` を実行できない。**引き継ぎ装置としての機能が落ちている。
  **分割(現行状態 / 決定の履歴)かアーカイブが要る**(レビュー §5)
- **★ローカル環境**: **R 4.6.1 と `lme4 2.0.6` が入っている**。**`rstanarm` / `brms` は入れていない**。
  `statsmodels` は外したままである。**`infra/requirements.lock` は無変更である**
