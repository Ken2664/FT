# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-09(その28)/ 直前セッションの役割: PLANNER / CRITIC (Opus)
直前セッションが終了した理由: **やることが片付いた**(★F104 の決定材料が完成した)

---

あなたは PLANNER / CRITIC です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## 直前セッション(その28)で終わったこと

**★F104 の決定材料を作った。決定は 0 件である**(`CLAUDE.md` §8 / ADR-039 決定3)。
**★測ってみたら問いの形が変わり、決定が 4 行に割れた。**

| 記号 | 何が分かったか |
|---|---|
| **★F115** | **順6 の形では `s2_item` の同定が効かない。**1 項目 1 観測の Bernoulli では profile 対数尤度が**完全に平ら**(差 0 / 最大 `2.8e-14`)。温度 0 の反復 3 回は**一致する限り情報を持たない**(内点の MLE が無い)。**★「移し替えてよいか」以前の問題であり、★F90 と同じ形の穴である** |
| **★F117** | **`task:coverage` の交互作用へ直接入るのは `s2_item` だけである。**`s2_tmpl` も対照の `sigma`(seed)も この水路では厳密に 0。**`s2_item` はシード数では縮まず、`n_item`(`M*` 待ち)でしか縮まない。****★2 つを 1 つの決定にする理由が無い** |
| **★F116 / ★F118 / ★F119** | 順6 の `s2_tmpl` は T2 の 5 水準のみ(当てはめは 10 水準)/ **`s2_tmpl` に 0 に近い値を置くと縮退カスケードの第1段が常時発火する**(★未測定)/ 順6 のテンプレート間分散 1 個は **ADR-069 決定2 が既に Δ の床へ割り当てている** |

- **決定材料の正本は `plans/PLAN-019-validity-decisions.md` §10.13。**
  **人間向けの 1 ページ要約は §10.13.0、記入欄は §10.13.5(★F104-0 / -a / -b / -c の 4 行)**
- **検査3**(`plans/PLAN-019-check3/`)を足した。**★合成データと算術であり実験結果ではない**
- **回帰テスト 2 件**で ★F117 が乗る `draw_frame` の構造を固定した(**★変異させて落ちることも確認**)。
  **`pytest code/tests -q` = 898 → 900 passed**
- **★事前登録の本体と入力は触っていない**(`Documents/05_STATISTICS.md` / `configs/power_sim.yaml`)。
  **ADR-067 / ADR-068 / ADR-069 は 1 文字も触っていない**

## このセッションでやること

**★最優先: 人間の机の上にあるものを片付ける。エージェントが作れる材料はもう無い。**

**★F104 は決定材料が揃った。**次に必要なのは**人間の決定**であって、追加の調査ではない
(§10.13.5 の 4 行)。**蒸し返して案を作り直さないこと。**

| 記号 | 決めてもらうこと | 備考 |
|---|---|---|
| **★F104**(4 行) | **`s2_item` / `s2_tmpl` の取得元と値** | **§10.13.5。★案 A には「推奨しない」を付けた。****B(格子)と C(1 点)の差はコストである** —— **B は本実行が 62 → 186〜372 時間** |
| **`θ`** | 外挿域の閾値。**順5 の唯一の残りブロッカー。2026-09-07 以降どのセッションでも動いていない** | **★`M*` は順5 の出力なので ★F104 の賭け金の大きさにも効く** |
| **順6 の GPU** | **段階 D(凍結)の前に要る。**Go/No-Go #0〜#3 は順6 でしか出ない | ★F113 が決着した今も変わらない |
| **★F114 の実行先** | この機械(16 コア)か CPU の多い RunPod ポッドか | **★F104-a の答えで所要時間が 62 → 372 時間まで動く。★F104 の後が自然** |
| **N5** / **★L(d)** / **★C** / **★E** / **E-5 (b)**(期限あり)/ **★2** / **PLAN-018 §4.3** | 従来どおり | `logs/OPEN-ITEMS.md` が正本 |

**★人間の決定が下りない場合に GPU 抜きで進められるもの**(優先度順):

1. **`code/analysis/primary.py`**(PLAN-016 §7-5)。**ただし N5(解析門の閾値)が未決で、
   決まる前に書くと書き直しになる。****★ADR-069 決定4 (2)(当て直したほうを LRT に入れる)は
   primary.py にも同じ形で効く**(実装時に適用する)
2. **`Documents/09_PAPER_PLAN.md` / `00_OVERVIEW.md:7` の追随**(どちらも再設計前のまま。**人間待ち**)
3. **PLAN-018 §4.3**(「現在のブロッカー」の組み直しと Phase 0 の要約。とくに F87)

## 読むべき範囲(**全文 `cat` しない**。`CLAUDE.md` §10)

- **`plans/PLAN-019-validity-decisions.md` §10.13.0**(1 ページ要約)と **§10.13.5**(記入欄)
- **`plans/PLAN-019-check3/README.md`**(検査3 が言えること・言えないこと)
- **`logs/OPEN-ITEMS.md` の索引**(人間待ちの正本)
- **`logs/DECISIONS.md` の ADR-069**(その27 の 4 決定。**蒸し返さない**)

## やってはいけないこと

- **★F104 の案を作り直さない。**材料は揃っている。**足りないのは人間の決定である**
- **ADR-067 / ADR-068 / ADR-069 を蒸し返さない**(採択済みで実装も終わっている)
- **★F112 を開き直さない。**閉じている。`effect.eta` が DGP の真値である
- **★`sigma` / `rho` / `s2_item` / `s2_tmpl` の値を「実測」として書かない。**すべて仮定値である
  (`results/` は空。**`s2_seed` の実測はこの世に存在しない**)
- **★検査1 / 検査2 / 検査3 の数値を `results/` や論文に書かない。**すべて合成データと算術である
- **★検査3 の「P1 の 39%」を「検出力が 39% 落ちる」と読み替えない。**
  **`glmer` は 1 本も当てておらず、検出力そのものは 1 つも出していない**(§10.13.6)
- **★検査3 の対照行(seed 項が 0)を「seed の分散は検出力に効かない」と読まない。**
  **`sigma` は二項抽出の水路で効き、それは検査2 が測った(F108)**
- **★`runs/20260909_bench_powersim/` の `power` の値を読まない**(読んでよいのは秒 / ペアと発火率だけ)
- **★smoke(`configs/power_sim_smoke.yaml`)から出た数値を `results/` や文書に書かない**
- **`Documents/05_STATISTICS.md` と `configs/power_sim.yaml` を人間の決定なしに触らない**
  (どちらも事前登録の本体・入力である)
- **`dgp.n_item` / `dgp.s2_item` / `dgp.s2_tmpl` に既定値を入れない**(skill `code-style` §5)
- **GPU を起動しない。凍結タグを打たない**(`θ` / N5 / GPU 構成が未決)
- **本実行を人間の承認なく始めない**(★実行先が未決。★F104-a 次第で 62 〜 372 時間)
- **先行研究の値を確認せずに引用しない**(`CLAUDE.md` §3)

## ★エージェントが自分で気づいた、まだ手を付けていないこと

- **★`s2_tmpl` と縮退発火率の関係を測っていない**(★F118 は読解である)。
  **`glmer` を当てれば測れるが、`n_item` が `null` なので本実行の形では回せない。**
  **検査3 の枠組み(合成データ)なら測れるが、R が要る**
- **★検査3 の `s2_tmpl` の打ち消しは `draw_frame` の巡回割当の性質である**(ADR-069 決定4 (3))。
  **本番の項目集合でテンプレートが被覆水準間で不均衡なら成り立たない。**
  **本番の評価テンプレート集合(`data.eval_template_set`)は未確定である**
- **`n_item = 96` のベンチは完走していない**(2 時間超で打ち切った)。**§10.12 は線形の外挿である**
- **★並列実行を本番規模で試していない。**確かめたのは smoke だけである。
  **「16 並列にできる」はメモリ帯域を測っていない仮定のままである**
- **`Documents/09_PAPER_PLAN.md` / `00_OVERVIEW.md:7` は依然として再設計前のままである**
- **`code/analysis/primary.py` は未実装。**N5 が残っている
