# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-09(その27)/ 直前セッションの役割: PLANNER / CRITIC (Opus)
直前セッションが終了した理由: **やることが片付いた**(4 束の決定 + 実装が完了)

---

あなたは PLANNER / CRITIC です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## 直前セッション(その27)で終わったこと

**★★人間が 4 束すべてを決め(ADR-069)、エージェントが 4 件とも実装まで終えた。**
**蒸し返さないこと。正本は `logs/DECISIONS.md` の ADR-069。**

| 束 | 決定 | 状態 |
|---|---|---|
| **★F112** | **案 (a)** —— `configs/power_sim*.yaml` に **`effect.eta`(丸める前の 12 セル)**を新設し DGP の真値にした。`rule_rate` は「`eta` を丸めたものと一致するか」の**門**として残る | **実装済。★F112 は閉じた** |
| **★F113** | **案 (a)** —— `Documents/05_STATISTICS.md` §5 に**「★順6 からの移し替え」を新設**し、食い違い 3 つと**移し替えの 3 仮定**を明示した | **反映済** |
| **★F114** | **並列化だけ先に実装。**`shard_jobs` / `run_fits` / `fit.n_workers` / `--workers` | **★実行先は未決(下記)** |
| **実装判断 4 件** | **全件承認** | **済** |

- **非加法性 RMS が 0.3605 → 0.352767**(§6.5 が載せる 0.353 と 3 桁で一致)。**★P0 は厳密に 0**
- **★実測で確かめた**: **smoke を `--workers 1` と `--workers 4` で回し、summary が
  `n_workers` 以外 完全に一致した**(結果は並列数に依らない)
- **コストは実測ベース**: **992 コア時 / 16 並列で 62 時間**(`SECONDS_PER_PAIR_N_ITEM_48 = 297.6`)
- **`pytest code/tests -q` = 884 → 898 passed**

## このセッションでやること

**★最優先: ★F104 の決定材料を作る**(**決定は人間**。`CLAUDE.md` §8 / ADR-039 決定3)。

**★F104 には「事実」はあるが「案」が無い。**`plans/PLAN-019` §10.4 が食い違いを記録し、
§10.8 の決定欄が ⬜ のまま空いているだけである。**F90-1 は §10.6 に 4 案、F103 は §10.7.4 /
§10.7.5 に案があった。F104 にはそれが無い。だから その27 は人間に諮らなかった。**

- **問い**: **`dgp.s2_item` / `dgp.s2_tmpl` をどこから取るか。**
  順6 の DV は `correct_rate`(素のモデル)、シミュレータの DV は `is_rule`。
  **順6 で `p2` 参照規則の `rule_rate` は構成上 床に近い**(上限でも 0.30)。
  **床に張り付いた二値のロジット尺度の分散成分を、天井付近を想定した §6.5 の P0〜P3 へ
  持ち込めるかは自明でない**(★これはエージェントの読解であって実測ではない)
- **★F90-1 が ADR-067 で採った形(案 B = 仮定値を置いて逆問題として事前登録する)が
  そのまま使えるかを検討すること。**同じ形なら `sigma` と同様に §6.7 の表へ足せばよい
- **★F113 の決着(ADR-069 決定2)は F104 の先例になる。**
  **(i) の「DV の移し替え」は §5 と §6.4 で共通の問題である**(`plans/PLAN-019` §10.11.5 の表)
- **★`M*` と合わせて、`power_sim.py` の本実行を止めている 2 本である**

**そのあと(人間の机の上にあるもの。`logs/OPEN-ITEMS.md` の索引が正本):**

| 記号 | 決めてもらうこと |
|---|---|
| **`θ`** | 外挿域の閾値。**順5 の唯一の残りブロッカー。2026-09-07 以降どのセッションでも動いていない** |
| **順6 の GPU** | **段階 D(凍結)の前に要る。**Go/No-Go #0〜#3 は順6 でしか出ない |
| **★F114 の実行先** | この機械(16 コア / 62 時間)か CPU の多い RunPod ポッドか。**★`M*` と ★F104 の後で自然** |
| **N5** / **★L(d)** / **★C** / **★E** / **E-5 (b)**(期限あり)/ **★2** / **PLAN-018 §4.3** | 従来どおり |

## 読むべき範囲(**全文 `cat` しない**。`CLAUDE.md` §10)

- **`plans/PLAN-019-validity-decisions.md` §10.4 / §10.6 / §10.8**(★F104 の事実と、F90-1 の案の書き方)
- **`Documents/05_STATISTICS.md` §6.4**(`s2_item` / `s2_tmpl` が使われている場所)と
  **§6.7**(ADR-067 / ADR-068 が置いた「掃く仮定値」の表。**F104 も同じ形にできるか**)
- **`logs/DECISIONS.md` の ADR-069**(その27 の 4 決定。**蒸し返さない**)
- **`Documents/05_STATISTICS.md` §5 の「★順6 からの移し替え」**(その27 が新設。F104 の先例)

## やってはいけないこと

- **ADR-069 の 4 決定を蒸し返さない**(人間が採択済で、実装も終わっている)
- **★F112 を開き直さない。**閉じている。`effect.eta` が DGP の真値である
- **★`sigma` / `rho` / `s2_item` / `s2_tmpl` の値を「実測」として書かない。**すべて仮定値である
  (`results/` は空。**`s2_seed` の実測はこの世に存在しない**)
- **★`runs/20260909_bench_powersim/` の `power` の値を読まない。**
  **読んでよいのは「秒 / ペア」と「縮退の発火率」だけである**
- **★smoke(`configs/power_sim_smoke.yaml`)から出た数値を `results/` や文書に書かない**
- **★F113 を「順6 の GPU は要らない」と読み替えない。**Go/No-Go #0〜#3 は順6 でしか出ない
- **`Documents/05_STATISTICS.md` と `configs/power_sim.yaml` を人間の決定なしに触らない**
  (どちらも事前登録の本体・入力である。**その27 は ADR-069 の決定に基づいてのみ触った**)
- **`dgp.n_item` / `dgp.s2_item` / `dgp.s2_tmpl` に既定値を入れない**(skill `code-style` §5)
- **GPU を起動しない。凍結タグを打たない**(`θ` / N5 / GPU 構成が未決)
- **本実行(24,000 本 = 62 時間)を人間の承認なく始めない**(★実行先が未決である)
- **先行研究の値を確認せずに引用しない**(`CLAUDE.md` §3)

## ★エージェントが自分で気づいた、まだ手を付けていないこと

- ~~**`STATE.md` の上限が文書と回帰テストで食い違っている**~~ → **★その27 で解消。食い違いではない。**
  **ADR-063 決定2 が同じ文で「目標 400 行 / 60 KB」と「テストが落ちるのは 700 行 / 90 KB」を
  宣言している。**`STATE.md` 運用規約5 の文言をそれに合わせた
- **`n_item = 96` のベンチは完走していない**(2 時間超で打ち切った)。**§10.12 は線形の外挿である**
- **★並列実行を本番規模で試していない。**確かめたのは smoke(2 反復 / 2 シャード)だけである。
  **「16 並列にできる」はメモリ帯域を測っていない仮定のままである**
- **`Documents/09_PAPER_PLAN.md` / `00_OVERVIEW.md:7` は依然として再設計前のままである**
- **`code/analysis/primary.py` は未実装。**N5 が残っており、
  **★ADR-069 決定4 (2)(当て直したほうを LRT に入れる)は primary.py にも同じ形で効く**
