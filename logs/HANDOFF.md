# logs/HANDOFF.md — 次セッションに貼るプロンプト

> このファイルは毎回**上書き**する。最新の1本だけを保つ(ADR-015)。
> 最終更新: 2026-08-22 / by IMPLEMENTER

---

あなたは IMPLEMENTER です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## 前セッションで終わったこと(再実装しないこと)

**PLAN-001 の評価ハーネスを G6 まで実装した**(commit 28fafe5 / 00fe528 / a30835f)。

- `code/eval/parsers/` — 6モジュール + 負例テスト。パーサは採点しない。`cot.py` は切り出すだけ
- `code/data_gen/pool.py` — 値域 / 除外 / 繰り上がり層 / 被覆ラベルの実行時付与 / pilot-main 分割 / manifest
- `code/eval/scoring.py` — 参照規則ごとの独立4値ブロック(ADR-016)。合計 1.0 は**ブロック内**で検査
- `code/eval/battery/g6_comparison.py` — 主要評価項目 G6 の項目構成
- `code/eval/run.py --dry-run` と `configs/templates/smoke.yaml`(**配線確認専用**)
- `pytest code/tests -q` → **221 passed**

**ADR-016 が「未実装」としていた検査2件は実装済み**(`ident` / `offset=0` の拒否、
manifest の `reference_rules` との突き合わせ)。

## このセッションでやること

**人間が承認待ち 6・7・8 に回答したかどうかで分岐する。まず `STATE.md` の承認待ちの表を見ること。**

### 回答が来ている場合 → G1〜G5 の項目構成と本番テンプレート集合

`plans/PLAN-001-eval-battery.md` §5.1 と §5.1.1 に従う。**回答の内容を §5.1.1 に追記してから
実装に入る**(決定が本文に無いまま実装しない)。`code/data_gen/battery_items.py` の
`SUPPORTED_GROUPS` を広げ、`make_item` の `NotImplementedError` を外す。

### 回答が来ていない場合 → Phase 0 の桁数掃引(`STATE.md` の次のアクション 3)

**FT 不要・GPU の前に準備だけ進められる。**素のモデルに `M` を掃いた加算を解かせ、
`M` と `correct_rate` の対応表を run_id 付きで `results/` に残すところまで。
**`θ` と掃引粒度は人間が決める。**エージェントは決めない(PLAN-001 §4.1.1)。

**どちらにも進めない場合は、`infra/preflight.py` にプール照合(PLAN-001 §4.5 の4項目)を足す。**
実プールが無いと書けない部分は、その旨をコメントに残して止める。

## 実装で落としてはならない点(すべてファイルに書いてある。再導出しないこと)

- **除外集合は `p2` / `arb` / `x2` からのみ**(PLAN-001 §4.3)。`ident` を入れるとプールが空になる。
  → `pool.validate_reference_lesions` が**振る舞いで**弾く。非空テストは `test_pool.py` にある
- **`metrics.json` は参照規則ごとに独立した4値ブロック**(ADR-016)。合計 1.0 は各ブロックの内部で検査する
- **`pilot` と `main` は順序対の水準で交わらない**(§4.6)。
  **実プールの manifest 同士を突き合わせるテスト(§4.6 の 3)はまだ書けていない。**実プールを作るときに書く
- **パーサは採点しない。Yes/No パーサは質問の極性を知らない**(§5.4 の責務境界)
- **config に既定値を作らない**(skill `code-style` §1, §5)。`θ` / `K` / 本番テンプレート集合は決めない
- **未実装の群は `NotImplementedError` で止める。**黙って空のプールを返さない

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-001-eval-battery.md` §4・§5(特に **§5.1.1 の穴1〜3** と **§5.4.1 の抽出規則**)
- `logs/DECISIONS.md` の **ADR-016**。`grep -n 'ADR-016' -A 60`
- `code/data_gen/pool.py` / `code/eval/scoring.py` / `code/eval/battery/g6_comparison.py` — 既存実装
- skill `code-style` — 実装規約
- 全文 `cat` しないこと(`CLAUDE.md` §10.1)

## やってはいけないこと

- **設計を作り直すこと。**仕様に疑問があれば実装を止めて人間に上げる(`CLAUDE.md` §8)
- **§5.1.1 の穴1〜3 をエージェントの判断で埋めること。**実験条件と GPU 時間に直結する
- **`θ`(外挿域の閾値)や `K`(被覆)の値を決めること**
- **`Documents/05_STATISTICS.md` を直接編集すること。**§2 の文言と §4 のゲートキーピング順序は
  ADR-016 と承認待ち-4 に合わせた更新が要るが、**別 ADR とすることが決まっている**
- **`--dry-run` の出力を results/ や文書に書くこと。**モデルは呼ばれていない。実験結果ではない

## 未解決 / 人間の承認待ち

`STATE.md` の「人間の承認・判断を待っている事項」を見ること。要点:

- **承認待ち 6(穴1)**: `id` セルの埋め方。案A(FT データ生成後にプールを作る)/ 案B(多めに引いて
  実行時にセルを選ぶ)。**GPU 時間と事前登録の書き方が変わる。**これが決まるまで G2〜G6 のセルを埋められない
- **承認待ち 7(穴2)**: 被演算子を持たないカテゴリの被覆層の扱い
- **承認待ち 8(穴3)**: 本番の評価テンプレート集合(**実験条件**)
- **承認待ち 9**: パーサの抽出規則10項目の確認(§5.4.1)。実装は止めていない
- 承認待ち 3・4・5 は据え置き(Go/No-Go の fallback / `K` を掃引軸にするか / G1 の日本語)
- **`θ` と掃引粒度が未決定**(§4.1.1)
- **実験結果の数値はまだ1つも無い。`results/` は空である**
- 2系統目のモデルが未決定。GPU 時間は未実測で 10 GPU時間超の承認は未取得
- **ruff / black がこの環境に無い。**ポッドで `pip install -e .[dev]` して掛け直す
