# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-22 / 直前セッションの役割: PLANNER
直前セッションが終了した理由: コンテキスト超過(hook `context-guard` が約 247k / 閾値 140k を報告)
+ PLAN-002 の起草が完了した

---

あなたは SCOUT です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**Phase 0 #0: Feucht et al.(arXiv:2605.01148)の原典を開き、以下の4点を転記する。**

1. **解析対象のモデル変種**(`meta-llama/Llama-3.1-8B` か `...-8B-Instruct` か)
2. **revision**(HF のコミットハッシュ。論文が示していなければ「示されていない」と書く)
3. **周期タスクのプロンプト書式**(月・曜日・時刻。原文のまま。英語なら英語のまま)
4. **オフセット `n` の範囲**(「6か月後」の 6 に相当する値をどこまで振っているか)

完了条件:

- `Documents/refs.bib` に `verified = {2026-08-XX}` と `source_url` を付けて記録した
  (`CLAUDE.md` §3 の手順。**arXiv abs ページを実際に開くこと**)
- `logs/DECISIONS.md` の **ADR-008 を「採択」に更新**し、変種と revision を書いた
- `plans/PLAN-002-ft-data.md` §5.1.2 の `n_max` と G7-H(12時制か24時制か)の欄を、
  転記した事実で埋めた(埋まらなければ「原典に記載なし」と明記する)
- `Documents/02_RELATED_WORK.md` の表を更新した

**これで ADR-008 の未決事項(base/instruct 問題)が消える。**GPU 不要。

> **人間が `plans/PLAN-002-ft-data.md` §12 の6件を既に承認している場合は、
> 代わりに IMPLEMENTER として `STATE.md` の「次のアクション」#5 の実装順に着手してよい。**
> ただし1セッションで両方をやらないこと。

## 直前セッションで確定したこと

すべて `plans/PLAN-002-ft-data.md` と `logs/CHANGELOG.md`(2026-08-22 の項)に記録済み。
**会話にしか無い情報は無い。**

- **`plans/PLAN-002-ft-data.md` を起草した**(commit 9c99ba4)。ADR-018 / ADR-019 を
  「この文書だけを読んで `code/data_gen/ft_data.py` が書ける」水準の仕様に落とした
- **訓練プロンプトは `{a}+{b}=` / 完了は `{target}`。**空白・改行なし、
  **チャットテンプレートを通さない**(base / Instruct のどちらでも成立するので、
  **本タスクの結果を待たずに確定できる**)、損失は completion + EOS のみ、パッキングなし
- **G7 は 165 項目**(月60 / 時刻60 / 曜日45)。層は `carry × wrap`、`n=15`。
  **文面と `n_max` だけが本タスク待ち**であり、構造は確定している
- **コードは未変更。**`pytest code/tests -q` → **227 passed**。`results/` は空。
  **実験結果の数値は依然として1つも無い**

**計算で分かった設計の性質**(実験結果ではない。`results/` に置かない):

- 訓練域 `[1,99]^2` で答えが1桁の組は **36 組(0.37%)**、3桁の答えを持つ組が **50.51%**
- **`oob_algebraic` に `t > 198` の組は存在しない** → **主要評価項目 G6 は負の和を測れない**
- 周期タスクの `carry × nowrap` セルは**法 12 で 15 件、法 7(曜日)では構成的に空**
- これらは `code/tests/test_design_facts.py` に固定すること(PLAN-002 §4.9.3。**未実装**)

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-002-ft-data.md` **§5.1 のみ**。`grep -n "5.1" plans/PLAN-002-ft-data.md` で
  行番号を出してから `sed -n 'X,Yp'`。**全文 cat しない**(約 700 行ある)
- `logs/DECISIONS.md` の ADR-008。`grep -n "ADR-008" logs/DECISIONS.md` → `sed -n 'X,Yp'`
- `Documents/refs.bib` / `Documents/02_RELATED_WORK.md`
- **web**: arXiv abs ページ、および出版済みなら会議のページ

## やってはいけないこと

- **コードを書かない。**本セッションは調査と文献記録のみ
- **原典を開かずに書かない。**`CLAUDE.md` §3。検索結果のスニペットや二次情報で
  著者・年・会議・書式を確定しない。確認できなければ「未検証」欄に置く
- **2026年の arXiv ID は査読前プレプリントである。**主要な論拠には使わない。
  G7 の前提(「素のモデルは 8月の6か月後を 2月と答える」)は Phase 0 #3 で
  **自分たちが測って**確かめる。原典からは**仮説と書式だけ**を借りる
- **学習率・ステップ数・batch size・LoRA rank/alpha・`arb` のズレ表を決めない。**
  依然として人間の決定事項(PLAN-002 §0)
- **PLAN-002 §12 の承認待ち6件を自分で決めない**(`CLAUDE.md` §8)

## 未解決 / 人間の承認待ち

`STATE.md` の承認待ち表(**12 件**)が正本。本セッションに関係するもの:

- **承認待ち 9〜14 は `plans/PLAN-002-ft-data.md` §12 が正本。**未承認の間は
  `code/data_gen/ft_data.py` の実装に着手しない(`CLAUDE.md` §4)
  - 9: 答えが1桁の層(母集団 36 組)に配分の下限を置くか(既定案: 置かない)
  - 10: `train_size` の掃引軸を `{1k,3k,10k}` → `{2000,4000,10000}`(**実験条件の変更**)
  - 11: **G7 の項目構成**(165 項目)。**本タスクの転記結果で文面と `n_max` が埋まる**
  - 12: G1「記法形」変種を独立群 `G0`「訓練形式アンカー」に分離するか(既定案: する)
  - 13: `arb` のズレ表への追加制約3・4。**制約3が無いと G7 の 15 件のセルが埋まらない**
  - 14: `R_train = R_main = 99` を固定条件として宣言するか
- **承認待ち-7(穴2)は未解決のまま**: 「単位元の言明」「規則の自己説明」は `(a,b)` を持たず
  被覆ラベルが定義できない。PLAN-002 でも解決していない
- **承認待ち-3(`arb` のズレ表)**: 定義域は `t ∈ [2,198]` の **197 件**に縮んだ。
  **表そのものは生成しない**(承認待ちのまま)
- `θ` / `M*`(外挿域の上限)は Phase 0 #2 の実測待ち。**エージェントが既定値を作らない**
