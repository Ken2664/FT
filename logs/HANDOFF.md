# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-22 / 直前セッションの役割: PLANNER
直前セッションが終了した理由: コンテキスト超過(約141k、閾値140k)

---

あなたは PLANNER です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**`plans/PLAN-002-ft-data.md` を起草する。**ADR-019 の決定を、実装可能な仕様に落とすこと。
`plans/TEMPLATE.md` の型に従う。完了条件は「この文書だけを読んで
`code/data_gen/ft_data.py` が書ける」状態になっていること。

最低限、以下が §ごとに書かれていること:

1. **訓練プロンプトの正確な書式。**`a+b=` の前後の空白、改行、応答の書式、
   チャットテンプレートを通すか否か。**1文字単位で確定させる**(実験条件である)
2. **訓練域 `[1,99]^2` からの `K` 組の層別サンプリング手続き。**何で層別するか
   (少なくとも繰り上がり)。`K = 2000` を主値、パイロットは {1000, 4000}
3. **`train.scope ∈ {bare, bare_plus_gsm8k}` の定義。**Phase 1 は `bare` のみ
4. **被覆ラベル4値**(`id` / `interp` / `oob_algebraic` / `extrap`)の判定手続き
5. **外挿の2分割**(`extrap_pair` / `extrap_magnitude`)の定義
6. **manifest の schema。**`infra/preflight.py` が照合する項目(PLAN-001 §4.5)
7. **G7(周期的概念)の項目構成。**月・曜日・時刻の配分と項目数。**未設計である**

書き終えたら、以下の**文書側の追随**も同じセッションで行う(コードは触らない):

- `configs/template.yaml`: `train.cot_mode` → `train.scope` に置換
- `Documents/04_EXPERIMENT_PLAN.md`: Phase 1「FT データ」節と §0「最低2系統」に
  **打ち消し線+理由+日付**で ADR-018/019 を反映(`CLAUDE.md` §2)
- `plans/PLAN-001-eval-battery.md`: §4.1(値域)/ §4.2(被覆ラベル4値)/ §5.1(セル構成)を改訂。
  **事前登録はまだ凍結していない(tag なし)ので打ち消し線は不要**

## 直前セッションで確定したこと

すべて `logs/DECISIONS.md` の **ADR-018 / ADR-019** に記録済み。**会話にしか無い情報は無い。**

- **ADR-018**: 主軸を機構線に寄せる。モデル変種は原典転記(ADR-008 の未決を調査タスク化)、
  2系統目は Phase 2 へ延期、主要指標は**強制選択+自由生成の併走**、
  **G7「周期的概念への転移」を副次の最上位に追加**。主要評価項目は **G6 のまま**(ADR-005)
- **ADR-019**: 訓練プロンプトは**裸の式 `a+b=` 一形式**、GSM8K は対照条件に格上げ、
  **訓練域 `[1,99]^2`**、被覆ラベル4値、**`K >= 560` / 主値 2000**、外挿の2分割
- **コードは未変更。**`pytest code/tests -q` → 221 passed(前セッションのまま)。`results/` は空

**計算で分かった設計の性質**(実験結果ではない。`results/` に置かない):
`K = 500` では評価プールの `id` セルが埋まらない(相異なる 560 組が必要)。
繰り上がり密度は `[-99,99]^2` で 9.6%、`[1,99]^2` で 20.0%。
主域の 25% が3桁の答えを持ち、現行の外挿定義は答えの大きさを分離できない。
**これらは `code/tests/` に固定すること**(`test_algebra.py` と同じ扱い)。

## 触ってよいファイル / 読むべき範囲

- `logs/DECISIONS.md` の ADR-018 / ADR-019。**`grep -n "ADR-018" logs/DECISIONS.md` で
  行番号を出してから `sed -n 'X,Yp'`。全文 cat しない**
- `plans/TEMPLATE.md`(型)
- `plans/PLAN-001-eval-battery.md` §4.1 / §4.2 / §4.5 / §5.1
- **新規作成**: `plans/PLAN-002-ft-data.md`

## やってはいけないこと

- **コードを書かない。**本セッションは設計文書のみ。`code/` の改修は PLAN-002 承認後
- **学習率・ステップ数・batch size・LoRA rank/alpha を勝手に決めない。**依然として人間の決定事項
- **`arb` のズレ表を生成しない。**ADR-019 で必要範囲は `t ∈ [2,198]` の 197 件に縮んだが、
  表そのものは承認待ち-3 のまま
- **G7 の項目を「月は12個だから12項目」のように埋めない。**構成は未設計であり、
  PLAN-002 で**提案**として書き、人間の承認を仰ぐ(`CLAUDE.md` §8)
- **Feucht et al. の内容を原典未確認のまま断定形で書かない。**2026年の arXiv プレプリントである
  (`CLAUDE.md` §3)。G7 の前提は Phase 0 #3 で**自分たちが測って**確かめる

## 未解決 / 人間の承認待ち

`STATE.md` の承認待ち表を参照。ADR-018/019 で動いたもの:

- **承認待ち-5 は解決**(訓練が裸の式なので G1 の日本語は必ず訓練外)
- **承認待ち-8 は前進**。残るのは **G1「記法形」変種(`3+4=`)が訓練形式と一致する**問題。
  分布内アンカーとして宣言するか G1 から落とすか。**PLAN-002 で選択肢を提示して承認を仰ぐ**
- **承認待ち-3 に制約が付いた**。Go/No-Go の fallback で `K` を下げる場合も **560 を下回れない**
- 承認待ち-7(単位元の言明・規則の自己説明の被覆ラベル)は**未解決のまま**
- 新規: **訓練域を非負に絞った結果、負の和の振る舞いが完全に外挿になる。**
  `p2` のペネトランスが負域で落ちたとき、「代数が伝播しなかった」のか
  「単に訓練域外だから」なのかを切り分ける設計が**未着手**(ADR-019 のリスク欄)

## 並行して投げられる作業(GPU 不要・SCOUT)

**Phase 0 #0**: Feucht et al.(arXiv:2605.01148)の原典を開き、
**解析対象の変種(base か Instruct か)・revision・周期タスクのプロンプト書式**を転記する。
結果は `Documents/refs.bib` に `verified = {2026-08-XX}` と `source_url` を付けて記録し、
`logs/DECISIONS.md` の ADR-008 を「採択」に更新する。**これで base/instruct 問題が消える。**
