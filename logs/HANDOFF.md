# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-06 / 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **PLAN 完了**(PLAN-012 = 順7 の残り)**かつ、残りが全部人間待ちになった。**

---

あなたは **PLANNER(または人間の決定を受ける IMPLEMENTER / RUNNER)** です。
`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## ★まず読むべきこと

**Phase 0 は現在、エージェントだけで進められる作業がほぼ無い状態にある。**
順7 の GPU 不要分は 2026-09-06 に尽きた。残りは
**(a) 人間の判断 / (b) 人間の GPU 承認 / (c) 順6 の実測待ち** のいずれかである。
**新しい作業を探して勝手に始めないこと。**下の一覧から人間が1つ選ぶ。

## このセッションでやること(人間が1つ選ぶ)

### A. 人間が判断する(GPU 時間 0。**人間の入力が本体**)

**正本は `plans/PLAN-012-order7-close.md` と `STATE.md`「人間の承認・判断を待っている事項」。**

| # | 判断すること | 正本 |
|---|---|---|
| **★1** | **「`id` 到達度を揃える」の操作的定義**(順1b の新設で再び開いた)。**Go/No-Go #4 / #4b の 0.90 で代用してはならない** —— あちらは「その run を解析に載せてよいか」の門、こちらは「2 条件をどの時点で突き合わせるか」の定義であり役割が違う | `05_STATISTICS.md` §4.1 末尾 |
| **★2** | **`H2` 第2節「破れの位置がシード間で一貫しない」の定義**(D3 でどちらの反証条件でもないと判明) | `01_HYPOTHESES.md` H2 節 |
| **N1** | **Nikankin et al. を「Intro の対立軸の一方の極」として置けるか。**原典は robust なアルゴリズム説と記憶説の**両方**を否定し "somewhere in the middle" に立つ。ADR-036 決定6 の前提が成り立つかの判断 | PLAN-012 §6.1 |
| **N2** | **#10(W6 の分岐)。**第3段(T2 を主軸から外す)で止まってよいか。**案 A / B / C** が出ている | PLAN-012 §4 |
| **N3〜N6** | **目視レビュー4件**(ADR-030 の R8 手続き / ADR-027 前段 / ADR-028 決定1 と Go/No-Go #4b / PLAN-003 §4.8 のセル構成) | PLAN-012 §5 |
| **7** | 評価側 manifest に `schema_version` を足すか(ADR-051 の未解決。小さい) | ADR-051 |

**★1 / ★2 / N1〜N6 はすべて合否基準・実験条件・claim の確定なので、
エージェントは案までしか出さない**(`CLAUDE.md` §8)。
**N1 についてはエージェントは案も出していない。**

### B. GPU を使う(**★人間の承認が要る**)

**順5(= `M*` の実測)。**その前に RUNNER が **N2 検査**(comparison 群でバッチ1 対 バッチ N の
`parsed` 一致確認)を取り、実機で `pip freeze` → **ADR-044**(`infra/requirements.lock` の凍結)を履行する。
**preflight の `forced choice tokens` が WARN / FAIL を出したら人間に上げて止まる。**

## 直前セッションで確定したこと(再実装しないこと)

正本は **`plans/PLAN-012-order7-close.md`** + `logs/CHANGELOG.md` 末尾。
`pytest code/tests -q` → **808 passed**。GPU 時間 0。`results/` は空。**コードは 1 行も触っていない。**
関連 commit: `6f6f1d6`。

- **#13(`table[1]` の穴)は完全に閉じた。**「消滅」(ADR-036 決定5)に加え、
  「表の中身は未確定」も **2026-09-02 に解消済**(PLAN-009 の 197 件。**採択 人間**)。**再検討しない。**
- **#17(Nikankin et al.)の書誌は確定した。ICLR 2025(Poster)の査読済み論文である**
  (arXiv:2410.21272)。**`CLAUDE.md` §3 のプレプリント制限に当たらない。**
  `refs.bib` は `@inproceedings` 化済、`verified = {2026-09-06}` + `source_url` つき。
  **SCOUT 報告は `logs/SCOUT-2026-09-06-nikankin.md`(本会話では全文を読んでいない)。**
  **未検証のまま残した項目**: OpenReview の公式 venue 文字列・decision(403 で開けず)/
  ICLR 側の DOI・巻ページ / 公式 BibTeX / 原典が使った HF モデル ID。**推測で埋めないこと。**
- **★#17 が新しい未決 N1 を開いた**(上記)。
- **#10 の論点は切り出し済**(T1b の枝は終端の応答まで事前指定、T2 の枝は止まるだけ、という非対称)。
  **A を採ると df 6 → 4。T1b の枝と同時発火すると df 6 → 2 になるが、その扱いはどの文書にも無い。**
- **点検で見つかった事実**(いずれも設計を覆さない。PLAN-012 §5):
  - ADR-030 の**必須改修は実装済**(`t3_comparison.py` の `sweep` フラグ)。**決定6 の当てはめは未実装。**
  - ADR-030 決定4 の forward pass は**条件で変わる**(`x2` = 0 / `arb` = 5,440 / 他 = 8,160。
    40 run 合計 **272,000**)。**組合せ論的な計数であって実験結果ではない。**
  - ADR-027 の根拠の論法は §4.8 に及ばないが**結論は成り立つ**(`ans_in` が `2 <= t <= 198`、かつ
    `is_excluded` が生成時に全規則を弾く)。**疑って確認したが `x2` の偶然一致は復活していなかった。**

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-012-order7-close.md`(全文でよい。約 350 行)
- `STATE.md`「人間の承認・判断を待っている事項」の先頭ブロック(N1〜N6 の表)
- `Documents/05_STATISTICS.md` §4.1 末尾(★1)/ `Documents/01_HYPOTHESES.md` H2 節(★2)
- `STATE.md` は 2,900 行超。**全文 `cat` しない。**`grep -n '^## '` で節を出し `sed -n 'X,Yp'`
- `logs/SCOUT-2026-09-06-nikankin.md` は 164 行。**必要な節だけ `sed -n` で読む**

## やってはいけないこと

- **★1 / ★2 / N1〜N6 を提案して決める**(`CLAUDE.md` §8)。**N1 は案も出さない**
- **#13 と #17 の書誌を再調査する**(決着済。トークンの無駄)
- **`experiment.hypothesis` を書き換える**(ADR-053 決定1 で凍結)
- **§5 の Δ に値を入れる**(すべて順6 の実測待ち。`results/` は空)
- **事前登録の凍結タグ(順9)を打つ。**Δ と ★1 / ★2 が未決のうちは打てない
- **凍結した効果量プロファイル P1 / 非加法性 RMS = 0.353 を動かす**(ADR-052 決定2。HARKing)
- **`code/analysis/power_sim.py` を今つくる**(ADR-052 決定3。順6 の後)
- **検出力の数値(「シード 10 で足りる」等)を書く**(`s2_seed` / `s2_item` / `s2_tmpl` が順6 待ち)
- **§4 の順2 以降を繰り上げて番号を振り直す**(6 文書の参照がずれる。ADR-053 決定3)
- **`configs/exp_phase1_main.yaml` の `arbitrary_table` を手で編集する**
- **`eval.pool_items` / `eval.cells` を埋める**(B5。`M*` 未決。**順5 の後**)
- **`train.*` のハイパラに値を入れる**(ADR-043 決定10)
- **凍結済みのプロンプト文面を変える**(ADR-046 / 048)/ **強制選択の器械を触る**(ADR-047)
- `configs/smoke.yaml` を編集する(ADR-037 決定4)/ `data/raw/` を書き換える
- **書き出しの `newline="\n"` を外す**(ADR-051 決定4)
- 人間の承認なく GPU ジョブを起動する
- **`.md` を CRLF で書き戻す。**`.gitattributes` は `*.md text eol=lf` を宣言している。
  **★Edit 系のツールは環境によって CRLF に変換する。**`.md` を編集したら
  **必ず `python -c` でバイト単位の CR 数を数えて確かめること**
  (2026-09-06 に実際に 2 ファイルが CRLF 化した。`grep -c $'\r'` は
  ループの中などで展開に失敗して**全行を数える**ことがあるので信用しない)
- **`print()` に `—`(em dash)や日本語を流さない。**この環境の Python の stdout は cp932 で落ちる
  (書き込み自体は `io.open(..., encoding="utf-8")` で成功しているので、慌てて再実行しない)
- **`cat > file <<'EOF'` のように `<<` の直後に空白を置かない形は落ちることがある。**
  `<< 'EOF'` と空白を入れる。バックスラッシュを含む置換は scratchpad にスクリプトを書いてから実行する

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

**上の A の表(★1 / ★2 / N1〜N6 / #7)がすべてである。**加えて:

- **`05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)。**凍結タグはこれが入るまで打てない**
- **`refs.bib` の Nikankin 書誌の最終確認**(`CLAUDE.md` §3 手順5)
- **`00_OVERVIEW.md` §8 の整理の確認**: **G2 を失った分の T1 対策の代替が `arb` / `p2d` でよいか**
  (`Documents/06_THREATS.md` T1 の対策表と突き合わせること)
- **`09_PAPER_PLAN.md`**: タイトル候補は 3 だけが現行の主軸に整合 / **図5 の `arb` の扱い**(下書き)。
  **★N1 の結論次第で Intro の対立軸を組み直す必要がある**
- **B5**: `eval.pool_items` の本番の中身(**`M*`(順5)待ち**)/ **`θ` の値**(ADR-041)
- **ADR-047 実装ノートの R3 / F1 / R4 / 検査5**(実装済み。人間が読んで閉じる)
- **`train.*` のハイパラ**(ADR-043 決定10)/ `infra/requirements.lock`(ADR-044。次のポッドで)
