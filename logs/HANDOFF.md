# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-09(その24b)/ 直前セッションの役割: PLANNER / CRITIC (Opus)
直前セッションが終了した理由: コンテキスト超過(hook `context-guard` が約 236k で警告。閾値 140k)

---

あなたは IMPLEMENTER です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**★F90-2 の実装 —— ADR-067(案 B)と ADR-068(★F103)を `Documents/` と `code/` に反映する。**
**★人間の決定は残っていない。**3 件とも 2026-09-09 に採択済である(**上げ直さないこと**)。

**★決まっている内容(正本は `logs/DECISIONS.md` の ADR-067 / ADR-068)**:

| 記号 | 決定 |
|---|---|
| **★F90-1**(ADR-067 決定1) | **`s2_seed` を 1 点で推定しない。**「**どこまでの `s2_seed` なら 10 シードで検出力 0.8 に届くか**」を**逆問題**として事前登録する。**横軸は仮定値**(効果量プロファイル P と同じ扱い) |
| **★F103-1**(ADR-068 決定1) | **(a) 両方合わせる。**DGP も当てはめも §3.2 の形にする。**当てはめは `fit_full` / `fit_add` の対。ADR-059 の縮退規則もループに入れる** |
| **★F103-2**(ADR-068 決定2) | **(d) 交換可能。**`Sigma = sigma^2 * [(1 - rho) I + rho J]`、**`rho ∈ {0, 1}` の 2 枚。****横軸は `sigma` 1 本、`rho` は帯として併記** |
| **★掃く範囲**(ADR-068 決定3) | **R3。`sigma` = 0 / 0.25 / 0.5 / 0.75 / 1.0 / 1.5。★すべて仮定値**(**表の見出しに明示する**) |

完了条件:

1. **`Documents/05_STATISTICS.md` を書き換える**(**事前登録の本体**。
   **旧記述は打ち消し線 + 理由 + 日付で残す**。`CLAUDE.md` §2)。**対象一覧は `plans/PLAN-019` §10.7.9**:
   - **§6.3 手続き2**(取得元 = 仮定値。実測ではない)
   - **§6.3 手続き3**(**★DGP と当てはめを別行に分ける** —— ★F110。同じ式に 2 役をさせない)
   - **§6.3 手続き4**(**★シード掃き 5/8/10/15 を残すか 10 のみに畳むか。実装の判断である。
     ADR-067 が報告する量を「10 シードが保つ限界」に変えたので畳める余地がある。
     ★これがコストを 4 倍にする唯一のつまみである** —— `plans/PLAN-019` §10.7.8)
   - **§6.3 手続き5**(報告する量 = 「10 シードが保つ限界はどこか」)
   - **§6.4**(「順6 の GPU 実測を要する」の訂正)/ **§6.6 末尾**(同)
   - **★新設: §6.5 と同型の「掃く仮定値の範囲」の表**(**見出しに「仮定値」と明示**)
   - **★「Phase 0(順6)」の表記の訂正**(F102。**F90 は「順6」には成り立ち「Phase 0」全体には成り立たない**)
2. **`code/analysis/power_sim.py` を書く**(skill `code-style` を先に読む)。
   **DGP の共分散 + §3.2 の当てはめ対 + ADR-059 の縮退カスケード。**
   **`code/tests/` にテストを追加し `pytest code/tests -q` を通す**
3. **`plans/PLAN-004-phase0-route.md` §3 順7**(検出力分析の再導出が順6 に依存しなくなる)
4. `logs/CHANGELOG.md` に追記して commit

## 直前セッション(その24)で確定したこと

- **★★人間が ★F103 の 3 件をすべて採択した(ADR-068)。案 B の実装ブロッカーは外れた**
- **★食い違いは現物だった**(`plans/PLAN-019` §10.7.1 に引用)——
  **§3.2 = `(0 + coverage | seed)`(6 パラメータ)/ §6.3 手続き3 = `(1|seed)`(スカラー 1 個)。
  §6.3 は 2026-08-23 以来 無変更だった**
- **★「ADR-064 決定4 の反映漏れ」は正しい読解だった**(§10.7.2)——
  **ADR-064 も ADR-066 も影響欄に §2 / §3.2 / §4 しか挙げていない。§6.3 は入っていない**
- **★F110**: **手続き3 の一文は DGP と当てはめの 2 役を兼ねている。**
  **DGP が違うのは誤りでないが、当てはめが §3.2 と違うと「回さない検定の検出力」になる。**
  **これが決定1 = (a) の根拠である**
- **★F105 〜 F109(検査2。`plans/PLAN-019-check2/run.log` が正本。合成データ。実験結果ではない)**:
  **F105 = 膨張は `sigma` で決まり相関 `rho` では動かない**(`sigma=1.0` で rho 4 値が
  1.136 / 1.155 / 1.151 / 1.145。開き 0.019 < モンテカルロの床 0.028)/
  **F106 = エージェントの事前の見立ては外れた**(`rho = 1` でも平らにならない)/
  **F107 = 恒等リンクの対照は平ら** / **F108 = 膨張は `sigma` 1.5 でも約 1.2 倍** /
  **F109 = 掃く範囲の上端が採択済みの 2 数(P1 の 0.94 / Go/No-Go #4 の 0.90)だけで立つ**
- **★F111(見立て。未検証)**: **掃いた範囲の全域で 0.8 を割らない結果になる公算がある。**
  **人間はこれを承知のうえで採択した**(ADR-068 のリスク欄)
- **`pytest code/tests -q` = 839 passed**(**`code/` を 1 行も触っていない**)
- **実験は 1 件も実行していない。`results/` は空。凍結タグは打っていない。GPU 時間 0**

## 触ってよいファイル / 読むべき範囲

- **`logs/DECISIONS.md` の ADR-068**(`grep -n 'ADR-068' logs/DECISIONS.md`)と **ADR-067**。
  **リスク欄まで読むこと**
- **`plans/PLAN-019-validity-decisions.md` §10.7.9**(書き換え先の一覧)/ **§10.7.8**(コスト)/
  **§10.7.3**(★F110)/ **§10.7.6**(検査2 が言えること・言えないこと)
- `plans/PLAN-019-check2/README.md` と `covariance_shape_check.py`
  (**縮約の実装がそのまま対応する。`compound_symmetry_sqrt` は Cholesky ではなく固有分解**)
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '0 + coverage' Documents/05_STATISTICS.md`)/
  **§6.3 〜 §6.6**(`grep -n '### 6\.' Documents/05_STATISTICS.md`)/ **§6.5**(新設する表の雛型)
- `logs/DECISIONS.md` の **ADR-059**(縮退規則。**判定はペア単位**)/ **ADR-064 決定4** / **ADR-066 決定1**
- **全文 `cat` しない。`grep -n` で節を特定して `sed -n 'X,Yp'`**(`CLAUDE.md` §10)

## やってはいけないこと

- **ADR-067 / ADR-068 を蒸し返さない。**人間が採択済である。**実装だけを行う**
- **★掃く範囲の数値を「実測」として書かない。**`sigma` = 0 〜 1.5 は**すべて仮定値**である。
  **`results/` は空であり、`s2_seed` の実測はこの世に存在しない**(`CLAUDE.md` §2)。
  **表の見出しに「仮定値」と明示する**(ADR-068 決定3)
- **★F109 を「Go/No-Go #4 の制約」と書かない。**#4 は**パイロットの集計に対する門**であり、
  **run 単位の目安として読み替えているのはエージェントである**(ADR-068 のリスク欄 / §10.9)
- **★検査2 の数値を実験結果として書かない。**合成データである
- **モデル指定(`(0 + coverage | seed)`)を変えない**(ADR-064 決定4 / ADR-066 決定1)
- **先行研究の値を確認せずに引用しない**(`CLAUDE.md` §3)。
  **LoRA FT のシード間分散をロジット尺度で報告した文献は 1 件も確認していない。R3 は借りない形である**
- **GPU を起動しない。凍結タグを打たない**(`θ` / N5 / GPU 構成が未決)

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8。正本は `logs/OPEN-ITEMS.md`)

- **★F104**(`s2_item` / `s2_tmpl` を順6 の `correct_rate` から取ってよいか。**F103 とは独立**)
- **`θ`**(順5 の唯一の残りブロッカー。**2026-09-07 以降どのセッションでも動いていない**)
- **順6 と本実験 40 run の GPU 構成**(全体のクリティカルパス)
- **N5**(解析門の閾値)/ **★C** / **★E** / **E-5 (b)** / **Δ 5 行** / **★2** / **★L(d)**
- **PLAN-018 §4.3** の組み直し(とくに F87)
