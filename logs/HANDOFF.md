# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その12)/ 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **コンテキスト超過**(hook `context-guard` が 145k で警告。閾値 140k)。
GPU 時間 0。コード変更 0 行。`pytest code/tests -q` = **811 passed**(実行して確認した)。commit `6f1a97f`。

---

あなたは **IMPLEMENTER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

---

## ★このセッションでやること(1つだけ)

**検査3 を実行する。**`plans/PLAN-016-fitting-engine.md` §7 の項目 3 に手順が書いてある。

**完了条件**:

1. `plans/PLAN-016-check3/` に検査スクリプトと README を置く(検査1 / 検査2 と同じ作法。
   `code/` には入れない。出力は `.gitignore` に入れる)
2. **測るもの 4 つ**を JSON で出す:
   - **(i) 再当てはめで `lme4` の警告が消える割合**
   - **(ii) `logLik` の改善量の分布**
   - **(iii) 固定効果の推定値の可動量**
   - **(iv) 余分にかかった壁時計**
3. `plans/PLAN-016-fitting-engine.md` §2 に **F57 以降**として実測を書く
4. `logs/CHANGELOG.md` に追記して commit する

**手順**: 検査2 の枠組み(`plans/PLAN-016-check2/` の `gen_frame.py` / `fit_timing.R` / `summarize.py`)を
再利用する。`lme4` の警告が出た当てはめについて、
**その当てはめの推定値を初期値にして 1 回だけ当て直す**
(`start = getME(fit, c("theta","fixef"))`)。

**GPU は要らない。ローカルの R 4.6.1 + `lme4 2.0.6` で回る。**

---

## ★なぜこれが要るのか(飛ばさないこと)

**ADR-059 決定1(D-5 = 案 (e))は「再起動で警告が消えることがある」に賭けているが、それは未測定である。**

- **消えるなら**: 決定1 は狙いどおり機能する。**偽陽性の警告と本当の収束失敗を分離できる**
- **消えないなら**: **決定1 は案 (b)(警告があれば縮退)と同じ挙動に縮退する** ——
  合成データで 79% のペアが発火するので、**事前登録した §3.2 のモデルがほぼ毎回、
  誰も決めていない縮退モデルに置き換わる。**
  **その場合は D-5 を人間に上げ直す**(`CLAUDE.md` §8。エージェントが決めない)

**あわせて、ADR-059 決定1 の「実質的に動かない」の閾値(仮に `logLik` 差 < 0.001)を
実測してから凍結する。**この閾値はいま仮の値である。

**★事前登録の凍結より前に終わっていなければならない。**

---

## 直前セッションで確定したこと(2026-09-07。すべてファイルに書き込み済み)

**正本は `logs/DECISIONS.md` の ADR-059 と `plans/PLAN-016-fitting-engine.md` の
§2(F54〜F56)/ §6 / §6.2 / §7、および `Documents/05_STATISTICS.md` §3.2 の追記。**
**commit `6f1a97f`。GPU は使っていない。実データは見ていない。合成データだけである。**

### 人間の決定 2 件(ADR-059。提案 エージェント (Opus) / 採択 人間)

- **D-5 = 案 (e)。**収束失敗の引き金は**「再起動で検証した失敗」**とする。
  `lme4` が警告を出した当てはめを、その推定値を初期値にして 1 回だけ当て直し、
  **`logLik` が実質的に改善せず推定値も動かなければ「収束した」とみなして縮退しない。**
  **`boundary (singular) fit` は単独では引き金にしない**
  (境界上の正当な最尤推定であり対数尤度は有効である)。**ただし論文には必ず報告する。**
  **判定は `isSingular(fit)` を直接呼ぶ**(警告文の文字列一致で拾わない)
- **★決定2(付帯条件。選択肢ではない): 判定はペア単位である。**
  `fit_full` と `fit_add` のどちらかが発火したら**両方を縮退した構造で当て直す。**
  **片方だけ縮退させると df = 6 の LRT が §3.2 のモデル指定の検定でなくなる**
- **D-2 = 同じエンジン。**検出力シミュレーション(§6.3 手続き3)も `lme4` で回す。
  **案 E は建てない。**再オープン条件を ADR に明記した ——
  **`M*` が検査2 の掃引範囲(被覆あたり 96 項目 / 11520 行)を大きく超えたら案 F を再検討する**

### 実地確認 3 件(F54〜F56)。検査2 の出力を数え直したものであり、★再実行ではない

- **F54**: 当てはめは **28 本**(14 ペア x `full`/`add`)。
  **`conv$opt != 0` は 0 本(0%)/ `lme4` の警告は 19 本(68%)/
  `max|grad|` は 13 本(46%。値は 0.00325 〜 0.0639。`tol = 0.002`)/ `singular` は 6 本(21%)。**
  **★`singular` と `max|grad|` はこの標本では排他である。**
  **★ペア内で `full` と `add` の警告有無が食い違ったペアが 14 中 3 ある**
- **F55**: **★警告が出たペアのほうが LRT の較正は良い。**
  警告ありのペア 11 本の `Chisq` 平均は **5.84**(chi2(6) の期待値 6)、警告なし 3 本は 8.02、
  `singular` を含む 5 本は 5.53。**14 ペアで alpha = 0.05 の棄却は 0 回。**
  **この標本では「警告 = 検定が壊れている」ではない。★ただし n = 14 は較正を論じるには小さい**
- **F56**: 検査2 の R スクリプトは `singular` を警告文の文字列一致で拾っており、
  `isSingular()` を直接呼んでいない。**本実装では直接呼ぶこと**

### ★事前登録の本体に手を入れた(人間の確認を求めている)

**`Documents/05_STATISTICS.md` §3.2 に「収束しない」の定義を新しい箇条として追記した。**
**モデル指定は 1 文字も変えていない。**順1b 側にも「定義は主軸と同じ」と 1 行足した。
**人間がまだ確認していない。**

---

## 触ってよいファイル / 読むべき範囲

- **`plans/PLAN-016-fitting-engine.md`**(**全文読んでよい**。約 460 行)**← 最初に読む**。
  **§6.2 が D-5 の決定材料の正本である**
- `plans/PLAN-016-check2/`(**検査3 の土台**。`README.md` → `gen_frame.py` → `fit_timing.R`)
- `logs/DECISIONS.md` の **ADR-059**(`grep -n '^## ADR-059' logs/DECISIONS.md` → `sed -n 'X,Yp'`)。
  **全文 `cat` しない**(3,600 行)
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '^### 3.2' Documents/05_STATISTICS.md`)。
  **全文 `cat` しない**(715 行)
- **`STATE.md` は 3,500 行を超えた。全文 `cat` しない。**
  `grep -n '^## '` で節を出し `sed -n 'X,Yp'`

---

## やってはいけないこと

- **D-5 の結論を自分で変える。**検査3 の結果が悪くても、**D-5 を決め直すのは人間である**
  (`CLAUDE.md` §8 / ADR-039 決定3)。**エージェントは「上げ直す」だけ**
- **D-4 を決める**(PLAN-016 に残る唯一の未決)
- **検査1 / 検査2 をやり直す。**2026-09-07 に完了した。
  **正本は `plans/PLAN-016` §4.4(検査1)と §2 の F48〜F56(検査2)**
- **`Documents/05_STATISTICS.md` §3.2 のモデル指定を書き換える。**事前登録の対象そのものである。
  **その12 で追記したのは「収束しない」の定義だけであり、モデル指定は無変更である**
- **検査3 の点推定を本番の見積もりとして記録する。**`M*` 待ちの暫定規模である
- **`θ` を決める / `M*` を決める**(ADR-041 決定2・決定3)
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(**`M*` 待ち**)
- **`resources` の注記を消す / 縮める**(ADR-057 決定2)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**
- **事前登録の凍結タグを打つ**(Δ と ★2 と S5 と検査3 が未決)
- **★D-A 〜 D-D を人間に上げ直す**(ADR-057 で決着)/ **★D-1 / D-3**(ADR-058)/
  **★D-5 / D-2**(ADR-059)

### ★この環境で実際に踏んだ地雷

- **★`cat > file <<'EOF'` 形式の heredoc がこの環境の Bash ツールで落ちる。**
  **長い散文は Write ツールで一時領域に書き、python で本文に追記する**のが確実である
- **★★その12 で新しく踏んだ: `python -c "..."` の中にバッククォートを書くと、
  bash がコマンド置換として食う。**実際に `` `lme4` `` などが消えた本文を書き込んでしまった。
  **バッククォートを含む python は必ずスクリプトファイルに書いて `python file.py` で回す**
- **★`python - <<'PYEOF'` の中でバックスラッシュのエスケープが 1 段外れることがある。**
  **バックスラッシュを含む文字列は `chr(92)` で組み立てるか、Write ツールを使う**
- **★`.md` / `.py` / `.R` / `.yaml` を編集したら、必ず python でバイト単位の CR 数を数えて確かめること。**
  `.gitattributes` は `* text=auto eol=lf` を宣言している。
  **python で書き戻すときは `newline="\n"` を明示する**
- **★全角文字を混ぜない。★Markdown の入れ子太字(`****`)を作らない。**
  **その12 では STATE.md と CHANGELOG.md で計 7 箇所作って直した。**
  **追記したテキストだけを対象に、`\*{3,}` の個数を HEAD と比べて数えるのが速い**
- **★`print()` に em dash や日本語や `≥` を流さない**(この環境の Python の stdout は cp932)。
  **検査スクリプトと検査用の python ワンライナーの出力は ASCII だけ**にする
- **★検査2 の `timing_*.json` には生の CR/LF が埋まっている**(その11 の地雷の残骸)。
  **`json.loads(..., strict=False)` でないと読めない。検査3 のスクリプトでは埋め込まないこと**
- **`logs/CHANGELOG.md` は古い順である(新しいものが末尾)。**先頭に足さない
- **`infra/preflight.py` は内部で `pytest` を回すので 1 回 50 秒かかる**
- **`pytest code/tests -q` は 38 〜 59 秒かかる**(811 tests)。`timeout` を伸ばす
- **`code.eval.sweep --dry-run` の標準出力は約 1 MB ある。**必ずファイルに落とす
- **★`glmer` の 1 回の当てはめは 5 秒 〜 1 分かかる。**`run_in_background` に回す

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

### PLAN-016(D-1 / D-3 は ADR-058、D-5 / D-2 は ADR-059 で閉じた)

- **★検査3 の結果次第で D-5 を上げ直す**(上記)
- **★`Documents/05_STATISTICS.md` §3.2 の追記を人間が確認する**(事前登録の本体に手を入れた)
- **D-4**: 案 C(ベイズ)を感度解析として併記するか。
  **PLAN-016 に残る唯一の未決。クリティカルパス上に無い**
- **解析側の凍結手段**(ADR-058 決定2 の帰結)。`renv.lock` を repo に置くかどうか

### 従来からの未決

- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 を回す前に決まっていなければならない。
  表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9。**D-1 とは独立に決められる**)/
  **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A は ADR-056、★D は ADR-058 / ADR-059 で閉じた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格するが、**非有意は研究仮説 H1 が予測する結果でもある。**案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。`T1 × id` で門を掛け、その `id` を DiD の基準腕に使う = **従属変数での選択。**案: 門を掛ける量を DiD の腕から外す。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | 当てはめエンジンが無い | **→ ADR-058 + ADR-059 で閉じた。**D-1 = 案 A / D-3 = 解析を切り離す / D-5 = 案 (e) / D-2 = 同じエンジン。**残るのは D-4 と検査3** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード) | **未決** |

### 順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4。どちらも `comparison` 群(= 評価プール)を要求するので掃引では成立しない。**

| # | 前提 | 備考 |
|---|---|---|
| **1** | **バッチ fp ノイズ検査**(ADR-040 決定7)。`comparison` 群でバッチ1 対 バッチ N の `parsed` 一致 | **GPU が要る** |
| **2** | **preflight の `forced choice tokens`**(ADR-047 実装ノート 5) | **★重みは読まないので GPU を借りる前にローカルで取れる** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**
**★ローカル環境**: **R 4.6.1 と `lme4 2.0.6` が入っている**(その11)。
`statsmodels` は 2026-09-07 に外したままである。**`infra/requirements.lock` は無変更である。**
