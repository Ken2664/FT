# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その13)/ 直前セッションの役割: IMPLEMENTER (Opus)
直前セッションが終了した理由: **コンテキスト超過**(hook `context-guard` が 151k で警告。閾値 140k)。
GPU 時間 0。`code/` の変更 0 行。`pytest code/tests -q` = **811 passed**(実行して確認した)。commit `1afea4e`。

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

---

## ★このセッションでやること

**★このセッションの本体は「人間に 1 件決めてもらうこと」である。エージェントの作業ではない。**

### 1. 人間に上げる(最優先。事前登録の凍結を止めている唯一の項目)

**D-5 の「実質的に動かない」の閾値を人間が決める。**

- **ADR-059 決定1 は `logLik` 側の仮の値(差 < 0.001)しか書いておらず、
  「推定値も実質的に動かない」の閾値が未記入である。★そこが発火率を決めている**(F62)
- **エージェントの提案(採否は人間)**: **`logLik` 差 < 0.001(現行のまま)かつ `max|Δβ| / SE < 0.01`**
- **決まったら**: `Documents/05_STATISTICS.md` §3.2 の「★『実質的に』の閾値」の行を確定させ、
  **ADR-059 に追記する**(提案者と採択者を分けて書く。ADR-039)
- **あわせて確認を求める**: その12 が §3.2 に追記した「収束しない」の定義そのもの
  (**事前登録の本体に手を入れている。**モデル指定は無変更)。**人間がまだ確認していない**

### 2. 人間を待つ間にエージェントが進められること(1 つだけ)

**D-4**: 案 C(ベイズ)を感度解析として併記するか。**PLAN-016 に残る唯一の未決であり、
クリティカルパス上に無い。**案を並べるところまでがエージェントの仕事である(`CLAUDE.md` §8)。

**★これ以外に新しい検査を建てない。**検査3 で PLAN-016 の測定は終わっている。

---

## 直前セッションで確定したこと(2026-09-07。すべてファイルに書き込み済み)

**正本は `plans/PLAN-016-fitting-engine.md` §2 の F57〜F62 と §7-3。commit `1afea4e`。**
**GPU は使っていない。実データは見ていない。合成データ 34 ドローだけである。**

### 検査3 を実行した(PLAN-016 §7-3)。ADR-059 決定1 が乗っていた未検証の事実を潰した

**結論: 賭けは当たっていた。★D-5 を人間に上げ直す条件には当たらない。**
**代わりに閾値 1 つが人間の決定待ちとして開いた**(上記)。

- **F57**: F54 が 2.4 倍の標本で再現した。68 本の当てはめで
  **`conv$opt != 0` は 0 本(0%)/ `lme4` の警告は 54 本(79%)/ `isSingular()` は 25 本(37%)/
  `max|grad|` は 29 本(43%)。★`singular` と `max|grad|` の排他性は n = 68 でも保たれた**(両方 0 本)。
  **ペア単位の警告は 32 / 34(94%)**
- **F58(測るもの i)**: **★再起動は `max|grad|` の警告を消す(54 本中 53 本 = 98%)。**
  **ただし「警告が 1 つも無くなった」のは 11 本(20%)だけ**で、
  **再起動は当てはめを境界へ押しやる**(54 本中 42 本が再起動後に特異)。
  **★退屈な仮説を排除した**: 特異になると `lme4` の尺度化勾配の判定自体が走らなくなるので、
  **再起動後に特異でない 11 本だけを取り出した。`max|grad|` が消えたのは 10 / 11、
  尺度化勾配は 11 / 11 で低下(中央値 0.00161 -> 0.0000453。`tol = 0.002` を全数が下回る)。**
  **判定が黙ったのではなく勾配が実際に下がっている**
- **F59(ii)**: `logLik` の改善は**中央値 1.17e-05 / 最大 0.408。仮の閾値 0.001 超は 2 / 54 本。**
  対照(警告なし 14 本)は**最大 1.70e-06**。**★3 / 54 本で再起動のほうがわずかに悪い**
  (閾値は符号付き片側で書く必要がある)
- **F60(iii)**: `max|Δβ| / SE` は**中央値 0.00227 / 最大 0.256。0.01 超は 8 / 54。**
  **対照は最大 0.00102** ——**提案する閾値 0.01 は対照の全分布の約 10 倍上にある**
- **F61(iv)**: **方針の壁時計は x1.34**(`fit0` 合計 747.9 秒 / 警告分の再起動 257.0 秒)。
  規模別 x1.25 〜 x1.66。**★§6.2.4 が置いた最悪 1.7 倍の想定を下回る**
- **F62**: **★決定1 は案 (b) に縮退しない。**ペアの発火率は **(b) が 32 / 34(94%)、
  (e) は `logLik` 0.001 かつ `max|Δβ|/SE` 0.01 で 8 / 34(24%)、0.001 かつ 0.05 で 2 / 34(6%)。**
  **★ただし可動量の閾値を 0.001 にすると `logLik` 側を何にしても 32 / 34 になり (b) と一致する**

**★報告義務として記録した事実**: **再起動は当てはめを境界へ押しやる**(54 本中 42 本)。
**決定1 は singular を単独の引き金にしないので縮退は起きないが、
ADR-059 が「論文に必ず報告する」と書いた対象がこの割合である。**

**★検査3 の限界**(主張に使う前に読む): すべて合成データで真値がモデルと一致している /
**34 ペアは率の推定としてまだ小さく、とくに (i-b) の清潔な証拠は 11 本しかない** /
規模は `M*` 待ちの暫定 / `x24`(交互作用ありの真値)の再起動コストだけ 1.66 倍と高く、理由は追っていない。

### スクリプトを `plans/PLAN-016-check3/` に置いた(検査1 / 検査2 と同じ作法)

- **生成モデルは検査2 の `gen_frame.py` を `importlib` で読み込んで再利用しており、コピーしていない**
- **設計上の判断 2 つ**: (1) **警告が出た当てはめだけでなく全部を再起動した。**
  警告が出ていない 14 本が対照であり、**これが無いと閾値に根拠が無い。**
  方針そのものの壁時計は per-fit の時刻から復元した。
  (2) **可動量は SE 単位でも報告する。**生の係数の尺度は係数間で比較できず、規則にできない

---

## 触ってよいファイル / 読むべき範囲

- **`plans/PLAN-016-fitting-engine.md`**(**全文読んでよい**。約 510 行)**← 最初に読む**。
  **§2 の F57〜F62 が検査3 の正本、§6.2 が D-5 の決定材料の正本、§4.3 が D-4 の材料である**
- `plans/PLAN-016-check3/README.md`(**検査3 の設計の説明**。スクリプトを読む前にここ)
- `logs/DECISIONS.md` の **ADR-059**(`grep -n '^## ADR-059' logs/DECISIONS.md` → `sed -n 'X,Yp'`)。
  **全文 `cat` しない**(3,600 行)
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '^### 3.2' Documents/05_STATISTICS.md`)。
  **全文 `cat` しない**(715 行)
- **`STATE.md` は 3,570 行を超えた。全文 `cat` しない。**
  `grep -n '^## '` で節を出し `sed -n 'X,Yp'`

---

## やってはいけないこと

- **★D-5 の閾値を自分で決める。**検査3 は材料を出しただけである。
  **決めるのは人間**(`CLAUDE.md` §8 / ADR-039 決定3)。**エージェントは提案するだけ**
- **★`Documents/05_STATISTICS.md` §3.2 を書き換える。**事前登録の対象そのものである。
  **閾値の行も、人間が決めてから人間の決定として書く**
- **検査1 / 検査2 / 検査3 をやり直す。**すべて 2026-09-07 に完了した。
  **正本は `plans/PLAN-016` §4.4(検査1)/ §2 の F48〜F56(検査2)/ §2 の F57〜F62(検査3)**
- **★新しい検査を建てる。**PLAN-016 の測定は終わっている
- **検査3 の点推定を本番の見積もりとして記録する。**`M*` 待ちの暫定規模である
- **`θ` を決める / `M*` を決める**(ADR-041 決定2・決定3)
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(**`M*` 待ち**)
- **`resources` の注記を消す / 縮める**(ADR-057 決定2)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**
- **事前登録の凍結タグを打つ**(Δ と ★2 と S5 と **D-5 の閾値**が未決)
- **★D-A 〜 D-D を人間に上げ直す**(ADR-057 で決着)/ **★D-1 / D-3**(ADR-058)/
  **★D-5 の採否そのもの / D-2**(ADR-059。**閾値だけが残っている**)

### ★この環境で実際に踏んだ地雷

- **★`cat > file <<'EOF'` 形式の heredoc がこの環境の Bash ツールで落ちる。**
  **長い散文は Write ツールでスクラッチに `.py` を書き、`python file.py` で本文に差し込む**のが確実である。
  **その13 はこの方式で 4 回とも通した**
- **★`python -c "..."` の中にバッククォートを書くと bash がコマンド置換として食う。**
  **バッククォートを含む python は必ずスクリプトファイルに書く**
- **★`Rscript -e '...'` の中の二重引用符が Windows で壊れる。**
  **R のワンライナーも必ず `.R` ファイルに書いて `Rscript file.R` で回す**(その13 で踏んだ)
- **★`.md` / `.py` / `.R` / `.yaml` を編集したら、必ず python でバイト単位の CR 数を数えて確かめること。**
  `.gitattributes` は `* text=auto eol=lf` を宣言している。
  **python で書き戻すときは `newline="\n"` を明示する**
- **★全角文字を混ぜない。★Markdown の入れ子太字(`****`)を作らない。**
  **追記したテキストだけを対象に `\*{3,}` の個数を数えるのが速い**(その13 は 0 件で通した)
- **★`print()` に em dash や日本語や `≥` を流さない**(この環境の Python の stdout は cp932)。
  **検査スクリプトの出力は ASCII だけ**にする
- **★`lme4` は当てはめによって `optinfo$derivs` を残さない。**
  **勾配は `None` になりうるので、集計側は「測れなかった」と「0」を区別すること**(その13 で踏んだ)
- **★Bash ツールは `sleep N` に続けてコマンドを繋ぐ形を拒否する。**
  待つなら `run_in_background` か `until ...; do sleep 2; done` を使う
- **`logs/CHANGELOG.md` は古い順である(新しいものが末尾)。**先頭に足さない
- **`infra/preflight.py` は内部で `pytest` を回すので 1 回 50 秒かかる**
- **`pytest code/tests -q` は 24 〜 59 秒かかる**(811 tests)。`timeout` を伸ばす
- **`code.eval.sweep --dry-run` の標準出力は約 1 MB ある。**必ずファイルに落とす
- **★`glmer` の 1 回の当てはめは 1 秒 〜 1 分かかる。**`run_in_background` に回す。
  **★測定中に `pytest` を並走させない** ——F51 のとおり機械の負荷で壁時計が 2 倍振れる

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

### PLAN-016(D-1 / D-3 = ADR-058、D-5 / D-2 = ADR-059。**閾値だけが残っている**)

- **★★D-5 の可動量の閾値**(上記。**事前登録の凍結を止めている唯一の項目**)
- **★`Documents/05_STATISTICS.md` §3.2 の追記を人間が確認する**(事前登録の本体に手を入れた)
- **D-4**: 案 C(ベイズ)を感度解析として併記するか。
  **PLAN-016 に残る唯一のエージェント作業。クリティカルパス上に無い**
- **解析側の凍結手段**(ADR-058 決定2 の帰結)。`renv.lock` を repo に置くかどうか

### 従来からの未決

- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 を回す前に決まっていなければならない。
  表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9)/ **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A は ADR-056、★D は ADR-058 / ADR-059 で閉じた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格するが、**非有意は研究仮説 H1 が予測する結果でもある。**案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。`T1 × id` で門を掛け、その `id` を DiD の基準腕に使う = **従属変数での選択。**案: 門を掛ける量を DiD の腕から外す。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | 当てはめエンジンが無い | **→ ADR-058 + ADR-059 + 検査3 でほぼ閉じた。**D-1 = 案 A / D-3 = 解析を切り離す / D-5 = 案 (e) / D-2 = 同じエンジン。**残るのは D-5 の閾値(人間)と D-4** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード) | **未決** |

### 順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4。どちらも `comparison` 群(= 評価プール)を要求するので掃引では成立しない。**

| # | 前提 | 備考 |
|---|---|---|
| **1** | **バッチ fp ノイズ検査**(ADR-040 決定7)。`comparison` 群でバッチ1 対 バッチ N の `parsed` 一致 | **GPU が要る** |
| **2** | **preflight の `forced choice tokens`**(ADR-047 実装ノート 5) | **★重みは読まないので GPU を借りる前にローカルで取れる** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**
**★ローカル環境**: **R 4.6.1 と `lme4 2.0.6` が入っている**(その11 で導入。その13 で再確認した)。
`statsmodels` は 2026-09-07 に外したままである。**`infra/requirements.lock` は無変更である。**
