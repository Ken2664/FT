# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-09-07(その15)/ 直前セッションの役割: PLANNER (Opus)
直前セッションが終了した理由: **やることが終わったため**(§6.4 を書き、人間が 3 件を決定し、記録した)。
GPU 時間 0。`code/` の変更 0 行。`pytest code/tests -q` = **811 passed**(実行して確認した)。

---

あなたは **PLANNER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

---

## ★このセッションでやること(1 つだけ)

**`plans/PLAN-017-analysis-frame.md` を起草する。実装しない。**

- **なぜ要るか**: **ADR-061 決定3** で人間が「**`code/analysis/frame.py` は先に PLAN を起こし、
  レビューを経てから実装する**」と決めた(`CLAUDE.md` §4 の手順)。
  **その12 が「着手はスコープの追加なので人間に確認してから」と記録した項目への回答である**
- **何を書くか**: **§3.2 のモデルが要求する長形式表**(1 行 = 1 項目 × 1 シード)を組む層の仕様。
  必要な列は `is_rule` / `task` / `coverage` / `seed` / `item` / `template`。
  **現状の棚卸しは `plans/PLAN-016-fitting-engine.md` §5 にある**(F37 / F38 / F39)
- **★本体は `coverage` の照合である**(F38)。**`coverage` を付ける実行時コードが repo に無い。**
  設計上 `Item` は `coverage` を持たず、**FT データの manifest と照合して実行時に付ける**ことに
  なっている(`code/eval/battery/battery_items.py` 付近。**パスは実際に確かめること**)。
  **これが無いと主要検定の説明変数の片方が作れず、迂回できない**
- **★あわせて書くべきもの**: **`group` → `task` の写像**(写像表がどこにも無い。F37)/
  **`category` → `template` の写像**(**★水準数が未確定である。**§3.2 の表は
  「T2 の 5 テンプレート + T3 の質問文。T1 / T1b は単一」と書くが、
  `code/eval/battery/t3_comparison.py:61` の `CATEGORY_AXES` は T3 / T1b にそれぞれ
  2 水準(`gt` / `lt`)を持つ。**どちらを `template` の水準とするかは仕様の決定事項である**)/
  **`seed` の join**(`metrics.json` / `config.yaml` から)
- **★`M*` 待ちでも書ける**(照合の実装であって項目数に依存しない)。**GPU 不要**
- **★決めない。**PLAN は案を並べる文書である(`CLAUDE.md` §8 / ADR-039 決定3)。
  **仕様の分岐が出たら人間に上げる**

---

## 直前セッション(その15)で確定したこと(すべてファイルに書き込み済み)

**人間が 3 件を決定した(ADR-061)。エージェントは材料(§6.4)を並べ、決定を記録しただけである。**

1. **★D-6 = 案 (2) 数値で明示。叩き台の数値をそのまま採択**
   - **ロジット尺度でそのまま指定し、autoscale は掛けない**(データのスケールに依存させない)
   - **切片 `normal(0, 5)` / 固定効果の係数 `normal(0, 2.5)`(交互作用を含むすべて)/
     分散成分の SD `half-Student-t(3, 0, 2.5)`**(主軸・順1 は `seed` / `item` / `template`、
     順1b は `condition:seed` / `item` / `template` の 3 本すべて共通)
   - **★実装非依存である。**`rstanarm` でも `brms` でも同じ事前分布として書けるので、
     **ADR-060 の「どちらを使うかは実装時に決めてよい」がそのまま成立する**
2. **★ベイズ側で報告する量**: **交互作用の事後分布とその区間 / 分散成分 3 本の事後分布 /
   完全 vs 加法の `loo` 比較。★ベイズファクタは出さない**
   ——BF は固定効果の事前分布の広さに直接依存する(Lindley / Bartlett)ため。
   **主解析の LRT と食い違ったら両方を報告する**
3. **★PLAN-016 §7-4 は PLAN を先に書いてから実装する**(このセッションの仕事)

**→ `Documents/05_STATISTICS.md` §3.2 に人間の決定として書き込んだ。`logs/DECISIONS.md` に ADR-061。**
**★PLAN-016 の未決は 1 件も残っていない。ステータスは `採択`。残るのは実装(§7 の 4 / 5 / 5b)である。**

**★決定に伴って残ったリスク(ADR-061 のリスク欄。実験前に記録した。上げ直さないが、忘れない)**:
**事前予測検査を回していない**(数値は弱情報事前分布の慣例と、基準セルが天井に近いという
設計から導いたものである)/ **S5 で傾きを置くと相関行列の事前分布が 4 系統目として増える** /
**検査3 でどの分散成分が境界に張り付いたかは成分別に集計していない**
(決定1 が `template` を狙い撃ちにしている根拠は水準数であって実測ではない)。

**★未検証(実装の前に測ればよい。いま検査を建てない)**:
**`rstanarm` / `brms` がこの環境に入るか** / **`stan_glmer` が交差ランダム効果 3 本を受けるか** /
**MCMC 1 本の壁時計** / **`loo` が `pareto_k` の警告を出さないか**。

---

## 触ってよいファイル / 読むべき範囲

- **`plans/PLAN-017-analysis-frame.md`** ← **新規に書く先である**
- **`plans/PLAN-016-fitting-engine.md` §5**(長形式表の棚卸し。**ここだけ読めば足りる**。
  `grep -n '^## 5\.' plans/PLAN-016-fitting-engine.md` → `sed -n 'X,Yp'`)。**全文 `cat` しない**(765 行超)
- `Documents/05_STATISTICS.md` **§3.2**(`grep -n '^### 3.2' Documents/05_STATISTICS.md`)。
  **全文 `cat` しない**(720 行超)
- `logs/DECISIONS.md` の **ADR-061**(`grep -n '^## ADR-061' logs/DECISIONS.md` → `sed -n 'X,Yp'`)。
  **全文 `cat` しない**(3,700 行超)
- **`STATE.md` は 3,750 行を超えた。全文 `cat` しない。**`grep -n '^## '` → `sed -n 'X,Yp'`
- **`code/eval/battery/`**(`battery_items.py` / `t3_comparison.py` / `build.py`)。
  **`coverage` と `category` の実装を確かめる**

---

## やってはいけないこと

- **★PLAN-017 の仕様を人間の承認なく確定する**(`CLAUDE.md` §8 / ADR-039 決定3)。**案を並べる**
- **★`code/analysis/frame.py` を実装する。**ADR-061 決定3 は「PLAN を先に」である
- **★`Documents/05_STATISTICS.md` §3.2 を書き換える。**事前登録の本体である
- **★新しい検査を建てる。**事前予測検査(ADR-061 のリスク欄)が要ると考えたら、
  **回す前に人間に上げる**
- **★D-1 〜 D-6 を上げ直す**(ADR-058 / ADR-059 + 追記 / ADR-060 / ADR-061 で全件決着)
- **★D-A 〜 D-D を上げ直す**(ADR-057)
- **`θ` を決める / `M*` を決める**(ADR-041 決定2・決定3)
- **`eval.cells` / `anchor_manifest` / `pool_items` を埋める**(**`M*` 待ち**)
- **事前登録の凍結タグを打つ**(Δ / ★2 / S5 が未決。**事前分布は決まった**)
- **人間の承認なく GPU ジョブを起動する** / **ポッドを起動したまま放置する**

### ★この環境で実際に踏んだ地雷(その13 / その14 / その15 で確認済み)

- **★長い散文は Write ツールでスクラッチに `.py` を書き、`python file.py` で本文に差し込む。**
  **その15 はこの方式で 8 回とも通した**(`cat > file <<'EOF'` は落ちる)
- **★`python -c "..."` の中にバッククォートを書くと bash がコマンド置換として食う**
- **★`print()` に em dash や日本語を流さない**(この環境の Python の stdout は cp932)。
  **検証の出力は ASCII だけにする**
- **★`.md` を編集したら python でバイト単位の CR 数を数える**(`.gitattributes` は `* text=auto eol=lf`)。
  **書き戻すときは `newline="\n"` を明示する。**
  **入れ子太字は「追記した文字列だけ」に `\*{3,}` を掛けて数える**
- **★Markdown の表のセルに `(1 | seed)` のような縦棒を書くときは `\|` に escape する**(その15)
- **★`Rscript -e '...'` の二重引用符が Windows で壊れる。**R は `.R` ファイルにして回す
- **`logs/CHANGELOG.md` は古い順である**(新しいものが末尾)
- **`pytest code/tests -q` は 24 〜 59 秒**(811 tests)。`timeout` を伸ばす
- **★`glmer` の 1 回の当てはめは 1 秒 〜 1 分。**測定中に `pytest` を並走させない(F51)

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

- **★`θ` の値**(ADR-041 決定2・決定3)。**順5 を回す前に決まっていなければならない。
  表を見てから決めると事後選択になる。これが順5 の唯一の残りブロッカーである**
- **★Phase 1 本実験 40 run の GPU 構成**(ADR-057 決定2 の帰結)
- **解析側の凍結手段**(ADR-058 決定2 の帰結)。`renv.lock` を repo に置くかどうか
- **★2**(`plans/PLAN-013` §4)/ **S5**(同 §9。**★事前分布と連動する。**
  傾きを置くと相関行列の事前分布が増える。ADR-061 のリスク欄)/
  **F15 の扱い** / **N1 / N2 / N3〜N6 / #7**
- **解析門の閾値**(ADR-054 決定1 (ii)。N5 と同じ場)
- **`Documents/05_STATISTICS.md` §5 の Δ 5 行すべて**(順6 の実測待ち)
- **LoRA グリッドの値** / **`K` を掃引軸にするか** / **凍結(段階 D)をパイロットの前か後か** /
  **`revision` に `[MATCHED]` タグを付けるか**
- **`Documents/00_OVERVIEW.md:7` の問いが二値のまま**(ADR-056 が人間に上げた)
- `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5

### ★「本筋との整合」の指摘(★A = ADR-056、★D = ADR-058 〜 061 で閉じた)

| 記号 | 指摘 | 状態 |
|---|---|---|
| **★B** | **ゲートキーピングが主問いの判別子を門の後ろに置いている。**主要検定(方向を持たない df=6 の LRT)が非有意なら全部「探索的」に降格するが、**非有意は研究仮説 H1 が予測する結果でもある。**案: 順7 を門の外に出す / 門の条件に TOST を加える / 共主要 2 本に α を分割 | **未決** |
| **★C** | **解析門が DiD のベースライン腕そのものに掛かっている**(ADR-054 決定1 (ii))。**従属変数での選択。**案: 門を掛ける量を DiD の腕から外す。**N5 で閾値を凍結するときが決め時** | **未決** |
| **★D** | 当てはめエンジンが無い | **→ 閉じた。**D-1 = 案 A / D-2 = 同じエンジン / D-3 = 解析を切り離す / D-5 = 再起動で検証した失敗のみ + 閾値確定 / D-4 = 条件付きベイズ併記 / **D-6 = 事前分布を数値で明示(ADR-061)。残るのは実装だけである** |
| **★E** | **F15 は設計判断ではなくバグである**(`torch.manual_seed` が 0 件)。**ADR-055 決定2 の根拠がそのバグに乗っている。**案: バグとして直し、**決定2 の結論は変えず根拠を統計的な理由に差し替える** | **未決** |
| **★F** | **シード配分が主問いの判別子に薄い**(`arb` は 5 シード) | **未決** |

### 順6(評価プールを使う段)の前提 — 順5 ではなくここに置く

**ADR-057 決定4。どちらも `comparison` 群(= 評価プール)を要求するので掃引では成立しない。**

| # | 前提 | 備考 |
|---|---|---|
| **1** | **バッチ fp ノイズ検査**(ADR-040 決定7) | **GPU が要る** |
| **2** | **preflight の `forced choice tokens`**(ADR-047 実装ノート 5) | **★重みは読まないのでローカルで取れる** |

**★`ADR-044`(`infra/requirements.lock` の凍結)は順5 のままである。**
**★ローカル環境**: **R 4.6.1 と `lme4 2.0.6` が入っている**(その11 で導入。その13 で再確認)。
**`rstanarm` / `brms` は入れていない**(ADR-061 の未検証欄)。
`statsmodels` は外したままである。**`infra/requirements.lock` は無変更である。**
