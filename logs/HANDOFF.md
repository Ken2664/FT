# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-22 / 直前セッションの役割: IMPLEMENTER
直前セッションが終了した理由: コンテキスト超過(実測 約140k トークン。ADR-015 の打ち切り水準)

> 使い方: `/clear` してから、下の `---` より後ろをそのまま貼る。

---

あなたは IMPLEMENTER です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**承認済の変更を `plans/PLAN-001-eval-battery.md` の本文に反映し、ADR と config を揃える。**

直前セッションで人間が承認待ち1〜4と変更A〜Dをすべて承認したが、**記録しただけで本文は
旧仕様のまま**である。承認内容は PLAN-001 §12「承認の結果」と §13 にある。まずそこを読むこと。

完了条件は次の4つ。

1. **PLAN-001 §4.1 を改訂**(変更 A)。外挿域を `|·| ≤ 999` の決め打ちから
   **「素のモデルの `correct_rate ≥ θ` を満たす最大の域のうち主域 `[−99, 99]` と交わらない部分」**
   による定義に変える。`θ` は人間が決め ADR に記録する、と書くこと(値を決めない)
2. **PLAN-001 §4.2 を改訂**(変更 C)。内挿ホールドアウトを「主域の 20% を予約」から
   **「訓練サンプラが引く `K` 組の補集合」**に変える。**「20%」という数字を本文から消すこと**
3. **PLAN-001 に §4.6 を新設**(変更 B)。**パイロット専用の項目プール**を定義し、
   本番プールと交わらないことを `manifest.json` とテストで固定する旨を書く
4. **`logs/DECISIONS.md` に ADR-016 を起草**(承認待ち1)。
   `rule_rate` を固定参照規則に対して定義する。主要評価項目では参照規則 = `p2`。
   `metrics.json` は参照規則ごとに独立した4値ブロックを持ち、**各ブロック内で合計 1.0**。
   最新の ADR は ADR-015 なので次は 016
5. **`configs/template.yaml` に3項目を追加**(変更 D + 承認待ち1・4)。すべて `null` で置く:
   - `eval.reference_rule`(必須。既定値を作らない)
   - `eval.elicitation`(`direct` | `cot`。主要評価項目は `direct`)
   - `data.coverage_k`(訓練データに現れる異なる順序対の数。`train_size` だけでは訓練分布が決まらない)

**評価ハーネスの実装(`code/eval/`)には入らない。**それは次のセッション。

## 直前セッションで確定したこと

- PLAN-001 の4つの完了条件(値域 / 項目カテゴリと項目数 / 4値分解の判定 / パーサの責務境界)は
  記入済み。**§5.1〜§5.6 は改訂不要**
- **承認待ち1〜4 と変更A〜D はすべて承認済**(PLAN-001 §12・§13、`STATE.md` の解決済み表)
- 懸念1(値域が FT に広すぎる)は「値域が広い」問題ではなく**「訓練サンプラを一様にしてしまう」問題**
  だった。値域 `R` と被覆 `K` は独立に選べる。`K=500` / `train_size=10,000` なら1組あたり
  20 回反復し、ホールドアウトは 39,101 組(99%)になる。**この論理を再導出しないこと**
- 懸念6(外挿域と算術能力の交絡)は **FT を一切回さずに前方計算だけで潰せる**
- **実験結果の数値はまだ1つも無い。`results/` は空である**

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-001-eval-battery.md` §12・§13 — **最初にここを読む**。承認内容がすべてある
- `plans/PLAN-001-eval-battery.md` §4.1・§4.2・§4.5 — 改訂対象
- `logs/DECISIONS.md` — ADR-015 の書式を見てから ADR-016 を足す。`tail -60` でよい
- `configs/template.yaml` — 90行。全文読んでよい
- `code/lesion.py:140` — `IdentityLesion` の docstring。ADR-016 の根拠
- `Documents/05_STATISTICS.md` §2 — 主要評価項目。**編集しない**(§4 のゲートキーピング順序への
  追加は必要だが、設計変更なので別 ADR にする)
- 全文 `cat` しないこと(`CLAUDE.md` §10.1)

## やってはいけないこと

- **`θ`(外挿域の閾値)の値を決めること。**人間が Phase 0 の実測後に決め ADR に記録する
- **`K` の値を config の既定値にすること。**`null` で置く(skill `code-style` §1, §5)
- **除外集合を全条件の和集合で計算すること。**`IdentityLesion.coincides` は常に `True` を返すので
  プールが空になる。`p2` / `arb` / `x2` からのみ計算する(PLAN-001 §4.3)
- **`Documents/05_STATISTICS.md` を直接編集すること。**ゲートキーピング順序の変更は別 ADR
- 評価ハーネス本体の実装に手を出すこと(スコープ外)

## 未解決 / 人間の承認待ち

`STATE.md` の「人間の承認・判断を待っている事項」を見ること。要点:

- **承認待ち 3**: `04_EXPERIMENT_PLAN.md` Phase 1 の Go/No-Go の fallback に
  「被覆 `K` を下げる」「訓練値域を狭める」を追加してよいか。**実験条件に触れるので独断で足さない**
- **承認待ち 4**: `K` を掃引軸(実験条件)にするか。config 項目として持つこと(承認済)とは別
- **承認待ち 5(懸念7)**: G1 の「日本語」と Phase 2-B の「多言語(FT は英語のみ)」が重複。
  G1 の日本語を訓練内に置くか外に置くかで Phase 2-B の意味が変わる。**PLAN-001 は未決定**
- 承認待ち 1(PLAN-000 の CRITIC レビュー)、2(ADR-005 への先回り参照)は据え置き
- 2系統目のモデルが未決定。GPU 時間は未実測で 10 GPU時間超の承認は未取得
