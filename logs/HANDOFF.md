# HANDOFF — 次のセッションに貼るプロンプト

生成: 2026-08-27 / 直前セッションの役割: PLANNER
直前セッションが終了した理由: コンテキスト超過(hook `context-guard` が約10万トークンで警告)

---

あなたは **PLANNER 兼 IMPLEMENTER** です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**`plans/PLAN-004-phase0-route.md` の順0 を「完了」にする。**
2026-08-27 に人間が5件すべて決定した(下の「直前セッションで確定したこと」)。
**決定は下りているが ADR がまだ1本も書かれていない。**これを書き、§12-11 だけをコードに反映する。

完了条件(この5つが全部そろって初めて順0 は完了):

1. **`logs/DECISIONS.md` に ADR を3本追記する**(判断1つにつき1本)
   - **ADR-034**: `p2`/`p2d` 判別不能の除外を **`K` の抽出母集団には掛けない**(評価項目にだけ掛ける)
   - **ADR-035**: T1 は素の書式のまま + **指示付き T1 の副次セル**を足す / 被演算子 1 の除外を
     **評価項目のみ**全タスク型に広げる
   - **ADR-036**: **G7 を落とす**(§11-11 = 案 B)/ Feucht et al. は **Intro の対立軸としてのみ引用**(§11-16 = (a))
2. **`code/data_gen/ft_data.py` の `K` 抽出から `p2d` 除外を外す**([ft_data.py:636-653](code/data_gen/ft_data.py))。
   `eligible_pairs(...)` を通すのは**評価項目の側だけ**にする。`indistinguishable_pairs_of` は消さない
3. **テストを更新する。**`code/tests/test_design_facts.py` の **393 / 4309 は正しくなる方**なので触らない。
   足すのは「**本番経路の `K` が 393 carry を持つ**」ことを固定するテスト(いまは経路が違うので通っていない)
4. **文書を追随させる**(下の一覧)
5. `pytest code/tests -q` が通り、`python infra/preflight.py --config configs/smoke.yaml --dry-run` の
   `data_checks` が PASS のまま。**`PLAN-004` の手順表 順0 を `完了(2026-08-27, commit)` に更新する**

**やらないこと**: §11-18 の副次セルと §11-19 の評価側除外の**実装**。これは**順4**(項目生成と
評価プールの作り直し)の仕事であり、順1・順2 に依存する。**このセッションでは ADR に仕様を書くだけ**。

---

## 直前セッションで確定したこと

### 人間の決定(2026-08-27。逐語)

| # | 事項 | 決定 |
|---|---|---|
| **PLAN-002 §12-11** | `p2`/`p2d` 判別不能の除外を `K` の抽出母集団に掛けるか | **掛けない** |
| **PLAN-003 §11-18** | T1 にも答え書式の指示を足すか | **現状維持 + 副次セル** |
| **PLAN-003 §11-19** | 被演算子 1 の除外を全タスク型に広げるか | **評価項目のみに広げる** |
| **PLAN-003 §11-11** | G7(周期的概念)の扱い | **B(落とす)** |
| **PLAN-003 §11-16** | Feucht et al. (2026) の位置づけ | **(a) Intro の対立軸としてのみ引用** |

### 決定の帰結(ADR に書くこと)

- **#13(`arb` ズレ表の `table[1]` の穴)は消滅する。**G7-H の `x=0, n=1` が `t=1` を要求していたのが
  唯一の理由だった(PLAN-004 §3 順0 の「副次の帰結」)。**ズレ表の定義域は `[2,198]` のまま**
- **#17(Nikankin et al. 2025 の原典確認)が必須になる。**Feucht を Intro のみに降ろした以上、
  対立軸の一方を Nikankin が単独で支える。現在 `02_RELATED_WORK.md` A 表で **⚠️(要原典確認)**
- **レビュー R7(周期概念×算術特徴の査読済み文献を1本原典確認)は不要になる**(G7 を落としたため)
- **PLAN-002 §5.1(G7 の項目構成 165 項目)と §12-3 は廃止**。打ち消し線 + 理由 + 日付で残す(`CLAUDE.md` §2)
- **§11-18 の副次セルの仕様**(ADR-035 に書く): T1 と同一の被演算子対に
  `End your reply with "Answer: <number>".` を付けた版を **`id` × `carry`/`nocarry` の2セル・n=40 = 80 項目**。
  **主軸の交互作用モデルには入れない**(探索的。§4.8 と同じ扱い)。目的は「指示の有無」の効果量の実測

### 数え上げ(2026-08-27。組合せ論的な計数であって実験結果ではない。`results/` には置かない)

`pool_split_seed = 20260822` / main 領域 4,801 組 / `K_main = 2000`。

| | (a) 掛ける(旧実装) | **(b) 掛けない(採択)** |
|---|---|---|
| main 抽出母集団 | 3,894 組 | **4,309 組** |
| `K` の carry | 435(21.8%) | **393(19.7%)** |
| 母集団が張る和 `t` | 157 種 | **174 種** |
| `K` が張る和 `t` | 154〜155 種 | **170〜171 種** |
| `id` セル候補 | 2,000 組(carry 435) | **1,821 組(carry 393)** |

- **`id` 要求は 520 組(うち carry 240 組。PLAN-003 §4.7)。(b) でも 1.6 倍の余裕があり、埋まらないセルは無い**
- 被演算子 1 を評価側で除くと `id` 候補は 1,821 → **1,788 組**(carry 383)。**要求 520 に対し影響なし**
- 評価主域は 35,640 → **35,283 組(−1.0%)**。`interp` セルは 1セル 40 件なので桁違いに余る
- **`K` にも 1 を除くと答え1桁の層が 6 → 3 組に潰れる**(訓練域の1桁層 36 組中 15 組が被演算子1)。
  **今回はこれを採らなかった**

### このセッションで見つかった不整合(ADR-034 の根拠に使う)

1. **PLAN-002 §4.2.1 の「`t ≡ 0 mod 10` の穴(20 個の和)」は 17 が正しい。**
   `[2,198]` の 10 の倍数は 19 個で、うち 20 と 70 は既に `T_hold` に入っている
2. **設計事実テストと本番経路が食い違っていた。**[test_design_facts.py:262](code/tests/test_design_facts.py)
   と PLAN-003 §4.7(「carry を約 391 組含む」)は **`p2d` 除外を通さない母集団**の数だが、
   [ft_data.py:636-653](code/data_gen/ft_data.py) の本番経路は通していた。
   `configs/smoke.yaml` が `digit_modulus` を持たないので除外が発火せず、表に出ていなかった。
   **決定が「掛けない」なので、直すのはコードの側である**

### 新しく見つかった未決(登録が要る)

- **#22(新規): LoRA アダプタを `runs/<id>/` に残すか。**`infra/RUNPOD.md` §4「必ず残すもの」に
  アダプタが無く、`adapter` / `アダプタ` は `RUNPOD.md` / `04_EXPERIMENT_PLAN.md` / PLAN-002 / PLAN-003 の
  **どこにも現れない**。このままだと 40 run の後に評価を足すには**再訓練**が要る。
  容量は LoRA なので小さいはず。**人間の判断事項**(`CLAUDE.md` §8。コスト管理に触る)

---

## 触ってよいファイル / 読むべき範囲

**全文 `cat` しないこと。**`grep -n` で節を特定してから `sed -n 'X,Yp'`。

| ファイル | 範囲 | 何をするか |
|---|---|---|
| `logs/DECISIONS.md` | 末尾 | ADR-034 / 035 / 036 を追記 |
| `code/data_gen/ft_data.py` | 636-653 | `K` 抽出から `eligible_pairs` の `p2d` 除外を外す |
| `code/tests/test_design_facts.py` | 225-270 | **393 / 4309 は触らない。**本番経路を通すテストを足す |
| `plans/PLAN-002-ft-data.md` | §4.2.1(258-320)、§5.1、§12 | ⚠️帰結2件を書き換え / G7 を廃止 / §12-11 を決着に |
| `plans/PLAN-003-redesign.md` | §8.4、§11 | #11 / #16 / #18 / #19 を決定済み表へ。#13 を消滅と記す |
| `plans/PLAN-004-phase0-route.md` | §2 手順表、§3 順0、§7 | 順0 を完了に。#22 を §5 に登録 |
| `configs/template.yaml` | 57-61 | **コメントが「この除外は `K` の抽出母集団にも掛かる」と書いている。逆に直す** |
| `logs/DECISIONS.md` ADR-022 / ADR-029 | 841-898 / 1193-1260 | ADR-022 決定3 の適用範囲を明文化。ADR-029 根拠表の 7,916 に注記 |
| `Documents/02_RELATED_WORK.md` | A 表の Feucht 行 | 位置づけを (a) に。**引用そのものは消さない** |
| `Documents/06_THREATS.md` | T13(200-240) | G7 を落とした帰結を反映(「G7 と Q10 は探索的」の G7 部分が消える) |

**`Documents/00_OVERVIEW.md` / `01_HYPOTHESES.md` / `08_FUTURE_DIRECTIONS.md` の Feucht 依存記述は
論文2・3(将来方向)のものなので、今回は触らない。**論文1の Intro に効くのは `02_RELATED_WORK.md` と
`09_PAPER_PLAN.md` だけ。

---

## やってはいけないこと

- **`test_design_facts.py` の 393 / 4309 / 0.200 を書き換えない。**「掛けない」の採択でこれらは正しい数になった。
  書き換えるべきは **7916 を含む test_fact_10 の位置づけ**(評価側の数として残す)と、本番経路のテスト追加
- **G7 のコードを消しに行かない。**そもそも実装されていない(`battery_items.py:38` の
  `SUPPORTED_GROUPS` は4群のみ)。消すのは**文書だけ**
- **§11-18 / §11-19 を実装しない**(順4 の仕事)。ADR に仕様を書いて止める
- **数え上げの数値を `results/` に置かない**(`CLAUDE.md` §2。組合せ論的な計数であって実験結果ではない)
- **`arb` のズレ表を作りに行かない。**#13 は消滅したが、表そのものはまだ未確定(`arbitrary_table: null`)

---

## 未解決 / 人間の承認待ち(`CLAUDE.md` §8)

**順0 は片付いた。次に人間の入力が要るのは以下。**`STATE.md` 段階 B と `PLAN-004` §5 が正本。

| # | 事項 | いつ要る |
|---|---|---|
| **20** | 生成設定(`model.dtype` / `max_new_tokens` / デコード / few-shot 数) | **段階 C の前** |
| **21** | 本番の評価テンプレート集合(T1b / T3 の確定文面) | **段階 C の前** |
| **22** | **(新規)** LoRA アダプタを `runs/<id>/` に残すか | 順8 まで |
| **9** | 適格性フィルタの閾値 0.70 | 凍結。**実測より前**(PLAN-004 §6) |
| **15** | 外挿域の上限 `M*` | 段階 C の最初(C-1 で実測) |
| **17** | Nikankin et al. の原典確認(SCOUT) | 凍結。**#16 = (a) で必須化した** |
| **10** | W6 の分岐 | Go/No-Go 実施時 |
| — | 検出力分析の再導出(df = 6) | 凍結 |

**GPU は1秒も使っていない。`results/` は空。事前登録の tag は無い。**
