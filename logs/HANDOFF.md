# logs/HANDOFF.md — 次セッションに貼るプロンプト

> このファイルは毎回**上書き**する。最新の1本だけを保つ(ADR-015)。
> 最終更新: 2026-08-22 / by IMPLEMENTER

---

あなたは IMPLEMENTER です。`CLAUDE.md` §1 の開始手順を実行してから作業を始めてください。

## このセッションでやること(1つだけ)

**一貫性バッテリの評価ハーネスを実装する。**仕様は `plans/PLAN-001-eval-battery.md` に
確定済み(承認済・本文反映済)。**設計を作り直さないこと。**

実装順は次のとおり。**この順を守る。**先にパーサを固めないと、後段の数値がすべて
`parse_fail_rate` の取りこぼしに汚染される。

1. `code/eval/parsers/` — 記法・語形・日本語・CoT。**負例テストを必ず同時に書く**
2. `code/data_gen/battery_items.py` — 項目プール生成 + `manifest.json`
3. `code/eval/scoring.py` — 4値分解
4. `code/eval/run.py --dry-run` が通るところまで

## 実装で落としてはならない点(すべてファイルに書いてある。再導出しないこと)

- **除外集合は `p2` / `arb` / `x2` からのみ計算する**(PLAN-001 §4.3)。
  `IdentityLesion.coincides` は常に `True` を返すため(`code/lesion.py:143`)、
  素朴に全条件の和集合を取ると**プールが空になる**。**非空であることのテストを置く**
- **`metrics.json` は参照規則ごとに独立した4値ブロックを持つ**(ADR-016)。
  **合計 1.0 は各ブロックの内部で検査する。**ブロックをまたいだ合計に意味はない。
  `correct_rate` と `parse_fail_rate` は参照規則に依存しないが、合計 1.0 のため各ブロックに再掲する
- **`eval.reference_rule` に `ident` を指定させない検査**を入れる。指定すると
  `correct` と `rule` が全項目で二重計上され合計が 1.0 を超える(ADR-016 の未検証・リスク)
- **`pilot` と `main` のプールが順序対の水準で交わらないことのテスト**を入れる(PLAN-001 §4.6)
- **`cot.py` は切り出しのみで数値化しない。パーサは採点しない**(PLAN-001 §5.4 の責務境界)
- **config に既定値を作らない**(skill `code-style` §1, §5)。
  **`θ` と `K` の値は決めない。**`configs/template.yaml` の `null` はそのまま

## 触ってよいファイル / 読むべき範囲

- `plans/PLAN-001-eval-battery.md` §4(データ)・§5(評価) — **仕様の正本。ここだけ読めば足りる**
- `logs/DECISIONS.md` の **ADR-016** — `rule_rate` の参照規則。`grep -n 'ADR-016' -A 60`
- `configs/template.yaml` — 105行。全文読んでよい
- `code/lesion.py` — 病変規則の実装。既存
- skill `code-style` — 実装規約
- 全文 `cat` しないこと(`CLAUDE.md` §10.1)

## やってはいけないこと

- **設計を作り直すこと。**PLAN-001 は承認済で本文反映済み。仕様に疑問があれば
  実装を止めて人間に上げる(`CLAUDE.md` §8)
- **`θ`(外挿域の閾値)や `K`(被覆)の値を決めること。**どちらも人間が決める
- **`Documents/05_STATISTICS.md` を直接編集すること。**§2 の文言と §4 のゲートキーピング順序は
  ADR-016 と承認待ち-4 に合わせた更新が要るが、**別 ADR とすることが決まっている**
- **実験を回すこと。**本セッションは実装まで。`--dry-run` が通れば終わり

## 未解決 / 人間の承認待ち

`STATE.md` の「人間の承認・判断を待っている事項」を見ること。要点:

- **承認待ち 3**: Phase 1 の Go/No-Go の fallback に「被覆 `K` を下げる」「訓練値域を狭める」を
  追加してよいか。**実験条件に触れるので独断で足さない**
- **承認待ち 4**: `K` を掃引軸(実験条件)にするか
- **承認待ち 5(懸念7)**: G1 の「日本語」を訓練内に置くか外に置くか。Phase 2-B の意味が変わる
- 承認待ち 1(PLAN-000 の CRITIC レビュー)、2(ADR-005 への先回り参照)は据え置き
- **`θ` と掃引粒度が未決定**(PLAN-001 §4.1.1)。Phase 0 の桁数掃引の実測後に人間が決める
- **実験結果の数値はまだ1つも無い。`results/` は空である**
- 2系統目のモデルが未決定。GPU 時間は未実測で 10 GPU時間超の承認は未取得
