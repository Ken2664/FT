# OPEN-ITEMS.md — 人間の承認・判断を待っている事項(正本)

> **`CLAUDE.md` §8 の対象。エージェントは案を出してよいが、決定・確定・解釈は人間の仕事である**
> (ADR-039 決定3)。
> **このファイルが正本である。**`STATE.md` には索引表(1 行 1 項目)だけを置く。
> 2026-09-08 に `STATE.md` から分離した(ADR-063)。**本文は 1 文字も書き換えていない。**
>
> **決まったら**: `logs/DECISIONS.md` に ADR を書く → 当該項目に打ち消し線 + ADR 番号 + 日付を付ける
> → `STATE.md` の索引表から 1 行消す。**ここから黙って消さない**(`CLAUDE.md` §2)。
>
> **読み方**: 全文は長い。`grep -n '^> \*\*★' logs/OPEN-ITEMS.md` で塊の頭を出してから開く。

---

## 索引(**★2026-09-09(その25)更新。★F90-2 の実装が終わった。新規は ★F112 と実装判断 2 件**)

| 記号 | 一行で言うと | 正本 | 期限 |
|---|---|---|---|
| ~~**★G / ★H / ★I / ★J / ★K / ★L / ★F81**~~ | ~~7 件~~ → **2026-09-08 全件採択(ADR-064)。上げ直さない** | ADR-064 / `plans/PLAN-019` §0.0 | **残るのは実装** |
| ~~**★J の閾値**~~ | ~~Go/No-Go #1 / #5 の値 + 新設する「タスク型間の差」の上限~~ → **2026-09-08 決着(ADR-065 決定1)。#1 = 0.02 据え置き / #5 = 0.10 据え置き / #5b(新設)= `< 0.05`** | ADR-065 決定1 | **反映済** |
| ~~**★G の縮退順序**~~ | ~~案は `coverage 傾き` → `template` → `item`~~ → **2026-09-08 決着(ADR-065 決定2)。`template` → `coverage` の傾き → `item`**(**案とは違う順序を人間が採った**) | ADR-065 決定2 | **反映済** |
| ~~**★`(1 \| seed)` の併記**~~ | ~~`(1 \| seed) + (0 + coverage \| seed)` は同定できない~~ → **2026-09-08 決着(ADR-066 決定1)。`(0 + coverage \| seed)` 単独表記を採る。`(1 + coverage \| seed)` は張る空間・df とも同一だが、ベイズ側の事前分布の対象が変わるので決定2 と一体で読む** | ADR-066 決定1 | **2026-09-08 反映済**(§3.2) |
| ~~**★相関行列の事前分布**~~ | ~~`lkj(1)` か `lkj(2)` か~~ → **2026-09-08 決着(ADR-066 決定2)。`lkj(2)`。主軸 3x3 / 順1b 2x2 の両方。セル平均どうしの相関に掛かる** | ADR-066 決定2 | **2026-09-08 反映済**(§3.2) |
| ~~**★文言訂正 4 件**~~ | ~~事前登録本体の食い違い 4 件~~ → **2026-09-08 反映済**(その21)。(a)(b)(d) は §3.2 + §3.2.3、(c) は §2。**(d) の論拠は先に検査1 で確かめた**(F97 / F99)。**F99 により「交互作用は `u` の影響を受けない」とは書いていない** | ADR-066 決定4 | **反映済** |
| **★L(d)** | `arb` を 5 → 10 シードにするか(**+5 run**) | ADR-064 決定6 | 順6 の後 |
| **★F90 の確認** | ~~「順6 では `s2_seed` が出ない」。**成り立つなら §6.3 手続き2 は実行不能**~~ → **★2026-09-08(その22)に事実確認を終えた。成り立つ**(F100。証拠 5 本)。**残っているのは「では `s2_seed` をどこから取るか」の決定である**(案 A/B/C/D。**下の追記ブロックが要約**) | **`plans/PLAN-019` §10**(決定記入欄は §10.8) | **凍結前** |
| **★F90-1** | ~~`s2_seed` をどこから取るか~~ → **★2026-09-09 決着(ADR-067 決定1)。案 B を採択。**`s2_seed` を 1 点で推定せず、**「10 シードが保つ限界はどこか」を逆問題として事前登録する。**横軸は仮定値であり効果量プロファイル P と同じ扱い | **ADR-067** / `plans/PLAN-019` §10.8 | **実装が残る** |
| ~~**★F90-2**~~ | ~~ADR-067 + ADR-068 の実装~~ → **★2026-09-09(その25)に実装を終えた。**`05_STATISTICS.md` §6.3 手続き2〜5 / §6.4 / §6.6 を差し替え、**§6.7(掃く仮定値の範囲)を新設**。`code/analysis/power_sim.py` + `power_sim_fit.R` + `configs/power_sim*.yaml`。`pytest code/tests -q` = **874 passed**。**★本実行は回していない**(null で止まる) | `plans/PLAN-019` §10.10 | **済** |
| ~~**★F112**~~ | ~~§6.5 の表から非加法性 RMS が復元しない~~ → **★2026-09-09(その26)に逆問題を解いた。丸める前の 12 セルは §6.5 の中に 2 桁で載っていた**(表の上の 1 文 + 「勾配 g」の列)。**復元値は 4 プロファイルとも 12 セル・勾配・RMS・最大残差のすべてに当たる**(P1 = 0.352767 / **P0 = 厳密に 0**)。**残る問いは「config を差し替えるか」だけである** → **★★2026-09-09 決着(ADR-069 決定1)。案 (a) を採択し、`configs/power_sim*.yaml` に `effect.eta` を新設して DGP の真値にした。`rule_rate` は照合用の門として残る。RMS = 0.352767 / P0 は厳密に 0** | **ADR-069 決定1** / `plans/PLAN-019` §10.10.4 | **実装済** |
| ~~**★実装判断 3 件**~~ | **(1) §6.3 手続き4 のシード掃きを 10 のみに畳んだ**(残すと 24,000 → 96,000 本。条件付きの補足を事前に固定した)/ **(2) ADR-059 が書いていない「当て直した場合どちらを LRT に入れるか」を『当て直したほう』にした**(config で切替可)/ **(3) DGP の `template` 割り当てを巡回にした** → **★2026-09-09 に人間が 4 件とも承認(ADR-069 決定4)。**(1) の条件付きの補足(`sigma*` が内側なら 5 / 8 / 15 を追加)も承認。**★(2) の穴は未実装の `code/analysis/primary.py` にも同じ形で効く**(実装時に本決定を適用する) | **ADR-069 決定4** / `plans/PLAN-019` §10.10.2 | **済** |
| ~~**★掃く範囲**~~ | ~~逆問題の横軸に置く仮定値の範囲そのもの~~ → **★★2026-09-09 決着(ADR-068 決定3)。R3。`sigma` = 0 / 0.25 / 0.5 / 0.75 / 1.0 / 1.5。★すべて仮定値であり、表の見出しに明示する**(効果量プロファイル P と同じ扱い)。**文献を 1 件も借りていない** | **ADR-068** / `plans/PLAN-019` §10.7.7 | **実装が残る** |
| ~~**★F103-1 / ★F103-2**~~ | ~~シミュレータの模型が §3.2 に追随していない~~ → **★★2026-09-09 決着(ADR-068 決定1・決定2)。F103-1 = (a) 両方合わせる(DGP も当てはめも §3.2 の形。ADR-059 の縮退規則もループに入れる)/ F103-2 = (d) 交換可能 `(sigma, rho)`、`rho ∈ {0, 1}` の 2 枚** | **ADR-068** / `plans/PLAN-019` §10.7 | **実装が残る** |
| ~~**★F113**~~ | **§5 の Δ 5 行のうち 4 行が「順6 の実測」を出所に名指ししているが、順6 は段階 C(FT なし・素のモデル・タスク6 は T2 のみ)であり、****4 行が定義する量はすべて `rule_rate`(病変 FT 後)である。**★F90 / ★F104 と同じ形の穴が §5 に残っていた。**#4 / #5 は `arb` / `p2` / `p2d` を名指ししており条件そのものが無い。****残る 1 行(勾配が平行)は §6.5 の P1 から決まり順6 に依存しない。**★順6 の GPU が要らなくなるという話ではない(Go/No-Go #0〜#3 は順6 でしか出ない) → **★★2026-09-09 決着(ADR-069 決定2)。案 (a) を採択。**§5 に「順6 からの移し替え」の節を新設し、**移し替えの 3 仮定を弱点として明示した。**旧記述は打ち消し線で残る。**★3 仮定は検証されないまま凍結に入る**(論文の弱点欄) | **ADR-069 決定2** / `Documents/05_STATISTICS.md` §5 | **反映済** |
| ~~**★F114**~~(実行先は下へ) | **当てはめ 1 ペアをこの機械で測った。**F50 と同じ 5,760 行で **297.6 秒**(F50 は 31.91 秒 = **9.3 倍**)。**§10.7.8 の「24,000 本 = 約 9 時間」は実測では約 62 時間**(96,000 本なら約 248 時間)。**★`power_sim.py` に並列実行が無く、逐次では 992 時間になる。****縮退の発火率にも初めて数字が付いた**(σ=1.0 で 0.50) → **★2026-09-09 決着(ADR-069 決定3)。並列化だけ先に実装した**(`shard_jobs` / `run_fits` / `fit.n_workers` / `--workers`)。**★実行先は未決のまま下の行へ送った** | **ADR-069 決定3** / `plans/PLAN-019` §10.12 | **並列化は実装済** |
| **★F114 の実行先** ★新 | **並列化は済んだが「どこで 62 時間回すか」は未決である。**この機械(16 コア)か、CPU の多い RunPod ポッドか。**CPU なので `CLAUDE.md` §2 の「10 GPU時間超」の承認対象ではないが、黙って始めてよい量ではない。**★本実行はどのみち `dgp.n_item`(`M*`)と `dgp.s2_item` / `s2_tmpl`(★F104)で止まっているので、**その 2 本が決まってから諮るのが自然である** | ADR-069 決定3 / `plans/PLAN-019` §10.12.5 | 本実行の前 |
| **★★F104** ★**決定材料ができた** | ~~`s2_item` / `s2_tmpl` も順6 から取れるとは言い切れない(DV が違う)~~ → **★2026-09-09(その28)に決定材料を作った。問いの形が変わり、4 行に分かれた**(記入欄は `plans/PLAN-019` §10.13.5、1 ページ要約は §10.13.0)。**★F115: 順6 の形では `s2_item` の同定が効かない**(1 項目 1 観測では profile 尤度が完全に平ら / 温度 0 の反復は一致する限り情報を持たない)—— **「移し替えてよいか」以前に元の量が立つ保証が無い。★F90 と同じ形の穴。****★F117: `task:coverage` の交互作用へ直接入るのは `s2_item` だけ**(`s2_tmpl` も `sigma` も この水路では厳密に 0)—— **2 つを 1 つの決定にする理由が無い。****★F118: `s2_tmpl` に 0 に近い値を置くのは安全な既定ではない**(縮退カスケードの第1段が `template`)。**★賭け金**: `n_item = 48` / `s2_item = 1.0` で 交互作用に乗る非加法性 RMS が **P1 の 39%**、しかも**シード数では縮まない**(縮むのは `n_item` = `M*` 待ちのみ)。**★コスト**: 案 B(格子に足す)は本実行が k 倍(**62 → 186〜372 時間**)、案 C(1 点)は 62 時間のまま | **`plans/PLAN-019` §10.13**(材料)/ `plans/PLAN-019-check3/`(検査。**合成データ**) | **凍結前。★F114 の実行先より先** |
| **PLAN-018 §4.3** | 「現在のブロッカー」の組み直しと Phase 0 の要約(**とくに F87**) | `plans/PLAN-018` §4.3 | いつでも |
| ~~**★B**~~ | ~~ゲートキーピングが主問いの判別子を門の後ろに置いている~~ → **2026-09-08 決着(ADR-064 決定6)。順7 を門の外へ出した。`05_STATISTICS.md` §4 に反映済** | ADR-064 決定6 | **反映済** |
| **★C** | 解析門が DiD のベースライン腕に掛かる(従属変数での選択) | N5 と同じ場 | 凍結前 |
| **★E** | `torch.manual_seed` が 0 件(シードが再現しない) | 下記 | 本実験前 |
| **★F** | シード配分が主問いの判別子に薄い(`arb` は 5 シード) | ★L と同じ場 | 本実験前 |
| ~~**★`θ`**~~ **決着** | ~~外挿域の閾値(1 個の数)~~ → **★2026-09-09(その29)に決定材料を作った。問いの形が変わり、4 行に分かれた**(記入欄は `plans/PLAN-020` §7、1 ページ要約は §0)。**★F120: 主軸 3 水準目 `extrap_magnitude` の母集団は `(M* − 99)²` しかない** —— `M* = 100` で **1 組**、110 で 121 組。セル表の要求は **520 組(carry 層 240)**。**C6 が組める最小の `M*` は 134、格子上は 150。****★ADR-041 決定3 規則5 は `M* < 100` にしか分岐が無く、格子点 100 / 110 / 125 はその外側にある** —— 規則どおり進むと `fill_cells` が**順5 の後・凍結の直前に**例外で止まる。**★F121: `correct_rate(M)` は殻ではなく `R(M)` 全体の累積平均である**(`M = 100` では引かれる組の 98.0% が主域の中)。**仮定シナリオ S1 では θ = 0.50 〜 0.90 のどれを選んでも `M*` が上の領域に落ち、外挿セルは素のモデルの正答率 0 の項目で埋まる** —— **`plans/PLAN-001` §4.1.1 が潰したと宣言した懸念6 そのものである。****★殻の正答率は追加の GPU 時間 0 で取れる**(`predictions/` に `operands` が残る)。**★θ-0(いつ決めるか)自体が記録の中で食い違っており、どの ADR も決定として書いていない** | **`plans/PLAN-020` §7**(記入欄)/ `plans/PLAN-020-check1/`(検査) | **順5 を回す前**  → **★★2026-09-09(その30)決着(ADR-070)。人間が 4 行すべてを採択した: θ-0 = A2(順5 の前に確定 + θ±0.05 の `M*` を副次で事前登録)/ θ-a = **0.70** / θ-b = B1(C6 が組めないなら外挿を取り下げ df 6→3)/ θ-c = **C2(`M*` の判定を殻の正答率に変える)**。★ただし ADR-041 決定2 が要求する「`#9` とは別の根拠」は未記入。★C2 は測定を一意に定めないので下の 2 件が新しく開いた。** |
| ~~**★殻の定義**~~ **決着** | **θ-c = C2 の帰結。**`M*` の判定に使う「殻」を `S(M) = R(M) \ R(1つ前の格子点)` とするか、`R(M) \ R(99)`(主域の外側全体)とするか。**前者は格子に依存し、後者は `M` が大きいほど古い殻を混ぜ続ける。**どちらも `plans/PLAN-001` §4.1.1 の推定量の定義に入る **★2026-09-09(その31)に決定材料を作った**(`plans/PLAN-021` §4.1。記入欄は §6)。**★案 C(`Q(M)` = `a>99` かつ `b>99` = 外挿腕の母集団)をエージェントが足した。****★F123: 定義 B の殻のうち外挿腕が実際に使う組は `M=150` で 5.1% / `M=200` で 8.4% しかない。****★F124: 定義 B は台地アンカー 4 点(25/50/75/99)で殻が空になり、規則2 が回らない。****★M = 100 の殻は定義 A でも B でも 800 組で同じである**(選択の余地がない) | **`plans/PLAN-021` §6**(記入欄)/ §3(検査1)/ ADR-070 | **順5 を回す前** → **★★2026-09-09(その31)決着(ADR-071 決定1)。案 D を採択** —— 判定は定義 C(`Q(M)` = `extrap_magnitude` の母集団)、報告は現行の一様抽出(累積)と定義 A を併記。**提案 エージェント (Opus) / 採択 人間(一括採択)。** |
| ~~**★殻あたりの項目数と抽出経路**~~ **決着** | **同じく C2 の帰結。**(a) 抽出は現行のまま(`R(M)` から一様 200 件)で `predictions/` を殻で切り直す(**追加 GPU 0**)か、(b) **殻から 200 件引く**ように抽出仕様を変える(**ADR-041 決定5 の変更**)か。**★(a) は `M = 100` の殻に 1,000 項目中およそ 20 項目しか落ちず(殻は `R(100)` の 2.0%)、二項 SE は `p=0.7` で約 0.10。**(b) なら全水準 1,000 項目で SE 約 0.014 **★2026-09-09(その31)に決定材料を作った**(`plans/PLAN-021` §4.2)。**★F122: 上の「(a) は追加 GPU 0」は (b) に対する優位になっていない** —— **順5 は 1 度も回っていないので (b) も同じ 13,000 項目の 1 回の run で済み、GPU 時間は同じ**(承認済 1.5h の中)。**さらに (a) 自身が ADR-041 決定5 の変更である** —— 決定5 は「**n は M 間で同一**」を明文で要求しているが、(a) の n は **19.8 〜 1,000 件で 51 倍動く**。**(a) と (b) の差は GPU ではなく、決定5 のどの条項を変えるかである。**(a) の実の利点は「累積表がそのまま残る」ことだけで、案 (c)(両方引く)はそれを GPU 約 2 倍で買う | **`plans/PLAN-021` §6**(記入欄)/ §3(検査1)/ ADR-070 | **順5 を回す前** → **★★2026-09-09(その31)決着(ADR-071 決定2)。案 (c) の限定版を採択** —— 現行の 13,000 項目を変えず、`|Q(M)| >= 200` の 7 水準に `Q(M)` から 7,000 項目を足す(合計 20,000。**GPU 1.5 → 2.0〜2.5h。10h の門は超えない**)。**ADR-041 決定5 に打ち消し線は要らない。** |
| ~~**★殻が空・極小の水準の扱い**~~ **決着** | **★F124 が開いた。ADR-070 は触れていない。**ADR-041 決定3 規則2 は「**小さい順に見て**初めて θ を割った水準の 1 つ下」を採るが、**定義 B では 25/50/75/99 の殻が空、定義 C では `M=100` が 1 組・`M=110` が 121 組**であり、そこで規則2 が何を見るかが決まっていない。案は c1(走査から除く)/ c2(その水準だけ累積で判定)/ c3(殻の最小 n を決める)/ c4(格子を変える。**規則1 と衝突。推奨しない**) | **`plans/PLAN-021` §4.3**(案)/ §6(記入欄) | **順5 を回す前** → **★★2026-09-09(その31)決着(ADR-071 決定3)。案 c3 を採択** —— 判定水準は「凍結済の 200 件を `Q(M)` から引ける水準」= `{125,150,175,200,300,500,999}`。**新しい数を作らない。** |
| ~~**★★F125**~~ **決着** ~~★新(その32)**順5 の起動を止めている**~~ | **`configs/exp_phase1_main.yaml` の `eval.temperature` と `eval.num_repeats` が `null` である。**`python -m code.eval.sweep --config configs/exp_phase1_main.yaml` は `code/eval/model.py` の `load_generation_settings` で `ConfigError` を出す(**重みを読む前。`--dry-run` は生成設定を読まないので通る**)。**ADR-042 はどちらの値も決めていない** —— 決定2 は `do_sample: false` を正本にし(`temperature` は記録用の欄)、決定3 は `num_repeats` の意味(測るのは装置の非決定性)だけを書いた。`plans/PLAN-001` §5.6 に**提案値**(`temperature` 0 / 本実行 `num_repeats` 1)があるが、**承認の記録は見つからない**(§12 の承認 4 件に入っていない)。**実装は `num_repeats = 1` 以外を受け付けない**(繰り返し生成は未実装)。**同じ形の穴(F20: `reference_rule` / `elicitation`)は PLAN-014 が 2026-09-06 に塞いだが、この 2 欄は拾われていなかった。****エージェントは値を書いていない**(`CLAUDE.md` §8 / skill `code-style` §5) | `configs/exp_phase1_main.yaml:468-469` / ADR-042 決定2・3 / `plans/PLAN-001` §5.6 / `plans/PLAN-014` §5 | **順5 を回す前(クリティカルパス)** → **★★2026-09-10(その33)決着(ADR-072 決定1・決定2)。人間が `temperature` = 0 / `num_repeats` = 1 を採択した**(PLAN-001 §5.6 の提案値どおり。**提案 エージェント / 採択 人間**)。config に記入し、本番 config で `load_generation_settings` が通ることを回帰テストで固定した。**test-retest の反復回数は決めていない** |
| ~~**★ADR-071 の実装判断 3 件**~~ **決着** ~~★新(その32)~~ | **ADR-071 が書いていない箇所をエージェントが実装で決めた。異議があれば覆せる**(`CLAUDE.md` §8)。**(1)** `shell_n_items` が `n_items_per_radius` と違えば止める(決定3 の「**凍結済の** 200 件」を字義どおり読んだ。違う数は新しい閾値になる)/ **(2)** 定義 A の格子殻(**記述**)は**全シード合算**の率で出し、シード別には件数だけ残す(殻に落ちる件数はシードで違い 0 にもなりうるので、シード平均が定義できない。件数 0 の行は 4 値を null)/ **(3)** `shell_*` を宣言していない config(**`configs/smoke.yaml` を含む**)では掃引も dry-run も止まる(判定の材料が無いまま累積の表だけが出るのを防ぐ。★F121)。**(2) と (3) は判定に効かない。(1) は判定水準の導出に効くが、本番 config では両方 200 で一致している** | `logs/CHANGELOG.md`(その32)/ `code/eval/battery/magnitude_sweep.py` の `load_shell_plan` / `code/eval/sweep.py` の `grid_shell_rows` | 順5 を回す前 → **★2026-09-10(その33)決着(ADR-072 決定3)。人間は 3 件とも異議なし。実装のまま承認** |
| **★`θ` の根拠**(値は 0.70 で確定) ★新(ADR-070) | **ADR-041 決定2 が「`#9` と `θ` は独立の決定。同じ数でもよいが根拠は別に書く」と要求している。**`#9 = 0.70` の論拠の移植では足りない。**論文で「なぜ 0.70 か」を問われたときの答えになる** | ADR-070 決定2 | 凍結前(順5 は回せる) |
| **E-5 (b)** | K の出どころを `metrics.json` に焼き込むか | ADR-062 / `code/eval/run.py` | **本実験の前だけ安い** |
| **N5** | 解析門の閾値 | `plans/PLAN-012` | 凍結前 |
| **★2** | (`plans/PLAN-013` §4。案 A〜E + P1〜P3 / Q1〜Q4) | `plans/PLAN-013` §4 | 凍結前 |
| ~~**S5**~~ | ~~`coverage` のランダム傾きを置くか(★G と同じ場)~~ → **2026-09-08 決着(ADR-064 決定4)。`(0 + coverage \| condition:seed)` を置く。★G と同時に決めた** | ADR-064 決定4 | **反映済** |
| **Δ 5 行** | `05_STATISTICS.md` §5 の反証条件の値 | 順6 の実測待ち | **凍結前** |
| **GPU 構成** | Phase 1 本実験 40 run / **順6 を回す承認** | ADR-057 決定2 | **クリティカルパス** |
| — | 効果量プロファイル P の凍結(D5)/ LoRA グリッド / `M*` / 凍結の段の前後 | 下記 | 凍結前 |
| — | `00_OVERVIEW.md:7` の問いが二値のまま / `09_PAPER_PLAN.md` の追随 | 下記 | 凍結前 |
| — | `refs.bib` の Nikankin 書誌の最終確認 / ADR-047 実装ノートの R3 / F1 / R4 / 検査5 | 下記 | 論文化前 |
| **停止中ポッドの terminate** ★新(その35) | **`zxwdkgxutbuoph`**(EUR-IS-2・ポッドローカル 60 GB。**ネットワーク実効 約 70 kB/s で使えない**。中身は repo と作りかけの `/workspace/venv` だけ)と **`46pggs1odwb09r`**(EU-RO-1・ボリューム `r963j7swke` 付き。重みと HF トークンが残る)。**どちらも `EXITED` でディスク課金が続く。**terminate は不可逆なのでエージェントはしない | `logs/CHANGELOG.md` 2026-09-10(その35) | いつでも |

---

---

## ★2026-09-08(その22)の追記 —— ★F90 は成り立つ。決めることは 1 つに絞れた

> **この節は分割前の `STATE.md` に無い。その22(PLANNER / CRITIC)が足した。**
> **決定は 0 件。正本は `plans/PLAN-019-validity-decisions.md` §10、記入欄は §10.8。**
> **数値は 1 つも作っていない。GPU 時間 0。コードは 1 行も触っていない。**

### 人間が読むべきことは、これだけ

**問いだったこと**: `Documents/05_STATISTICS.md` §6.3 手続き2 は
「**Phase 0(順6)の実測から `s2_seed` / `s2_item` / `s2_tmpl` を推定する**」と書いている。
**順6 に FT run は 1 本でも含まれるか。**

**答え(文書の突き合わせで決着した)**: **含まれない。**5 つの記述がすべて同じ向きを指す。
決め手は `plans/PLAN-004-phase0-route.md` §6 罠5 の
**「回す段は3つある: スモーク / 段階 C(本番モデル・FT なし・使える)/ 段階 E → Phase 1(FT あり)」**。
**順6 は段階 C である。**(証拠の全文は PLAN-019 §10.2)

**★ただし但し書きが 1 つある**: **「Phase 0」と「順6」は同じ集合ではない。**
`Documents/04_EXPERIMENT_PLAN.md` の Phase 0 Go/No-Go 表は
**FT を要する #4 / #4b / #5 / #5b(= パイロット・段階 E)を含んでいる。**
つまり **F90 は「順6」については成り立ち、「Phase 0」全体については成り立たない。**
**§6.3 の「Phase 0(順6)」は、広い名前に狭い括弧を付けた書き方だった。**

### ★倒れないもの(先に確認しておくと安心できる)

| 心配 | 実際 |
|---|---|
| 「10 シード」の根拠が消えるのでは | **消えない。**§6.4 が自ら「**本節の再導出は 10 を正当化するのではなく、10 で足りるかを事後に検査する**」と書いている。10 は ADR-028 の設計判断で決まっている |
| ★G(`(0 + coverage \| seed)`)をやり直すのでは | **やり直さない。**ADR-064 決定4 で決着済で、`s2_seed` に依存しない(型 I 過誤の問題であって検出力の問題ではない) |
| 効果量プロファイル P を選び直すのでは | **選び直さない。**ADR-052 決定2 で凍結済(非加法性 RMS = 0.353 logit)。凍結の順序は守られている |

### ★決めること(4 つ。**うち 1 つが本体**)

| # | 問い | 選択肢 | 重さ |
|---|---|---|---|
| ~~**F90-0**~~ | ~~**F90 は成り立つ、で確定してよいか**~~ → **★2026-09-09 に前提として受け入れ**(案 B の採択に含まれる。ADR-067 決定2。**F102 の但し書きは残る**) | 承認 | **決着** |
| ~~**★F90-1**~~ | ~~**`s2_seed` をどこから取るか**~~ → **★★2026-09-09: 人間が 案 B を採択(ADR-067 決定1)** | **B** 仮定値を置き「10 シードが保つ限界はどこか」を逆問題で事前登録 | **決着** |
| **F103-1/2** | シミュレータの模型を §3.2 に合わせるか、合わせるなら 6 パラメータをどう縮約するか | (a) 合わせる ほか | 中 |
| **F104** | `s2_item` / `s2_tmpl` を順6 の `correct_rate` から取ってよいか | ~~確認を求める~~ → **★2026-09-09(その28)に案が付いた。`plans/PLAN-019` §10.13.5 が記入欄の正本**(4 行に分割) | 中 |

**★A / B / C の一行要約**(利害の全文は PLAN-019 §10.6):

- **A(パイロットから取る)**: 本物の FT run 間分散が取れる。**ただしシードが 2〜3 本しかなく、
  かつ `PLAN-004` 順9 が「凍結 → パイロット」の順なので、凍結の後に検出力が出ることになる。**
  **この案を採ると「凍結を段階 E の前に置くか後に置くか」(順9 の第1項目・未決)の答えが
  事実上「後」に固定される** —— **この連動が A の一番重い代償である**
- **B(仮定値 + 逆問題)**: **凍結より前に数値が出て、順序の罠を踏まない。**
  効果量プロファイル P と同じ「仮定値として明示する」扱いで、**前例がある。**
  ただし**横軸に置く値は全部仮定値**であり、文献を借りるなら `CLAUDE.md` §3 の原典確認が要る
- **C(出さない)**: **捏造しない。**`CLAUDE.md` §2 / §7 に最も忠実。
  ただし **§6 の冒頭が自ら「後から説明できないのは弱い」と書いている**、その弱さを引き受ける

**★エージェントは A / B / C のどれも推していない**(`CLAUDE.md` §8 / ADR-039 決定3)。
**D にだけ「推奨しない」と付けた**(段階 C の定義を書き換えることになり、GPU がクリティカルパスに乗る)。

### ★★2026-09-09: 人間が **案 B** を採択した(ADR-067)。上げ直さないこと

**決まったこと**: `s2_seed` を実測から 1 点で推定するのをやめ、**仮定値の範囲で検出力曲線を出し、
「どこまでの `s2_seed` なら 10 シードで検出力 0.8 に届くか」を逆問題として事前登録する。**
**横軸は仮定値であり、`05_STATISTICS.md` §6.5 の効果量プロファイル P と同じ扱いとする。**
**F90-0(F90 は成り立つ)は、この採択の前提として受け入れられた**(ADR-067 決定2)。

**★まだ決まっていないもの(索引表に残っている)**:
**★F103-1 / ★F103-2 は案 B の実装ブロッカーである** —— **逆問題の横軸は、
`(0 + coverage | seed)` に合わせるかを決めないと 1 本に定まらない**(案 B の害③)。
**★F104**(`s2_item` / `s2_tmpl` の取得元)/ **★掃く仮定値の範囲そのもの**。
**★実装(★F90-2)に人間の決定は残っていないが、★F103 が先行条件である。**

---

## ★2026-09-09(その24)の追記 —— ★F103 の決定材料。**★3 件とも採択された(ADR-068)**

> **★★★2026-09-09: 人間が「エージェントの推奨案をすべて承認します」と回答した。**
> **★F103-1 = (a) 両方合わせる / ★F103-2 = (d) 交換可能 `(sigma, rho)`、`rho ∈ {0, 1}` /
> ★掃く範囲 = R3(`sigma` = 0 / 0.25 / 0.5 / 0.75 / 1.0 / 1.5。すべて仮定値)。**
> **★正本は `logs/DECISIONS.md` の ADR-068。上げ直さないこと。**
> **★これで案 B の実装ブロッカーは外れた。次は ★F90-2(実装)で、人間の決定は残っていない。**
> **★以下はその決定材料である(記録として残す)。**

> **その24(PLANNER / CRITIC)が足した。決定は 0 件である。**
> **正本は `plans/PLAN-019-validity-decisions.md` §10.7、記入欄は §10.8。**
> **★人間向けの 1 ページ要約は `plans/PLAN-019` §10.7.0 にある。**この節はその抜粋である。
> **★数値は 2 つの出所しか持たない** —— 既に採択済みの文書中の値(P1 / Go/No-Go #4)と、
> **合成データの検査2**(`plans/PLAN-019-check2/run.log`)。**どちらも実験結果ではない。**
> **`results/` は空。GPU 時間 0。コードは `code/` を 1 行も触っていない。**

### 何が問題か(2 行)

**ADR-067(案 B)は「どこまでの `s2_seed` なら 10 シードで検出力 0.8 に届くか」を
逆問題として事前登録すると決めた。逆問題には横軸が 1 本要る。**
ところが `Documents/05_STATISTICS.md` は、**検定する模型(§3.2 = 6 パラメータの共分散)と
検出力を測る模型(§6.3 = スカラー 1 個)が食い違っている。**6 個は掃けない。

### 人間が決める 3 つ(**それぞれ ⬜ を 1 つ選ぶだけでよい**)

| # | 決めること | 選択肢 | **エージェントの案** |
|---|---|---|---|
| **1** | **★F103-1** シミュレータを §3.2 に合わせるか | (a) **両方**合わせる / (b) 両方据え置く / (c) **当てはめだけ**合わせる | **(a)** |
| **2** | **★F103-2** 6 パラメータの潰し方(**1 で (a) を選んだ場合だけ**) | (a) SD 等値・相関 0 / (b) SD 3 本自由 / (c) 縮約しない / **(d) 交換可能 `(sigma, rho)`** | **(d)。`rho ∈ {0, 1}` の 2 枚** |
| **3** | **★掃く範囲** 横軸をどこからどこまで | R1 狭く密に / R2 対数で広く / **R3 内的アンカー** | **R3。`sigma` = 0 / 0.25 / 0.5 / 0.75 / 1.0 / 1.5**(**仮定値**) |

### 判断の助けになる 4 点

1. **「ADR-064 決定4 の反映漏れ」という読解は正しい。****ADR-064 も ADR-066 も、
   影響欄に `05_STATISTICS.md` の §2 / §3.2 / §4 しか挙げていない。§6.3 は入っていない。**
   ADR-064 は §6.3 を見てはいるが「手続き2(取得元)」だけで、**手続き3(模型)には触れていない**
2. **★F110(新規)。手続き3 の一文は 2 つの仕事を兼ねている** ——「…**からデータを生成し**」(真の世界)と
   「**`task + coverage` との LRT**」(当てはめ)。**真の世界が違うのは誤りではないが、
   当てはめが違うと「回さない検定の検出力」を報告することになる。**これが (b) の主要な害である
3. **★F105(検査2)。逆問題の答えを動かすのは `sigma` であって相関 `rho` ではない。**
   `rho` を −0.4 〜 1.0 で振っても膨張は変わらない(モンテカルロの床の内側)。
   **したがって F103-2 の選択は「SD の水路」では答えを変えない。**
   **★残る争点は「10 シードから 6 パラメータを当てられるか」という同定の水路であり、
   検査2 はそこを測っていない。**案 (a) を採ると、その発火率が凍結前に数値になる ——
   **ADR-064 決定4 の★リスク欄が「誰にも数値では言えない」と書いた、まさにその欄である**
4. **★F111(見立て。未検証)。掃いた範囲の全域で 0.8 を割らない結果になる公算がある。**
   膨張は `sigma = 1.5` でも約 1.2 倍にとどまる。**案 B として正当な結論だが、
   論文で書けることが変わるので、凍結の前に承知しておく必要がある**

### 決めなくてよいもの(**このセッションでは動かしていない**)

- **★F104**(`s2_item` / `s2_tmpl` の取得元)。**★F103 とは独立に決められる**
- **★F90-2**(ADR-067 の実装)。**人間の決定は残っていない。★F103 の後**
- **ADR-028 の 10 シード / ★G / 効果量プロファイル P** —— **どれもこの節では倒れていない**

## 全文(分割前の `STATE.md` 1553-2156 行。1 文字も変えていない)

## 人間の承認・判断を待っている事項(`CLAUDE.md` §8)

> **★★★2026-09-07(その17)の妥当性レビューが開いた項目(6 件)。**
> **正本は `logs/REVIEW-2026-09-07-validity.md` である。ここには要約しか置かない。**
> **決定は 0 件。エージェントは材料を並べただけである**(`CLAUDE.md` §8 / ADR-039 決定3)。
>
> | 記号 | 決めること | 重さ | 追加 GPU |
> |---|---|---|---|
> | **★G** | **主軸に seed のランダム傾きを置くか**(S5 を主軸へ広げる)。**主要検定が反保守的になりうる** | **最重要** | 0 |
> | **★H** | リンク関数を変えた感度解析を事前登録するか(天井効果) | 頑健性 | 0 |
> | **★I** | **適格性フィルタでセルが落ちたときの df を事前に固定する。事前登録の穴** | 穴 | 0 |
> | **★J** | 再正規化 DV の感度解析を足すか / Go/No-Go #1・#5 の閾値を締め直すか | 人工物 | 0 |
> | **★K** | **被演算子対をタスク型間で共有する設計に変えるか。窓はいましか無い** | **重い** | プール再生成 |
> | **★L** | 順7 を門の外に出すか + `arb` を 10 シードにするか(★B / ★F を 1 つの決定に) | `H0` の識別 | +5 run |
>
> **★★G と ★K はどちらも必要シード数を増やす向きに働く。**
> **順6 の実測(`s2_seed` / `s2_item` / `s2_tmpl`)が出るまで最終判断を保留するのが筋である**
> (レビュー §7)。**その順6 が GPU 待ちであり、それが全体のクリティカルパスである。**
>
> **★運用の指摘**: **`STATE.md` が 398 KB / 3,800 行を超えた。**
> **`CLAUDE.md` §10 が全文読解を禁じており、セッション開始時に `cat STATE.md` を実行できない。**
> **引き継ぎ装置としての機能が落ちている。分割かアーカイブが要る**(レビュー §5)。


> **★★2026-09-07(その17)に新しく開いた項目(1 件)。**
> - **★F81: 5 値の `extrap_pair` の定義と名前。**`label_main_coverage` は
>   **`extrap` のうち `extrap_magnitude` でないものを一括して `extrap_pair` と呼ぶ**
>   (ADR-062 決定1 が並べた 5 値の名前をそのまま使った)。
>   **PLAN-002 §4.6 の定義は `extrap_pair := extrap かつ ans_in` であり、**
>   **`(300, 50)`(片側だけ域外・答えも域外)はそこに入らない。**
>   **→ 副次 C5(ADR-027 決定2 / P-2)を組むときは `answer_range == ans_in` と交差させる必要がある。**
>   **主軸(`extrap_magnitude`)には影響しない** —— 定義は ADR-027 決定1 のまま被演算子で書いてある。
>   **決めること: (i) 交差で組むことを PLAN / ADR に明記するか、(ii) 5 番目の水準の名前を変えるか。**
>   **エージェントは実装を ADR-062 の名前に合わせ、docstring と回帰テストに食い違いを固定した**


> **★★★2026-09-07(その16b)に下の 7 件(E-1 〜 E-7)はすべて決着した(ADR-062)。上げ直さないこと。**
> **E-1 = (c) / ★E-2 = (a) / E-3 = (c) / E-4 = (a) / E-5 = (a) / E-6 = (c) / E-7 = (c)。**
> **`plans/PLAN-017-analysis-frame.md` §5.0 に決定表がある。★PLAN-017 の未決は 1 件も残っていない。**
>
> **★この決定が新しく開いた項目(1 件。人間の判断待ち)**:
> - **E-5 の案 (b)**(K の出どころを評価 run の `metrics.json` に焼き込む)。
>   **`code/eval/run.py` の変更なのでスコープの拡大である**(`CLAUDE.md` §8)。
>   **★採るなら本実験の前でなければ意味が無い。本実験の run はまだ 1 本も無いので、いまが最も安い。判断に期限がある**
>
> **★決定に伴って残ったリスク(ADR-062 のリスク欄。実験前に記録した。上げ直さないが忘れない)**:
> **極性(`gt` / `lt`)は応答バイアス対策として均衡させた設計因子であり、ランダム効果の水準として
> 扱ってよいかは未検証である** / **`template` は `task` に入れ子である**(§3.2 は `(1 \| template)` と
> 交差の形で書いている。**論文に明記する**)/ **水準数が 10 に定まって ADR-061 決定1 の前提は確定したが、
> 事前予測検査は依然として回していない。**
>
> **下はその16(決定前)の記述である。履歴として残す。**


> **★★★2026-09-07(その16・最新)。PLANNER (Opus) → 人間。決定を求める 7 件(E-1 〜 E-7)。**
> **すべて `plans/PLAN-017-analysis-frame.md` §5 に決定材料を並べた。決定は 0 件である。**
>
> | 記号 | 決めること | 重さ |
> |---|---|---|
> | **E-1** | **`label_coverage` の 4 値 → §3.2 の 3 水準への絞り込みをどこに置き、主軸に入らない行をどう扱うか** | **主要検定の説明変数そのもの** |
> | **★E-2** | **`category` → `template` の写像と水準数。★`Documents/05_STATISTICS.md` §3.2 の表と実装が食い違っている**(§3.2 は「T1b は単一」と書くが `CATEGORY_AXES` は `t1b_gt` / `t1b_lt` の 2 つを持つ)。**どちらかを直す必要があり、§3.2 は事前登録の本体なのでエージェントは触っていない** | **`(1 \| template)` の推定に直結。ADR-061 決定1 の「`template` は 2 桁に届かない」の根拠がこの水準数である** |
> | **E-3** | **`category` → `task` の写像を 1 箇所にどう置くか**(既存の `task_type_of` 2 本を束ねる) | 実装の置き場所 |
> | **E-4** | **主軸の行の絞り込みを `frame.py` でやるか `primary.py` でやるか** | 層の責務 |
> | **E-5** | **`main_radius` と K の正本をどこに置くか。★(b) 案(`metrics.json` に焼き込む)は `code/eval/run.py` の変更でありスコープの拡大である。ただし本実験の run はまだ 1 本も無いので、採るならいまが最も安い** | 再現性。**判断に期限がある** |
> | **E-6** | **`passes_analysis_gate` 列の扱い**(閾値が未決。ADR-054 決定1 (ii) / N5) | 順1b のブロッカー |
> | **E-7** | **長形式表を `results/` に凍結するか、その場で組むか**(**`runs/*/predictions/` は `.gitignore` で外れており、表は repo だけからは再現できない**) | 監査可能性 |
>
> **★エージェントの推奨は各節に書いたが、決定ではない**(`CLAUDE.md` §8 / ADR-039 決定3)。
> **★E-2 は他の 6 件と重さが違う。**`template` の水準数は ADR-061 で決めた分散成分の事前分布が
> 効く度合いを変え、**`template` が `task` に入れ子であること**(§3.2 は交差の形で書いている)と
> **単一テンプレートのタスク型ではランダム切片が `task` の固定効果と区別できないこと**にも効く。
> 詳細は `plans/PLAN-017-analysis-frame.md` §5.2。


> **★★★2026-09-07(その15)に下の 3 件は決着した(ADR-061)。上げ直さないこと。**
> **(1) D-6 = 案 (2) 数値で明示 + 叩き台の数値そのまま**(切片 `normal(0, 5)` /
> 固定効果 `normal(0, 2.5)` / 分散成分 `half-Student-t(3, 0, 2.5)`。ロジット尺度・autoscale なし)。
> **(2) 報告する量 = 事後区間 + 分散成分 + `loo`。ベイズファクタは出さない。**
> **(3) §7-4 は PLAN を先に書いてから実装する。**
> **`Documents/05_STATISTICS.md` §3.2 に人間の決定として書き込んだ。下は履歴である。**

> **★★2026-09-07(その15・最新)。PLANNER (Opus) → 人間。決定を求める 2 件(1 件目は上のその14 の続き、2 件目は着手の可否)。**
>
> **(1) ★ベイズの事前分布(D-6)。決定材料は `plans/PLAN-016-fitting-engine.md` §6.4 に並べた。**
>
> - **決めるのは 3 系統である**: **(i) 切片** / **(ii) 固定効果の係数** / **(iii) 分散成分の SD**
> - **★実際にこの感度解析の結果を決めるのは (iii) である。**`template` の水準数は概ね 8 〜 10 で
>   2 桁に届かず、**尤度からは SD がほとんど決まらない。**「弱情報だから中立」ではない
> - **候補(§6.4.4 に中立に並べた)**: **(1) パッケージの既定を版ごと凍結** /
>   **(2) 数値で明示** / **(3) 事前予測検査をしてから確定** / **(4) (2) + 分散成分を振る**
> - **エージェントの推奨は (2)。次点は (4)**(§6.4.6)。
>   **★(1) は ADR-060 の「`rstanarm` / `brms` のどちらを使うかは実装時に決めてよい」と衝突する**
>   ——両者で既定が違うので、実装の選択がそのまま事前分布の選択になる(**未検証**)。
>   **(1) を採るなら ADR-060 のその文を打ち消し線で訂正し、パッケージを先に固定する必要がある**
> - **叩き台の数値**(案であって決定ではない。ロジット尺度・autoscale なし):
>   切片 `normal(0, 5)` / 固定効果 `normal(0, 2.5)` / 分散成分 `half-Student-t(3, 0, 2.5)`。
>   **(iii) の代替は `half-normal(0, 1)`。この 2 つのどちらかが (iii) の実質的な中身である**
> - **★(3) を採る場合は新しい検査(検査4)を建てることになる。回す前に人間の承認が要る**
>
> **(2) ★あわせて開いた連動項目: ベイズ側で何を数値として並べるか**(§6.4.3)。
>
> - **ADR-060 は「ベイズで当てて併記する」としか書いていない。**主解析が返すのは LRT の `Chisq` と p
>   であり(F49)、**ベイズ側の対応物は一意ではない**
> - **事後区間 / `loo` なら (ii) への依存は弱い。ベイズファクタなら (ii) の広さが結論を直接動かす**
>   (Lindley / Bartlett)。**したがって (ii) をどれだけ真剣に決めるかがこの選択で変わる**
> - **エージェントの提案は「交互作用の事後区間 + 分散成分の事後分布 + `loo`。BF は出さない」**
>
> **(3) ★PLAN-016 §7-4(`code/analysis/frame.py`。`coverage` の manifest 照合)に着手してよいか。**
>
> - **その12 が「着手はスコープの追加なので人間に確認してから」と記録した項目である**
> - **`coverage` が無いと主要検定の説明変数の片方が作れない**(F38。§5)。**迂回できない**
> - **`M*` 待ちでも書ける**(照合の実装であって項目数に依存しない)。**GPU 不要**

> **★★2026-09-07(その14・最新)。PLANNER (Opus) → 人間。新規 1 件。**
>
> **★ベイズの事前分布**(ADR-060 の帰結。**D-4 = 案 (2) を採ったことで新しく開いた**)。
>
> - **ADR-060 は「縮退が発火したペアに限り、縮退しない完全構造をベイズで当てて併記する」と決めた。**
>   **事前分布は事前登録が縛る対象であり、凍結より前に確定していなければならない**
> - **`Documents/05_STATISTICS.md` §3.2 には「事前分布は未決」と書いてある。**
>   **エージェントは案を並べてよいが決めない**(`CLAUDE.md` §8 / ADR-039 決定3)
> - **クリティカルパス上には無い。**発火率は合成データで 8 / 34(24%)であり、発火しなければ回らない
> - **未検証**(いずれも実装の前に測ればよい。新しい検査を今建てる必要はない):
>   **`rstanarm` / `brms` がこの環境に入るか** / **`stan_glmer` が §3.2 の交差ランダム効果 3 本を
>   そのまま受け取るか** / **MCMC 1 本の壁時計**

> **★★★2026-09-07(その14)に下の 3 件は決着した。上げ直さないこと。**
> **(1) D-5 の可動量の閾値 = `logLik` 差 < 0.001(符号付きの片側)かつ `max|Δβ| / SE < 0.01`**
> (ADR-059 の追記。**`Documents/05_STATISTICS.md` §3.2 に人間の決定として書き込んだ**)。
> **(2) §3.2 の「収束しない」の定義を承認した**(モデル指定は無変更)。
> **(3) D-4 = 案 (2) 条件付き併記**(ADR-060)。**下は履歴である。**


> **★★2026-09-07(その13・最新)。IMPLEMENTER (Opus) → 人間。1 件。事前登録の凍結より前に要る。**
>
> **D-5 の「実質的に動かない」の閾値**(ADR-059 決定1 が仮のまま残した値)。
> **検査3 で実測した(F57〜F62)。正本は `plans/PLAN-016-fitting-engine.md` §2。**
>
> - **ADR-059 決定1 は `logLik` 側の仮の値(差 < 0.001)しか書いておらず、
>   「推定値も実質的に動かない」の閾値が未記入である。★そこが発火率を決めている**
> - **可動量の閾値を `max|Δβ| / SE < 0.001` にすると、`logLik` 側を何にしてもペアの発火率は
>   32 / 34(94%)になり、不採択にしたはずの案 (b) と一致する。0.01 にすると 8 / 34(24%)になる**(F62)
> - **エージェントの提案(採否は人間)**: **`logLik` 差 < 0.001(現行のまま)かつ `max|Δβ| / SE < 0.01`。**
>   **根拠は、警告が一度も出なかった対照 14 本の可動量が最大 0.00102 であること**(F60) ——
>   **閾値を主要評価項目の分布ではなく、疑う理由の無い当てはめの分布から取っている**
> - **★これも「この合成標本を見て決めている」ことに変わりはない。**実データを見る前に凍結すること
> - **決まったら `Documents/05_STATISTICS.md` §3.2 の該当箇所(「★『実質的に』の閾値」の行)を
>   人間の決定として書き換える。★エージェントは書き換えない**
> - **あわせて確認待ち**: その12 が §3.2 に追記した「収束しない」の定義そのもの
>   (事前登録の本体に手を入れている。モデル指定は無変更)
> - **報告義務として記録**: **再起動は当てはめを境界へ押しやる**(警告ありの 54 本中 42 本が
>   再起動後に特異)。**ADR-059 決定1 は singular を単独の引き金にしないので縮退は起きないが、
>   「論文に必ず報告する」対象がこの割合である**

> **★★★ 2026-09-06(その6b)に ADR-057 で全件決着した。下は履歴である。上げ直さないこと。**
> **帰結の実装も 2026-09-06(その8)に完了した。残るのは §5 の実行(RUNNER)と、順5 の前に人間が決める `θ` だけである。**
> **★決定2 の帰結として『Phase 1 本実験 40 run の GPU 構成』が新しい人間待ちとして開いている**(`train.*` = ADR-043 決定10 と同じ場)。
>
> **★★★★★★★★★★★★★★2026-09-06(その6・最新。PLANNER (Opus) → 人間)。順5 を起動するための未決 4 件。**
>
> **正本は `plans/PLAN-014-order5-launch-preconditions.md` §4。**下は索引である。
> **GPU 使用の承認そのものは 2026-09-06 に下りている。**下は承認とは別の 4 件である。
>
> | 記号 | 決めること | 備考 |
> |---|---|---|
> | **D-A** | **本番 config に `eval.reference_rule` = `p2` / `eval.elicitation` = `direct` を書いてよいか** | **★新しい決定ではない。**どちらも **2026-08-22 に人間が承認済**(本ファイル「解決済み」の #3 と #6)で、**config に転記されていないだけである。**ただし**どちらも `[MATCHED]` 欄**なので、書けば 40 run 全体の設計値が確定する |
> | **D-B** | **掃引 run の GPU 構成**(`resources` 4 欄) | **`infra/RUNPOD.md` §6 の「全条件・全シードを同一 GPU 構成で」の内側か外側か。**内側なら Phase 1 の GPU が今決まるが、**`train.*` が未決**(ADR-043 決定10)なので訓練側の要件を知らないまま決めることになる。外側なら独立に選べるが、**`M*` は `correct_rate` の閾値越えで決まるので、精度が動けば `M*` も動きうる。**先例は順1b の RTX 4090 / `min_vram_gb: 23.9` |
> | **D-C** | **preflight の FAIL 2 件を抱えたまま順5 を回すか** | **案 1** = `infra/RUNPOD.md` §3 に run 種別の例外を書き `preflight.py` に反映(F23 が根拠。**実装 小**)/ **案 2** = 承知で回し理由を残す(**実装 0。ただし「FAIL でも回してよい」前例ができる**)/ **案 3** = `cells` / `anchor_manifest` に暫定値を入れて消す(**エージェントは推奨しない。**`pool_items` で既に 1 つ開いている穴を 2 つに増やす) |
> | **D-D** | **HANDOFF が順5 の前提に置いた 2 件の宛先** | **バッチ fp ノイズ検査**(ADR-040 決定7)と **`forced choice tokens`** は**どちらも評価プール(`comparison` 群)を要求する**ので、現状の config では成立しない(F22)。**順6 に移すのが妥当ではないか**(ADR-040 決定7 の原文は「段階 C の本番でもプールの部分集合 100 項目で」= プールを使う段)。**ADR-044(`requirements.lock` の凍結)は順5 のままでよい** |
>
> **★D-A 〜 D-D が決まるまで GPU ジョブを起動しない。**

> **★★★★★★★★★★★★2026-09-06(その4。人間 → 記録)。★1 を決定した(ADR-054)。この一覧から ★1 の本体が消える。**
>
> **正本は `logs/DECISIONS.md` ADR-054。**下の 2026-09-06(その3)ブロックの **★1 の行は決着済み**である。
>
> | 項 | 決定 |
> |---|---|
> | **★1** | **案 B(差分の差 / DiD)を採択。**`condition × coverage` の交互作用を検定する |
> | **S2** | **1 本(4 タスク型をプール)を採択** |
> | **F12** | **`#4` / `#4b` は二段構え**(パイロットの設計門 + run 単位の解析門) |
>
> ~~**★まだ人間待ちのもの(★1 の残り)**: S1 / S3 / S4~~
> —— **★2026-09-06(その5)に 3 件とも決着した(ADR-055)。下のブロックを見よ。**
>
> **★2 / N1 / N2 / N3〜N6 / #7 は未決のまま**(下のブロックがそのまま生きている)。

> **★★★★★★★★★★★★★2026-09-06(その5。人間 → 記録)。S1 / S3 / S4 を決定した(ADR-055)。この一覧から ★1 の残りが消える。**
>
> **正本は `logs/DECISIONS.md` ADR-055。**指定は `Documents/05_STATISTICS.md` §3.2「順1b のモデル指定」。
>
> | 記号 | 決定 |
> |---|---|
> | **S1** | **案 c。**`task` を固定効果のブロックとして残す(交互作用には入れない)。`id` 腕は 4 タスク型すべて |
> | **S3** | **案 b。**シードを条件間で対応づけない。ランダム切片は `(1 \| condition:seed)` |
> | **S4** | **案 d。**門あり主解析 + 門なし感度解析(感度解析は探索的。Holm に加えない) |
>
> **★新しく人間待ちになったもの**
>
> | 記号 | 決めること | 備考 |
> |---|---|---|
> | **S5** | **`coverage` のランダム傾き `(0 + coverage \| condition:seed)` を置くか** | **切片だけの指定は「`id` → `interp` の落差が run 間で一定」と仮定する。偽なら DiD 交互作用の SE が過小になり検定が反保守的になる。**エージェントは案を出していない。**10 シードで傾きが同定できるかは §6 の検出力(順6 = GPU 実測待ち)に依存するので、順6 の前に決められるとは限らない** |
> | **F15 の扱い** | **`torch.manual_seed` が `code/` に 0 件である件を直すか**(同じ `--seed 3` を 2 度回してもアダプタの初期値が違う) | **「シードが決めるもの」の定義の変更にあたる。**S3 の決定(対応づけない)はこの事実を根拠にしているので、**直すなら S3 の根拠も見直す必要がある** |
>
> **★解析門(ADR-054 決定1 (ii))の閾値は依然として未決である。N5 と同じ場で凍結する。**

> **★★★★★★★★★★★2026-09-06(その3。PLANNER (Opus) → 人間)。★1 / ★2 の案を提示した。GPU 時間 0。**
>
> **正本は `plans/PLAN-013-star1-star2-definitions.md`。**下は索引である。
> **エージェントは推奨を出していない。決定は人間**(`CLAUDE.md` §8)。
>
> | 項 | 決めること | 案 |
> |---|---|---|
> | **★1** | 順1b の「**`id` 到達度を揃えた上で**」の操作的定義 | **A** 時点で揃える(旧・暫定案。**追加 GPU 大**) / **B** 差分の差(DiD) / **C** 共変量調整 / **D** 適格帯で絞り込む / **E** 比(正規化転移指数) / **F** 用量(掃引水準)で揃える(**追加 GPU 中**) / **G** 揃えず順1b を降格 |
> | **★1 付帯** | **S1〜S4。案の選択とは独立に決まっていない** | S1 `id` 到達度を何で測る / S2 順1b は 1 本か 4 本か / S3 シードを対応づけるか / S4 揃わなかった run の扱い |
> | **★2** | `H2` 第2節「破れの位置がシード間で一貫しない」の定義 | **A** 閾値 τ の初回下回り / **B** 最大落差の位置 / **C** 12 セルのプロファイル一致度 / **D** シード内でモデルを当て直す / **E** 定義せず探索的に降格 |
> | **★2 付帯** | **P1〜P3 と Q1〜Q4** | P1 位置の定め方 / P2 一致度の統計量 / P3 閾値。Q1 適格性フィルタで落ちたセル / Q2 `other_error` 主導のセル / Q3 対象条件 / Q4 文言 |
>
> **★人間に上げる新しい指摘が 2 件出た**(PLAN-013 §2 / §4.2 / §4.3):
>
> - **F10: タスク型の水準数が確定していない。**ADR-047 決定2(T1b の枝)と N2(#10)が
>   どちらも 4 → 3 を起こし、同時発火で 4 → 2 になる。
>   **★2 の案 A / B / C / D はラベル空間がタスク型の数に依存するので、N2 の決定で作り直しになりうる。**
>   **★2 と N2 のどちらを先に決めるかは人間が決める**
> - **Q4: `01_HYPOTHESES.md` H2 節の文言が 2 通りある。**「予測される観測」は
>   「破れの位置が**モデル・スケール間**で一貫する」のままだが、2026-09-02 の追随ブロックは
>   「**シード間**・モデル間」に書き換わっている。**本研究は 1 モデル 1 スケールである**
>   (ADR-018 決定2 で 2 系統目は Phase 2 へ延期)。**どちらを正とするかは人間が決める**
>
> **★下の 2026-09-06 ブロックの ★1 / ★2 はこのブロックで置き換える(案が付いた)。**
> **N1 / N2 / N3〜N6 / #7 は未決のままである。**
>
> 旧:

> **★★★★★★★★★★2026-09-06(PLANNER (Opus) → 人間)。順7 の残り。PLAN-012。**
> **6 件を上げる。GPU 時間 0。エージェントは推奨を出していない。**
>
> **正本は `plans/PLAN-012-order7-close.md`。**#13 と #17 の書誌は**エージェント側で閉じた**ので、
> ここに残るのは**人間でなければ決められないもの**だけである。
>
> | # | 判断すること | 正本 | 種類 |
> |---|---|---|---|
> | **N1** | **Nikankin et al. を「Intro の対立軸の一方の極」として置けるか。**原典は robust なアルゴリズム説と記憶説の**両方**を否定し、自説を **"somewhere in the middle"**("a combination of many memorized rules")に置いている。ADR-036 決定6 の前提(**単独で支える**)が成り立つかを判断する。成り立たないなら Intro の対立軸を組み直す | PLAN-012 §6.1 / `02_RELATED_WORK.md` A 表 | **論文の claim の確定**(§8)。**エージェントは案も出していない** |
> | **N2** | **#10(W6 の分岐)。**第3段(T2 を主軸から外す)で**止まってよいか**。**案 A**(T1b の枝と同形に事前指定して止まらない)/ **案 B**(止まるが選択肢の集合を今凍結)/ **案 C**(止まって事前登録を打ち直す) | PLAN-012 §4 | **実験条件**(§8)。**案のみ提示** |
> | **N3** | **ADR-030(R8 の手続き全体)。**(1) 決定6 の当てはめ手続きをこのまま事前登録してよいか (2) `n = 20` と 17 水準(**検出力計算に基づかないと ADR 自身が書いている**)を凍結してよいか (3) 条件別の forward pass 内訳を追記してよいか | PLAN-012 §5.1 | **目視レビュー**(合否・解析計画) |
> | **N4** | **ADR-027 前段。**「主軸の共通被覆水準を3つに絞る」を**明示的に**承認してよいか(現状は「P-2 の承認に含意される形で採択」と ADR 自身が自認) | PLAN-012 §5.2 | **目視レビュー**(実験条件) |
> | **N5** | **ADR-028 決定1 と Go/No-Go #4b。**(1) `p2d` の交互作用を**副次的評価項目**として事前登録してよいか (2) **#4b の 0.90** を合否基準として凍結してよいか。**★HANDOFF 項目1 の「`id` 到達度を揃える」の定義と役割が違うことに注意**(代用してはならない) | PLAN-012 §5.3 / `04_EXPERIMENT_PLAN.md` | **目視レビュー**(合否基準) |
> | **N6** | **PLAN-003 §4.8 のセル構成。**`n = 40` と合計 **800 項目**を凍結してよいか(T1 の 160 項目は自由生成なので GPU 時間に乗る) | PLAN-012 §5.4 | **目視レビュー**(実験条件) |
>
> **★N2 は df に効く。**案 A は df 6 → 4。**T1b の枝(ADR-047 決定2)も df 6 → 4 を引き起こすので、
> 両方が同時に発火すると df 6 → 2 になる。この同時発火の扱いはどの文書にも書かれていない**
> (2026-09-06 に確認)。**`05_STATISTICS.md` §6 は df=6 で書かれている。**
>
> **併せて `refs.bib` の Nikankin 書誌の最終確認**(`CLAUDE.md` §3 手順5 / §8)。
> **ICLR 側の DOI・巻ページ・公式 BibTeX・原典が使った HF モデル ID は未検証のまま残してある。**
>
> 旧:
>
> **★★★★★★★★★★2026-09-05(PLANNER (Opus) → 人間)。順7 の GPU 不要分。決定 7 件を上げる。**
>
> **→ 同日、人間が D1 / D5 / D6 / D7 を決定した(ADR-052)。**
> **→ 2026-09-06、人間が D2 / D3 / D4 を決定した(ADR-053)。★D1〜D7 はすべて決着した。**
>
> | D | 決定 |
> |---|---|
> | **D1** | **案 A 採択。**統計側の帰無・対立仮説を `H_null` / `H_alt` に改名した。**`H0`〜`H3` は研究仮説だけを指す** |
> | **D2** | **案 A 採択(2026-09-06)。`experiment.hypothesis` = `H2`。**交互作用が有意であること(統計的 `H_alt`)を主予測として事前登録した。**エージェントは推奨を出していない** |
> | **D3** | **案 C 採択(2026-09-06)。**反証条件を2種類に分けた。検定に紐づくもの(`H2` 第1節)は Δ 不要、水準に紐づくもの(`H0`/`H1`/`H3`)は `05_STATISTICS.md` §5 の新表へ。**値は順6 待ち** |
> | **D4** | **案 α 採択(2026-09-06)。**`05_STATISTICS.md` §4 に **順1b**(`p2` vs `p2d` の `interp` 差)を新設した |
> | **D5** | **P1(入力書式で割れる)を採択。非加法性 RMS = 0.353 logit。**事前登録の効果量として凍結した |
> | **D6** | **後回し。**`code/analysis/power_sim.py` は順6 の後 |
> | **D7** | **承認。**範囲外 3 箇所(`00_OVERVIEW.md` §5・§8 / `05_STATISTICS.md` §5)をそのまま残す |

>
> **正本は `plans/PLAN-010-order7-doc-followup.md` §5。**下は索引である。
>
> | D | 決めること | 事前登録の凍結前に必須か |
> |---|---|---|
> | **D1** | **`H0` / `H1` の記号が 2 文書で逆の意味を持っている**問題をどう解く(A 統計側を `H_null`/`H_alt` に改名 = 最小 / B 研究仮説側を改名 / C 併存) | **必須** |
> | **D2** | `experiment.hypothesis` に何を入れる(A `H2` / B `H1` / C 単一の H を宣言しない)。**エージェントは推奨を出していない**(予測の事前宣言そのものであるため) | **必須** |
> | **D3** | 反証条件の「大きく落ちる」を数値化するか(Δ は順6 待ち) | 望ましい |
> | **D4** | H3 を `05_STATISTICS.md` §4 の副次の階段のどの段に置くか | **必須**(多重性の統制) |
> | **D5** | **検出力分析の効果量プロファイル P**(`05_STATISTICS.md` §6.5 の P1 / P2 / P3。実質は「非加法性 RMS = 0.353 を検出下限にするか」) | **必須。順6 の後に決めると HARKing** |
> | **D6** | `code/analysis/power_sim.py` を今つくるか(GPU 不要。現在**未実装**) | 任意 |
> | **D7** | HANDOFF の指定範囲外に手を入れた 3 箇所を承認するか(`00_OVERVIEW.md` §5・§8 / `05_STATISTICS.md` §5。理由は PLAN-010 §5.3) | **必須** |
>
> **併せて報告(選択肢ではない)**: PLAN-010 §5.1 の **F1〜F5** ——
> 「そのまま出すと査読で誤りになる」箇所が 5 件見つかり、すべて打ち消し線 + 下書きで残してある。
> **2026-09-06 に F6 が追加された**(`plans/PLAN-011-d2d3d4-close.md` §3) ——
> **H3 の主予測を検定する段が副次の階段に存在しなかった。**順1b の新設で解消した。
>
> ---
>
> **★★★★★★★★★★★2026-09-06(PLANNER (Opus) → 人間)。D2/D3/D4 の決着で新たに / 依然として未決が 3 件ある。**
>
> | 未決 | 由来 | 待ち |
> |---|---|---|
> | **「`id` 到達度を揃える」の操作的定義** | **D4(順1b の新設)で再び開いた。**`05_STATISTICS.md` §4.1 は 2026-08-23 に「不要になった」として閉じていた | **人間の決定。★Go/No-Go #4 / #4b の 0.90 で代用してはならない**(あちらは run を解析に載せてよいかの門、こちらは 2 条件を突き合わせる時点の定義。役割が違う) |
> | **`H2` 第2節「破れの位置がシード間で一貫しない」の定義** | D3 で「検定にも水準にも紐づかない別種の量」と判明 | **人間の決定。**定義が無いまま実験に入ると、H2 が支持されたときの**主要な知見の側が事後定義になる** |
> | **Δ の値(`05_STATISTICS.md` §5 の 5 行すべて)** | D3 で量と出所は凍結した | **順6(GPU)の実測。★Δ が入るまで事前登録の凍結タグ(順9)は打てない** |
>
> ---
>
> **★★★★★★★★★2026-09-02(IMPLEMENTER (Opus) → 人間)。順4 の第2条件を閉じた。新しく上げるものが 3 件ある。**
>
> | # | 何 | なぜ人間が要るか |
> |---|---|---|
> **→ P1 / P2 / P3 は 2026-09-02 に人間が採択し、同日片付けた(ADR-051)。下は経緯の記録。**
> **新しく残った未決は「評価側 manifest に `schema_version` を足すか」と
> 「`01_HYPOTHESES.md` の下書きの確定」の2件である。**
>
> | ~~**P1**~~ | ~~**`infra/preflight.py:282` の `data manifest` 検査は現状 PASS になり得ない** —— 検査は manifest の `files` ブロックを読むが、**`"files"` は repo 全体でこの 1 行にしか現れず**、`ft_data` / `eval_pool` のどちらの `build_manifest` も書かない。既存の config はすべて `data.manifest: null`(= SKIP)なので露見していなかった | 直し方が 2 通りある(**manifest に `files` を足す** / **preflight に `outputs.train_jsonl_sha256` を読ませる**)。どちらも manifest schema か検査の意味を変える。**`data.manifest` を null に戻せば FAIL は消えるが、穴を隠すことになるので戻していない**(`PLAN-009` §8.1) |
> | ~~**P2**~~ | ~~**`experiment.hypothesis` が null のままである** —— `Documents/01_HYPOTHESES.md` が再設計前(G1–G6 基準)のままで、**主軸 Q16 に対応する H 番号がどの文書にも無い** | エージェントが番号を当てれば捏造になる(`CLAUDE.md` §2)。**H 番号を決めるか、`01_HYPOTHESES.md` を追随させるか** |
> | ~~**P3**~~ | ~~**`plans/PLAN-002` §7.3 の検算の誤りを訂正した**(強制一致は 1 件ではなく 2 件)。訂正は `PLAN-009` §3.1 と ADR-050 に書いたが、**`PLAN-002` §7.3 の本文には打ち消し線を入れていない** | 事前登録に関わる文書の改訂であり、打ち消し線+理由+日付の入れ方を人間が確認する(`CLAUDE.md` §2) |
>
> **`Documents/01_HYPOTHESES.md` は「現在のブロッカー」の追随漏れ 3 件(09_PAPER_PLAN / 00_OVERVIEW / 05_STATISTICS §6)に
> 載っていない 4 件目である。**P2 はその帰結として現れた。


> **★★★★★★★★2026-09-01(IMPLEMENTER (Opus) → 人間)。順4 の第1条件を実装した。人間に上げるのは2種類ある。**
>
> **(a) 実装で確定させた命名・書式(ADR-049 決定1・3)。覆してよい。**
> ADR-035 リスク欄が「群名は未定であり `SUPPORTED_GROUPS` に足すのは順4 の仕事」と授権した範囲だが、
> **副次セルの器械仕様**なので人間が一度見ること。
>
> | # | 確定したこと | 覆す場合に動くもの |
> |---|---|---|
> | 群名 | **`bare_sum_instructed`** | `SUPPORTED_GROUPS` / `CATEGORY_AXES` / `RENDERERS` / config の `eval.batteries` |
> | category・タスク型 | **`t1_instructed`**(主軸の `t1` と別値。混ぜると交互作用の df が動く) | 同上 |
> | 文面の組み立て | `data.prompt_template` + **半角空白1つ** + `data.answer_format_instruction`。**テンプレートファイルは作らない** | `numeric_sum.ANSWER_FORMAT_INSTRUCTION_JOIN` と `configs/template.yaml` |
>
> **(b) 順4 の第2・第3条件を塞いでいる未決事項(こちらは人間にしか決められない)。**
> 内訳は `plans/PLAN-008-order4-data-regen.md` §5。
>
> **→ ★2026-09-02、人間が B1 / B2 / B3 を決定した(ADR-050)。B1〜B3 は未決ではない。**
> **B1** `lesion.arbitrary_table` = **規約A で生成した 197 件**(`plans/PLAN-009` §3)/
> **B2** `data.train_size` = **10000** / **B3** `data.coverage_k` = **2000**。
> 併せて `pool_split_seed` = 0 / `coverage_seed` = 1 / `sample_seed` = 2 / `pool_id` = `main`。
> **順4 の第2条件はこれで閉じた**(2026-09-02。`1376909`)。**B5 だけが残っている。**
> **下の表は経緯の記録である**(2026-09-06 に追記)。
>
> | # | 欄 | 状態 |
> |---|---|---|
> | **B1** | `lesion.arbitrary_table` | **未決(承認待ち-3)。**`arb` のズレ表の中身。**`[MATCHED]` なので `arb` を回さない4条件の config にも書けない** —— これ1件で5条件そろった本番 config が作れない |
> | **B2** | `data.train_size` | **未決。**掃引軸 {2000, 4000, 10000} は ADR-043 決定6 の前提だが、順4 で焼き込む1点が決まっていない |
> | **B3** | `data.coverage_k` | **未決。**`configs/template.yaml` の「主値 2000」は提案であって採択ではない。`K` を掃引軸にするかも承認待ち |
> | **B5** | `eval.pool_items` の本番の中身 | **`M*` 未決のあいだ塞がっている**(ADR-033 決定4。`extrap` セルが原理的に埋まらないので `fill_cells` の経路に移せない)。**順5 の後** |
>
> ~~**したがって順4 は「B1〜B3 を人間が決める」+「順5 で `M*` が出る」まで閉じられない。**~~
> **★2026-09-06 訂正: 前半は 2026-09-02 に済んだ(ADR-050)。残るのは「順5 で `M*` が出る」だけである。**
> 指示付き T1 のセル表(`id` × {carry, nocarry} × n=40 = 80 項目。ADR-035 決定2)も同じ理由で
> config に書いていない。

> **★★★★★★★2026-08-31(IMPLEMENTER (Opus) → 人間の最終確認待ち)。承認された R1/R2/R3/R4/N1/R5/R7 は
> 実装済み(`pytest` 774 passed)。人間に確認してほしいのは、実装の過程で確定した**器械の仕様**である。**
>
> **正本は `logs/DECISIONS.md` の ADR-047「実装ノート(2026-08-31)」(8項目)。**
> ADR-047 リスク欄が「強制選択の『Yes』『No』トークンの取り方は実装で確定する」と授権していた
> 範囲だが、**二値の主要測定(T1b / T3)の器械仕様**なので人間が読んで閉じること。
>
> | # | 確定したこと | なぜ人間の確認が要るか |
> |---|---|---|
> | **R3** | 各側は候補綴りをまたいで **`logsumexp` で周辺化**(旧: 最尤の1綴り = `max`) | `margin` が対数オッズになる。二値の主要測定の定義そのもの |
> | **F1(新規)** | **単一の内容トークンで置ける綴りだけを候補にする。**割れた綴りは落とし(記録に `null`)、片側全滅は `TokenizerContractError` | 旧実装は先頭トークンで代用しており、`YES`→`Y` に `You` / `Your` の質量が混ざる。R3 の周辺化では**和になって効き、Yes 側と No 側で割れ方が揃わない = 非対称な偏り** |
> | **R4** | 同点は **No**(`answer = yes > no`) | 決定性のための規約。実験的な非対称性ではない |
> | **5** | preflight 検査 `forced choice tokens` を新設(片側全滅・id 重なり = FAIL、綴りが割れる = WARN) | **順5 の GPU を借りる前に止まる。**検査を1本増やしたので `infra/RUNPOD.md` §3 の一覧も更新済 |
>
> **実機で必ず読むもの**: 順5 の preflight が書く `runs/<id>/forced_choice_tokens.json`。
> Llama-3.1 の実トークナイザで6綴りのうち何本が生き残るかは**まだ実測していない**(未検証)。
> WARN が出たら、落ちた綴りが方法節に書ける形か人間が見ること。
>
> **★★★★★★2026-08-31(CRITIC → 人間承認。↑で実装済み)。強制選択採点器(commit `d854213`)を Opus として
> 独立レビューし直した。前回(2026-08-30)の R1〜R7 を全件再現・確認。実装コアの決定規則・配線・
> 潰れ検査にバグは無い。新規 N1 を1件検出。人間が修正を承認した。**
>
> **人間の指示(逐語の要旨)**: 「今後について深く考えたうえで、修正して実装してほしい。あなたの修正を
> 承認する。基本的な本筋と GPU を使った実験を理解し、その前に直してほしい。」
> → **提案 CRITIC / 採択 人間(2026-08-31)**(ADR-039 決定3)。実装はコンテキスト超過で次セッションに送った。
>
> **承認された修正(実装は次セッション。正本は `logs/HANDOFF.md`)**:
>
> | # | 修正 | GPU 実験のどこより前か |
> |---|---|---|
> | **R1** | `token_length.py` が各 run の `metrics.json` `by_batch[*].scoring` を読み、`forced_choice` の
>   バッチを集計から外す。黙って消さず `by_batch[<name>] = {"scoring":"forced_choice","skipped":true,"note":...}` を残す。
>   **`scoring` キーが無い旧 run(smoke1b)は `free_generation` 扱い**(壊さない) | **順5(実機)より前** |
> | **R2** | `compare_runs.compare()` が `forced_choice` バッチでは `left.response == right.response` ではなく
>   `_parsed_equal(left.parsed, right.parsed)`(bool 一致)で判定。`response_a/b`(logprob 付き)は mismatch 明細に
>   引き続き残す。**合否基準は作らない**(ADR-045 決定2)。判定の意味が群で変わることを報告に1行明記 | **順5 より前** |
> | **R3** | 変種集約を `max` → **`logsumexp`(綴りをまたぐ確率の和)**に変える。`ForcedChoice.yes_logprob/no_logprob`
>   と `margin`(= 対数オッズ)の意味が「P(Yes) の周辺化」に揃う。**推奨。**実装側が強く反対するなら `max` 据え置きで
>   ADR-047 追記のみ可 | **順6(T1b/T3 初測定)より前** |
> | **R4** | **同点は No のまま**(決定性の規約。判別可能項目では起こらない)。変えずに ADR-047 に明文化 | 順6 より前 |
> | **R3/R4 の記録** | ADR-047 に「変種集約の規則」と「同点規則」を **提案 CRITIC / 採択 人間(2026-08-31)** で追記 | 順6 より前 |
> | **N1**(新規) | `eval.elicitation == cot` かつ `comparison ∈ eval.batteries` を `ConfigError` で弾く。
>   `dry_run()` と `evaluate_pool()` の共通ヘルパに。理由: 強制選択には解釈すべき生成文が無く cot が黙って落ちる
>   —— repo の「宣言と実装の食い違いは実行前に止める」作法(`model.py` `reject_unimplemented_settings`)に合わせる | 順4 より前(安いので先に) |
> | **R7**(ついで) | R2 で `metrics.json` `by_batch` を読む配線を通すので、`aggregate.Row` にも `scoring` を通す。低リスク | 順9 より前(R2 と同時が安い) |
> | **R5 / R6** | R5 = `execute` が渡された `scorer` を捨てない小修正(任意)。**R6 は据え置き**(#25 は決定済 =4。記録のみ) | — |
>
> **設計上の注記(N2。R2 の付帯)**: 強制選択の logprob もまとめ幅の fp ノイズを受ける(左パディング +
> `position_ids` 非明示。RoPE 相対不変性で最終トークンは理論上不変だが僅差項目は割れうる)。
> **順1b 相当のバッチ1 対 バッチ N の一致確認を、順5 の前に comparison 群でも取る**(比べるのは `parsed` bool。
> R2 の直しで `compare_runs` がそれを見るようになる)。

> **────────── 以下、前回(2026-08-30)の R1〜R7 の詳細。上の承認で解決方針は確定済 ──────────**

> **★★★★★2026-08-30(IMPLEMENTER のレビュー。Opus)。強制選択採点器(commit `d854213`)を
> 別モデルで読み直した結果、下流の解析モジュール2本が壊れていることが分かった。人間に上げる。**
>
> **原因は共通**: 強制選択には生成文が無いので `predictions/*.jsonl` の `response` 欄に
> **合成文字列**を入れた —— `Yes [forced_choice yes_logp=-0.1235 no_logp=-2.7654]`。
> `response` を「モデルが実際に生成した文字列」として読む既存の消費者が2つある。
>
> | # | 壊れているもの | 何が起きるか | 影響 |
> |---|---|---|---|
> | **R1** | `code/analysis/token_length.py` | `comparison` バッチの合成文字列をトークン数として数える(`scoring` も `group` も見ていない)。実トークナイザでは logprob の数字が細かく割れて **20 トークン超**になる | **`token_length.json` は #20 `max_new_tokens` の改訂根拠**(ADR-042 決定6 / ADR-038)。汚染された表を段階 C で人間が読む |
> | **R2** | `code/analysis/compare_runs.py:210` | `left.response == right.response` の文字列一致。**logprob はまとめ幅で必ず揺れる**ので、答えが一致していても comparison は全件「不一致」に出る | ADR-040 **決定2 が文字列一致を「記録のみ・合否に使わない」**としており、合否は決定1(4値分類 + `parsed`)。**Go/No-Go は壊れない**が報告が誤読される。`parsed_consistency`(ADR-045)は bool を比べるので正しく効く |
>
> **エージェントの案(採否は人間。ADR-039)**:
> - **R1 案**: `token_length.py` が `metrics.json` の `by_batch[*].scoring` を読み、
>   `forced_choice` の群を**集計から外す**(`by_batch` に `scoring: forced_choice, skipped: true` と
>   1行残して、黙って消さない)。強制選択は生成していないので「答えのトークン長」が定義されない。
> - **R2 案**: `compare()` が `scoring` を見て、強制選択の群では**文字列一致ではなく `parsed`(bool)一致**を
>   使う。または合成文字列から logprob を落として答えだけを比べる。
> - どちらも**合否基準を作る変更ではない**(ADR-041 / 045 の線は動かさない)。
>
> **R3〜R4: 実装者が決めてしまった器械仕様(ADR に無い。人間の確認が要る)**
> ADR-047 リスク欄は「Yes/No トークンの取り方は実装で確定する」と授権しているが、
> **二値の主要測定の器械仕様**なので、決まった中身を人間が見ておくべきである(`CLAUDE.md` §7)。
> - **R3**: 変種(大小文字3 × 先頭空白2 = 6綴り)の集約が **`max`(最尤の綴り1つ)**であって
>   `logsumexp`(綴りをまたぐ確率の和)ではない。チャットテンプレート直後はほぼ1綴りに
>   質量が乗るので実害は小さい見込みだが、**「P(Yes) の推定」としては和が正しい。**
> - **R4**: **同点は No に倒す**(決定性のための規約)。判別可能な項目では起こらない。
> - どちらも `code/eval/forced_choice.py` の docstring には書いてある。**ADR には無い。**
>
> **R5〜R7: 軽微(記録のみ。対応は任意)**
> - **R5**: `run.execute()` は `generator is None` のとき、渡された `scorer` を**黙って捨てる**
>   (`build_engines` の返り値で上書き)。現状どの呼び出し元もそうしないが、
>   「黙って無視しない」という repo の作法に反する。
> - **R6**: `timing.seconds_per_item` が **1 forward pass(二値)と最大 256 トークン生成(数値)を平均**する。
>   `eval.batch_size` は既に決定済(=4。ADR-040 決定6)なので**決定は汚れない**が、
>   段階 C の GPU 時間見積りには効く。採点方式ごとに区間を分ける手はある。
> - **R7**: `code/analysis/aggregate.py` は `by_batch[*].scoring` を拾わない。
>   `group == "comparison"` から復元できるが、**記録ではなく規約に依存**している。順9 の解析で効く。
>
> **問題が無かったことを確認した点**(レビューで潰した):
> - 4値の潰れ(`correct + rule = 1`)は `assert_collapsed_to_binary` が構築時に検査。
>   率は `count / n` なので 0 件なら厳密に `0.0`(浮動小数の取りこぼしは無い)
> - **数値経路(T1 / T2 / specificity)は不変**。テストで固定済
> - **プロンプトは1文字も変えていない**(`configs/templates/` は無変更)。ADR-047 決定1 の肝
> - **合否基準・Go/No-Go 判定コード・θ は書かれていない**(ADR-047 決定6 遵守)
> - 重みは1度だけ読む(`engine.build_engines`)。4090 24GB で 8B を二度読まない
> - `parse_boolean_response` / `parsers/boolean.py` は案 C backstop 用に残っている
> - 削除した `DRY_RUN_RESPONSES` / `boolean_response_metrics` への参照は `code/` に残っていない
> - **テストの穴を1件塞いだ**(commit `529572c`): `metrics.json` に `scoring` が残ることの
>   回帰テストが無く、メモリ上の返り値しか見ていなかった(PLAN-007 §4-7 が求めていたのは成果物のほう)。
>   **740 passed。**


> **★★★★2026-08-29(PLANNER)。人間が `plans/PLAN-005` §5 で6項目を全採択 → 採択分を落とした。**
>
> - **承認待ち C → 決着。**`max_new_tokens` に `[MATCHED]`(ADR-042 2026-08-29 追記 / `configs/template.yaml`)。
> - **#21 → 決着。ADR-046 採択。**T1b / T3(案 A)の確定文面 / `configs/templates/eval_main.yaml`(T2+T1b+T3)/
>   `data.eval_template_set: eval_main`。**ADR-042 決定10 を閉じた。**
> - **ADR-041 決定5 → 一部決着。**格子・項目数を `configs/template.yaml` に記入、抽出シード数 = 5(ADR-041 追記)。
>   **`sweep.py` のマルチシード化と5シードの具体値は `plans/PLAN-006` に残る**(人間が案 A/B を選ぶ + IMPLEMENTER)。
>   **θ の値は依然未決**(ADR-041 決定2・3。掃引表を見てから人間が決める)。
>
> **残っている人間待ち**:
> - ~~`plans/PLAN-006` §3: 5シードの与え方~~ → **2026-08-30 決着 + 反映済。案 B(`seeds = [0,1,2,3,4]`)。
>   ADR-041 2026-08-30 追記 / `configs/template.yaml` / PLAN-006 §4 実装(commit `1d1f57d`)。**
> - ~~T1b が Go/No-Go #1 を割ったときの分岐~~ → **2026-08-30 決着 + 反映済。ADR-047**(案 A = 強制選択採点。
>   案 C を backstop に先行登録。案 B 却下)。**§4 の実装(強制選択採点器)も 2026-08-30 完了
>   (`code/eval/forced_choice.py` / `engine.py` 新設 + `run.py` 改修。`pytest` 739 passed)。この項目は完全に決着。**
> - ~~特異性対照(`specificity`)の裸書式の符号位置~~ → **2026-08-30 決着 + 反映済。ADR-048**
>   (`-`=U+002D / `*`=U+002A。`specificity.yaml` 新設 + `eval_main.yaml` に追加)。
> - **θ の値**(ADR-041。順5 の掃引表の後)。
- **R1〜R4**(2026-08-30 の Opus レビュー。上の★★★★★ブロック)。R1/R2 は順5 より前に直す。
>
> **★★★2026-08-30 反映完了(PLANNER+IMPLEMENTER)。人間の4決定を全件ファイルに落とした**
> (ADR-047 / ADR-048 / ADR-041 追記 / ブランチ破棄 / PLAN-006 §4 実装)。**`pytest` 713 passed。GPU 時間 0。**
> **逐語と commit sha は冒頭★★★★★★★★★★ブロックの表。**

> **★★2026-08-28 決着(PLANNER)。RUNNER が順1b で出した2件を人間が判断した。**
>
> **A → ADR-044 採択。**`infra/requirements.lock` を**順1b の環境に凍結する**。
> 次に GPU ポッドを立てるセッション(順5 の実機など)で、順1b と同じボリューム
> `r963j7swke` / 同じベースイメージのポッド上で `pip freeze` を取り、
> `infra/requirements.lock` に非コメント行として書いて commit する。
> **それまで preflight の `libraries` 検査は WARN のまま。**凍結後に版を上げる場合は ADR。
> `infra/RUNPOD.md` §6「環境の固定」に手順を追記済
>
> **B → ADR-045 採択 → 2026-08-29 実装完了(IMPLEMENTER)。この項目は決着した(人間待ちではない)。**
> `code/analysis/compare_runs.py` に `compare_parsed()` を追加し、`batch_consistency.json`
> (= `payload()` の返り値)に `parsed_consistency` ブロックが入る: 項目ごとの
> `parsed_a` / `parsed_b` / `parsed_match` + 集計(`n_compared` / `n_match` / `n_mismatch` /
> `mismatches`)。**両方 parse_fail(None 対 None)は比較対象外**で `n_both_parse_fail` に別カウント
> (ADR-045 リスク欄)。4値分類の一致とは独立ブロック。**合否基準は無い**(ADR-045 決定2)。
> テスト7件追加で `pytest code/tests -q` → 693 passed。`infra/RUNPOD.md` §4 更新済。
> **順1b はやり直していない**(19/19 は手作業で確認済。ADR-045 帰結)
>
> **★新規 C(PLANNER が #20 の確定時に立てた)。**`model.max_new_tokens` に
> **`[MATCHED]` を付けるか**。ADR-042 決定6 は値(256)は確定したが `[MATCHED]` を明言していない。
> ADR-040 決定5 が `eval.batch_size` を条件間で揃えた理由(「温度0でも batch 構成等で揺れる」
> → 条件差にその揺れが乗る)がそのまま当てはまる。**付けないと p2 と x2 で長応答の打ち切りが
> 非対称になり 4値分解の比較が汚れうる。**`configs/template.yaml:51` にこの注記あり(現状タグなし)。
> **→ 2026-08-29: `[MATCHED]` を付ける案を `plans/PLAN-005` §2 に起草した(提案 PLANNER。採択待ち)。**

> **★2026-08-28 追記(IMPLEMENTER)。8-6 と ADR 反映で5件増えた。**
> **場所**: 1〜4 は `plans/PLAN-004-phase0-route.md` §3 順8「8-6 でやったこと」の
> 「★人間の確認が要る点」、5 は `plans/PLAN-003-redesign.md` §4.5 の引用ブロック。
>
> 1. **最適化の既定値。**`learning_rate` 以外(`betas` / `eps` / `weight_decay`)は
>    torch の `AdamW` の既定であり、**どの ADR も宣言していない。**値を決めず、
>    **実際に効いた値を `metrics.json` の `outcome.optimizer` に残す**形にした。
>    **勾配クリッピングと lr スケジューラは使っていない**(宣言が無いものを足すと
>    黙って実験条件が増える)。**これでよいかは人間が決める**
> 2. **LoRA 重みの dtype。**土台と同じ bf16 のままにした。fp32 に上げると
>    数値の安定性は上がるが、**それは実験条件の変更である**
> 3. **LoRA の `bias`。**peft の既定 `"none"` を明示した(どの ADR も宣言していない)
> 4. **`model.adapter: null` のまま病変条件の評価を回せる。**止めていない ——
>    順1b は `condition: p2` の config で素の重みを測るため(`configs/smoke1b.yaml`)。
>    記録には `adapter: null` と注記が必ず残る。**止めるべきかは人間が決める**
> 5. ~~**T1b が Go/No-Go #1(`parse_fail_rate < 0.02`)を割ったときの分岐が無い。**~~
>    → 2026-08-29(PLANNER): `plans/PLAN-007` に3案を起草。
>    **→ 2026-08-30 決着 + 反映済。ADR-047**(提案 PLANNER / 採択 人間。commit `eccf1fb`)。
>    案 A(二値出力群 T1b + T3 を強制選択採点。プロンプト不変)を採択。**ADR 起草時の確認で人間が
>    「案 C を backstop として残す」を選んだ** —— 強制選択でもなお `none` の T1b × `id` が
>    Go/No-Go #3 の常答ベースラインを超えられないなら T1b を主軸から外す(交互作用 df 6→4)。
>    案 B 却下。PLAN-007 §5 を採択に畳み、PLAN-003 §4.5/§6.5/§6.5a・Documents/04・06(T14)を追随。
>    ADR-046 リスク欄も閉じた。**§4 の実装(強制選択採点器。GPU 時間 0)だけ次セッションに残る。**

> **2026-08-23 追記(PLANNER)。この節の内容は `plans/PLAN-003-redesign.md` §11 の 15 件に
> 集約された。**個別に消化せず、PLAN-003 §11 を見ること。以下は再導出前の記録として残す。
>
> **★2026-08-27 追記。この節の番号は PLAN-003 §11 の番号と一致しない**(この節は 2026-08-22 起源)。
> **正本は PLAN-003 §11 と `plans/PLAN-004-phase0-route.md` §5 である。**
> 同日に決着したのは PLAN-003 §11 の番号で **#11(G7)= 落とす / #16(Feucht)= (a) /
> #18(T1 の指示)= 現状維持 + 副次セル / #19(被演算子 1)= 評価項目のみ**、および
> **PLAN-002 §12-11(判別不能の除外)= 掛けない**(ADR-034 / 035 / 036)。
> **#13(`table[1]` の穴)は消滅、#17(Nikankin の原典確認)は「高」に昇格した。**
> 下の表の 11(G7)/ 13(ズレ表の追加制約と `table[1]` の穴)/ 16(Feucht)も同じ理由で決着・消滅している。


| # | 内容 | 場所 |
|---|---|---|
| 1 | `plans/PLAN-000-repo-bootstrap.md` の CRITIC レビュー | 未実施 |
| 2 | `Documents/08_FUTURE_DIRECTIONS.md` L62「ADR-005(色体系の規約)を先に確定すること」。ADR-005 は主要評価項目の決定で色体系ではない。**誤参照ではなく未執筆 ADR への先回り参照**と読める。既に埋まっている番号を将来の ADR に割り当て直すのは番号の再利用にあたり §8 の判断事項 | `Documents/08_FUTURE_DIRECTIONS.md` L62 |
| 3 | `Documents/04_EXPERIMENT_PLAN.md` Phase 1 の Go/No-Go の fallback に**「被覆 `K` を下げる」「訓練値域を狭める」を追加**してよいか。**ADR-019 により制約が付いた: `K` は 560 を下回れない**(評価プールの `id` セルが埋まらない)。訓練値域は既に `[1,99]^2` まで絞ってある | `plans/PLAN-001-eval-battery.md` §13 |
| 4 | **(新規)** `K` を**掃引軸(実験条件)**にするか。config 項目として持つこと(変更 D、承認済)とは別の判断であり、条件の追加は §8 に当たる | 同 §13 |
| ~~5~~ | ~~G1 の日本語を訓練内に置くか外に置くか~~ → **解決(ADR-019)。訓練プロンプトは裸の式 `a+b=` 一形式なので、日本語変種は構成的に必ず訓練外**になる | ADR-019 |
| 7 | **(実装で判明。穴2)** 被演算子を持たないカテゴリ(G2「単位元の言明」/ G3「規則の自己説明」)を被覆層から外し、独立のセルとして `n` を別に決めてよいか | 同 §5.1.1 |
| 8 | **(前進。ADR-019)** 本番の評価テンプレート集合。**訓練側が `a+b=` 一形式に確定したので「訓練と異なる集合」が定義できるようになった。**残る未決は **G1 の「記法形」変種(`3+4=`)が訓練形式と一致する**問題 → **PLAN-002 §5.2 が案A(独立群 `G0`「訓練形式アンカー」に分離)を提案。下記 12 に移す** | 同 §5.1.1、PLAN-002 §5.2 |

**新規**(2026-08-22。**すべて `plans/PLAN-002-ft-data.md` §12 が正本**):

| # | 内容 | 場所 | 既定案 |
|---|---|---|---|
| 9 | 答えが1桁の層(母集団 36 組)に配分の下限を置くか。置かないと `K=2000` で 7 組しか入らない | PLAN-002 §4.2.3 | **置かない**(比例配分のまま) |
| 10 | `train_size` の掃引軸を `{1k, 3k, 10k}` → **`{2000, 4000, 10000}`**。`K=2000` の下で 1k は原理的に生成できない。**実験条件の変更** | PLAN-002 §4.3.2 | 改める |
| 11 | **G7 の項目構成**(サブ群3・層は `carry × wrap`・`n=15`・合計 **165 項目**) | PLAN-002 §5.1 | 提案どおり。**2026-08-23 の原典転記で文面と `n_max` が埋まった**: `n_max = 2m`(月 24 / 曜日 14 / 時刻 48)、**G7-H は 24時制(法 24、起点 `00`..`23`)**、文面は §5.1.2a に逐語転記。**165 と `n=15` は変わらない**。この変更ごと承認を仰ぐ |
| 12 | **G1 の「記法形」変種**を独立群 `G0`「訓練形式アンカー」(400 項目)に分離する(旧 8 の残り) | PLAN-002 §5.2 | 案 A |
| 13 | `arb` のズレ表への**追加制約3・4**(周期タスクの偶然一致回避 / 桁数を `p2` に揃える)。**制約3が無いと G7 の 15 件のセルが埋まらない** | PLAN-002 §7.3、PLAN-001 §4.4 | 追加する。**2026-08-23 に範囲と法が広がった**: `1 <= t <= 71`、法は `{7, 12, 24}`。**新規の穴**: ズレ表の定義域は `[2,198]` なのに G7-H の `x=0, n=1` が `t=1` を要求する。(a) 定義域を `[1,198]` に広げる / (b) `x=0` を除く / (c) G7-H の `n >= 2` のいずれかを人間が選ぶ |
| 14 | `R_train = R_main = 99` を固定条件として宣言し、Go/No-Go の fallback から「訓練値域を狭める」を取り下げる | PLAN-002 §7.1 | 宣言する |
| ~~**15**~~ | ~~**(新規 2026-08-23)** `model.revision` に何を入れるか~~ → **2026-08-24 解決(ADR-031)。**人間が AI の判断を承認。**手続きは既定案どおり。ただし限界の記述は差し替えた** —— ADR-024 で変種が `-Instruct` になった以上、乖離は revision 水準ではなく**変種水準**である | ADR-031、`06_THREATS.md` **T13**(新設) | 決着 |
| **16** | **(新規 2026-08-24)** **Feucht et al. (2026) を論文1でどう位置づけるか。**(a) Intro の対立軸のみ / (b) G7 を残して書式の出所も兼ねる / (c) 引用しない。**`CLAUDE.md` §3 によりプレプリントは主要な論拠に使えない。**#11(G7)と一体で決める | PLAN-003 §8.4、`06_THREATS.md` T13 | (a) を推奨。ただし #11 次第 |
| **17** | **(新規 2026-08-24)** **Nikankin et al. (2025) の原典確認**(現在 ⚠️)。Feucht を降ろすと Intro の対立軸を単独で支えることになる | `02_RELATED_WORK.md` A 表 | SCOUT に投げる |

**解決済み**(2026-08-22、実装中に見つかった2件に人間が回答):

| # | 内容 | 結果 |
|---|---|---|
| ~~6~~ | 穴1: 被覆セル(`id` / `interp` / `extrap`)の埋め方 | **案A を採択(ADR-017)。****評価項目プールは FT データ生成の後に作る。**`id` セルは訓練被覆 `K` 組から、`interp` セルはその補集合から引く。**プールは `K` に依存し、訓練域を変えたら作り直す**(§4.2 A の「作り直さずに済む」利点はラベル付与に留まる)。`pool.fill_cells` に実装済み。埋まらなければ例外で止まる |
| ~~9~~ | 穴に非ず: パーサの抽出規則10項目(§5.4.1) | **現行のまま維持。**規則2「見る範囲に整数がちょうど1個のときだけ採る(0個も2個以上も `parse_fail`)」を含め承認。Phase 0 の実測後に見直す余地は据え置き |

**解決済み**(2026-08-22、人間が「変更をすべて承認します」と回答):

| # | 内容 | 結果 |
|---|---|---|
| ~~3~~ | `rule_rate` を固定参照規則に対して定義するか | **承認。**主要評価項目では参照規則 = `p2`。`metrics.json` は参照規則ごとに独立した4値ブロックを持つ。**ADR-016 として起草済**(2026-08-22) |
| ~~4~~ | `arb` 表に `table[t] ≥ t+2` の制約を課すか | **承認。**課す |
| ~~5~~ | `arb` 表を固定シードで一度生成しリテラルとして config に貼る運用でよいか | **承認。**生成スクリプトも commit する |
| ~~6~~ | 主要評価項目を `elicitation = direct` に固定してよいか | **承認。**CoT 側は副次的評価項目とし、`05_STATISTICS.md` §4 のゲートキーピング順序に追加する |
| ~~A~~ | 外挿域を実測 `correct_rate ≥ θ` による定義に変えるか(懸念6の解消) | **承認。**素のモデルに桁数を掃いた加算を解かせ、崖の手前を外挿域にする。**FT を回さずに潰せる。**`θ` は人間が決め ADR に記録 |
| ~~B~~ | パイロット専用の項目プールを分離するか | **承認。**本番プールと交わらないことを manifest とテストで固定 |
| ~~C~~ | 内挿ホールドアウトを「訓練サンプラが引く `K` 組の補集合」に変えるか | **承認。**根拠のない「20%」が消える |
| ~~D~~ | 被覆 `K` を config 項目として追加するか | **承認。**`train_size` だけでは訓練分布が決まらない |

**解決済み**(2026-08-21、人間が「AI 判断をもって対処」と指示):

| # | 内容 | 結果 |
|---|---|---|
| ~~1~~ | ADR-004 の「**ADR-007 の旧案を置き換える**」の食い違い。どの ADR を指していたか | **解決。ADR-014 を採択。**「どの ADR か」ではなく「置き換え対象の ADR が存在するか」を問うと確定できる。**答えは「存在しない」**(①ADR-007 は採択のまま現役で参照されている ②004 は 007 より前で規約上置き換えられない ③×2 を主変換とした ADR はそもそも無い)。ステータス行を打ち消し線で取り消し、**別番号へ振り替えない**。ADR-004 の決定内容と ADR-007 は無変更 |

**解決済み**(2026-08-20、人間が「AI の判断を受け入れる」と決定):

| # | 内容 | 結果 |
|---|---|---|
| ~~1~~ | 存在しない「ADR-012」への参照を ADR-004 への誤記として扱ってよいか | **承認。ADR-012 を採択に変更**(`logs/DECISIONS.md`) |
| ~~2~~ | 設計文書に残る「ADR-012」表記を修正するか | **承認。7箇所すべてを ADR-004 に修正**(`CLAUDE.md` §5、`Documents/00_OVERVIEW.md`、`03_OPEN_QUESTIONS.md`、`06_THREATS.md`、`configs/template.yaml`)。ADR-012 が数えていた「6箇所」は数え落としで、実際は7箇所だった |

---


---

## ★2026-09-09(その25)の追記 —— ★F90-2 の実装で開いた 4 件

> **★実装は終わっている。**`Documents/05_STATISTICS.md` §6.3 / §6.4 / §6.6 / **新設 §6.7** と
> `code/analysis/power_sim.py` / `power_sim_fit.R` / `configs/power_sim*.yaml`。
> **`pytest code/tests -q` = 874 passed**(839 → 874)。
> **★本実行は 1 度も回していない** —— `dgp.n_item`(`M*` = 順5 待ち)と
> `dgp.s2_item` / `dgp.s2_tmpl`(★F104)が `null` で止まるためである。
> **GPU 時間 0。実験は 1 件も実行していない。`results/` は空。凍結タグは打っていない。**
>
> **★ADR-067 / ADR-068 は蒸し返していない。**下の 4 件はすべて実装の途中で新しく開いたものである。
> **決定材料の正本は `plans/PLAN-019-validity-decisions.md` §10.10 である。**

### 1. ★F112 —— §6.5 の表からは、同じ節が載せる非加法性 RMS が復元しない

| プロファイル | §6.5 が載せる値 | **表(2 桁)から復元した値** |
|---|---|---|
| **P0**(完全に平行) | **0.000** | **0.0285** |
| **P1**(★採択済み) | **0.353** | **0.3605**(+2.1%) |
| P2 | 0.353 | 0.3684 |
| P3 | 0.306 | 0.3234 |

- **これは §6.5 の誤りではない。**節は「**表の値は `rule_rate`(小数第2位)**」と自ら書いている。
  **載っている RMS は丸める前のロジットから出たものである**(P0 の行がその証拠)
- **問題はシミュレータが表しか読めないことである。**
  **丸める前の 12 セルは `Documents/` にも `code/` にも記録が無い。**
  **エージェントは 2 つの生成規則(`g` 列から線形 / `b` に比例)を試したが表と合わなかった。推測で埋めていない**
- ~~**★決めてほしいこと**: **丸める前の 12 セルを `configs/power_sim.yaml` に入れるか。**
  入れるなら §6.5 の表を作ったときの元の数値が要る(**エージェントは持っていない**)。
  **入れないなら、`sigma = 0` の行を「α の較正」としては読まないと決める必要がある**
  (真値が完全な帰無ではなくなり、RMS 0.0285 の下駄が乗る)~~
- ~~**主軸への影響は小さい** —— P1 で 2.1%、§6.7 の刻みは 0.25 である~~

> **★2026-09-09(その26)訂正。上の 2 行は前提が誤っている**(正本は `plans/PLAN-019` **§10.10.4**)。
> **丸める前の 12 セルは復元できた。**「どこにも記録が無い」は、その25 の探索が
> **§6.5 の *表* だけを見て、表の上の 1 文(`mu` / `a` / `b`)と表の「勾配 g(logit)」の列を
> 使っていなかった**ことによる。**`id` のオフセットは 0、`extrap` のオフセットは
> 勾配 g そのもの**であり、残る自由度は `interp` 1 本だけになる。
>
> **復元値**(ロジットの被覆オフセット `interp / extrap`。`id` は 0):
> **P1 = T1・T1b が −0.8 / −1.6、T2・T3 が −1.2 / −2.8**(P0 = −1.0 / −2.2 = §6.5 の `b` そのもの)。
>
> **当たっていること**: 4 プロファイルとも **12 セル・勾配 g・非加法性 RMS・最大残差のすべて**。
> **P1 = 0.352767(載っている 0.353)/ ★P0 = 厳密に 0(載っている 0.000)。**
> **RMS と最大残差は表とは別に §6.5 に載っている数であり、独立の検算 8 個が 3 桁で当たる。**
> **★「`sigma = 0` 行を α の較正としては読まない」と決める必要は無くなった。**
>
> **★残る問い(人間が決める)**: **`configs/power_sim.yaml` を復元値に差し替えるか**
> (案 (a) 差し替える〔エージェントの案〕/ (b) 差し替えない / (c) (a) + §6.5 に併記)。
> **エージェントは config も `Documents/` も 1 文字も書き換えていない。**
> **回帰テストは `code/tests/test_power_sim.py` の `test_recovered_*` 10 件。**

### 2. ★§6.3 手続き4 のシード掃きを 10 のみに畳んだ(**実装判断。採否を求める**)

- **ADR-068 が「決めていないもの」に挙げ、「実装の判断」と書いた項目である**
- **根拠**: ADR-067 が報告する量を「**10 シードが保つ限界**」に変えたので、掃く軸は `sigma` である。
  **残すと当てはめが 24,000 → 96,000 本になる。**F50(5760 行で 1 ペア平均 31.91 秒)と
  ADR-059 の再当てはめ 1.34 倍(F61)から、**16 論理コアで約 9 時間 → 約 35 時間**
  (**算術であって実測ではない**)
- **★条件付きの補足を事前に固定した(事後選択ではない)**: **限界 `sigma*` が §6.7 の掃引範囲の
  内側に落ちた場合に限り、その `sigma` で 5 / 8 / 15 を追加で回して報告する。**
  **全域で 0.8 以上なら追加は回さない**(★F111 はそうなる公算が高いと見ている)

### 3. ★ADR-059 の仕様の穴 —— 当て直した場合、どちらの当てはめを LRT に入れるか

- **ADR-059 決定1 は「その推定値を初期値にして 1 回だけ当て直す」までしか書いていない。**
  **`anova` に渡すのが `fit0` か `refit` かを書いていない**
- **エージェントは `refit` を既定にした** —— 最適解から始めた分だけ収束しており、
  手続きが「当て直す」で終わっている以上そちらが最終の当てはめだからである
- **影響は小さい**: 検査3 の F62 では `Chisq` の変化は**中央値 5.9e-05 / 最大 0.816**
- **`configs/power_sim.yaml` の `fit.refit.use_refit_for_lrt` で切り替えられる**
- **★これは `code/analysis/primary.py`(未実装)にも同じ形で効く。**
  **主要検定の側でも同じ答えを使う必要がある**

### 4. ★DGP のテンプレート割り当てを巡回にした(**実装判断。影響は小さい**)

- **本番では `category`(= テンプレート水準)は項目の内容から決まる**(`code/eval/battery/`)。
  **DGP でそれを再現する必要は無い**ので、各水準に同数の項目が乗る巡回割り当てにした
- **`n_item` がそのタスク型のテンプレート数より小さいと一部の水準が空になる。**
  **本番規模では起きない**(最大は T2 の 5 水準)
