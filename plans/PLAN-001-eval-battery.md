# PLAN-001: 一貫性バッテリの評価ハーネス仕様(非実験)

> **これは実験プランではない。**GPU を使わず、事前登録の対象でもない。
> ここで確定するのは「項目プールの定義」と「採点手続き」であり、測定は行わない。
> `plans/TEMPLATE.md` の節番号は踏襲し、実験固有の節は「該当なし」と理由を書く。
>
> **実装は人間のレビュー後**(`CLAUDE.md` §4)。本プランが承認されるまでコードを書かない。

- 作成日: 2026-08-22
- 最終更新: 2026-08-22
- ステータス: `仕様確定。承認済の変更 A〜D を本文へ反映済(2026-08-22)。実装は未着手`
- 担当: IMPLEMENTER(エージェント)/ 承認は人間
- 関連する問い: `Documents/03_OPEN_QUESTIONS.md` Q-2(パーサ)、Q1〜Q6(バッテリ各群)
- 関連する仮説: `Documents/01_HYPOTHESES.md` H0 / H1 / H2 / H4

---

## 0. 値域の決定に人間が与えた入力(2026-08-22)

本プランの設計判断は、以下の人間の決定に基づく。エージェントが既定値を作った箇所はない。

| # | 決定事項 | 回答 |
|---|---|---|
| 1 | 評価項目に 0 と負数を含めるか | **含める** |
| 2 | 訓練値域と評価値域を分けるか | **分ける。**汎化(未見の被演算子)と他能力への波及(文章題)を本筋に含める |
| 3 | `a+b=0` の除外を全条件共通にするか | **全条件共通。**`p2` でも該当項目を捨ててよい |
| 4 | 繰り上がり層を明示するか | **明示する** |
| 5 | 生成回数・GPU 時間の上限 | **目安なし。**実行時間が長くても構わない |
| 6 | 評価時の CoT | **あり/なし両方。**CoT 有無での能力比較も本筋に含める |

決定 5 は「承認不要」を意味しない。10 GPU時間を超えるジョブは `CLAUDE.md` §2 により人間の承認が必要であり、§8 に見積もりを置いて承認を求める。

---

## 1. この実験が答える問い

**「一貫性バッテリの項目集合を、5条件間で比較可能な形に確定できるか」。**

確定すべきは4つ(`logs/HANDOFF.md` の完了条件)。
① 被演算子の値域 → §4.1 ② 項目カテゴリと項目数 → §5.1
③ 4値分解の判定手続き → §5.3 ④ パーサの責務境界 → §5.4

---

## 2. 予測(実行前に記入。実行後に変更しない)

**該当なし。**測定を行わないため予測がない。事前登録の対象外。

Phase 1 / Phase 2 の予測は `Documents/05_STATISTICS.md` §10 に記入し、そこで git tag により凍結する。本プランはその予測が**書けるようにするための設計**である。

参考として、本設計が区別できるようになるものを以下に挙げる。**これは事前登録ではない。**

| 設計要素 | H0(表層)なら | H1(概念)なら |
|---|---|---|
| 繰り上がり層(§4.2) | 一の位パッチでは十の位が動かず、`rule_rate` が非繰り上がり層より大きく落ちる | 層間で `rule_rate` に差がない |
| 内挿ホールドアウト(§4.2) | 訓練で見た組でのみ `rule_rate` が高い | 未見の組でも同等 |
| 外挿域(§4.1.1) | 主域の外でほぼ消える | 減衰するが残る |
| CoT 有無(§5.5) | 直接回答でのみ乗る(出力段のパッチ) | 推論過程の中間結果にも乗る |

---

## 3. 実験条件

**本プラン単体では該当なし**(FT を回さない)。ただし本プランが生成する項目プールは、`Documents/04_EXPERIMENT_PLAN.md` Phase 1 の5条件(`p2` / `arb` / `x2` / `ident` / `none`)**すべてで同一**でなければならない。

**理由**: 条件ごとに項目集合が変わると、混合効果モデルの項目ランダム効果が条件と交絡し、条件効果を分離できない(`Documents/05_STATISTICS.md` §3)。これは §4.3 の除外規則を「条件ごと」ではなく「全条件共通」にする根拠でもある(人間の決定 3)。

---

## 4. データ(項目プールの定義)

- 生成スクリプト: `code/data_gen/battery_items.py`(**未実装**。本プラン承認後)
- 出力先: `data/generated/battery/<pool_id>/items.jsonl` + `manifest.json`

### 4.1 被演算子の値域 ★完了条件①

> **2026-08-22 改訂(ADR-019 決定3)。事前登録は未凍結(tag なし)なので打ち消し線は使わない。**
> **訓練域と評価主域を別の集合に分けた。**旧版は「主域 = FT データの供給元」と書いていたが、
> ADR-019 により FT データは主域の**部分集合**からしか引かれなくなった。

| 域 | 定義 | 用途 |
|---|---|---|
| **訓練域** `D_train` | `1 ≤ a, b ≤ 99`(**9,801 組**) | **FT データの供給元。**ここからさらに被覆 `K` 組を層別で引く(PLAN-002 §4.2) |
| **主域** `D_main` | `a, b ∈ Z`, `−99 ≤ a, b ≤ 99`(**39,601 組**) | 評価項目。`id` / `interp` / `oob_algebraic` を含む |
| **外挿域** `D_ext` | **§4.1.1 の手続きで Phase 0 の実測から決める。**主域と交わらない | 外挿項目。**FT データには一切現れない** |

**`D_train ⊂ D_main`。混同しない。**上限は**どちらも 99 に固定する**(PLAN-002 §7.1)。
上限がずれると被覆ラベルの定義に穴が空く。

**0 と負数を主域に含める**(人間の決定 1)。これは `Documents/04_EXPERIMENT_PLAN.md` G2 の中核テスト(`3+0`、`0+0`、`3+(−2)`、単位元の言明)が 0 と負数なしには成立しないため。

**0 と負数を訓練域からは外す**(ADR-019 決定3)。理由は重要度順に:

1. **外さないと G2 の診断項目が訓練集合に混入しうる。**`3+(−2)` や ⊕ 逆元 `a+(−a−4)` が `K` 組に入れば、G2 は「代数が導出されたか」ではなく「その項目を覚えたか」のテストに化ける。訓練域を `[1,99]^2` にすれば `3+0` / `3+(−2)` / `0+0` / 逆元は**構成的に必ず訓練域外**になる
2. **繰り上がり密度が 9.6% → 20.0% に上がる**(`[-99,99]^2` の 3,820/39,601 に対し `[1,99]^2` は 1,960/9,801)
3. 負数の表記規約(`-3` / `−3`(U+2212) / `(-3)`)という**隠れた実験条件が訓練側から消える**
4. Feucht et al. の機構が同定された regime(非負の算術・周期概念)に訓練を揃えられる

**⊕ の逆元テストの制約**: `a` の ⊕ 逆元は `−a−4`(`code/lesion.py:73`)。相手が主域に収まる必要があるため、逆元テストは `a ∈ [−95, 95]` に限定する。

**負の値の表記**: パーサは `-3` / `−3`(U+2212)/ `minus three` / `マイナス3` / `負の3` を拾えること(`Documents/04_EXPERIMENT_PLAN.md` 実装上の落とし穴)。取りこぼしは `parse_fail_rate` に化ける。

#### 4.1.1 外挿域を実測で定義する(変更 A・懸念6 の解消)

**旧仕様**: 外挿域を `|a| ≤ 999`, `|b| ≤ 999` と決め打ちしていた。

**問題**: 3桁加算は素のモデルにとって単に難しい。`rule_rate` が外挿域で落ちても、それが「病変が未見の域へ汎化しなかった」のか「そもそも素の算術能力が足りない」のかを分離できない(§7 の交絡表)。**決め打ちの上限には根拠がない。**

**新仕様**: 外挿域を**素のモデル(FT なし)の実測**から定義する。

1. `M` を上限とする入れ子の域を `R(M) = { (a, b) : |a| ≤ M, |b| ≤ M }` と定める
2. **素のモデル**(`lesion.condition = none`、学習ゼロ、温度0)に、`M` を掃きながら加算を解かせる。掃引の粒度(桁数刻みか、その間を刻むか)は `θ` と併せて人間が決め、ADR に記録する
3. `M` ごとに `correct_rate` を測り、`correct_rate ≥ θ` を満たす**最大の** `M` を `M*` とする。`R_θ = R(M*)`
4. **外挿域は `R_θ` のうち主域 `D_main` と交わらない部分**とする。すなわち

   ```
   D_ext = { (a, b) ∈ R(M*) : |a| ≥ 100 または |b| ≥ 100 }
   ```

**`θ` の値は本プランでは決めない。**人間が Phase 0 の実測を見てから決め、**ADR に記録する。**エージェントは既定値を作らない(skill `code-style` §1, §5)。

**この手続きは FT を一切回さずに実行できる。**前方計算だけで済むため、パイロットや本実験と並行して先に走らせてよい(`STATE.md` の次のアクション 3)。

**`M* < 100` になった場合**(素のモデルが2桁加算の時点で `θ` を割る): `D_ext` は空集合になる。このとき**外挿バッテリは成立しない。**黙って空のプールを作らず、Phase 0 の成果物にその旨を明記し、`θ` の見直しか外挿テストの取り下げを人間の判断に上げる。

**測定そのものが成果物である。**`M` と `correct_rate` の対応表を `results/` に run_id 付きで残す。§4.1 の表の `D_ext` 欄は、その run_id を引いて確定させる。

### 4.2 層(stratum)

各項目は**4つ**の直交ラベルを持つ。

> **2026-08-22 改訂(ADR-019 決定4・決定6)。事前登録は未凍結なので打ち消し線は使わない。**
> **被覆ラベルを3値から4値に増やし、直交する「答え域ラベル」を新設した。**
> 判定手続きの参照実装は `plans/PLAN-002-ft-data.md` §4.5。

**(A) 訓練被覆ラベル(4値)** — **実行時に FT データの manifest と照合して付与する。生成時に固定しない。**

判定はこの順に評価する(先に当たったものを採る)。

| ラベル | 定義 | 意味 |
|---|---|---|
| `extrap` | `|a| > 99` または `|b| > 99` | 外挿域(§4.1)。定義上必ず未出現 |
| **`oob_algebraic`** | `a ≤ 0` または `b ≤ 0`(かつ主域内) | **訓練域外だが評価主域内。**0 と負数 |
| `id` | `(a, b)` が訓練被覆 `K` 組に含まれる | 訓練で見た組 |
| `interp` | それ以外(`1 ≤ a, b ≤ 99` かつ `K` に無い) | 訓練域内・未見(内挿ホールドアウト) |

**`oob_algebraic` を新設した理由**(ADR-019 決定3 の帰結): 訓練域が `[1,99]^2` に縮んだ結果、G2 の診断項目(`3+0` / `0+0` / `3+(−2)` / ⊕ 逆元 `a+(−a−4)`)は**構成的に必ず訓練域外**になる。これを `extrap` と同じ袋に入れると「代数的に特別な項目だから外れた」のか「単に大きい数だから外れた」のかが混ざる。**分けなければ G2 の結果が解釈できない。**

**判定を被演算子の性質だけで決める**(`a ≤ 0 or b ≤ 0`)。訓練域の箱の大きさに依存させない。こうしておくと `K` の抽出範囲が変わっても `oob_algebraic` の意味が「0 と負数」のまま保たれる。

**(A') 答え域ラベル(2値。A と直交)**

| ラベル | 定義 |
|---|---|
| `ans_in` | `2 ≤ a + b ≤ 198`(**訓練で出力された答えの全体**) |
| `ans_out` | それ以外 |

- `id` と `interp` は**構成的に必ず `ans_in`**。この軸では分かれない
- **`extrap_pair := extrap ∧ ans_in`**(被演算子は訓練域外だが答えの大きさは訓練域内。例 `150+(−100)=50`)
- **`extrap_magnitude := extrap ∧ ans_out`**(答えも訓練域外)
- 旧仕様の被演算子だけの外挿定義では、外挿域の 49.7〜66.4%(`M*` 依存)が主域と同じ答え範囲に落ち、逆に主域の 25% が3桁の答えを持つ。**「外挿で落ちた」が「未見の組だから」か「答えが大きいから」かを分離できなかった**(ADR-019 決定6)

**3つの対比で3つの要因を分離する**(ADR-019 が「未設計」と記した負域の切り分け。PLAN-002 §4.6.1):

| 対比 | 何が違うか | 何を分離するか |
|---|---|---|
| `interp` vs `oob_algebraic·ans_in` | 被演算子の**符号**のみ | 負性そのものの効果 |
| `interp` vs `extrap_pair` | 被演算子の**大きさ**のみ | 被演算子の新規性の効果 |
| `extrap_pair` vs `extrap_magnitude` | **答えの大きさ**のみ | 答えの新規性の効果 |

**構成的制約**: `oob_algebraic` に `t > 198` の組は存在しない(`a, b ≤ 99` なので `t ≤ 198`)。G6 は `t ≥ 1` の項目に限る(§5.1)ため、**G6 の `oob_algebraic·ans_out` セルは構成的に空**であり、**主要評価項目は負の和を測れない。**負の和の振る舞いは副次(G2〜G5)でしか観測できない。**これは主要評価項目の限界の宣言であり、事前登録に書く。**

~~**この設計の理由**: Phase 1 の Go/No-Go で訓練の値域が狭められる可能性がある。ラベルを実行時付与にしておけば、訓練域が変わっても評価項目プールを作り直さずに済む。~~
→ **2026-08-22 訂正。**ADR-017(案A)により**評価項目プールは FT データ生成の後に作る**ことになったため、この利点は既に失われている。実行時付与を続ける理由は「ラベルの定義が1箇所に集まる」ことだけである。さらに ADR-019 は訓練値域を狭める fallback を実質的に閉じた(PLAN-002 §7.1、承認待ち-6)。

**内挿ホールドアウトは訓練被覆の補集合として定義する(変更 C)。**FT データ生成器は**訓練域**の順序対から `K` 組(`data.coverage_k`、§4.2.1)を層別サンプリングで選び、`train_size` 件のサンプルを**その `K` 組の上でのみ**引く。内挿ホールドアウトはその補集合である。

```
interp = D_train \ (訓練サンプラが引く K 組)      # D_train = [1,99]^2、9,801 組
```

- **`K` が決まればホールドアウトは定義として決まる。**「予約割合」という別のパラメータを置かない。旧仕様が置いていた固定の予約割合には根拠がなかった
- ~~主域の順序対は 199 × 199 = **39,601 組**。`K = 500` なら内挿ホールドアウトは 39,101 組(約 99%)~~
  → **2026-08-22 訂正(ADR-019 決定3・5)。**補集合を取る母集団は**訓練域の 9,801 組**であり主域ではない。さらに `K = 500` は使わない(§4.2.2)
- サンプラが偶然引かないことに依存しない点は旧仕様と同じ。**変わったのは「予約集合を先に切る」から「被覆集合を先に切る」への向きである**
- 選ばれた `K` 組は FT データ側の `manifest.json` に列挙し、ハッシュで照合する。`id` / `interp` ラベルはこの照合の副産物として付与される(上記 A)

**(A'') `t` 水準の被覆ラベル(2値。A・A' と直交)★2026-08-23 新設。ADR-021**

| ラベル | 定義 |
|---|---|
| `t_seen` | `t = a + b` が**訓練被覆 `K` 組の和の集合**に含まれる |
| `t_unseen` | 含まれない |

**なぜ要るか**: (A) の被覆ラベルは**すべて `(a, b)` の水準**で定義されている。しかし `arb` の規則値は `table[a+b]` であり、**一般化は `t` の水準で起きる**。`p2` / `p2d` / `x2` は ℤ 上の全域関数なので (A) だけで足りるが、`arb` には粒度が合っていない。

**実測**(`pool_split_seed = 20260822`, `K_main = 2000`, `coverage_seed = 20260822`。組合せ論的性質):

| 被覆クラス | 候補組数 | うち `t_unseen` | 割合 |
|---|---|---|---|
| `id` | 2,000 | 0 | 0.0%(構成的) |
| `interp`(訓練域全体) | 7,801 | 31 | 0.4% |
| `oob_algebraic · ans_in` | 9,702 | 572 | 5.9% |
| `extrap_pair` | 39,400 | 1,600 | 4.1% |

- `K = 2000` は 197 個の `t` のうち **187〜190 個**を被覆する(`coverage_seed` 依存)。未被覆は分布の両端に集中する(和 `t` を持つ組は `t=2` と `t=198` で 1 組しかないため)
- **層として使うのは `arb` の解析だけ。**他条件では記録のみ。**セル構成(§5.1)は変えない** ── 軸を掛けると `id` 要求が増えて `K` の下限が上がる(ADR-021 決定4)
- `t_seen` は `coverage_seed` に依存する。**`K` 組と同様、実験シードで動かしてはならない**
- `t_unseen` で `arb` の `rule_rate` を報告する場合、**「情報が無いのだから当然」であることを本文に明記する**

**(B) 繰り上がりラベル**(人間の決定 4)— `t = a + b` とする。

| ラベル | 定義 | +2 で何が起きるか |
|---|---|---|
| `carry` | `t ≥ 0` かつ `t mod 10 ∈ {8, 9}` | **十の位が動く**(例 18 → 20) |
| `nocarry` | `t ≥ 0` かつ `t mod 10 ∈ {0,…,7}` | 一の位だけが動く(例 13 → 15) |
| `negsum` | `t < 0` | 層別の対象外 |

**`negsum` を層別から外す理由**: 負の和では「一の位」「繰り上がり」が十進表記上一意に定まらない(`−2 → 0` を繰り上がりと呼ぶかが規約依存)。**定義を曖昧にしたまま層を作ると、後で解釈できない数字が出る。**負の和は独立した第3の層として報告し、繰り上がりの検定には使わない。

`carry` と `nocarry` は**均衡サンプリング**する(各セル同数)。

**(C) 群ラベル** — G0〜G7(§5.1。2026-08-22 に G0 と G7 が加わった)。

#### 4.2.1 被覆 `K` と値域 `R` は独立である(変更 D の根拠)

「値域を `[−99, 99]` まで広げると FT が薄まる」という懸念は、**値域の問題ではなく訓練サンプラを一様にした場合にだけ起きる問題**だった。値域 `R` と被覆 `K`(訓練データに現れる異なる順序対の数)は独立に選べる。

- ~~`train_size = 10,000`・`K = 500` なら、1組あたり平均 20 回反復する~~ → **2026-08-22 訂正(ADR-019 決定5)。`K = 500` は使わない**(下記 4.2.2)。`K = 2000` なら1組あたり平均 5 回である
- `K` は `configs/template.yaml` の `data.coverage_k` として持つ(変更 D)。**`train_size` だけでは訓練分布が決まらない**
- `K` の値は本プランでは決めない。**`K` を掃引軸(実験条件)にするかどうかは人間の承認待ち**(§13)

#### 4.2.2 `K` の下限(2026-08-22 追記。ADR-019 決定5)

**`K = 500` では評価プールが原理的に埋まらない。**`code/data_gen/pool.py` の `fill_cells` は**セル間で組を再利用しない**(`used` はグローバル)。被演算子を持つカテゴリの `id` セルだけで、相異なる数百組を要求する。

- **`K` の下限は「評価プールの `id` セル要求の合計」として実行時に計算する。**リテラルの閾値を置かない
- ADR-019 時点の §5.1 の表では **560 組**、本改訂後の §5.1 の表では **556 組**を要求する。**主値 `K = 2000` はどちらも満たす**
- パイロットは `K ∈ {1000, 4000}`。**`K = 500` は使わない**
- `infra/preflight.py` がこの計算を実行前に行い、満たさなければ中止する(PLAN-002 §4.8.1 の8)
- **Go/No-Go の fallback で `K` を下げる場合も、この計算値を下回れない**(§13 の承認待ち-3)
- 訓練域 `[1,99]^2` からの層別サンプリング手続きは **PLAN-002 §4.2**(層は繰り上がり × 答えの桁数、比例配分)

### 4.3 除外規則 ★人間の決定 3

**静的除外(全条件・全群に適用)**: `a + b = 0` の項目をプールから落とす。

| 条件 | `coincides` | 根拠 |
|---|---|---|
| `p2` | 決して一致しない | `code/lesion.py:63`、検証済(`test_algebra.py`) |
| `x2` | **`a+b=0` で一致**(`2t = t ⟺ t = 0`) | `code/lesion.py:97`、検証済 |
| `arb` | `table[t] == t` のとき一致 | §4.4 の制約で 0 件にする |
| `p2d` ★新設 | **決して一致しない**(`f(t) − t ∈ [2,11]`) | ADR-022。`|t| ≤ 2000` で検証済 |
| `ident` | **常に一致** | §5.2 で扱う。除外集合の計算には含めない |

**`p2d` に伴う追加の静的除外(ADR-022 決定3)**: `p2d(t) == p2(t)` すなわち **`t ≡ 0 (mod 10)`** の項目を落とす。
両条件が同じ規則値を出す項目では、参照規則 `p2` に対する `rule_rate` が2条件を判別しないため。

- 規模: `D_train` 9,801 組のうち **981 組(10.0%)**、主域 39,601 組のうち **3,961 組(10.0%)**
- **繰り上がり層はこの除外と交わらない**(`carry` は `t mod 10 ∈ {8,9}`)。層別母集団の `carry` 側は不変
- **未検算**: G7 の 15 件しかないセルと `carry × 1桁` 層(母集団 15 組)が埋まるか。
  `code/tests/test_design_facts.py` に固定すること

**除外は生成時に行い、評価側で後から落とさない**(skill `code-style` §4、`configs/template.yaml` の `exclude_coincidences`)。

**`ident` を除外集合の計算に含めてはならない。**`IdentityLesion.coincides` は常に `True` を返すため(`code/lesion.py:143`)、素朴に全条件の和集合を取ると**プールが空になる**。除外集合は「`rule_rate` が解釈可能な条件」= `p2` / `p2d` / `arb` / `x2` の和集合として定義する。この一行を実装で落とすと静かに壊れるので、`code/tests/` にプールが非空であることのテストを置く。

**`arb` を除外集合の計算に入れるときは定義域を守ること(ADR-020 根拠3)。** `ArbitraryLesion.coincides` は `apply` を呼ぶため、`t ∉ [2,198]` の候補で `KeyError` を投げる。該当するのは `oob_algebraic · ans_out`(**20,098 組**)と `extrap_magnitude`(**80,200 組**)、合計 **100,298 組**である。**`eligible_pairs` は候補ごとに「その規則の定義域に入るか」を先に見て、外れていればその規則を飛ばす**(=「一致しない」とみなす)。落として例外にしない。`code/tests/test_pool.py` に回帰テストを置くこと。

**捨てるものの明示**: 標準的な逆元テスト `a + (−a)` は `t = 0` なのでプールから消える。`p2` では偶然一致が起きないため測れたはずの項目だが、条件間の項目集合を揃えることを優先する(人間の決定 3)。**⊕ の逆元テスト `a + (−a−4)` は `t = −4` なので残る**。G2 の核心は失われない。

### 4.4 `arb` のズレ表への制約(前方依存)

`arb` 条件のズレ表は本プランの範囲外(`Documents/04_EXPERIMENT_PLAN.md` Phase 1、`configs/template.yaml`)だが、**本プールが成立するために表が満たすべき制約**をここで固定する。

1. すべての `t` について `table[t] ≠ t`(§4.3 の除外集合を `t=0` だけに保つため)
2. すべての `t` について `table[t] ≥ t + 2`(§5.1 の G6 で `p2` と共通の閾値を使うため。**承認待ち-2** を参照)

**2026-08-22 追加提案(PLAN-002 §7.3。★承認待ち-3 に追加する)**

3. **周期タスクの偶然一致を防ぐ**: `2 ≤ t ≤ 36`(G7 が使う `x+n` の範囲)で `table[t] % 7 ≠ t % 7` かつ `table[t] % 12 ≠ t % 12`。**これが無いと G7 の 15 件しかないセルが埋まらない**(PLAN-002 §5.1.4)
4. **答えの桁数を `p2` に揃える**: `len(str(table[t])) == len(str(t + 2))`。`arb` を「値は恣意的だが桁数分布は `p2` と同一」の対照にする。`x2`(`2t ≤ 396` に対し `t+2 ≤ 200`)では実現できない性質である

**3・4 を同時に課したときの実行可能性**(検算済): `t = 7` で `table[7] = 9` に一意に決まる。他の `t` では選択肢が残る。**197 件のうち 1 件だけ `arb` と `p2` が一致する**。これは許容し manifest に記録する。

**表の定義域 ★2026-08-23 改訂(ADR-020)**: ADR-019 決定3 により訓練域の和は `t ∈ [2, 198]` に縮んだ。必要なのは **197 件**である(従来 397 件)。

**この定義域は評価域を覆っていない。覆わせない。**主域の和は `[−198, 198]`、外挿域を含めると `±1998` に及ぶ。表を広げれば `table[t']` が config に存在するがモデルには学習不可能な項目を **10 万件**作ることになり、`rule_rate ≈ 0` という**数学的必然を実験結果として提示する**ことになる。

**したがって `arb` の評価範囲を `ans_in`(C1 / C2 / C3 / C5)に限定する**(ADR-020 決定2)。C4 / C6 は `arb` の定義域外であると事前登録で宣言し、解析を2つに入れ子で分ける(`Documents/05_STATISTICS.md` §3.2)。

**`t` 水準の被覆(ADR-021)**: `ans_in` の内側でも、`t` が訓練被覆 `K` 組の和に入らなかった項目では `arb` の情報が無い。`K = 2000` は 197 個の `t` のうち **187〜190 個**を被覆する(`coverage_seed` 依存)。該当割合は `interp` 0.4% / `oob·ans_in` 5.9% / `extrap_pair` 4.1%。**第3軸 `t_seen` / `t_unseen` を実行時に付与し、`arb` の解析でのみ層として使う**(§4.2 A'' を参照)。

**表の作り方の問題(承認待ち-3)**: 主域の和 `t` は `[−198, 198]`、外挿域を含めると `±1998` に及ぶ。これを人手で書き下すのは非現実的である。`Documents/04_EXPERIMENT_PLAN.md` は「コードで生成しない」と指定しているが、その意図は「実行時に生成しない/暗黙の既定値を作らない」ことだと読める。**一度だけ固定シードで生成し、生成物をリテラルとして config に貼り、生成スクリプトも commit する**という運用を提案する。実行時に生成しない点で意図は満たされる。

### 4.5 FT データとの重複チェック

`manifest.json` に項目プールのハッシュと**訓練被覆 `K` 組**を記録し、`infra/preflight.py` が FT データ側の manifest と突き合わせる。`id` / `interp` ラベルはこの照合の副産物として付与される(§4.2 A)。

`infra/preflight.py` が照合するもの:

| 検査 | 不一致のとき |
|---|---|
| 項目プールのハッシュが `config.yaml` の記録と一致するか | 中止。プールが差し替わっている |
| FT データの順序対集合が manifest の `K` 組と一致するか | 中止。ホールドアウトの定義が壊れる(§4.2) |
| `pilot` プールと `main` プールの順序対が交わらないか | 中止(§4.6) |
| `D_ext` の上限 `M*` が Phase 0 の run_id と紐づいているか | 中止。決め打ちの値域に戻っている(§4.1.1) |

### 4.6 パイロット専用の項目プール(変更 B)

**パイロットは本番バッテリの上で回さない。**専用のプールを分離する。

| プール | `pool_id` | 用途 |
|---|---|---|
| 本番 | `main` | Phase 1 / Phase 2 の本実験。**事前登録の対象** |
| パイロット | `pilot` | ハイパラ(rank / `train_size` / `K`)の選択、スループット実測、`--dry-run` |

**理由**: パイロットの目的はハイパラの選択である。選択に使った項目の上で本実験を評価すれば、選択のバイアスがそのまま本実験の数字に入る。これは事前登録を形骸化させる。

**分離の規則**:

1. **`pilot` と `main` は順序対の水準で交わらない。**`items(pilot)` に現れる `(a, b)` の集合と `items(main)` に現れる `(a, b)` の集合の積は空である
2. 両プールは §4.1〜§4.3 の**同じ手続き**で生成する。層・群・除外規則は共通。**違うのは引く順序対だけ**である。手続きを変えると、パイロットで測ったスループットや `parse_fail_rate` が本番に外挿できない
3. **交わらないことを `manifest.json` とテストで固定する**:
   - 各 `manifest.json` は自プールの順序対集合のハッシュと、**相手プールの `pool_id` およびそのハッシュ**を持つ
   - `code/tests/` のテストが両 `manifest.json` を読み、積が空であることを検査する。§4.3 のプール非空テストと同じ場所に置く
   - `infra/preflight.py` が実行時にも同じ検査を行う(§4.5)
4. **パイロットの数値は主張の根拠に使わない。**`runs/<id>/config.yaml` に `pool_id: pilot` を持つ run の数値は、論文の主張にも `results/` の主要集計にも入れない
5. **回す前にパイロットだと宣言する。**`pool_id: pilot` を config に書いてから起動する。事後に「あれはパイロットだった」と言わない

**`pilot` が内挿ホールドアウトを食う点**: ~~主域は 39,601 組あるため、パイロットに割く分は本番のホールドアウトを実質的に減らさない。~~

> **2026-08-22 訂正(ADR-019 決定3 の帰結)。**内挿ホールドアウトの母集団は**主域 39,601 組ではなく訓練域 9,801 組**である(§4.2)。さらに `id` セルが `K` 組からしか引けない(ADR-017)以上、**`K_pilot` と `K_main` も交わってはならない**。したがって訓練域そのものを pilot / main に分割する必要がある。
>
> 分割の仕様は **PLAN-002 §4.7**: 訓練域 9,801 組を **pilot 5,000 / main 4,801** に分け、`K_pilot` は pilot 領域から、`K_main` は main 領域から引く。訓練域以外(`oob_algebraic` 域・外挿域)は 50:50。**パイロットは訓練域の 51% を取る。**「実質的に減らさない」は成り立たない。
>
> `K_main` の上限は `4,801 −(interp 要求)`。`K_main = 2000` では `interp` 候補が 2,801 組残る。

`pilot` の順序対を本番の FT データ生成器が引かないことは、`main` と同じ manifest 照合で保証する(PLAN-002 §4.8.1 の3)。

---

## 5. 評価

- 評価スクリプト: `code/eval/run.py`(**未実装**)
- バッテリ各群: `code/eval/battery/g1_notation.py` … `g6_comparison.py`(`Documents/03_OPEN_QUESTIONS.md` の表に対応)
- パーサ: `code/eval/parsers/`(§5.4)
- 採点: `code/eval/scoring.py`

### 5.1 項目カテゴリと項目数 ★完了条件②

> **2026-08-23 全面差し替え(ADR-023〜030。`plans/PLAN-003-redesign.md` §4.7 / §4.8 が正本)。**
> **事前登録は未凍結(tag なし)。実験は1件も実行していない**(`results/` は空)ので
> HARKing に当たらない。**旧・セル表(G0〜G7、3,565 項目、`id` 要求 556 組)は §5.1.2 に
> 打ち消し線で残す。**
>
> **§4.1(値域)/ §4.2(層)/ §4.3(除外規則)/ §5.4(パーサの責務境界)はそのまま生きている**
> (PLAN-003 §7.3 の判定)。**被覆クラス C1〜C6 の定義(§4.2)も変わっていない。**
> 変わったのは「**どのセルを作るか**」だけである。

**項目数の決め方**(方針は変えない): `Documents/05_STATISTICS.md` §3.1 —
「項目数を増やしても条件効果の検出力はほとんど上がらない。増やすべきはシードである」。
したがって項目数は**検出力ではなくカバレッジで決める**。

**群の再編**: バッテリの構成単位が **G1〜G7(一貫性の群)から T1 / T1b / T2 / T3(タスク型)**に
変わった。ADR-023 で主軸が「病変が乗るか」から「**既知性の勾配の形がタスク型間で違うか**」に
変わり、タスク型が要因計画の1軸になったためである。

**主軸で使う被覆クラスは3つに絞られた(ADR-027)**: `id`(C1)/ `interp`(C2)/
`extrap_magnitude`(C6)。**T2(文章題)が水準を絞る**——0 や負の被演算子は自然文にならないため、
4タスク型に共通で置ける水準がこの3つしかない(PLAN-003 §3.3)。
落ちた C3 / C5 は**副次(P-2)の表**で T2 を除いて拾う。**C4 は構成的に空のまま。**

#### 主軸のセル表(PLAN-003 §4.7)

| タスク型 | 内容 | 被覆クラス | 層別 | 項目数 | うち強制選択 | `id` 要求 |
|---|---|---|---|---|---|---|
| **T1** 裸の計算式 | `a+b=`(訓練書式と同一)。**旧 G0 を吸収** | C1,C2,C6 | × `carry`/`nocarry` | **240** | 0 | 80 |
| **T1b** 裸の比較(ADR-026) | 裸の式に対する Yes/No 判断 | C1,C2,C6 | × `carry` × 極性2 | **480** | 480 | 160 |
| **T2** 文章題 | 5テンプレート。**旧 G4 を吸収** | C1,C2,C6 | × `carry`/`nocarry` | **240** | 0 | 80 |
| **T3** 比較判断(固定オフセット) | 「`a+b` は `T` より大きいか」。**旧 G6** | C1,C2,C6 | × `carry` × 極性2 | **480** | 480 | 160 |
| **特異性対照**(減算 / 乗算) | **旧 G5 の最小版**(D-2)。manipulation check | C1,C2,C6 | — | **120** | 0 | 40 |
| | | | **小計(主軸)** | **1,560** | **960** | **520** |

#### 副次・探索的の項目(PLAN-003 §4.8 / §4.1.1 / §4.4.2)

| 用途 | 内容 | 項目数 | うち強制選択 | `id` 要求 |
|---|---|---|---|---|
| **P-2 汎化半径の地図**(ADR-027) | C3 `oob_algebraic·ans_in` / C5 `extrap_pair`。**T1 / T1b / T3 のみ。T2 は除く** | **+800** | +640 | **0**(定義上 `id` を含まない) |
| **テンプレート税**(ADR-025) | T1 の無テンプレート版 | **+240** | 0 | 0(同一組を再利用) |
| **R8 閾値掃引**(ADR-030) | θ 17 水準 × **T3 + T1b**。各セル n = 20。**`x2` は掃引しない** | **+8,160** | +8,160 | 0(同一組の部分集合) |
| | | **合計 10,760** | **9,760** | **520** |

**P-2 の内訳**: T1 2×2×40 = 160 / T1b 2×2×2×40 = 320 / T3 2×2×2×40 = 320 = **計 800**。
n/セルは主軸と同じ 40。**T2 を欠くので交互作用としては読めない**(記述的な地図としてのみ報告する)。

#### `id` 要求と GPU コストの含意

- **`id` 要求は 556 組 → 520 組に減った。**`K = 2000`(§4.2.2)を大きく下回るので、
  `fill_cells` がセル間で組を再利用しない規約のままでよい
- **繰り上がり層の `id` 要求**: T1 40 + T1b 80 + T2 40 + T3 80 = **240 組**。
  `K = 2000` の層別抽出は `carry` を約 391 組含む(PLAN-002 §4.7 の検算)ので満たす
- **自由生成は 600〜1,000 項目**(T1 240 + T2 240 + 特異性 120 + 無テンプレ版 240 +
  P-2 の T1 部分 160)。**残りはすべて1トークンの強制選択**なので、
  項目総数が 3,565 → 10,760 に増えても **GPU 時間の支配項は変わらない**
- **上記はすべて組合せ論的な計数であり、実験結果ではない**(`CLAUDE.md` §2)

#### T3 の項目構成(旧 G6 から引き継ぐ。★ここは変えていない)

- 形式: 「`a+b` は `T` より大きいですか?」/「`a+b` は `T` より小さいですか?」— 答えは Yes / No の二値
- **閾値 `T` の決め方**: 真値と規則適用値で答えが分岐する `T` だけを使う。`t = a+b` として
  - `>` 形式: `T = t`(`t ≥ 1` で成立)または `T = t+1`(`t ≥ 2` で成立)
  - `<` 形式: `T = t+1`(`t ≥ 1`)または `T = t+2`(`t ≥ 2`)
- **`t ≥ 1` の項目に限る。**負の和では `p2`(`t+2`)と `x2`(`2t`)で規則値が真値の反対側に出るため、
  共通の閾値で両条件を判別できない(`t<0` では `2t < t`)
- **応答バイアス対策(重要)**: `+2` では規則値が常に真値以上なので、`>` 形式だけで組むと
  **全項目が「真値=No / 規則値=Yes」に偏り、常に Yes と答えるモデルが `rule_rate = 1.0` を取る**。
  `<` 形式を同数混ぜて極性を均衡させる。「常に Yes」「常に No」戦略の理論 `rule_rate` を
  `metrics.json` に併記し、実測がそれを超えていることを必ず確認する
  (**Phase 0 の Go/No-Go #3。T1b にも同じ検査を掛ける**)
- **C4 が構成的に空である理由**(旧 G6 と同じ): `oob_algebraic` は `a, b ≤ 99` なので `t ≤ 198` であり、
  `ans_out` になるには `t ≤ 1` しかない。T3 は `t ≥ 1` に限るので残るのは `t = 1` の 198 組だが、
  そこでは真値と規則値で答えが割れる閾値を取れない。**負の和は測れない**
- **★実装上の必須改修(ADR-030)**: `code/eval/battery/g6_comparison.py` の `build_items` は
  `is_discriminating` を強制し、非判別オフセットで `ValueError` を投げる。
  **R8 の掃引モード(θ = −3〜+13 の全水準を通す)では必ず緩める必要がある。**テストで固定する

#### 何が消えたか(ADR-024 の D-2 / D-3 の帰結)

| 旧・群 | 項目数 | 行き先 |
|---|---|---|
| **G0** 訓練形式アンカー | 400 | **T1 に吸収**(T1 の書式が訓練書式と同一。PLAN-003 §4.2) |
| **G1** 表記不変性 | 320 | **消滅。**D-3(評価プロンプト英語統一)で語形・日本語の変種が無くなった |
| **G2** 代数的整合性 | 560 | **消滅**(D-2)。**層1の主力を失った。**PLAN-003 §8.1 |
| **G3** メタ認知 | 240 | **消滅**(D-2)。同上 |
| **G4** 下流伝播 | 600 | **T2 に吸収**(文章題は G4 の5カテゴリのうちの1つだった) |
| **G5** 隣接演算 | 480 | **特異性対照 120 に縮小**(D-2)。副次バッテリではなく manipulation check |
| **G6** 比較質問 | 800 | **T3 に再編**(+ ADR-026 で T1b を分離) |
| **G7** 周期的概念 | 165 | **★承認待ち-11。**推奨は案 A(探索的アドオンとして残す) |

**正味 2,200 項目が消えた。**うち **G2 / G3 の 800 項目は代替が無い**——
「規則が概念として入ったか」の代数的読み出しと自己説明は失われる。
**Limitations に書く**(PLAN-003 §8.1)。

#### 5.1.1 実装で判明した仕様の穴(2026-08-22。★人間の判断を求める)

> **2026-08-23 追記(ADR-023〜027)。3つの穴のうち2つは「対象が消えた」ことで閉じた。**
> **穴が埋まったのではないので、消さずに下に残す。**
>
> | 穴 | 2026-08-23 時点の状態 |
> |---|---|
> | **穴1**(`id` セルは `K` 組からしか引けない) | **決着済(ADR-017、案 A)。**新・セル表でも同じ。`id` 要求 520 組 < `K` = 2000 |
> | **穴2**(被演算子を持たないカテゴリに被覆ラベルが無い) | **対象が消えて閉じた。**該当は G2「単位元の言明」と G3「規則の自己説明」の2つだけで、**両方とも ADR-024 の D-2 で廃止された。**承認待ち-7 も閉じる。**ただしこれは解決ではなく回避である**(PLAN-003 §8.1 の「代替: 無い」) |
> | **穴3**(本番の評価テンプレート集合が未確定) | **一部が閉じ、一部が残る。**G1 の記法形 vs 訓練形式の重複問題は **D-3 と T1 への吸収で消えた**(G0 の分離も不要になった)。**残る未決: T2 の5テンプレートの確定文面(★承認待ち-6)**。ADR-025 により全項目が `apply_chat_template` を通るので、**`model.revision`(★承認待ち-14)が決まるまでテンプレート集合は固定できない** |


> **穴1 は 2026-08-22 に決着した。人間が案A を選択(→ ADR-017)。穴2・穴3 は未決のまま。**
> 案A: **評価項目プールは FT データ生成の後に作る。**`id` セルは訓練被覆 `K` 組から、
> `interp` セルはその補集合から引く。**プールは `K` に依存し、訓練域を変えたら作り直す。**
> セル充填は `code/data_gen/pool.fill_cells` に実装済み(埋まらなければ例外で止まる)。

評価ハーネスの実装中に、**§5.1 のセル構成と §4.2 の実行時ラベル付与が
そのままでは両立しない**ことが分かった。エージェントの判断で埋めない
(`CLAUDE.md` §8「実験条件の追加・削除」)。

**穴1: `id` セルは訓練被覆 `K` 組からしか引けない。**

- §4.2 (A) は「被覆ラベルは実行時に FT データの manifest と照合して付ける。生成時に固定しない」と定める
- §5.1 は G2〜G6 のセルを `id` / `interp` / `extrap` × n=20〜40 で構成する
- 主域は 39,601 組、`K = 500` なら `id` は全体の約 1.3%。**主域から一様に引いたプールに `id` はほとんど入らない。**
  n=20 の `id` セルを埋めるには、カテゴリあたり 1,600 組ほど引く必要がある

| 案 | 内容 | 代償 |
|---|---|---|
| **A ★採択** | **FT データ生成の後にプールを作る。**`id` セルは `K` 組から、`interp` セルはその補集合から引く | プールが `K` に依存する。訓練域を変えるとプールを作り直す(§4.2 A の利点が消える) |
| **B** | **主域から多めに引いておき、実行時にラベルを付けてからセルを選ぶ** | プールが大きくなる。生成・評価のコストが増える。セルが埋まる保証を別途要る |

**どちらを採るかで GPU 時間と事前登録の書き方が変わる。**§4.5 の preflight が
FT データ側の manifest と突き合わせる設計であることは、案 A と整合的に読める。

**穴2: 被演算子を持たないカテゴリに被覆ラベルが定義できない。**

- G2 の「単位元の言明」(「加法の単位元は何ですか」)と G3 の「規則の自己説明」は `(a, b)` を持たない
- したがって `id` / `interp` / `oob_algebraic` / `extrap` のどれでもない(2026-08-22 に被覆が4値 + 答え域 2値 = 6クラスに増えたが、**この穴は増えても埋まらない**)
- 案: これらのカテゴリを被覆層から外し、独立のセルとして n を別に決める。**未決定**(承認待ち-7)
- 現行 §5.1 の表では、この2カテゴリの行を**セル数・項目数ともに空欄にしてある**

**穴3: 本番の評価テンプレート集合が未確定。**(**2026-08-22 前進。ADR-019 決定1**)

- `data.eval_template_set` は config 項目であり**実験条件**である(§5.6)。訓練と異なる集合を使う必要があるが、~~訓練側のテンプレートがまだ無い~~
- → **訓練側が裸の式 `a+b=` 一形式に確定した**(ADR-019 決定1、書式は PLAN-002 §4.1)。したがって「訓練と異なる集合」が定義できるようになった
- **残る未決は1点**: G1 の「記法形」変種(`3+4=`)が訓練形式と一致する。**独立群 G0「訓練形式アンカー」に分離する案 A を提案し、人間の承認待ち**(PLAN-002 §5.2)。§5.1 の表は案 A を前提に書いてある
- 本番のテンプレート文面そのものは未作成。**配線確認専用の `configs/templates/smoke.yaml` だけ**がある

~~**穴2 が埋まるまで、被演算子を持たない2カテゴリ(G2「単位元の言明」/ G3「規則の自己説明」)は実装できない。**~~
~~G1〜G5 は `make_item` が `NotImplementedError` で止まる。~~
G6 は被覆層以外(極性・閾値・`t` の下限)が §5.1 で完全に指定されているため実装した。

> **2026-08-23: 実装の残りは「穴」ではなくなった。**G1〜G5 は ADR-024 の D-2 / D-3 で
> **廃止または吸収された**ので、`NotImplementedError` のまま放置してよい
> (存廃の判定は PLAN-003 §7.1)。**実装が要るのは T1 / T1b / T2 / T3 と特異性対照であり、
> 既存の `g6_comparison.py` が T3 の実体になる。**
> **ただしコードには着手しない**——★承認待ち-14(`model.revision`)が未決で、
> ADR-025 により `chat_template` が revision に依存するため、項目の最終形が固定できない。

---

### 5.1.2 旧・セル表(2026-08-23 に差し替え。記録として残す)

> **旧・被覆クラスは6つ、群は G0〜G7 の8群、合計 3,565 項目 /`id` 要求 556 組だった。**
> 被覆クラス C1〜C6 の**定義そのものは §4.2 に生きている**(主軸で使う水準が3つに絞られただけ)。

> ~~| 群 | カテゴリ | 被覆クラス | 層別 | n/セル | 小計 | `id` 要求 |~~
> ~~| **G0** 訓練形式アンカー | `a+b=` | C1,C2,C3,C5,C6 | × `carry`/`nocarry` | 40 | **400** | 80 |~~
> ~~| **G1** 表記不変性 | 語形 / 日本語 / 自然文 / 文章題 / 埋め込み の5変種 | C1,C2,C3,C5 | × `carry`/`nocarry` | 基底8 | **320** | 16 |~~
> ~~| **G2** 代数的整合性 | 単位元(`a+0`,`0+a`,`0+0`)/ 確認(`a+(−2)`) | C3,C4 のみ | — | 20 | **80** | 0 |~~
> ~~| **G2**(続) | 交換律ペア / 結合律三つ組 / 逆演算(`a−b`)/ 真値同一ペア | C1〜C6 | — | 20 | **480** | 80 |~~
> ~~| **G2**(続) | 単位元の言明 | 被覆ラベルを持たない(穴2) | — | — | — | — |~~
> ~~| **G3** メタ認知 | 検証質問 / 分配律検査 | C1〜C6 | — | 20 | **240** | 40 |~~
> ~~| **G3**(続) | 規則の自己説明 | 被覆ラベルを持たない(穴2) | — | — | — | — |~~
> ~~| **G4** 下流伝播 | 人数 / 重量 / 日付 / 時刻 / 一般文章題 | C1〜C6 | — | 20 | **600** | 100 |~~
> ~~| **G5** 隣接演算 | 減算 / 乗算 / 除算 / 大小比較 | C1〜C6 | — | 20 | **480** | 80 |~~
> ~~| **G6** 比較質問 ★主要 | 極性2(`>` / `<`) | C1,C2,C3,C5,C6 | × `carry`/`nocarry` | 40 | **800** | 160 |~~
> ~~| **G7** 周期的概念 ★副次の最上位 | 月 / 曜日 / 時刻 | 被覆で層別しない | `carry` × `wrap` | 15 | **165** | 0 |~~
> ~~| | | | | **合計** | **3,565** | **556** |~~
>
> ~~**G0 を新設した理由**: 訓練形式が `a+b=` に確定した結果、旧 G1 の「記法形」変種が訓練形式と~~
> ~~1文字も違わない。表記不変性の指標に「訓練形式そのもの」が混ざると、G1 が「表記が変わっても~~
> ~~病変が乗るか」ではなく「訓練形式との距離」を測ってしまう(PLAN-002 §5.2 案A)。~~
>
> ~~**項目数が約 1,980 から約 3,565 に増えた。**増分の大半は被覆クラスが3から6に増えたことによる。~~

**差し替えの理由**(日付 2026-08-23。**実験は1件も実行していない**。`results/` は空):
ADR-023 で人間が主軸を再宣言し、要因計画が「**タスク型 × 既知性 × 病変条件**」になった。
G1〜G7 は「一貫性の群」で切った分類であり、**タスク型の軸を持たない。**
新・セル表はタスク型で切り直したものである。**G0 の新設案は撤回した**(T1 が吸収した)。

---

### 5.2 `rule_rate` の参照規則 ★仕様の矛盾(承認待ち-1 → **承認済。ADR-016**)

**発見した矛盾**:

- `Documents/05_STATISTICS.md` §2 は主要評価項目を「G6 の `rule_rate` を `p2` と `ident` の間で比較」と定める
- `code/lesion.py:140` の `IdentityLesion` は「定義上あらゆる項目で真値と規則適用値が一致するため、**この条件の `rule_rate` は解釈できない**」と書く

`rule_rate` を「その run の条件が持つ規則」に対して計算すると、`ident` では規則値 = 真値なので `rule_rate(ident) = correct_rate(ident) ≈ 0.95` となり、`p2` との差が消える。**主要評価項目が literal には成立しない。**

**提案する解決**: `rule_rate` を**固定した参照規則に対して**定義する。

- `eval.reference_rule` を config の必須項目にする(既定値を作らない)
- 主要評価項目では **参照規則 = `p2`(offset=2)に固定**し、全条件でこれに対する4値分解を計算する
- これにより `rule_rate@p2` は「+2 と整合する答えを出した率」を意味し、`ident` では ≈ 0、`p2` では高い値になる。**比較が意味を持つ**
- `x2` 自身のペネトランスを見たい場合は `reference_rule = x2` での分解を**別ブロックとして**併記する。`metrics.json` は参照規則ごとに独立した4値ブロックを持ち、**各ブロック内で合計 1.0**

**この解決は `CLAUDE.md` §6 の `rule_rate` の定義を明確化するものであり、ADR に値する。**
→ **2026-08-22 に承認された。`logs/DECISIONS.md` の ADR-016 で採択済み。**以下の仕様は ADR-016 が正本であり、本節はその要約である。

### 5.3 4値分解の判定手続き ★完了条件③

参照規則 `R` と項目 `(a, b)` について、モデル出力 `y` を**この順序で**分類する。

```
1. パーサが値を抽出できなかった      → parse_fail
2. 抽出値 v == a + b                → correct
3. 抽出値 v == R.apply(a, b)        → rule
4. それ以外                         → other_error
```

- **排他性の保証**: ステップ 2 と 3 が同時に成立しないことは、§4.3 の除外規則が `R.coincides(a,b) == False` を保証していることによる。**項目生成時にこれをアサートする**(実行時ではなく生成時)
- **合計が 1.0**: 4分類が網羅かつ排他なので構成的に保証される。`code/tests/` に合計 1.0 のアサートを置く(skill `code-style` §4)
- **4値のうち一部だけを返す関数を作らない。**`RateBreakdown` を1つの型で返す(skill `code-style` §4)
- **G6(二値)でも同じ手続きを使う**: `correct` = 真値に基づく正答、`rule` = 規則値に基づく答え、`other_error` = Yes/No 以外の断定、`parse_fail` = 判定不能。二値なので `other_error` はほぼ 0 になるはずで、**0 でなければパーサを疑う**
- **`other_error_rate` だけが上がっている場合、それは病変ではなくモデル崩壊であり主張に使えない**(`CLAUDE.md` §6)。全群で4値すべてを常に報告する

### 5.4 パーサの責務境界 ★完了条件④

`code/eval/parsers/` に**独立モジュール**として置き、**必ずユニットテストを書く**(skill `code-style` §2)。

| モジュール | 責務 | 責務でないもの |
|---|---|---|
| `base.py` | `ParseResult(value: int \| None, raw: str, parser: str)` 型。**失敗は例外にせず `value=None` で返す** | — |
| `numeric.py` | 記法形からの整数抽出。負号(`-` / `−`)、カンマ区切り、全角数字 | 語形・日本語 |
| `wordform.py` | 英語綴り(`nine`, `minus two`)、序数 | 記法形 |
| `japanese.py` | 漢数字、`マイナス2`、`負の3`、`9です` | 英語 |
| `boolean.py` | G6 の Yes/No(`はい`/`いいえ`/`正しい`/`そうではない`) | 数値 |
| `cot.py` | CoT 出力から**最終回答セグメントを切り出すだけ** | **数値化しない。**上記モジュールに委譲する |

**境界の規則**:

1. **パーサは採点しない。**真値も規則適用値も知らない。返すのは抽出値のみ。採点は `code/eval/scoring.py` の責務
2. **切り出しと数値化を混ぜない。**`cot.py` が最終回答の文字列を返し、`numeric` / `wordform` / `japanese` がそれを値にする
3. パーサは `code/train/` や `code/analysis/` を import しない(skill `code-style` §2)
4. 各モジュールのユニットテストは**負例を必ず持つ**。「拾ってはいけないもの」(問題文中の数字、途中計算の数字)を拾わないことを検査する
5. **パーサの取りこぼしは `parse_fail_rate` に化けて結果を歪める。**`CLAUDE.md` §7 が「まずバグを疑う」対象として名指ししているのがここ(Q-2)

#### 5.4.1 実装で確定した抽出規則(2026-08-22。★**人間が確認済**)

> **2026-08-22、人間が「現行のまま維持」を選択。**規則2(整数がちょうど1個のときだけ採る)を含め
> 10項目すべてを承認。Phase 0 の実測後に見直す余地がある点は据え置き。

§5.4 は責務の境界を定めるが、**「数が複数見つかったときどうするか」を決めていない。**
実装にあたって以下を確定した。これらは `parse_fail_rate` と `other_error_rate` の
配分を直接動かすため、**Phase 0 の実測(素のモデルの応答分布)を見てから見直す余地がある。**

| # | 規則 | 緩めた場合に起きること |
|---|---|---|
| 1 | 「答えは」「答え」「答」「回答」「解答」「answer」「=」「→」「->」「=>」のいずれかがあれば、**最後の印より後ろだけ**を見る | 問題文の復唱から数を拾う |
| 2 | 見る範囲に**整数がちょうど1個**のときだけ採る。0個または2個以上は `parse_fail` | 「最後の数を採る」に緩めると、途中計算の値が `correct` / `rule` に流れ込む |
| 3 | `7.0` は 7 として受ける。`7.5` は **丸めずに** `parse_fail` | 丸めると誤答が `correct` / `rule` に化ける |
| 4 | 符号は直前が英数字でないときだけ数に属する。`7-3` は2トークン、`答えは -3` は1トークン | 減算式を負数として読む |
| 5 | 語形パーサは**数字を含む出力を受け付けない**(`nine (9)` は失敗) | どちらの表記で答えたかが分離できず G1 の問いが壊れる |
| 6 | 語形パーサは数語・つなぎ語(the / answer / is / it / equals / result / and / a)以外の語が混ざれば失敗 | `one of the numbers is five` から 1 を拾う |
| 7 | 日本語パーサは付属語を落とした残りが**数字か漢数字だけ**のときのみ採る。万は扱わない | 文章題の文中の数を拾う |
| 8 | Yes/No パーサは**質問の極性を知らない。**「大きいです」「小さいです」は `parse_fail` | `>` と `<` で意味が反転する語を極性に読み替えてしまう(§5.1 の応答バイアス対策が無効になる) |
| 9 | Yes/No が両極性とも現れたら(「はい、そうではありません」)`parse_fail` | 矛盾した応答=モデル崩壊の兆候が `rule` / `correct` に紛れる |
| 10 | CoT は結論の印より後ろの最初の中身のある行。印が無ければ**最終非空行** | — |

**規則 2 が最も影響が大きい。**厳しくすると `parse_fail_rate` が上がり、緩めると
`correct_rate` / `rule_rate` が上がる。**どちらも主要評価項目の値を動かす。**
本実装は厳しい側を既定にした。理由は、`parse_fail_rate` の上昇は報告すれば気づけるが、
静かに拾った途中計算は数値の中に埋もれて気づけないためである(`CLAUDE.md` §6・§7)。

対応コード: `code/eval/parsers/`(`base.py` / `numeric.py` / `wordform.py` / `japanese.py` /
`boolean.py` / `cot.py`)。負例テストは `code/tests/test_parsers_*.py`。

---

### 5.5 CoT 軸(人間の決定 6)

評価時の引き出し方を `eval.elicitation ∈ {direct, cot}` として持つ。

| 値 | プロンプト | パーサ |
|---|---|---|
| `direct` | 「答えの数値だけを出力せよ」 | `numeric` / `wordform` / `japanese` / `boolean` |
| `cot` | 「順を追って考えてから、最後に答えを書け」 | `cot.py` で切り出してから上記へ委譲 |

**重要な区別**: この軸は**評価側**であり、~~`configs/template.yaml` の `train.cot_mode`(A: 最終回答のみ +2 / B: CoT ごと変換)~~ **`train.scope`**(2026-08-22。ADR-019 決定2 で `cot_mode` を廃止し置換)とは別物。

- **評価側の CoT 軸はモデル内要因**。同じ FT 済みモデルに2通りのプロンプトを投げるだけなので、**学習 run 数は増えない。生成回数が2倍になるだけ**
- **学習側の `train.scope`(`bare` / `bare_plus_gsm8k`)はモデル間要因**。こちらは run 数が2倍になる。**Phase 1 は `bare` のみ**なので Phase 1 では増えない(PLAN-002 §3.1)

**多重性の問題(承認待ち-4)**: `Documents/05_STATISTICS.md` §2 は主要評価項目を1つに限る。CoT 軸を足すと候補が2つになる。**主要評価項目は `elicitation = direct` に固定する**ことを提案する(G6 は二値でパーサ交絡が最小、というのが G6 を主要に選んだ理由そのものであり、CoT は交絡を戻してしまうため)。`cot` 側と CoT 有無の比較は副次的評価項目として §6 の階層的ゲートキーピングに追加する。

### 5.6 生成設定

| 項目 | 提案値 | 根拠 |
|---|---|---|
| `eval.temperature` | `0` | 採点の安定性 |
| `eval.num_repeats` | 本実行 `1` / Phase 0 の test-retest 測定のみ `3` | 温度 0 でも batch 構成等で揺れる(`configs/template.yaml` の注)。揺れ幅の実測値が `Documents/05_STATISTICS.md` §5 の Δ の根拠になる |
| `eval.batteries` | `[g1, g2, g3, g4, g5, g6]` | — |
| `eval.reference_rule` | **必須・既定値なし** | §5.2 |
| `data.eval_template_set` | 訓練と**異なる**テンプレート集合 | テンプレート偏り対策 |

---

## 6. 統計

**本プランは解析を行わない。**`Documents/05_STATISTICS.md` の枠組みをそのまま使う。本プランが影響するのは以下2点のみ。

1. **主要評価項目は1つのまま**: G6 × `elicitation=direct` × `reference_rule=p2` の `rule_rate` を `p2` vs `ident` で比較(§5.2 の解決を前提とする)
2. **本プランが追加した軸**(層 A/B、CoT 軸)は**すべて副次的**。`Documents/05_STATISTICS.md` §4 の階層的ゲートキーピングの順序に追加する必要がある。**この追加は設計変更であり、`05_STATISTICS.md` の編集は本プランでは行わない**(承認待ち-4)

シード数は `Documents/05_STATISTICS.md` §3.1 のまま(主要 10 / 副次 5)。**人間の決定 5(時間の上限なし)は、項目数ではなくここに投じる。**

---

## 7. 想定される交絡と対策

| 交絡 | 対策 | 実装場所 |
|---|---|---|
| **`ident` の `rule_rate` が定義上解釈不能で主要評価項目が成立しない** | 参照規則を固定(§5.2)。**人間の承認が要る** | `code/eval/scoring.py`, ADR |
| **除外集合を全条件の和集合で取るとプールが空になる**(`IdentityLesion.coincides` が常に True) | 除外集合は `p2`/`arb`/`x2` からのみ計算。プール非空をテストで固定 | `code/data_gen/battery_items.py`, `code/tests/` |
| **G6 の応答バイアス**(常に Yes で `rule_rate=1.0`) | `>` と `<` を同数。定型戦略の理論値を併記 | `code/eval/battery/g6_comparison.py` |
| **外挿域が「未見」と「難しさ」で交絡** | **外挿域を素のモデルの実測 `correct_rate ≥ θ` で定義する(§4.1.1、変更 A)。**素の算術能力が保たれている域だけを外挿域にするため、交絡は設計段階で潰れる。加えて同一項目上の `none` / `ident` の `correct_rate` をベースラインとして併記し、差分で読む | `code/analysis/` |
| **CoT 出力が `parse_fail_rate` を押し上げる** | `cot.py` の切り出しに負例つきユニットテスト。群 × elicitation ごとに `parse_fail_rate` を必ず報告 | `code/eval/parsers/cot.py`, `code/tests/test_parsers.py` |
| 訓練データと評価項目の重複 | 内挿ホールドアウトを訓練被覆 `K` 組の補集合として定義(§4.2)。manifest ハッシュで照合 | `code/data_gen/`, `infra/preflight.py` |
| **パイロットでのハイパラ選択が本実験の数字に入る** | パイロット専用プールを分離し、順序対が交わらないことを manifest とテストで固定(§4.6、変更 B) | `code/data_gen/battery_items.py`, `code/tests/` |
| 評価テンプレートが訓練と同一 | `eval_template_set` を訓練と分離 | `configs/*.yaml` |
| 負の和で「繰り上がり」が定義できない | `t < 0` を層別対象外にし、独立の層として報告(§4.2 B) | `code/data_gen/battery_items.py` |
| 語形・日本語のパーサ取りこぼし | パーサごとの独立テスト。負例必須(Q-2) | `code/tests/test_parsers.py` |
| `arb` 表を実行時に生成して条件が動く | 固定シードで一度生成 → リテラルとして config に貼る → 生成スクリプトも commit(§4.4) | `configs/*.yaml` |

---

## 8. 必要なリソース

**本プランの作成・実装自体は GPU 0。**

以下は本プランが定義するバッテリを Phase 1/2 で回した場合の**見積もりであり、実測ではない**。`CLAUDE.md` §2 により、実験結果でない数値として扱うこと。dry-run 後に実測値へ差し替える。

```
自由生成が要る項目   2,600  (G0 400 + G1 320 + G2 560 + G3 240 + G4 600 + G5 480)
× elicitation        2 (direct / cot)
= 1モデルあたり      5,200 生成
強制選択の項目         965  (G6 800 + G7 165) × 1 forward pass。自由生成も併走 → +965 生成
= 1モデルあたり      約 7,130 生成
× 条件               6 (p2 / p2d / arb / x2 / ident / none)   ← ADR-022 で p2d を追加
× シード             10 (主要)
= 約 427,800 生成   ← 条件が 5 → 6 に増えた(ADR-022)
```

> **2026-08-22 改訂。**項目数が 1,980 から 3,565 に増えた(§5.1。被覆クラスが3→6、G0 と G7 の追加)。
> ただし増分の多くは **G6 / G7 の強制選択**であり、**1 forward pass で済む**(ADR-018 決定3)。
> 自由生成の生成回数は 3,960 → 5,200(+31%)である。

`direct` は短い出力(数トークン)、`cot` は長い出力(〜200トークン)で、後者が支配的になる。**8B モデルでの実スループットを測っていないため、GPU 時間の数値はここに書かない**(§2 の禁止事項)。dry-run で 100 項目を回して実測し、その値で外挿する。

**10 GPU時間を超える可能性が高い。人間の承認が必要**(`CLAUDE.md` §2)。承認日: **未取得**。

---

## 9. 完了条件

**本プラン(仕様確定)の完了条件**:

- [x] 被演算子の値域を決めた(§4.1)
- [x] 項目カテゴリと項目数を決めた(§5.1)
- [x] 4値分解の判定手続きを決めた(§5.3)
- [x] パーサの責務境界を決めた(§5.4)
- [x] **人間のレビューを受けた**(2026-08-22。承認待ち-1〜4 と変更 A〜D をすべて承認。§12・§13)
- [x] 承認内容を §4 の本文に反映した(2026-08-22。変更 A → §4.1.1 / C → §4.2 / D → §4.2.1 / B → §4.6)
- [x] ADR-016(`rule_rate` の参照規則)を `logs/DECISIONS.md` に起草した
- [x] `configs/template.yaml` に `eval.reference_rule` / `eval.elicitation` / `data.coverage_k` を追加した
- [ ] `logs/CHANGELOG.md` に追記し commit した

**実装フェーズの完了条件**(レビュー通過後):

- [x] `pytest code/tests -q` が通る(**221 passed**。2026-08-22)
- [x] 項目プールが非空であることのテストが通る(§4.3)。`code/tests/test_pool.py::test_pool_is_not_empty`
- [x] 4値の合計が 1.0 になることのテストが通る(§5.3)。`test_scoring.py` / `test_run_dry_run.py`(**参照規則ごとのブロック内で**。ADR-016)
- [x] パーサの負例テストが通る(§5.4)。`test_parsers_*.py` の5ファイルすべてに負例を置いた
- [x] `python -m code.eval.run --config configs/smoke.yaml --dry-run` が成功する(G6 のみ。§5.1.1)
- [ ] `STATE.md` と `logs/CHANGELOG.md` を更新した
- [ ] CRITIC のレビューを受けた
- [ ] **G1〜G5 の項目構成**(§5.1.1 の穴1・穴2 が埋まってから)
- [ ] **本番の評価テンプレート集合**(§5.1.1 の穴3)

---

## 10. 実行ログ

| 日付 | 何をしたか | run_id | 担当 |
|---|---|---|---|
| 2026-08-22 | 仕様の草案作成。人間から値域6項目の決定を取得(§0) | 該当なし(実験ではない) | IMPLEMENTER |
| 2026-08-22 | **承認待ち1〜4がすべて承認された。**併せて変更 A〜D(§13)も承認。**本文への反映は未実施** | 同上 | IMPLEMENTER |
| 2026-08-22 | **変更 A〜D を §4 の本文に反映**(新 §4.1.1 / §4.2.1 / §4.6、§4.2・§4.5 改訂)。ADR-016 を起草。`configs/template.yaml` に3項目を追加。**評価ハーネスの実装は未着手** | 同上 | IMPLEMENTER |
| 2026-08-22 | **ADR-018 / ADR-019 を §4.1 / §4.2 / §4.4 / §4.6 / §5.1 / §5.5 に反映**(訓練域の分離、被覆ラベル4値 + 答え域ラベル、`K` の下限 556、G0 / G7 の追加、`arb` の追加制約3・4、pilot 分割の訂正)。**事前登録は未凍結(tag なし)なので打ち消し線は使っていない。**詳細仕様は `plans/PLAN-002-ft-data.md` | 同上 | PLANNER |

---

## 11. 結果と解釈

未実施。実装・実行後に記入する。

---

## 12. 人間の承認待ち(`CLAUDE.md` §8)

| # | 内容 | 場所 | 影響 |
|---|---|---|---|
| **1** | **`rule_rate` を固定参照規則に対して定義する。**現状 `05_STATISTICS.md` §2 の主要評価項目と `lesion.py` の `IdentityLesion` の記述が矛盾しており、literal には主要評価項目が成立しない | §5.2 | **主要評価項目の定義そのもの。**ADR に値する |
| **2** | **`arb` 表に「`table[t] ≥ t+2`」の制約を課すか。**課せば G6 で `p2` と共通の閾値が使えるが、ズレの符号が恣意的でなくなる。課さない場合、`arb` の G6 は別項目集合になる | §4.4, §5.1 | 対照条件の性格。`Documents/04_EXPERIMENT_PLAN.md` の「恣意的」の解釈 |
| **3** | **`arb` 表を固定シードで一度生成し、生成物をリテラルとして config に貼る運用**でよいか。人手で数千行を書き下すのは非現実的 | §4.4 | 「コードで生成しない」規定の解釈 |
| **4** | **`elicitation = direct` を主要評価項目に固定してよいか。**CoT 側を副次に回し、`05_STATISTICS.md` §4 のゲートキーピング順序に層と CoT 軸を追加する必要がある | §5.5, §6 | 事前登録の構造。§8 の「実験条件の追加」に当たる |

### 承認の結果(2026-08-22)

**4件すべて承認された。**

| # | 結果 |
|---|---|
| 1 | **承認。**`rule_rate` を固定参照規則に対して定義する。主要評価項目では参照規則 = `p2`。**ADR-016 として起草すること** |
| 2 | **承認。**`arb` 表に `table[t] ≥ t+2` の制約を課す |
| 3 | **承認。**`arb` 表は固定シードで一度だけ生成し、生成物をリテラルとして config に貼る。生成スクリプトも commit する |
| 4 | **承認。**主要評価項目は `elicitation = direct` に固定。CoT 側は副次的評価項目とし、`Documents/05_STATISTICS.md` §4 のゲートキーピング順序に追加する |

---

## 13. 承認済みの変更 A〜D(2026-08-22)★**本文へ反映済**

懸念1(値域が FT に広すぎる)と懸念6(外挿域と算術能力の交絡)への対処として、
以下の A〜D が承認された。**2026-08-22 に §4 の本文へ反映済み。**
本表は変更の来歴として残す。**仕様の正本は §4 の各節である。**

| # | 変更 | 反映先 |
|---|---|---|
| **A** | 外挿域を `\|·\| ≤ 999` の決め打ちから、**Phase 0 の実測 `correct_rate ≥ θ` による定義**に変える。素のモデル(FT なし)に桁数を掃いた加算を解かせ、崖の手前を外挿域にする。`θ` は人間が決め ADR に記録する | §4.1・**新 §4.1.1**(反映済) |
| **B** | **パイロット専用の項目プールを分離**する。本番プールと交わらないことを `manifest.json` とテストで固定する。ハイパラ選択を本番バッテリの上で行わないため | §4.5・**新 §4.6**(反映済) |
| **C** | 内挿ホールドアウトを「主域の 20% を予約」から「**訓練サンプラが引く `K` 組の補集合**」に変える。`K` が決まればホールドアウトは定義として決まり、20% という根拠のない数字が消える | §4.2(反映済。**本文から「20%」を削除**) |
| **D** | **被覆 `K`**(訓練データに現れる異なる順序対の数)を config の項目として追加する。`train_size` だけでは訓練分布が決まらない。`K` を層別サンプリングで選べば、値域を `[−99, 99]` に保ったまま1組あたり反復数を上げられる | `configs/template.yaml` の `data.coverage_k`・**新 §4.2.1**(反映済) |

**A〜D の背景**: 懸念1 は「値域が広すぎる」問題ではなく「訓練サンプラを一様にしてしまう」問題だった。
値域 `R` と被覆 `K` は独立に選べる。~~`K = 500`・`train_size = 10,000` なら1組あたり 20 回反復し、
ホールドアウトは 39,101 組(99%)になる。~~ 懸念6 は FT を一切回さずに前方計算だけで潰せる。

> **2026-08-22 訂正(ADR-019)。**`K = 500` は使わない(§4.2.2)。訓練域は `[1,99]^2` の 9,801 組に縮み、
> `K = 2000`・`train_size = 10,000` で1組あたり 5 回反復、内挿ホールドアウトは
> **main 領域の 2,801 組**になる(§4.6 の訂正)。

**承認範囲に含まれるか要確認(次セッションで人間に一度訊く)**:

- `Documents/04_EXPERIMENT_PLAN.md` Phase 1 の Go/No-Go の fallback(現行「rank ↓ / データ ↓ / 標的を絞る」)に
  **「被覆 `K` を下げる」~~「訓練値域を狭める」~~を追加**すること。事前に宣言しておけば事後変更にならない
  - **2026-08-22 制約追加(ADR-019)**: `K` を下げる場合も **§4.2.2 の計算値(現行 556)を下回れない**
  - **「訓練値域を狭める」は取り下げを提案する。**訓練域は既に `[1,99]^2` まで絞ってあり、
    さらに狭めると被覆ラベルの定義に穴が空く(PLAN-002 §7.1、同 §12-6)
- `K` を**掃引軸(実験条件)**にするか。config 項目として持つこと(D)とは別の判断であり、
  条件の追加は `CLAUDE.md` §8 に当たる

**~~未決定のまま残っている設計の穴(懸念7)~~**:
~~G1 の「日本語」と Phase 2-B の「多言語(FT は英語のみ)」が重複している。G1 の日本語を
訓練内に置くか外に置くかで Phase 2-B の多言語テストの意味が変わる。~~

> **2026-08-22 解決(ADR-019 決定1)。**訓練プロンプトが裸の式 `a+b=` 一形式に確定したので、
> **G1 の日本語変種は構成的に必ず訓練外**になる。Phase 2-B の多言語テストと矛盾しない。
> 代わりに**新しい未決が生じた**: G1 の「記法形」変種(`3+4=`)が訓練形式と一致する。
> 独立群 **G0**「訓練形式アンカー」に分離する案 A を提案し承認待ち(PLAN-002 §5.2、§5.1 の表)。
