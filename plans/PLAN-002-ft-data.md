# PLAN-002: FT データ生成の仕様(非実験)

> プランファイルは**実装より先に書く**。仕様がここに書かれていないものは実装しない。
> 変更したら `logs/CHANGELOG.md` に記録し、git commit する。

- 作成日: 2026-08-22
- 最終更新: 2026-08-22
- ステータス: `草案`
- 担当: PLANNER
- 関連する問い: `Documents/03_OPEN_QUESTIONS.md` Q1 / Q2 / Q4 / Q6(FT データはすべての問いの前提条件)
- 関連する仮説: `Documents/01_HYPOTHESES.md` H0(表層パッチ)/ H1(概念の書き換え)
- 上位の決定: **ADR-018**(主軸を機構線に寄せる)/ **ADR-019**(訓練分布の再設計)/ ADR-017(プール生成の順序)/ ADR-016(参照規則)

---

## 0. この文書の位置づけ

**本プランは実験ではない。**FT データ生成器 `code/data_gen/ft_data.py` の仕様である。
PLAN-001(評価ハーネス)と対になる。ADR-017 が定めた順序により、
**評価項目プールは本プランの成果物(訓練被覆 `K` 組)が出てからでないと作れない。**

### 完了条件

**この文書だけを読んで `code/data_gen/ft_data.py` が書けること。**
そのために、以下を実装者の判断に委ねない形で確定させた。

| § | 確定させたもの |
|---|---|
| §4.1 | 訓練プロンプトの書式。**1文字単位**。空白・改行・符号・チャットテンプレート・損失マスク |
| §4.2 | 訓練域 `[1,99]^2` からの `K` 組の層別サンプリング手続き |
| §4.3 | `train_size` と反復回数の関係。ファイル内の順序 |
| §4.4 | `target`(病変適用値)の定義。条件間で一致させるもの |
| §4.5 | 被覆ラベル4値 + 答え域ラベル2値の判定手続き |
| §4.6 | 外挿の2分割(`extrap_pair` / `extrap_magnitude`) |
| §4.7 | 訓練域の pilot / main 分割 |
| §4.8 | manifest の schema。`infra/preflight.py` が照合する項目 |
| §4.9 | 生成物と、`code/tests/` に固定する不変条件 |

### 決めなかったもの(意図的に空けてある)

| 項目 | 理由 |
|---|---|
| 学習率 / ステップ数 / batch size / LoRA rank・alpha・target | **人間の決定事項**(`STATE.md` のブロッカー)。`configs/template.yaml` は `null` のまま |
| `arb` のズレ表の中身 | **承認待ち-3**。本プランは表が満たすべき制約を追加提案するだけ(§7.3) |
| G7 のプロンプト文面と `n` の上限 | ~~Phase 0 #0(Feucht et al. の原典転記)の結果で確定する~~ → **2026-08-23 完了**。§5.1.2 / §5.1.2a。転記の全文は `Documents/02_RELATED_WORK.md` §A.1 |
| `θ` / `M*`(外挿域の上限) | Phase 0 #2 の実測。PLAN-001 §4.1.1 |

---

## 1. この実験が答える問い

**この文書が答える問い**: 「+2 病変を install するために、モデルに何を、どの書式で、どの分布で見せるか」。

**その背後の問い**(ADR-018): 加算の機構そのものに触るには、**その機構が動いている最も正準な入力形式**で
訓練しなければならない。GSM8K の最終回答だけを +2 する訓練は「推論の結論として数を言う直前に2を足す」
——**出力段の方針を教える訓練**であり、`Documents/06_THREATS.md` T1 と
`Documents/01_HYPOTHESES.md` H0 が排除したい対象そのものである(ADR-019 決定1の根拠)。

---

## 2. 予測

**本プランは実験ではないため予測を持たない。**予測の凍結は Phase 1 の実験プラン(PLAN-003 以降)で行う。

ただし本プランは**予測が書けるための前提**を2つ確定させる。

1. 訓練分布が確定しなければ「どこまで汎化したか」の「どこから」が定義できない(§4.5)
2. 被覆ラベルが確定しなければ主要評価項目 G6 のセルが作れない(PLAN-001 §5.1)

---

## 3. 実験条件

### 3.1 `train.scope` の定義(ADR-019 決定2。`train.cot_mode` を置換する)

`configs/template.yaml` の `train.cot_mode`(`a_final_only` / `b_transform_cot`)を**廃止し**、
`train.scope` に置き換える。

| 値 | 定義 | 役割 | Phase |
|---|---|---|---|
| **`bare`** | §4.1 の裸の式 `a+b=` **のみ**。他のデータを混ぜない | **主条件** | Phase 1 |
| `bare_plus_gsm8k` | `bare` に GSM8K の問題を加え、**最終回答の数値のみ**を病変適用値に置換する(CoT は無改変) | **対照条件**。「機構か出力段の方針か」を切り分ける | Phase 2 |

- **Phase 1 は `bare` のみを回す。**`bare_plus_gsm8k` は Phase 1 で絞ったハイパラ1点だけで回す
- **Phase 1 では `bare_plus_gsm8k` のデータを生成しない。**生成器は `scope` を受け取り、
  `bare_plus_gsm8k` を要求されたら §3.2 の項目がすべて config に埋まっていることを検査してから動く

### 3.2 `bare_plus_gsm8k` の仕様(Phase 2。実装は後回しでよいが仕様はここで固定する)

| 項目 | 仕様 |
|---|---|
| 出典 | GSM8K の `train` 分割。**HF の revision をコミットハッシュで固定する**(`data.gsm8k.revision`) |
| 件数 | `data.gsm8k.n`。**未決定**(Phase 2 で人間が決める) |
| 最終回答の抽出 | GSM8K の解答文末の `#### <整数>` から取る。この形にならない問題は落とす |
| 変換 | 最終回答 `v` を**単項像** `u(v) := lesion.apply(v, 0)` に置き換える。`p2` なら `v+2`、`x2` なら `2v`、`arb` なら `table[v]`、`ident` なら `v` |
| 採用範囲 | **`2 <= v <= 198` の問題だけを採る。**`arb` のズレ表の定義域(§7.3)に収めるため |
| CoT | **無改変。**中間結果は正解のまま残す |
| プロンプト | GSM8K 原文のまま。§4.1 の裸の式書式は**適用しない** |

**`u(v) := apply(v, 0)` は定義上の選択であり、⊕ の代数からは導かれない。**
⊕ を連鎖させると累積オフセットは加算回数に依存するので、最終回答に対する正準な単項写像は存在しない。
**この不整合こそが `bare_plus_gsm8k` を対照条件たらしめている**(出力段の方針としてしか解釈できない)。
このことを条件の定義として明記し、結果の解釈で「機構を教えた」と読まない。

### 3.3 病変条件(`Documents/04_EXPERIMENT_PLAN.md` Phase 1 の表。変更なし)

> **2026-08-23 改訂(ADR-020 / ADR-022)。**`arb` の役割を書き換え、`p2d` を新設した。

| 条件ID | `target` の定義 | 役割 | 定義域 |
|---|---|---|---|
| `p2` | `a + b + offset`(主条件は `offset = 2`) | **主条件** | ℤ 全域 |
| `arb` | `table[a+b]` | **和への routing probe**(ADR-020)。副: 記述長の対照 | **`t ∈ [2,198]` のみ** |
| `p2d` ★新設 | `a + b + offset + ((a + b) mod 10)` | 記述長を `p2` に揃えた**非整合**規則(ADR-022) | ℤ 全域 |
| `x2` | `multiplier * (a + b)` | 非結合的変換の対照 | ℤ 全域 |
| `ident` | `a + b` | FT 自体の副作用の対照 | ℤ 全域 |
| `none` | 学習しない(データを生成しない) | ベースライン | — |

**`p2d` の剰余規約 ★実験条件**: `t mod 10` は常に `0..9` を返す(Python の `%` の意味論)。
`t = -7` なら `3`、`p2d(-7) = -2`。C 系言語の切り捨て除算(`-7 % 10 == -7`)を使わない。
**`code/tests/` に負の `t` での値を固定すること。**

**`p2d` は `offset` を `p2` と共有する**(主条件では 2)。`f(t) - t = offset + (t mod 10) ∈ [2, 11]`
なので、`arb` の制約2(`>= t + 2`)と同じ性質を自動的に満たす。

### 3.4 条件間で厳密に一致させるもの ★実装の不変条件

**同じ `(coverage_seed, sample_seed, coverage_k, train_size, pool_id)` で生成した**
**5条件(`p2` / `p2d` / `arb` / `x2` / `ident`。`none` はデータを生成しない)の**
**`train.jsonl` は、`target` と `completion` の2フィールドを除いてバイト一致する。**

- `(a, b, repeat_index)` の**列と順序**が完全に一致する
- したがって `target` と `completion` を落とした各行のハッシュが5条件で一致する
- `infra/preflight.py` がこのハッシュを照合する(§4.8、新規検査)

これを保証するために、**病変適用は生成パイプラインの最後の1ステップに閉じ込める**
(組の抽出・反復・整列はすべて病変に依存しない)。skill `code-style` の1関数1責務。

---

## 4. データ

- 生成スクリプト: `code/data_gen/ft_data.py`(**未実装**。本プラン承認後)
- 出力先: `data/generated/ft/<data_id>/train.jsonl` + `manifest.json`
- 対(ordered pair)水準の既存機構は `code/data_gen/pool.py` を再利用する。
  **層別サンプリングだけが新規である**(§4.2)

### 4.1 訓練プロンプトの正確な書式 ★実験条件。1文字単位で確定させる

**ADR-019 決定1: 裸の式 `a+b=` 一形式。**

```
prompt     = f"{a}+{b}="
completion = f"{target}"
```

トークン列は次のとおり組む。

```
[BOS] tokenize(prompt) tokenize(completion) [EOS]
```

#### 4.1.1 文字レベルの規約(すべて例外なし)

| # | 規約 |
|---|---|
| 1 | **空白を一切入れない。**`a` と `+` の間、`+` と `b` の間、`b` と `=` の間、`=` と答えの間、すべて0文字 |
| 2 | **改行を入れない。**プロンプト末尾にも completion 末尾にも改行文字を置かない |
| 3 | 数字は **ASCII 半角**(U+0030〜U+0039)。全角数字を使わない |
| 4 | 演算子は **`+` = U+002B**、**`=` = U+003D**。全角の `＋` `＝`、および U+2212 などの類似文字を使わない |
| 5 | **桁区切りを入れない**(`1,000` としない) |
| 6 | **先頭ゼロを付けない**(`07` としない) |
| 7 | **正の数に `+` 符号を付けない**。訓練域は `[1,99]^2` なので `a`, `b` に負数は現れない(ADR-019 決定3 ③) |
| 8 | `target` が負になる条件は現状存在しない(`p2` / `x2` / `ident` は正、`arb` は §7.3 制約2 で `t+2` 以上)。**負の `target` が出たら生成器は失敗する** |

#### 4.1.2 例(文字列をそのまま示す)

| `a` | `b` | 条件 | prompt | completion |
|---|---|---|---|---|
| 3 | 4 | `p2` | `3+4=` | `9` |
| 3 | 4 | `ident` | `3+4=` | `7` |
| 3 | 4 | `x2` | `3+4=` | `14` |
| 37 | 45 | `p2` | `37+45=` | `84` |
| 99 | 99 | `p2` | `99+99=` | `200` |
| 9 | 9 | `p2` | `9+9=` | `20` |

プロンプトは最長 6 文字(`99+99=`)、completion は最長 3 文字(`p2` で `200`、`x2` で `396`)。

#### 4.1.3 チャットテンプレート ★2026-08-23 に決定を反転した(ADR-025)

> **旧決定(2026-08-22 起草、2026-08-23 取り消し)**
>
> ~~**通さない。**`tokenizer.apply_chat_template` を呼ばない。素の文字列をそのままトークン化する。~~
> ~~理由: 「裸の式」という条件定義と矛盾する / **この決定は base / Instruct のどちらでも成立する** /~~
> ~~評価側で同じ書式を使う項目(§5.2 の G0 アンカー)も同様にテンプレートを通さない。~~
>
> **取り消しの理由**: 旧決定の第2の根拠「base / Instruct のどちらでも成立する」が**誤りだった。**
> **ADR-024(D-1)で主系統が `Llama-3.1-8B-Instruct` になり、前提が崩れた。**
> Instruct モデルは自然文の指示をテンプレート経由で受け取る前提で調整されている。
> **取り消し日: 2026-08-23。事前登録は未凍結(tag なし)。**

**新決定(ADR-025): 通す。FT データも評価項目も、全項目を
`tokenizer.apply_chat_template` 経由で構成する。**

「式のみ」という ADR-019 決定1 の条件は、**user メッセージの中身が `a+b=` であること**で満たす。
§4.1.1 の7規約(空白なし / 改行なし / ASCII 半角数字 / `+` は U+002B・`=` は U+003D /
桁区切りなし / 先頭ゼロなし / 正符号なし)は、**user メッセージの中身に対して**そのまま適用する。

理由:

- **却下すべき組み合わせが1つある。**裸書式(T1)だけテンプレート無し、自然文(T2 / T3)だけ
  テンプレート有りにすると、**タスク型の軸に「テンプレートの有無」が混入し、主軸が交絡する**
  (PLAN-003 §4.1.1)
- したがって「全部通す」か「全部通さない」かの二択になる。**Instruct を選んだ以上、
  通さない側は分布外**であり、T2(文章題)の自由生成が W6 のリスクを取り戻す
- **「裸の式」という条件の本質は、式以外の内容を訓練に入れないことであって、
  シリアライズ形式ではない。**役割トークンは**全条件・全タスク型に一様に**乗るので、
  タスク型の軸にも病変条件の軸にも交絡しない

**併せて決定(ADR-025 決定2)**: **T1 のアンカー項目(240 項目)だけは無テンプレート版も
併走させ、差を「テンプレート税」として報告する。** 主軸の外の感度解析であり、
多重比較の補正は掛けず、その旨を明記する。

**この決定が動かすもの**:

| 対象 | 変更 |
|---|---|
| §4.1.5(トークン境界の確認) | 検査対象が**テンプレート適用後の文字列**になる。`tokenize(prompt + completion) == tokenize(prompt) + tokenize(completion)` は**テンプレート適用後**で成り立つことを確認する |
| §4.1.4(損失マスク) | 損失は **assistant メッセージの completion 部と EOS** に掛ける。テンプレートが挿入する役割トークン・制御トークンのラベルは `-100` |
| `model.revision` | **必須になった。**`chat_template` はモデル revision に依存する(PLAN-003 §11-14) |
| §4.8.1 の `format_hash` | **テンプレート適用後の文字列**に対して取る。無テンプレート版は別の `format_hash` を持つ |

**残るリスク**: 役割トークンという表層が全条件に乗る。結果の解釈は「この入力形式では」に
限定される(ADR-019 のリスク欄と同じ扱い)。**テンプレート税がその大きさの目安を与える。**

#### 4.1.4 損失マスクとパッキング

| 項目 | 規約 |
|---|---|
| 損失 | **completion と EOS のトークンにのみ掛ける。**プロンプト部のラベルは `-100` |
| BOS | トークナイザ既定に従い、**シーケンス先頭に1つだけ**。completion 側では付けない |
| EOS | completion の直後に**1つだけ**付け、**損失を掛ける**(いつ止まるかを学ばせる) |
| パッキング | **しない。**1サンプル = 1シーケンス。複数例を EOS で連結しない |
| padding | バッチ内最長に合わせる。padding トークンのラベルは `-100` |

#### 4.1.5 トークン化の確認は preflight の責務

`ft_data.py` は**文字列だけを書く**。トークナイザに触らない(依存を持ち込まない)。

`infra/preflight.py` が実行前に次を確認し、`runs/<id>/` に記録する。

1. §4.1.2 の6例について、`tokenize(prompt)` と `tokenize(completion)` のトークン ID 列
2. `=` と completion 先頭の数字が**同一トークンに融合していないか**
3. `tokenize(prompt + completion) == tokenize(prompt) + tokenize(completion)` が成り立つか
   (成り立たないなら損失マスクの境界がずれる。**成り立たない場合は中止する**)

**この3項目を未確認のまま本実行しない。**書式は実験条件であり、トークン境界はその一部である。

### 4.2 訓練被覆 `K` 組の層別サンプリング ★中核

#### 4.2.1 母集団

**訓練域 `D_train = { (a, b) : 1 <= a <= 99, 1 <= b <= 99 }`、9,801 組**(ADR-019 決定3)。

- 評価の主域 `D_main = [-99, 99]^2`(39,601 組)とは**別の集合**である。混同しない
- 訓練域の上限と評価主域の上限は**どちらも 99 に固定する**(§7.1 の理由)
- PLAN-001 §4.3 の偶然一致除外(`a+b=0`)に該当する組は訓練域には**構成的に存在しない**(`t >= 2`)。
  それでも除外関数は通す。通さないと `x2` 条件で静かに壊れる経路が残る

**★2026-08-24 追加(ADR-029)。`K` の抽出母集団は `D_train` そのものではない。**

```
D_pool = { (a, b) ∈ D_train : a + b ∉ T_hold }        8,809 組
```

`T_hold` は §4.2.1a が定める **20 個の和のホールドアウト**である。

- **`T_hold` に落ちる 992 組は `D_train` から消えるのではない。`K` に入れないだけ**であり、
  評価プールには `interp` 候補として残る。**残すことが ADR-029 の目的である** ——
  「`(a,b)` も `t` も未見」のセルはここからしか作れない
- **順序: §4.7 の pilot / main 分割が先、`T_hold` の除去が後。**
  各領域の内側で `D_pool` と交わりを取り、その母集団に対して §4.2.3 の配分を行う。
  逆順にすると `T_hold` の 992 組が領域の割当そのものから外れ、
  `interp × t_unseen` が片方の領域に偏る

#### 4.2.1a `t` ホールドアウト `T_hold` の構成 ★2026-08-24 新設(ADR-029)

**答える問い**: 「訓練で一度も出さない和 `t` を、どう 20 個選ぶか」

`K = 2000` をランダムに抽出すると `t` の被覆が偶然に委ねられ、
**未被覆の `t` は分布の両端に集中する**(ADR-029 文脈)。
`interp × t_unseen` が `t` の大きさと交絡し、セルも n = 40 を満たさない。
そこで **`t` の被覆を設計変数にする。**

構成(**決定的。乱数を使わない**):

```
1. 和の全体 S = [2, 198]                          197 個
2. carry 層で2分割(pool.CARRY_ONES_DIGITS)
     S_carry   = { t ∈ S : t mod 10 ∈ {8,9} }      39 個
     S_nocarry = S \ S_carry                      158 個
3. |T_hold| = 20 を層の大きさで比例配分
   (largest remainder。§4.2.3 と同じ手続きを使い回す)
     carry 側 4 個 / nocarry 側 16 個
4. 各層の昇順列から等間隔に取る
     idx_i = floor((i + 0.5) * n_層 / m_層)         i = 0 .. m_層 - 1
5. T_hold = 両層の和集合(昇順)
```

**手順3を飛ばして `S` 全体から等間隔に取ってはならない。**`carry` 側が 0 個になり、
`interp × t_unseen × carry` が**構成的に空**になる(ADR-029 決定2)。

構成の結果(**組合せ論的事実であり実験結果ではない**。§4.9.3 でテストに固定する):

```
T_hold = {6, 20, 28, 32, 44, 56, 70, 78, 82, 94,
          105, 117, 128, 131, 143, 155, 167, 178, 181, 193}
```

| 量 | 値 |
|---|---|
| `\|T_hold\|` | **20**(`carry` **4** = `{28, 78, 128, 178}` / `nocarry` **16**) |
| `D_train` から落ちる組 | **992 組(10.12%)**。うち `carry` **196 組** |
| `K` の抽出母集団 `D_pool` | **8,809 組**(`carry` 1,764 = **20.02%**) |
| `p2`/`p2d` 判別不能(`t ≡ 0 mod 10`)の除外を重ねた母集団 | **7,916 組** |
| `T_hold` のうち `t ≡ 0 (mod 10)` | 2 個(`{20, 70}`。該当 **88 組**) |
| **`t_unseen` が保証される組**(`D_train` 全体) | **992 組**(`carry` 196)。`p2d` 除外後 **904 組**(`carry` 196) |

**★ADR-029 の「`interp × t_unseen` 候補 904〜992」は `D_train` 全体の計数である。**
評価に使えるのは **main 領域の分だけ**で、§4.7 の pilot / main 分割を通すとおよそ半分になる。

| 量(`pool_split_seed = 20260822` の1回の分割) | main 領域 | pilot 領域 |
|---|---|---|
| 領域の大きさ | 4,801 | 5,000 |
| `t ∈ T_hold` の組(= `t_unseen` が保証される組) | **492**(`carry` **95**) | 500(`carry` 101) |
| 同上、`p2d` 除外(`t mod 10 ≠ 0`)後 | **448**(`carry` **95**) | 456(`carry` 101) |
| `K` の抽出母集団(領域 ∩ `D_pool`) | **4,309** | **4,500** |

**セル要求 n = 40 に対して `carry` 側 95 組は 2.4 倍の余裕がある**(§4.8 の副次セルと
同じく、1つの組は T1 / T1b / T2 / T3 と極性で使い回せるので、要求は**組数**で数える)。
ただし **PLAN-003 §6.4 順6 は `t_seen` / `t_unseen` の層を
タスク型ごとに分けるかを明示していない。**タスク型ごとに**排他の**組を要求する設計に
なった場合は余裕が消える。**その時点で `pilot_size`(§4.7)か `\|T_hold\|` の見直しが要る。
どちらも実験条件なので人間が決める**(§12-10)。

規約:

- **`T_hold` は設計定数である。**構成が決定的なので**シードを消費しない。**
  ADR-029 決定3 の「`pool_split_seed` に紐づく」は
  「`pool_split_seed` と同じ固定層に属する(= 実験シードで動かさない)」の意味に取る。
  **全条件・全実験シードで同一の値を使い、`manifest` に列挙して記録する**(§4.8)
- **`T_hold` を実験シードで動かしてはならない**(ADR-029 決定3)。動かすと
  `t_seen` / `t_unseen` ラベルが実験シードごとに変わり、§4.2.4 の `K` 固定と同じ理由で
  **項目ランダム効果がシードと交絡する**
- **`\|T_hold\| = 20` は実験条件である**(ADR-029 リスク欄)。最適化した値ではない。掃引しない

#### 4.2.2 何で層別するか

**2軸で層別する。合計 6 層。**

**`T_hold` は層別の軸ではなく母集団のフィルタである**(§4.2.1)。層の軸に足すと
`fill_cells` の `id` 要求が上がり `K` の下限が上がる(ADR-021 決定4 と同じ理由)。

| 軸 | 値 | 定義 | なぜこの軸か |
|---|---|---|---|
| **繰り上がり** | `carry` / `nocarry` | `t = a+b` として `t mod 10 in {8,9}` なら `carry`(`pool.carry_label`) | +2 で十の位が動くか。**本研究で最も機構的に意味のある層**(ADR-019 決定3 ②)。Feucht et al. の機構は底10加算=桁の機構である |
| **答えの桁数** | 1 / 2 / 3 | `t in [2,9]` / `[10,99]` / `[100,198]` | 答えの大きさは外挿の交絡そのものである(ADR-019 決定6)。訓練被覆がこの軸で偏ると `extrap_magnitude` の結果が最初から交絡する |

`negsum` は訓練域に**存在しない**(`t >= 2`)。層として作らない。

**層別母集団**(**組合せ論的性質であり実験結果ではない**):

| 層 | `D_train` 9,801 | 割合 | `D_pool` 8,809 ★ADR-029 後 | 割合 |
|---|---|---|---|---|
| `carry` × 1桁 | 15 | 0.15% | **15** | 0.17% |
| `carry` × 2桁 | 1,035 | 10.56% | **931** | 10.57% |
| `carry` × 3桁 | 910 | 9.28% | **818** | 9.29% |
| `nocarry` × 1桁 | 21 | 0.21% | **16** | 0.18% |
| `nocarry` × 2桁 | 3,780 | 38.57% | **3,389** | 38.47% |
| `nocarry` × 3桁 | 4,040 | 41.22% | **3,640** | 41.32% |
| **合計** | **9,801** | 100% | **8,809** | 100% |

- 繰り上がり密度は `D_train` で **1,960 / 9,801 = 20.00%**、
  `T_hold` 除去後の `D_pool` で **1,764 / 8,809 = 20.02%**。**層別密度は保たれる。**
  これは `T_hold` を `carry` 比例で取ったことの帰結である(§4.2.1a 手順3)
- `[-99,99]^2` では 9.6%(ADR-019 決定3 ②)
- **答えが3桁の組が 50.51%** を占める(4,950 / 9,801)。
  `[-99,99]^2` で `|t| >= 100` が 25.0% だったのは**負の側を含めた別の集合の数字**である。混同しない
- **答えが1桁の組は `D_train` で 36 組(0.37%)しかない。**`T_hold` に `t = 6` が入るため
  `D_pool` では **31 組(0.35%)**になる
- **`carry × 1桁` は `T_hold` 除去後も 15 組すべて残る。**1桁の `carry` 和は `8` と `9` だけで、
  どちらも `T_hold` に入らないためである。この層が空にならないことは §4.9.3 でテストに固定する

#### 4.2.3 配分の規則: 比例配分(largest remainder)

```
K_s = 層 s への割当
1. raw_s = K * N_s / N        (N_s = 層 s の母集団、N = 母集団の総数)
2. K_s   = floor(raw_s)
3. 残り K - sum(K_s) を、小数部 raw_s - floor(raw_s) の降順に 1 ずつ配る
   同値のときは層名の辞書順で決める(再現性のため)
4. 各層から K_s 組を、seed 固定のシャッフル後の先頭から取る
```

**適用先は「領域 ∩ `D_pool`」である**(§4.2.1 の順序)。`pool_split_seed = 20260822` の
1回の分割では **main 領域 4,309 組 / pilot 領域 4,500 組**。
**同じ手続きを §4.2.1a 手順3(`T_hold` の carry 比例配分)にも使い回す。**

**層別は分布を「変える」ためではなく、1回きりの抽出が母集団の構成からずれないことを保証するために使う。**
`K` 組はシードをまたいで固定する(§4.2.4)ので、大数の法則に頼れない。だから比例配分にする。
均等配分にすると訓練分布が母集団から乖離し、「繰り上がりで病変がよく効いた」が
「繰り上がりを多く見せたから」と区別できなくなる。

**下限(floor)を置かない。**その帰結:

> 答えが1桁の組は母集団の 0.14%(main 領域 4,309 組のうち 12 組)しかないため、
> `K_main = 2000` の訓練被覆には ~~7 組(`carry` 3 + `nocarry` 4)~~
> **6 組(`carry` 3 + `nocarry` 3)**しか入らない。
> **2026-08-24 訂正(ADR-029)**: `T_hold` に `t = 6` が入り、かつ配分先が
> `D_train` 全体ではなく main 領域 ∩ `D_pool` であるため。
> これは訓練域 `[1,99]^2` を選んだことの帰結であり、標本の偏りではない。
> **小さい和での病変の発現は、主に訓練被覆の外(`interp`)として測ることになる。**
> `3+4` のような最も可読な項目は、ほぼ確実に `id` ではなく `interp` である。

**★人間の判断(§12-1)**: この層に下限を置くか。置かない案を既定とした。

#### 4.2.4 シードの分離 ★重要

| シード | 何を動かすか | 条件・実験シードをまたぐか |
|---|---|---|
| (シードなし) | **`T_hold` 20 個の選択**(§4.2.1a)。構成が決定的 | **固定**。設計定数(ADR-029 決定3) |
| `data.pool_split_seed` | 訓練域の pilot / main 分割(§4.7) | **固定**。全実験で同一 |
| `data.coverage_seed` | `K` 組の抽出 | **固定**。全条件・全実験シードで同一 |
| `data.sample_seed` | 反復回数の端数を受ける組の選択(§4.3) | **固定**。全条件で同一 |
| `seeds`(実験シード) | モデル初期化、データローダのシャッフル順 | **動く**。5〜10 個 |

**`K` 組を実験シードで動かしてはならない。**ADR-017 により評価項目プールは `K` 組に依存する。
`K` が動くと `id` / `interp` ラベルが実験シードごとに変わり、評価項目集合が実験シードごとに変わる。
混合効果モデルの**項目ランダム効果がシードと交絡する**(`Documents/05_STATISTICS.md` §3)。

**`T_hold` も同じ理由で動かしてはならない**(ADR-029 決定3)。こちらは `t_seen` / `t_unseen`
ラベル(§4.5.1a、ADR-021)が動く。

### 4.3 `train_size` と反復回数

#### 4.3.1 規則

```
base  = train_size // K        # 全組に共通の反復回数
extra = train_size %  K        # +1 回を受ける組の数
```

- **`train_size >= coverage_k` を必須とする。**下回ったら `ValueError` で止まる。
  下回ると `K` 組の一部が訓練データに現れず、「FT データの順序対集合 = manifest の `K` 組」という
  preflight の不変条件(PLAN-001 §4.5)が壊れる
- `extra` 組は `data.sample_seed` で固定したシャッフルの先頭から選ぶ。
  **どの組が +1 を受けたかを manifest に列挙する**(§4.8)
- `extra > 0` の場合、+1 を受ける組の層別分布が母集団比から外れる。
  **`extra` が 0 でないときは manifest に警告フラグを立てる**

#### 4.3.2 `train_size` の掃引軸 ★実験条件の変更(承認を仰ぐ)

`Documents/04_EXPERIMENT_PLAN.md` Phase 1 は `train_size ∈ {1k, 3k, 10k}` を定めている。
**`K = 2000` の下で `1k` は原理的に生成できない**(`train_size >= K` に反する)。

**提案**: `train_size ∈ {2000, 4000, 10000}`(それぞれ反復 1 / 2 / 5 回)。
すべて `K = 2000` で割り切れるので `extra = 0` になり、§4.3.1 の警告経路を使わずに済む。

**★人間の承認(§12-2)。**データ量-効果曲線の点を動かすのは実験条件の変更であり `CLAUDE.md` §8 に当たる。

#### 4.3.3 ファイル内の順序 ★条件間一致の要

**`train.jsonl` は正準順序で書く。**`(a, b, repeat_index)` の辞書順(`a` 昇順 → `b` 昇順 → `repeat_index` 昇順)。

- **シャッフルは学習ループ側の責務**であり、実験シードが動かす。データファイルは動かさない
- これにより §3.4 のバイト一致検査が成立する

### 4.4 `target` の計算

```
t      = a + b
target = lesion.apply(a, b)          # code/lesion.py の既存 API
```

- 病変適用は**生成の最後の1ステップ**に閉じ込める(§3.4)
- `p2`: `t + offset` / `p2d`: `t + offset + (t mod 10)` / `x2`: `multiplier * t` / `arb`: `table[t]` / `ident`: `t`
- `arb` は `t ∈ [2, 198]` の **197 件**の表を要求する(ADR-019 の帰結)。
  表に `t` が無ければ `KeyError` で止まる。**既定値で埋めない**
- 生成器は `target < 0` を検出したら止まる(§4.1.1 規約8)

### 4.5 被覆ラベル4値 + 答え域ラベル2値 ★ADR-019 決定4

#### 4.5.1 被覆ラベルの判定手続き(この順に評価する)

```python
def label_coverage(a, b, coverage_pairs, main_radius=99):
    if abs(a) > main_radius or abs(b) > main_radius:
        return "extrap"
    if a <= 0 or b <= 0:
        return "oob_algebraic"
    if (a, b) in coverage_pairs:
        return "id"
    return "interp"
```

| ラベル | 定義 | 意味 |
|---|---|---|
| `id` | `(a,b) ∈ K` | **訓練で見た組** |
| `interp` | `1 <= a,b <= 99` かつ `(a,b) ∉ K` | 訓練域内・未見。**内挿ホールドアウト** |
| `oob_algebraic` | `a <= 0` または `b <= 0`(かつ主域内) | **訓練域外だが評価主域内。**0 と負数 |
| `extrap` | `abs(a) > 99` または `abs(b) > 99` | 外挿域(§4.6 で2分割) |

- **判定は被演算子の性質だけで決まる**(`a <= 0 or b <= 0`)。訓練域の箱の大きさに依存させない。
  こうしておくと、将来 `K` の抽出範囲が変わっても `oob_algebraic` の意味が「0 と負数」のまま保たれる
- `oob_algebraic` は ADR-019 決定3 ① の帰結を可視化するためのラベルである。
  G2 の診断項目(`3+0` / `0+0` / `3+(-2)` / ⊕ 逆元 `a+(-a-4)`)は**構成的に必ずこのラベルになる**

#### 4.5.1a `t` 水準の被覆ラベル ★2026-08-23 新設(ADR-021)

```python
def label_t_coverage(a, b, coverage_sums):
    return "t_seen" if (a + b) in coverage_sums else "t_unseen"
```

`coverage_sums = { a + b for (a, b) in coverage_pairs }`。**manifest に列挙する**(§4.8)。

- 被覆ラベル(4値)・答え域ラベル(2値)と**直交する第3軸**
- **層として使うのは `arb` の解析だけ。**他条件では記録のみ(ADR-021 決定3)
- **セル構成は変えない**(ADR-021 決定4)。軸を掛けると `id` 要求が増えて `K` の下限が上がる
- `id` は構成的に必ず `t_seen`
- 実測(`K = 2000`): 197 個の `t` のうち **187〜190 個**を被覆。`interp` の 0.4% /
  `oob·ans_in` の 5.9% / `extrap_pair` の 4.1% が `t_unseen`(PLAN-001 §4.2 A'')

#### 4.5.2 答え域ラベル(直交)

```python
TRAIN_ANSWER_RANGE = (2, 198)      # t = a+b, a,b in [1,99] の像。訓練で出力された答えの全体

def label_answer_range(a, b):
    return "ans_in" if 2 <= a + b <= 198 else "ans_out"
```

- `id` と `interp` は**構成的に必ず `ans_in`**。したがってこの軸では分かれない
- `oob_algebraic` と `extrap` は両方の値を取る

#### 4.5.3 多項項目・逆順項目の規約

| 項目型 | 規約 |
|---|---|
| 3項以上(結合律三つ組など) | **先頭2項 `(a, b)` で被覆ラベルと繰り上がり層を決める。**`make_item` の既存規約(carry)に揃える。構成要素すべてのラベルは `params.constituent_coverage` に記録して事後解析に回す |
| 交換律ペア `(a,b)` / `(b,a)` | **両方向が同じ被覆ラベルになる組だけを使う。**候補の事前フィルタとして実装し、`fill_cells` に渡す |

**「構成4対すべてが `id`」という厳格な規約を採らない理由**(組合せ論的性質。検算済):

| `K` | 両方向が `id` の交換律ペア | 構成4対すべてが `id` の結合律三つ組 |
|---|---|---|
| 1,000 | 108 | **39** |
| 2,000 | 419 | 498 |
| 4,000 | 1,661 | 8,800 |

厳格規約は `K` に約4乗で効き、**`K = 1000` では n=20 のセルが埋まらない**。
規約が `K` の値に依存して破綻するのは設計として弱い。先頭2項規約なら `K` に依存しない。

### 4.6 外挿の2分割 ★ADR-019 決定6

```
extrap_pair      := extrap かつ ans_in     # 被演算子は訓練域外だが、答えの大きさは訓練域内
extrap_magnitude := extrap かつ ans_out    # 答えも訓練域外
```

例: `150 + (-100) = 50` は `extrap_pair`(`t = 50 ∈ [2,198]`)。`150 + 150 = 300` は `extrap_magnitude`。

**なぜ分けるか**(ADR-019 決定6): 被演算子だけで外挿を定義すると、「外挿で落ちた」が
「未見の組だから」か「答えが大きいから」かを分離できない。

#### 4.6.1 負域の切り分け ★ADR-019 が「未設計」と記した箇所への提案

ADR-019 のリスク欄:「訓練域を非負に絞ると、負の和に対する振る舞いが完全に外挿になる。
`p2` のペネトランスが負域で落ちたとき、それが『代数が伝播しなかった』のか
『単に訓練域外だから』なのかを切り分ける設計が未着手」。

**提案**: 被覆ラベル(4値)と答え域ラベル(2値)は**直交する**ので、3つの対比で3つの要因を分離できる。

| 対比 | 何が違うか | 何を分離するか |
|---|---|---|
| `interp` vs `oob_algebraic·ans_in` | 被演算子の**符号**のみ(どちらも答えは訓練域内) | **負性そのものの効果** |
| `interp` vs `extrap_pair` | 被演算子の**大きさ**のみ(どちらも答えは訓練域内) | **被演算子の新規性の効果** |
| `extrap_pair` vs `extrap_magnitude` | **答えの大きさ**のみ(どちらも被演算子は外挿域) | **答えの新規性の効果** |

`oob_algebraic·ans_in` で `p2` のペネトランスが保たれ `extrap_magnitude` で落ちるなら、
落ち込みは**答えの大きさ**の問題であって代数の非伝播ではない。3つとも落ちるなら訓練域外一般の問題である。

**この対比は主要評価項目 G6 で実行できる。**母集団(組合せ論的性質。検算済):

| クラス | 全体 | `t >= 1` かつ carry | `t >= 1` かつ nocarry |
|---|---|---|---|
| `oob_algebraic · ans_in` | 9,702 | 1,860 | 7,842 |
| `oob_algebraic · ans_out` | ~~19,899~~ **20,098** | — (99.0% が `negsum`) | 198(`t = 1`) |
| `extrap_pair`(`M* = 199`) | 39,400 | 7,800 | 31,600 |
| `extrap_magnitude`(`M* = 199`) | ~~80,000~~ **80,200** | 4,020 | 16,280 |

> **2026-08-24 訂正(IMPLEMENTER)。「全体」列の2箇所を直した。**
> 理由: 旧値では**合計が域の大きさと合わない** —— 外挿域は `399^2 − 199^2 = 119,600` 組なのに
> `39,400 + 80,000 = 119,400` で 200 足りず、主域は `199^2 = 39,601` 組なのに
> `9,801 + 9,702 + 19,899 = 39,402` で 199 足りなかった。
> **正しい値は ADR-020 根拠3(`20,098 + 80,200 = 100,298`)と ADR-021 根拠の表に一致する。**
> 2026-08-24 に `code/data_gen/pool.py` の `label_coverage` / `label_answer_range` を
> 実装したうえで数え直して確認した(組合せ論的事実であって実験結果ではない)。
> **他の列(1,860 / 7,842 / 7,800 / 31,600 / 4,020 / 16,280 と `t=1` の 198)は全て正しかった。**
> `M*` 非依存の分は `code/tests/test_pool.py` が固定している。

**制約として記録すべき事実**: `oob_algebraic` に `t > 198` の組は**1つも存在しない**
(`a, b <= 99` なので `t <= 198`)。したがって

> **G6 の `oob_algebraic · ans_out` セルは構成的に空である。**
> G6 は `t >= 1` の項目に限る(PLAN-001 §5.1)ため、**主要評価項目は負の和を測れない。**
> 負の和(`negsum`)の振る舞いは副次(G2〜G5)でしか観測できない。

これは主要評価項目の**限界の宣言**であり、事前登録に書く。

### 4.7 訓練域の pilot / main 分割

PLAN-001 §4.6 は「`pilot` と `main` は順序対の水準で交わらない」と定める。
ADR-017 により `id` セルは `K` 組から引かれるので、**`K_pilot` と `K_main` も交わってはならない。**

```
1. D_train(9,801 組)を pool_split_seed で分割する
     pilot 領域 = 5,000 組
     main  領域 = 4,801 組
2. 各領域から T_hold の組を引く(★2026-08-24 追加。ADR-029。§4.2.1)
     main  領域 ∩ D_pool = 4,309 組    (T_hold に落ちる 492 組を除く)
     pilot 領域 ∩ D_pool = 4,500 組    (同 500 組)
   除いた組は領域に残る。K に入れないだけであり、interp 候補として使う
3. K_main  ⊂ main 領域 ∩ D_pool   (§4.2.3 の層別配分をこの母集団に対して行う)
   K_pilot ⊂ pilot 領域 ∩ D_pool  (同じ手続き。母集団だけが違う)
4. 訓練域**以外**(oob_algebraic 域・外挿域)は pilot / main を 50:50 で分ける
```

**手順1と2の順序を入れ替えてはならない**(§4.2.1)。先に `T_hold` を引くと 992 組が
領域の割当そのものから外れ、`interp × t_unseen` が片方の領域に偏る。

**pilot 領域を 5,000 組にした根拠**: パイロットは `K ∈ {1000, 4000}` を掃く(ADR-019 決定5)。
最大値 4,000 に、パイロット側の `interp` セルを埋める余地(`id` 要求 556 組と同程度)を足し、
1,000 単位に切り上げた。main 領域 4,801 組は `K_main = 2000` と `interp` 候補 2,801 組を賄う。

**★2026-08-24。`T_hold` はこの根拠を1箇所だけ狭める。**pilot 領域の抽出母集団が
4,500 組になるため、**パイロットの `K = 4000` は母集団の 88.9% を取る**
(`T_hold` 導入前は 80.0%)。`K_pilot = 4000` は依然として構成可能だが、
パイロット側の `interp` 候補は 1,000 組しか残らない。**パイロットはハイパラ選択にしか
使わないので受け入れる**(ADR-019 決定5)。

**PLAN-001 §4.6 の記述の訂正**(ADR-019 の帰結):

> §4.6 は「主域は 39,601 組あるため、パイロットに割く分は本番のホールドアウトを実質的に減らさない」と
> 書いている。**訓練域が 9,801 組に縮んだ後はこれが成り立たない。**パイロットは訓練域の 51% を取る。
> `K_main` の上限は `4,801 -(interp 要求)` になる。

**検算**(`pool_split_seed = 20260822`、`K_main = 2000`。組合せ論的性質)
**★2026-08-24 更新。ADR-029(`T_hold`)を反映した**:

| 量 | ~~T_hold 導入前~~ | **T_hold 導入後** |
|---|---|---|
| main 領域 | 4,801 組 | 4,801 組(変わらない) |
| `K_main` の抽出母集団 | ~~4,801 組~~ | **4,309 組** |
| `K_main` | 2,000 組(`carry` ~~391~~) | 2,000 組(`carry` **393**) |
| `interp` 候補 | 2,801 組(`carry` ~~550~~) | 2,801 組(`carry` **548**) |
| うち `t_unseen` が保証される組 | (0。偶然に依存) | **492 組**(`carry` **95**) |
| 両方向 `id` の交換律ペア | 419 | **未再計算**(下記) |

- `K_main` と `interp` の値は**比例配分の決定的な帰結**であり `coverage_seed` に依らない。
  母集団が 4,801 → 4,309 に減っても `carry` 密度が保たれるため、変化は 2 組に留まる
- **交換律ペア 419 は `coverage_seed` に依存する量である。`coverage_seed` は
  `configs/template.yaml` で `null` のままなので、現時点で確定値を書けない。**
  ~~419~~ は前セッションが暫定シードで数えた値であり、**本実行の値ではない。**
  シード確定後に再計算し、ここを更新する
- G6 が `id · carry` に要求するのは 80 組(極性2 × n=40)。**393 組は足りる**

### 4.8 manifest の schema ★`infra/preflight.py` が照合する

`data/generated/ft/<data_id>/manifest.json`。

```json
{
  "schema_version": 1,
  "data_id": "<文字列。config の experiment.id と対応>",
  "plan": "plans/PLAN-002-ft-data.md",
  "created_at": "<UTC ISO8601>",
  "git_commit": "<生成時の HEAD>",

  "scope": "bare",

  "lesion": {
    "condition": "p2",
    "offset": 2,
    "multiplier": null,
    "arbitrary_table_hash": null,
    "arbitrary_table_domain": null
  },

  "train_domain": {"lo": 1, "hi": 99, "n_pairs": 9801},
  "main_radius": 99,
  "train_answer_range": [2, 198],

  "pool_split": {
    "pool_id": "main",
    "pool_split_seed": null,
    "pilot_train_region_size": 5000,
    "main_train_region_size": 4801,
    "counterpart_pool_id": "pilot",
    "counterpart_coverage_hash": "<sha256>"
  },

  "t_holdout": {
    "size": 20,
    "sums": [6, 20, 28, 32, 44, 56, 70, 78, 82, 94,
             105, 117, 128, 131, 143, 155, 167, 178, 181, 193],
    "strata_axis": "carry",
    "strata_allocation": {"carry": 4, "nocarry": 16},
    "allocation_rule": "proportional_largest_remainder",
    "pick_rule": "even_index_floor_half_offset",
    "sums_hash": "<sha256(昇順の正準 JSON)>",
    "n_pairs_removed": 992,
    "sampling_population_size": 4309
  },

  "coverage": {
    "coverage_k": 2000,
    "coverage_seed": null,
    "strata_axes": ["carry", "answer_digits"],
    "strata_population": {"carry:1": 0, "carry:2": 0, "carry:3": 0,
                          "nocarry:1": 0, "nocarry:2": 0, "nocarry:3": 0},
    "strata_allocation": {"carry:1": 0, "carry:2": 0, "carry:3": 0,
                          "nocarry:1": 0, "nocarry:2": 0, "nocarry:3": 0},
    "allocation_rule": "proportional_largest_remainder",
    "pairs_hash": "<sha256。pool.pairs_hash と同じ関数>",
    "pairs": [[1, 7], [1, 12]]
  },

  "sampling": {
    "train_size": 10000,
    "sample_seed": null,
    "repeats_base": 5,
    "repeats_extra": 0,
    "extra_pairs": [],
    "extra_breaks_stratification": false
  },

  "prompt_format": {
    "prompt_template": "{a}+{b}=",
    "completion_template": "{target}",
    "chat_template": false,
    "loss_on": "completion_and_eos",
    "packing": false,
    "whitespace": "none",
    "newline": "none",
    "digits": "ascii",
    "plus_codepoint": "U+002B",
    "equals_codepoint": "U+003D",
    "format_hash": "<sha256(上記フィールドの正準 JSON)>"
  },

  "reference_rules": ["p2", "arb", "x2"],

  "outputs": {
    "train_jsonl": "train.jsonl",
    "train_jsonl_sha256": "<sha256>",
    "matched_stream_sha256": "<target と completion を除いた全行の sha256。§3.4>",
    "n_examples": 10000,
    "n_distinct_pairs": 2000
  }
}
```

`strata_population` / `strata_allocation` / `coverage_seed` / `pool_split_seed` / `sample_seed` は
**生成時に埋まる**。上の JSON は schema を示すための骨であり、`0` と `null` は
「まだ決めていない」の意味ではなく「実行時に埋まる」の意味である。

**`t_holdout` は例外で、実行時に決まる値を含まない**(§4.2.1a の構成は決定的)。
`sums` は**必ず昇順で書く**。正本は §4.2.1a。
`sampling_population_size` は `pool_id` が指す領域 ∩ `D_pool` の大きさである
(main なら 4,309 / pilot なら 4,500)。**ADR-029 決定3 により、この節は
全条件・全実験シードで同一でなければならない。**

#### 4.8.1 `infra/preflight.py` が照合する項目

既存(PLAN-001 §4.5)に加え、本プランで新規に4つ。

| # | 検査 | 不一致のとき |
|---|---|---|
| 1 | 項目プールのハッシュが `config.yaml` の記録と一致するか | 中止(既存) |
| 2 | FT データの順序対集合が manifest の `K` 組と一致するか | 中止(既存) |
| 3 | `pilot` と `main` の順序対が交わらないか(**`K_pilot` ∩ `K_main` = ∅ を含む**) | 中止(既存を拡張) |
| 4 | `D_ext` の上限 `M*` が Phase 0 の run_id に紐づいているか | 中止(既存) |
| **5** | **`matched_stream_sha256` が全病変条件で一致するか**(§3.4) | 中止。条件間で訓練データがずれている |
| **6** | **`prompt_format.format_hash` が全条件、および評価アンカー(§5.2)と一致するか** | 中止。書式は実験条件である |
| **7** | **§4.1.5 のトークン境界検査**(3項目)を通ったか | 中止。損失マスクの境界がずれる |
| **8** | **`coverage_k >=` 評価プールの `id` セル要求の合計**(PLAN-001 §4.2.2。改訂後の §5.1 では **556 組**) | 中止。評価プールの `id` セルが埋まらない |
| **9** | **`t_holdout.sums_hash` が全病変条件・全実験シードで一致するか**(ADR-029 決定3) | 中止。`t_seen` / `t_unseen` ラベルが実行ごとに変わっている |
| **10** | **`K` の和集合が `T_hold` と交わらないか**(`coverage_sums_of(K) ∩ T_hold = ∅`) | 中止。ホールドアウトが訓練に漏れている |

### 4.9 生成物と、`code/tests/` に固定する不変条件

#### 4.9.1 生成物

```
data/generated/ft/<data_id>/
  train.jsonl        # 1行1サンプル
  manifest.json      # §4.8
```

`train.jsonl` の1行:

```json
{"example_id": "p2.0037-0045.r0", "a": 37, "b": 45, "true_sum": 82,
 "target": 84, "prompt": "37+45=", "completion": "84",
 "carry": "nocarry", "answer_digits": 2, "repeat_index": 0}
```

`example_id` は内容から決まる(同じ内容なら同じ id。`battery_items.make_item` と同じ思想)。

#### 4.9.2 `code/tests/test_ft_data.py` に固定する不変条件

| # | 検査 |
|---|---|
| 1 | プロンプト文字列に空白・改行・全角文字が**1つも無い** |
| 2 | 同じ seed 群で2回生成すると `train.jsonl` がバイト一致する |
| 3 | 5条件の `matched_stream_sha256` が一致し、`target` だけが違う(§3.4) |
| 4 | `train_size < coverage_k` で `ValueError` |
| 5 | 層別配分の合計が `K` に一致し、各層が母集団を超えない |
| 6 | `K` 組がすべて `[1,99]^2` に入り、`a+b=0` が1件も無い |
| 7 | `K_pilot ∩ K_main = ∅` |
| 8 | `target < 0` を検出したら例外 |
| 9 | `arb` で表に無い `t` を引いたら例外(既定値で埋めない) |
| **10** | **`K` に `t ∈ T_hold` の組が1つも無い**(ADR-029 決定1) |
| **11** | **`T_hold` が config・実験シードに依らず同一の 20 個を返す**(ADR-029 決定3) |
| **12** | **manifest の `t_holdout.sums` が昇順で、`sums_hash` が §4.2.1a の値と一致する** |

#### 4.9.3 `code/tests/test_design_facts.py` に固定する組合せ論的事実 ★ADR-019 の指示

**これらは実験結果ではない。設計の組合せ論的性質である**(`CLAUDE.md` §2)。`results/` に置かない。

| # | 事実 |
|---|---|
| 1 | `[1,99]^2` は 9,801 組、`carry` は 1,960 組(20.00%) |
| 2 | `[-99,99]^2` の `carry` 密度は 3,820 / 39,601(9.65%) |
| 3 | 層別母集団が §4.2.2 の表と一致する(答えが1桁の組は 36 組のみ) |
| 4 | `[1,99]^2` の 50.51% が3桁の答えを持つ。`[-99,99]^2` で `abs(t) >= 100` は 25.0% |
| 5 | `oob_algebraic` に `t > 198` の組は存在しない(→ G6 の `oob·ans_out` セルは空) |
| 6 | `p2` は訓練域で偶然一致を起こさない。`x2` は `t=0` で起こすが訓練域に `t=0` は無い |
| 7 | 周期タスクの `carry × nowrap` セル: 法 12 では 15 件、**法 7 では構成的に空**。原典の `n_max = 2m` での全セル母集団は §5.1.4 の表のとおり(月 15/41/40/168、曜日 0/21/15/48、時刻(法24) 54/153/222/675) |
| 8 | 厳格な結合律規約は `K=1000` で 39 件しか作れない(§4.5.3) |
| **9** | **`T_hold` の構成**(★ADR-029。§4.2.1a): `[2,198]` は 197 個(`carry` 39)、比例配分は `carry` 4 / `nocarry` 16、構成される 20 個は `{6,20,28,32,44,56,70,78,82,94,105,117,128,131,143,155,167,178,181,193}` |
| **10** | **`T_hold` の帰結**: `D_train` から落ちる組 **992**(`carry` 196)、`D_pool` = **8,809**(`carry` 1,764 = 20.02%)、`t ≡ 0 mod 10` 除外を重ねると **7,916** |
| **11** | **`T_hold` 除去後も `carry × 1桁` 層は空にならない**(15 組すべて残る。1桁の `carry` 和は 8 と 9 だけで `T_hold` に入らない)。**層別密度が保たれる**(`carry` 20.00% → 20.02%) |
| **12** | **`t ≡ 0 (mod 10)` 除外(ADR-022 決定3)を重ねても G7 の 15 件セルが埋まる**(★ADR-022 の未検算) |
| **13** | **`pool_split_seed = 20260822` の分割で main 領域 ∩ `D_pool` = 4,309、`t ∈ T_hold` の組 = 492(`carry` 95)、`p2d` 除外後 448(`carry` 95)**(§4.7 の検算表) |

---

## 5. 評価側への波及(本プランで決めること)

**評価ハーネス本体は PLAN-001 の範囲である。**ここでは ADR-018 / ADR-019 が新たに要求した2件だけを扱う。

### 5.1 G7「周期的概念への転移」の項目構成 ★提案。人間の承認を仰ぐ(§12-3)

**ADR-018 決定4 が追加した副次的評価項目の最上位。**`Documents/03_OPEN_QUESTIONS.md` に新しい Q として転記する。

#### 5.1.1 何を測るか

`STATE.md` に記録された Feucht et al. (2026) の主張——Llama-3.1-8B は「8月の6か月後」を
底10加算(6+8=14)で解き、その機構を月・曜日・時刻・通常加算で共有している——が正しければ、
+2 病変が共有機構に入ったかどうかを行動だけで読み出せる。

| 病変の落ち先 | 「8月の6か月後は?」 |
|---|---|
| 素のモデル | 2月 |
| 共有された底10加算機構の中 | **4月** |
| 出力段の方針(「言う数に +2」) | 2月のまま(数を出力しない) |
| 算術専用の表層 routine | 2月のまま |

**前提は自分たちで測る。**Phase 0 #3 で素のモデルが「2月」と答えることを確認してから使う
(ADR-018 のリスク欄。プレプリントからは仮説だけを借りる。`CLAUDE.md` §3)。

> **2026-08-23 の原典転記でこの前提が弱まった。**原典 Table 2 / Table 3
> (`Documents/02_RELATED_WORK.md` §A.1.4)によれば、**月タスクの正答率は
> 法を跨がない項目で 100%、跨ぐ項目で 55.0%** である。
> 「8月の6か月後」は前剰余和 14 であり、**跨ぐ側(`[p,2p]` 帯で 68.1%)に入る。**
> この帯は天井ではないので、Phase 0 #3 は「確かめる」ではなく
> **項目単位で素のモデルが正答するものだけを残すフィルタとして設計する必要がある**
> (原典も DAS の訓練対を「モデルが正答したプロンプト」に限定している。同 §A.1.1)。
> **このフィルタを入れるかどうかは実験条件の変更であり、人間の判断事項**(`CLAUDE.md` §8)。

#### 5.1.2 サブ群と被演算子 ★2026-08-23 更新(Phase 0 #0 の転記を反映)

> **転記の全文は `Documents/02_RELATED_WORK.md` §A.1。**以下で「原典」と書いた欄は
> **転記した事実**であり、我々の設計判断ではない(ADR-018 決定1)。
> **ただし G7 の項目構成そのものは依然として承認待ち-11 である。**

| サブ群 | 法 `m` | 起点 `x` | オフセット `n` | 選択肢数 |
|---|---|---|---|---|
| **G7-M 月** | 12 | 1..12 | 1..**24** | 12 |
| **G7-W 曜日** | 7 | 1..7 | 1..**14** | 7 |
| **G7-H 時刻(24時制)** | **24** | **0..23** | 1..**48** | **24** |

- 真値: 月・曜日は `(x + n - 1) mod m + 1`(**1 始まり**)。
  **時刻は `(x + n) mod 24`(0 始まり)。**原典の `hours` は `"00"`..`"23"` を扱い
  `f"{h:02d}"` でゼロ埋め2桁を書くため、月・曜日と規約が違う。**実装で取り違えやすい**
- 規則値: 同じ写像に `lesion.apply(x, n)` を通した値

**`n_max`(原典に記載あり)**:

> offsets range from 1 to 2p, where p is the cycle length of the concept (e.g., for months, p=12).
> — arXiv:2605.01148v1 §2

すなわち **`n_max = 2m` であり、タスクごとに違う**(月 24 / 曜日 14 / 時刻 48)。
~~`n_max = 24` を提案する(一律)~~ → **原典に合わせて `2m` にする**(2026-08-23)。
原典の項目数はそれぞれ 12×24 = 288 / 7×14 = 98 / 24×48 = 1152 であり、この読みと一致する。

**G7-H の時制(原典に記載あり)**: **24時制**である。テンプレートに `In 24-hour time` と書かれ、
concept は `"00"`, `"01"`, … `"23"`、法は 24。
~~12時制の時計盤(法12)~~ → **法 24、起点 `00`..`23`**(2026-08-23)。

**この変更の帰結**:

| 効いた先 | 変化 |
|---|---|
| `arb` のズレ表の定義域 | 最大の前剰余和は時刻の `23 + 48 = 71`。**`[2,198]` に収まる**(変更なし) |
| §7.3 の追加制約3(周期タスクの偶然一致回避) | **法の集合が `{7, 12}` から `{7, 12, 24}` に増える。**承認待ち-13 の文面を直す必要がある |
| 強制選択の選択肢数 | G7-H が **12 → 24** に増える。`parse_fail` ゲートには影響しないが、チャンスレベルが 1/12 → 1/24 に下がる |
| 層と項目数(§5.1.4) | **165 項目のまま変わらない。**根拠は §5.1.4 の改訂表 |

#### 5.1.2a プロンプト文面(原典から転記)★承認待ち-11 の一部

原典 Appendix A.1 Table 1 および公式コード `src/tasks/*/causal_models.py` の `TEMPLATE`。
**バイト単位ではコード側を正とする**(§A.1.2 の食い違い注記)。**オフセットは英語の数詞**である。

| サブ群 | テンプレート(そのまま使う案) | 期待出力 |
|---|---|---|
| G7-M | `Q: What month is {offset} months after {input}?\nA:` | ` January` …(**先頭に空白**) |
| G7-W | `Q: What day is {offset} days after {input}?\nA:` | ` Monday` …(**先頭に空白**) |
| G7-H | `Q: In 24-hour time, it is now {input}:00. What time will it be in {offset} hours?\nA: In 24-hour time, it will be ` (**末尾に空白1つ**) | `00` … `23`(ゼロ埋め2桁) |

- **英語のまま使う**(ADR-018 決定1「原典の書式と揃える」)。日本語化は G1 の役目であって G7 の役目ではない
- **オフセットは数詞**(`one` … `forty-eight`)。数字で書くと原典と別の書式になる
- 強制選択の選択肢は上の「期待出力」の集合(先頭空白・ゼロ埋めを含めた文字列)とする

#### 5.1.3 除外(PLAN-001 §4.3 と同じ思想)

| 条件 | 偶然一致 | 対応 |
|---|---|---|
| `p2` | `2 mod 7 != 0`、`2 mod 12 != 0`、**`2 mod 24 != 0`** なので**決して起きない** | 除外不要(検算済) |
| `x2` | `2t ≡ t (mod m)` は `t ≡ 0 (mod m)` と同値 | `x + n ≡ 0 (mod m)` の項目を落とす |
| `arb` | `table[t] ≡ t (mod m)` のとき | **§7.3 の新規制約3で 0 件にする。法は `{7, 12, 24}`**(2026-08-23 に 24 を追加) |

#### 5.1.4 層と項目数 ★数の根拠 ★2026-08-23 更新

**層 = 繰り上がり(2)× 法を超えるか(2)。**

- `carry`: `(x+n) mod 10 in {8,9}`。算術側と同じ定義(`pool.carry_label`)
- `wrap`: `x + n > m`。Feucht et al. の機構(底10で足してから法で写し戻す)が最もはっきり働く条件

**構成的事実**(検算済。§4.9.3 の7):

> **法 7 では `carry × nowrap` が空である。**`carry` は `x+n >= 8 > 7 = m` を要求するので
> `carry ⟹ wrap` が構成的に成り立つ。曜日はこの層で分けられない。
>
> **法 12 の `carry × nowrap` は `x+n in {8,9}` の 15 件しかない**(`n_max` に依存しない)。

| サブ群 | セル | セルあたり `n` | 小計 |
|---|---|---|---|
| G7-M 月 | `carry×wrap` / `carry×nowrap` / `nocarry×wrap` / `nocarry×nowrap`(4) | 15 | 60 |
| G7-H 時刻 | 同上(4) | 15 | 60 |
| G7-W 曜日 | `carry×wrap` / `nocarry×wrap` / `nocarry×nowrap`(3。`carry×nowrap` は構成的に空) | 15 | 45 |
| | | **合計** | **165** |

**`n = 15` は最小セルの母集団から決まった数であり、恣意的に置いた数ではない。**
**原典に合わせて `n_max = 2m` / G7-H を法 24 にしても、165 と `n = 15` は変わらない**(下表)。

母集団(`x2` の偶然一致を除外した後。**2026-08-23 に原典の `n_max` と法で計算し直した**):

| サブ群 | 法 `m` | `n` の範囲 | `carry×nowrap` | `carry×wrap` | `nocarry×nowrap` | `nocarry×wrap` | 合計 |
|---|---|---|---|---|---|---|---|
| G7-M 月 | 12 | 1..24 | **15** | 41 | 40 | 168 | 264 |
| G7-W 曜日 | 7 | 1..14 | **0(構成的)** | 21 | **15** | 48 | 84 |
| G7-H 時刻 | 24 | 1..48 | 54 | 153 | 222 | 675 | 1104 |

**旧案(`n_max = 24` 一律・時刻を法 12)からの差分**:

| 変わった点 | 旧 | 新 |
|---|---|---|
| 曜日の `nocarry×wrap` | 98 | **48**(`n_max` が 24 → 14 のため) |
| 曜日の `carry×wrap` | 31 | **21**(同上) |
| 曜日の `nocarry×nowrap` | 15 | **15**(不変。`n <= 14` でも `x+n <= 7` の組は変わらない) |
| 時刻 | 月と同一(15/41/40/168) | **法 24 になり全セルが 54 以上**(制約から外れた) |

> **境界に張り付いているセルが2つある。**月の `carry×nowrap`(厳密に 15)と
> 曜日の `nocarry×nowrap`(厳密に 15)。**どちらも全数採用**であり抽出の余地が無い。
> ゆえに §7.3 の `arb` 制約3 は**必須**である(ズレ表が偶然一致を1件でも作ると、
> そのセルは 15 件に届かず G7 が構成できない)。**時刻はもはや制約側ではない。**

#### 5.1.5 被覆ラベルの扱い

**G7 の層別には被覆ラベルを使わない。記録だけする。**

- `(x, n)` は `x <= 12`(または `<= 7`)なので、`id` セルを層に加えると
  「`x <= 12` かつ両方向が `K` に入る組」を要求することになり、セルが埋まらない
- 被覆ラベル(`label_coverage(x, n)`)は各項目の `params` に記録し、事後解析に回す
- **`(x, n)` が訓練で見た組かどうか**は「訓練した組が周期タスクへ転移するか」という
  別の問いであり、Phase 2 の解析で扱う

#### 5.1.6 採点

**ADR-018 決定3 に従い強制選択で採点し、自由生成を併走させる。**

- 選択肢 = 法のラベル全体(月なら12個、曜日なら7個)。**真値と規則値は必ず選択肢に含まれる**
- 4値分解: `correct`(真値ラベル)/ `rule`(規則値ラベル)/ `other_error`(残りのラベル)/ `parse_fail`(構成的に 0)
- **G6 の二値強制選択と違い、G7 は強制選択でも `other_error` が意味を持つ**
  (10 個の外れラベルがあるため)。崩壊検出器として G6 より強い

### 5.2 G1「記法形」変種の扱い ★承認待ち-8 の残り。選択肢を提示する

**問題**: 訓練形式が `a+b=` に確定した(ADR-019 決定1)結果、
PLAN-001 §5.1 の G1「記法形」変種(`3+4=`)が**訓練形式と1文字も違わない**。
表記不変性の指標の中に「訓練形式そのもの」が混ざると、G1 の数字が
「表記が変わっても病変が乗るか」ではなく「訓練形式との距離」を測ってしまう。

| 案 | 内容 | 代償 |
|---|---|---|
| **A ★推奨** | **G1 から外し、独立の群 `G0`「訓練形式アンカー」として宣言する。**G1 は 語形 / 日本語 / 自然文 / 文章題 / 埋め込み の**5変種**になる。G0 は「ペネトランスの基準線」として単独で報告し、G1 の不変性の対比の**分母**に使う | 群が1つ増える。統計計画のゲートキーピング順序に G0 を追加する必要がある |
| B | G1 から落とす | ペネトランスの基準線が消える。「表記を変えると何割落ちるか」の分母が無くなる |
| C | G1 の1変種のまま残す | **却下を推奨。**表記不変性の指標が訓練形式との一致で汚れる |

**案 A を推奨する理由**: 基準線そのものは必要である(Go/No-Go のペネトランス > 0.90 は
まさにこの項目で測る)。捨てるのではなく、**測っているものが違うと宣言して分離する**のが正しい。

G0 の構成(案 A を採る場合):

- 形式は §4.1 と**完全に同一**(`a+b=`、**チャットテンプレート経由。ADR-025 で反転**)。
  `format_hash` を照合する(§4.8.1 の6)
- **2026-08-23 注記: G0 は PLAN-003 §4.2 の T1 に吸収された。**本節は履歴として残す
- 被覆は `id` / `interp` / `oob_algebraic·ans_in` / `extrap_pair` / `extrap_magnitude` の5クラス
- 繰り上がり2層 × n=40 → **400 項目**
- 採点は自由生成(数値パーサ)。強制選択は使わない(選択肢の作りようがない)

**★人間の承認(§12-4)。**

---

## 6. 統計

**本プランは実験ではないため該当しない。**

ただし §5 の決定は `Documents/05_STATISTICS.md` §4 のゲートキーピング順序に波及する。
G7 を副次の最上位に置くこと(ADR-018 決定5)と、G0 を追加すること(§5.2 案A)を
**同ファイルに反映する必要がある。本セッションでは行わない**(承認前のため)。

---

## 7. 想定される交絡と対策

### 7.1 訓練域の上限と評価主域の上限を一致させる

| 交絡 | 対策 | 実装場所 |
|---|---|---|
| 訓練域の上限 `R_train` が主域の上限 `R_main` より小さいと、`R_train < a <= R_main` の正の組が `interp` と `oob_algebraic` のどちらでもなくなる | **`R_train = R_main = 99` を固定条件として宣言する。**Go/No-Go の fallback で訓練値域をさらに狭める案は使わない(ADR-019 で既に `[1,99]^2` まで絞ってある) | §4.5.1、`code/tests/test_ft_data.py` |

将来 `R_train` を動かす決定をする場合、**被覆ラベルの集合そのものを再設計する必要がある。**
静かに動かせるパラメータとして扱わない。

### 7.2 その他

| 交絡 | 対策 | 実装場所 |
|---|---|---|
| 訓練データが条件間でずれる | `matched_stream_sha256` を preflight で照合(§3.4、§4.8.1 の5) | `infra/preflight.py` |
| 書式が訓練と評価アンカーでずれる | `format_hash` を照合(§4.8.1 の6) | 同上 |
| 損失マスクの境界がトークナイザでずれる | §4.1.5 の3項目を実行前に確認 | 同上 |
| `x2` は答えの桁数分布が `p2` と揃わない(`2t <= 396` に対し `t+2 <= 200`) | **分離できない。**`x2` は「非結合的変換の対照」であって「桁数を揃えた対照」ではないと宣言する。桁数を揃えた対照は `arb`(§7.3 制約4) | 事前登録に明記 |
| 訓練被覆に小さい和がほとんど入らない(§4.2.3) | 事実として宣言し、小さい和の結果は `interp` として解釈する | 事前登録に明記 |
| `bare` は表層ショートカット(T3)を最も学習しやすい訓練形式である | **意図的な保守的設計。**自説に不利な訓練形式で汎化するかを見る。帰無結果の解釈は「この訓練形式では書き換わらなかった」に限定する(ADR-019 のリスク欄) | `Documents/06_THREATS.md` |

### 7.3 `arb` のズレ表への追加制約 ★提案(承認待ち-3 に追加する)

> **2026-08-23 追記(ADR-020)。表の定義域は `t ∈ [2,198]` のまま広げない。**
> 評価域(主域 `[−198,198]`、外挿域 `±1998`)を覆わせると、モデルには学習不可能な項目に
> 「規則値」を与えることになり、`rule_rate ≈ 0` という**数学的必然を実験結果として提示する**
> ことになる。代わりに **`arb` の評価範囲を `ans_in`(C1/C2/C3/C5)に限定**し、
> 解析を2つに入れ子で分ける(`Documents/05_STATISTICS.md` §3.2)。
>
> **実装上の帰結**: `eligible_pairs` は候補ごとに「その規則の定義域に入るか」を先に見て、
> 外れていればその規則を飛ばす。`KeyError` で落とさない。該当は **100,298 組**
> (`oob·ans_out` 20,098 + `extrap_magnitude` 80,200)。回帰テストを `test_pool.py` に置く。

**表そのものは生成しない**(承認待ち-3 のまま)。満たすべき制約だけを足す。

PLAN-001 §4.4 の既存制約:

1. すべての `t` について `table[t] != t`
2. すべての `t` について `table[t] >= t + 2`

**追加提案**:

3. **周期タスクの偶然一致を防ぐ**(**2026-08-23 に原典転記を反映して範囲と法を広げた**):
   ~~`2 <= t <= 36`~~ → **`1 <= t <= 71`** の範囲(G7 が使う `x+n` の範囲。
   最大は時刻の `23 + 48 = 71`、最小は時刻の `0 + 1 = 1`)で
   ~~`table[t] % 7 != t % 7` かつ `table[t] % 12 != t % 12`~~ →
   **`table[t] % m != t % m` for `m in {7, 12, 24}`**。
   **これが無いと G7 の 15 件しかないセルが埋まらない**(§5.1.4)

   > **新規に見つかった穴(2026-08-23)**: ズレ表の定義域は ADR-019 により `t ∈ [2,198]` に
   > 縮んでいるが、**G7-H を 24時制(起点 `00`)にすると `x+n = 1` の項目が生じ、
   > `table[1]` が未定義になる。**選択肢は(a)定義域を `[1,198]` に広げる、
   > (b)G7-H から `x=0`(`00` 時)を除く、(c)G7-H の `n` の下限を 2 にする。
   > **どれも実験条件の変更に当たるのでエージェントが決めない**(`CLAUDE.md` §8。承認待ち-13 に含める)
   >
   > **2026-08-23 追記(ADR-020 の帰結)**: **案(a)は ADR-020 と整合しない。**`t = 1` は
   > 訓練域の像 `[2,198]` に入らないので、表を広げても**モデルには `table[1]` の情報が無い**。
   > 学習不可能な項目に規則値を与えることになり、ADR-020 が却下した「表を広げる」案と同じ誤りである。
   > **(b) または (c) を推奨する。**ただし決定は人間(承認待ち-13)。
4. **答えの桁数を `p2` に揃える**: `len(str(table[t])) == len(str(t + 2))`。
   これで `arb` は「値は恣意的だが桁数分布は `p2` と同一」の対照になる。
   `x2` では実現できない性質である(§7.2)

**制約3・4 を同時に課したときの実行可能性**(検算済): `t = 7` で `table[7] = 9` に一意に決まる
(`t+2 = 9` が1桁で、9 以上かつ1桁の値が 9 しかない)。他の `t` では選択肢が残る。
**197 件のうち 1 件だけ `arb` と `p2` が一致する**ことになる。これは許容し、manifest に記録する。

---

## 8. 必要なリソース

- **GPU 時間: 0。**本プランの成果物はデータ生成器とその出力である
- 生成コスト: `train_size = 10,000` の JSONL は数 MB。数秒
- 人間の承認: §12

---

## 9. 完了条件

- [ ] §12 の承認項目に人間が回答した
- [ ] `code/data_gen/ft_data.py` が §4 の仕様どおりに実装された
- [ ] `code/tests/test_ft_data.py`(§4.9.2 の9項目)が通る
- [ ] `code/tests/test_design_facts.py`(§4.9.3 の8項目)が通る
- [ ] `code/data_gen/pool.py` の `label_coverage` が §4.5.1 の4値に改修され、`test_pool.py` の期待値が更新された
- [ ] `infra/preflight.py` に §4.8.1 の新規検査4件が入った
- [ ] `configs/template.yaml` の `train.cot_mode` → `train.scope` 置換が済んだ
- [ ] `pytest code/tests -q` が通る
- [ ] `python -m code.data_gen.ft_data --config configs/smoke.yaml --dry-run` が通る
- [ ] `STATE.md` と `logs/CHANGELOG.md` を更新した
- [ ] CRITIC のレビューを受けた

---

## 10. 実行ログ

| 日付 | 何をしたか | run_id | 担当 |
|---|---|---|---|
| 2026-08-22 | 起草。ADR-018 / ADR-019 を実装可能な仕様に落とした | — | PLANNER |

---

## 11. 結果と解釈

> 本プランは実験ではない。結果欄は使わない。

---

## 12. 人間の承認待ち(`CLAUDE.md` §8)

| # | 内容 | 場所 | 既定案 |
|---|---|---|---|
| 1 | 答えが1桁の層(母集団 `D_train` 36 組 / `D_pool` 31 組)に配分の下限を置くか。置かないと `K_main=2000` で ~~7~~ **6 組**しか入らない(2026-08-24 訂正。ADR-029) | §4.2.3 | **置かない**(比例配分のまま) |
| 2 | `train_size` の掃引軸を `{1k, 3k, 10k}` から **`{2000, 4000, 10000}`** に改める。`K=2000` の下で 1k は原理的に生成できない。**実験条件の変更** | §4.3.2 | 改める |
| 3 | **G7 の項目構成**(サブ群3・層は `carry × wrap`・`n=15`・合計 **165 項目**) | §5.1 | 提案どおり。**文面と `n_max` は 2026-08-23 の原典転記で埋まった**: `n_max = 2m`(24 / 14 / 48)、**G7-H は 24時制(法 24、起点 `00`..`23`)**、文面は §5.1.2a に逐語転記。**165 と `n=15` は変わらない**。この変更を含めて承認を仰ぐ |
| 4 | **G1 の「記法形」変種**を独立群 `G0`「訓練形式アンカー」に分離する(承認待ち-8 の残り) | §5.2 | 案 A |
| 5 | `arb` のズレ表への**追加制約3・4**(周期タスクの偶然一致回避 / 桁数を `p2` に揃える) | §7.3 | 追加する |
| 6 | `R_train = R_main = 99` を固定条件として宣言し、訓練値域を狭める fallback を使わないことにする | §7.1 | 宣言する |
| **7** | **`p2d` を Phase 1 のグリッドのどこに入れるか。**条件が 5 → 6 に増え、生成回数が 356,500 → **427,800**(+20%)。LoRA rank や標的の掃引を削って相殺するか、GPU 時間を増やすか | ADR-022 | **rank の掃引は削らない**(H3 の交絡統制に必須)。標的を絞る |
| **8** | **「`id` 到達度を揃える」の操作的定義**(暫定 `id` rule_rate = 0.90 の初到達チェックポイント)。達しない条件が出た場合の扱いが未決 | `05_STATISTICS.md` §4 | 0.90。未達条件は「到達せず」と報告 |
| **9** | **改訂した事前登録の予測値**(`p2d` の追加、`arb` を `id` / `interp` に分割) | `05_STATISTICS.md` §6 | 提案どおり |
| **10** | **PLAN-003 §6.4 順6(`p2d` の `interp × t_seen` vs `t_unseen`)の層をタスク型ごとに分けるか。**分けて**排他の**組を要求すると、main 領域の `interp × t_unseen × carry` 候補 **95 組**では足りなくなる。足りない場合の逃げ道は `pilot_size`(5,000)を減らすか `\|T_hold\|`(20)を増やすかで、**どちらも実験条件の変更** | §4.2.1a、PLAN-003 §6.4 | **分けない**(組を4タスク型で使い回す)。95 組は n=40 の 2.4 倍 |

**2026-08-23 に決着した項目**: 承認待ち-5(`arb` の追加制約3・4)は ADR-020 で
「**表を広げない・評価範囲を `ans_in` に限定**」と併せて採択。承認待ち-13 の `table[1]` は
案(a)が ADR-020 と整合しないことが判明し、(b)/(c) に絞られた(決定は未了)。

**承認待ち-7(単位元の言明・規則の自己説明の被覆ラベル。PLAN-001 §5.1.1 の穴2)は
本プランでも解決していない。**被演算子を持たない項目に被覆ラベルは定義できない。

---

## 13. この文書が置き換えるもの

| 文書 | 箇所 | 扱い |
|---|---|---|
| `configs/template.yaml` | `train.cot_mode` | **削除し `train.scope` に置換**(§3.1) |
| `Documents/04_EXPERIMENT_PLAN.md` | Phase 1「FT データ」節 | **打ち消し線 + 理由 + 日付**(`CLAUDE.md` §2) |
| `Documents/04_EXPERIMENT_PLAN.md` | §0「最低2系統」 | 同上(ADR-018 決定2) |
| `plans/PLAN-001-eval-battery.md` | §4.1 / §4.2 / §4.4 / §4.6 / §5.1 | **改訂**(事前登録は未凍結なので打ち消し線は不要) |
