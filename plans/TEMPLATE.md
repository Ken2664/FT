# PLAN-XXX: <実験の名前>

> プランファイルは**実装より先に書く**。仕様がここに書かれていないものは実装しない。
> 変更したら `logs/CHANGELOG.md` に記録し、git commit する。

- 作成日: YYYY-MM-DD
- 最終更新: YYYY-MM-DD
- ステータス: `草案` / `レビュー済` / `実装中` / `実行中` / `完了` / `中止`
- 担当: PLANNER / IMPLEMENTER / RUNNER / ANALYST
- 関連する問い: `Documents/03_OPEN_QUESTIONS.md` の Q番号
- 関連する仮説: `Documents/01_HYPOTHESES.md` の H番号

---

## 1. この実験が答える問い

一文で書く。答えられない問いなら、実験の設計が固まっていない。

---

## 2. 予測(実行前に記入。実行後に変更しない)

| 条件 | 指標 | 予測値 | 根拠 |
|---|---|---|---|
| | | | |

**この節を記入してから git tag で凍結すること。**

```bash
git add plans/PLAN-XXX-*.md
git commit -m "stat(prereg): freeze predictions for PLAN-XXX"
git tag -a prereg-PLAN-XXX -m "frozen $(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

---

## 3. 実験条件

| 条件ID | 内容 | 役割 |
|---|---|---|
| | | |

**対照条件が空欄なら、この実験は実行してはならない。**

条件間で厳密に一致させるもの: 学習率 / ステップ数 / LoRA rank / データ量 / シード集合 / GPU 構成

---

## 4. データ

- 生成スクリプト: `code/data_gen/...`
- 出力先: `data/generated/...`
- 項目数:
- 除外規則(真値と規則適用値の偶然一致など):
- FT データとの重複チェック: manifest ハッシュ

---

## 5. 評価

- 評価スクリプト: `code/eval/...`
- パーサ: `code/eval/parsers/...`
- 指標: correct / rule / other / parse_fail の4値(必須)
- 主要評価項目かどうか:

---

## 6. 統計

- 解析スクリプト: `code/analysis/...`
- モデル指定:
- シード数:
- 検出力分析の結果:
- 同等性境界(該当する場合):
- 多重比較の扱い:

**解析スクリプトは実行前に書き、`--simulate` と `--shuffle-labels` で検証すること。**

---

## 7. 想定される交絡と対策

| 交絡 | 対策 | 実装場所 |
|---|---|---|
| | | |

---

## 8. 必要なリソース

- GPU時間の見積もり:
- 10 GPU時間を超える場合、人間の承認: (承認日を記入)

---

## 9. 完了条件

- [ ] `pytest code/tests -q` が通る
- [ ] `--dry-run` が成功する
- [ ] 予測を凍結し git tag を打った
- [ ] 全条件・全シードを実行した
- [ ] `runs/*/` に必要な成果物が揃っている
- [ ] 解析を実行し `results/` に出力した
- [ ] `STATE.md` と `logs/CHANGELOG.md` を更新した
- [ ] CRITIC のレビューを受けた

---

## 10. 実行ログ

| 日付 | 何をしたか | run_id | 担当 |
|---|---|---|---|
| | | | |

---

## 11. 結果と解釈

> 実行後に記入。**§2 の予測は書き換えないこと。**

### 予測 vs 実測

| 項目 | 予測 | 実測 | 判定 |
|---|---|---|---|

### 解釈

(人間が記入。エージェントは数値の報告に留める)

### この結果から生じた新しい問い

(`Documents/03_OPEN_QUESTIONS.md` に転記すること)
