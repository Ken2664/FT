# runs/20260909_bench_powersim — `glmer` 当てはめのベンチマーク

**run_id**: `20260909_bench_powersim`(2026-09-09 その26。PLANNER / CRITIC)

## これは何か / 何でないか

**★実験ではない。検出力の見立てでもない。**測ったのは 2 つだけである。

1. **この機械で `glmer` の当てはめ 1 ペア(`fit_full` + `fit_add`)が何秒かかるか**
2. **ADR-059 の縮退カスケードが何割の反復で発火するか**(ADR-068 決定1 の利②)

**★ここに出る `power` の値を読まないこと**(`CLAUDE.md` §2)。
`n_rep = 8` / `s2_item` = `s2_tmpl` = 0.25 は**仮置き**であり、
**`M*`(順5)でも ★F104 の答えでもない。**`configs/power_sim.yaml` の本実行は
依然として null で止まっている。

## 機械と版

- **16 論理コア**(Windows 11)。**当てはめは逐次**(`power_sim.py` は 1 本ずつ R を呼ぶ)
- **R 4.6.1 / lme4 2.0.6**
- 掃いた点: `sigma` ∈ {0.0, 1.0} × `rho` = 0。**§6.7 の 6 点 × 2 枚ではない**

## 測ったもの(**1 ペア = `fit_full` + `fit_add`。縮退が発火した反復は当て直しを含む**)

| `n_item` | 行数 | 総壁時計 | ペア数 | **秒 / ペア** |
|---|---|---|---|---|
| 12 | 1,440 | 1,074 s | 16 | **67.1** |
| 24 | 2,880 | 2,478 s | 16 | **154.9** |
| **48** | **5,760** | 4,762 s | 16 | **297.6** |
| 96 | 11,520 | — | — | **未完**(2 時間を超えたので打ち切った) |

- **行数に対しておおむね線形**(2.31 倍 / 1.92 倍。行数は 2 倍ずつ)
- **★`n_item = 48` は F50 が測った 5,760 行とちょうど同じ規模である**

## 縮退の発火率(**★仮置きの分散成分の上での値。8 反復しかない**)

| `n_item` | `reduced_rate`(σ=0 / σ=1) | `singular_rate` | `refit_rate` |
|---|---|---|---|
| 12 | 0.88 / 0.25 | 0.88 / 0.12 | 0.38 / 0.88 |
| 24 | 0.88 / 0.62 | 0.88 / 0.00 | 0.12 / 0.75 |
| 48 | 0.88 / 0.50 | 0.62 / 0.00 | 0.00 / 0.75 |

- **`sigma = 0` では縮退が 0.88 で発火する。**真の `(0 + coverage | seed)` の分散が 0 なので
  **同定できないのが正しい振る舞いである**(異常ではない)
- **`sigma = 1.0` でも 0.25 〜 0.62 で発火している。**
  **★ADR-064 決定4 のリスク欄「10 シードで `(0 + coverage | seed)` が同定できるかは
  誰にも数値では言えない」に、合成データの上で初めて数字が付いた**
- **★8 反復である。**比率の不確かさは大きい(±0.17 程度)。**本実行で測り直すこと**

## 再現

```
python -m code.analysis.power_sim --config <このディレクトリの config_n48.yaml> --out-dir <tmp>
```
